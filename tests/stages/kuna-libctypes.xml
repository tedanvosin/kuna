<decompilertest>
<binaryimage arch="x86:LE:64:default:gcc">
<!--
    Named libc/POSIX aggregate types in the prototype tables (`option libctypes
    off|opaque`).

    The built-in prototype tables' type vocabulary is width-stable, so every
    aggregate pointer in them is spelled `void *`: `fopen` returns one, `fclose`
    takes one, `__uflow` takes one. That is honest about the width and silent
    about the pointee, and the pointee is the one thing a declaration actually
    knows.

    The XML wrapper only supplies the harness architecture (the `option` command
    needs one before the first `load file`). Every pass clears it and loads a
    real ELF.

    Passes 1 and 2 load `libctypes_stat_x86_64` (built from the `.c` beside it),
    which was written to hold the two halves apart:

      * it declares its stream handles `void *` itself and never names `FILE`,
        so nothing in its DWARF defines a stream type and the named table has to
        MINT one  -  `fclose(void *)` in pass 1, `fclose(FILE *)` in pass 2;
      * it hands `stat` a real `struct stat *`, so its DWARF defines `struct stat`
        with its real members. `st->st_mode` must read the same in BOTH passes
        (#5) and no `field_0x` fallback may appear anywhere (#6).

    #5 and #6 are the ones that can fail. A named shell interned BEFORE the DWARF
    importer completes the same name is completed for everybody except the
    pointers already built against it  -  completion re-keys the struct into a new
    object  -  and this function degrades to
    `*(int *)&st->field_0x18`. The pass runs after the DWARF importer and adopts
    what is already there, which is what these two assertions pin.

    Pass 3 loads the real coreutils `fmt`, whose DWARF DOES define the stream
    type, under glibc's own `struct _IO_FILE` spelling. The named table adopts
    that one rather than minting a second stream type beside it (#7, #8), and it
    keeps its real field layout (#9).

    Passes 4 and 5 load `libctypes_glibc_x86_64`, which carries NO debug info, so
    every named aggregate in it is minted by the table rather than adopted  -  the
    only arm in which `option libctypes glibc` installs a field layout at all.
    Pass 4 is `opaque` (the shipped default) and pass 5 is `glibc`, over the same
    two functions: the `getc_unlocked` refill body, whose two stream accesses go
    from `*(uint1 **)&amp;a0-&gt;field_0x8` to `a0-&gt;_IO_read_ptr` (#10, #11), and a
    `stat` buffer read, which goes from a cast at an offset to `a1-&gt;st_size`
    (#12, #13) and takes the field's SIGNED type with it, so the `-1` sentinel
    stops printing as `0xffffffffffffffff` (#14).

    The script restores the default (`opaque`) in its last command: the gate is a
    process environment variable, and the corpus runs in one process, so a stale
    `off` would silently disable the table for every later test in the file.

    Assertions span the combined output of all ten passes. `FILE \*` is a
    substring of `_IO_FILE \*`, so every stream assertion pins the text from the
    return type or the `(` onwards.

    Passes 6-9 are the stdio STREAM slots: `libctypes_streams_x86_64` and the
    shared object built from the same source, each once `off` and once `opaque`.
    The two link kinds bind a stream differently and their slots hold different
    things  -  the executable gets an `R_X86_64_COPY` relocation and a `.bss` word
    holding libc's own `FILE *stdout`, the shared object an `R_X86_64_GLOB_DAT`
    relocation on an UNDEFINED `stdout` and a GOT word holding its ADDRESS. So
    the exe slot is named `stdout` and the library's `stdout_ptr`, and the
    library's accesses carry the extra indirection the program really performs.
    The library is loaded by address because its four functions are exported and
    each name also resolves to a PLT stub.

    `pending` is the witness: it reads two fields of the stream and calls
    nothing, so the relocation is the only thing in the image that says what the
    word holds (#16/#17 off, #18/#19 on). The other three are the wrong-output
    controls. `flush_mark` stores THROUGH the stream pointer and must still store
    to the same address (#20/#21); `hand_off` reads the stream OBJECT into one of
    the image's own functions and must still read it whole  -  `sink(stdin)`,
    never `sink(*stdin)` (#22/#23); `say` passes it to a libc call whose
    prototype already named it, which must not move at all (#24), and whose
    library form only loses the cast the untyped slot needed (#25).

    Pass 10 is the option interaction: `datasyms off` restores the raw
    `dat_<addr>` rendering for every global the DWARF pass does not name, which is
    that option's whole contract, so a stream slot stays unnamed there too and the
    typed symbol has nothing to install onto (#26). It runs last and restores
    `datasyms on`, because the corpus runs in one process.

    Passes 11 and 12 are the second round of names, on `libctypes_obstack_x86_64`
    (no debug info, so every name in it is MINTED). `grow` is the one channel
    that is new in kind: `_obstack_newchunk` is DEFINED by the image rather than
    imported  -  which is how gnulib's obstack reaches every coreutils, grep and
    tar binary  -  and it is the only thing in the file that says what `grow`'s
    pointer addresses (#27, #28). `emit` is the ordinary import channel with two
    named slots at once (#29, #30). Neither body reads a field, so #15 does not
    move.

    #6 is narrowed to the two aggregates a DWARF definition already described
    (`st`, `f`), because pass 4 is the `opaque` arm and legitimately prints
    `a0-&gt;field_0x8`. #15 pins the count of LINES carrying an offset-named field
    the same way: four in pass 4 and four in the two `opaque` stream passes (one
    `pending` line and one `flush_mark` line per link kind), so a new fallback in
    any pass moves it.
-->
<bytechunk space="ram" offset="0x100000" readonly="true">
c3
</bytechunk>
<symbol space="ram" offset="0x100000" name="harness_stub"/>
</binaryimage>
<script>
  <com>option libctypes off</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_stat_x86_64</com>
  <com>read symbols</com>
  <com>load function fclose</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function fopen</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function mode_of</com>
  <com>decompile</com>
  <com>print C</com>
  <com>option libctypes opaque</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_stat_x86_64</com>
  <com>read symbols</com>
  <com>load function fclose</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function fopen</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function mode_of</com>
  <com>decompile</com>
  <com>print C</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/regglobal_fmt_x86_64</com>
  <com>read symbols</com>
  <com>load function fclose</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function get_prefix</com>
  <com>decompile</com>
  <com>print C</com>
  <com>option libctypes opaque</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_glibc_x86_64</com>
  <com>read symbols</com>
  <com>load function next_byte</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function size_of</com>
  <com>decompile</com>
  <com>print C</com>
  <com>option libctypes glibc</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_glibc_x86_64</com>
  <com>read symbols</com>
  <com>load function next_byte</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function size_of</com>
  <com>decompile</com>
  <com>print C</com>
  <com>option libctypes off</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_streams_x86_64</com>
  <com>read symbols</com>
  <com>load function pending</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function flush_mark</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function hand_off</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function say</com>
  <com>decompile</com>
  <com>print C</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_streams_so_x86_64.so</com>
  <com>read symbols</com>
  <com>load function 0x11b9</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function 0x11d0</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function 0x11f1</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function 0x120d</com>
  <com>decompile</com>
  <com>print C</com>
  <com>option libctypes opaque</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_streams_x86_64</com>
  <com>read symbols</com>
  <com>load function pending</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function flush_mark</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function hand_off</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function say</com>
  <com>decompile</com>
  <com>print C</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_streams_so_x86_64.so</com>
  <com>read symbols</com>
  <com>load function 0x11b9</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function 0x11d0</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function 0x11f1</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function 0x120d</com>
  <com>decompile</com>
  <com>print C</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_streams_x86_64</com>
  <com>option datasyms off</com>
  <com>read symbols</com>
  <com>load function pending</com>
  <com>decompile</com>
  <com>print C</com>
  <com>option datasyms on</com>
  <com>option libctypes off</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_obstack_x86_64</com>
  <com>read symbols</com>
  <com>load function grow</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function emit</com>
  <com>decompile</com>
  <com>print C</com>
  <com>option libctypes opaque</com>
  <com>clear architecture</com>
  <com>load file decompiler/crates/kuna-analysis/tests/fixtures/libctypes_obstack_x86_64</com>
  <com>read symbols</com>
  <com>load function grow</com>
  <com>decompile</com>
  <com>print C</com>
  <com>load function emit</com>
  <com>decompile</com>
  <com>print C</com>
  <com>quit</com>
</script>
<stringmatch name="LIBCTYPES #1: off, the stream parameter is an opaque void *" min="1" max="1">int4 fclose\(void \*a0\)</stringmatch>
<stringmatch name="LIBCTYPES #2: on, the same parameter is a minted FILE *" min="1" max="1">int4 fclose\(FILE \*a0\)</stringmatch>
<stringmatch name="LIBCTYPES #3: off, the fopen result is an opaque void *" min="1" max="1">void \* fopen\(char \*a0,char \*a1\)</stringmatch>
<stringmatch name="LIBCTYPES #4: on, the fopen result is a minted FILE *" min="1" max="1">FILE \* fopen\(char \*a0,char \*a1\)</stringmatch>
<stringmatch name="LIBCTYPES #5: the DWARF layout of `stat` reads the same in both passes -- the named table adopted it instead of shadowing it" min="2" max="2">st-&gt;st_mode</stringmatch>
<stringmatch name="LIBCTYPES #6: and neither aggregate a DWARF definition already described fell back to an offset-named field" min="0" max="0">(st|f)-&gt;field_0x</stringmatch>
<stringmatch name="LIBCTYPES #7: on a binary whose DWARF DOES define the stream type, that definition is what the table points at" min="1" max="1">int4 fclose\(_IO_FILE \*a0\)</stringmatch>
<stringmatch name="LIBCTYPES #8: including the parameter the whole option exists for" min="1" max="1">int4 get_prefix\(_IO_FILE \*f\)</stringmatch>
<stringmatch name="LIBCTYPES #9: with its real field layout, not a shell" min="1" max="1">f-&gt;_IO_read_end &lt;= v4</stringmatch>
<stringmatch name="LIBCTYPES #10: glibc off, a MINTED stream shell has no members, so the refill body reads an offset" min="2" max="2">\*\(uint1 \*\*\)&amp;a0-&gt;field_0x8</stringmatch>
<stringmatch name="LIBCTYPES #11: glibc on, the same two accesses are the published field" min="2" max="2">a0-&gt;_IO_read_ptr</stringmatch>
<stringmatch name="LIBCTYPES #12: glibc off, a MINTED stat shell reads its size through a cast at an offset" min="1" max="1">&amp;a1-&gt;field_0x30</stringmatch>
<stringmatch name="LIBCTYPES #13: glibc on, that read is the field" min="1" max="1">return a1-&gt;st_size;</stringmatch>
<stringmatch name="LIBCTYPES #14: and the value carries the FIELD's type, so the sentinel is a signed -1" min="1" max="1">return -1;</stringmatch>
<stringmatch name="LIBCTYPES #15: and the lines carrying an offset-named field anywhere in this file are exactly the four of pass 4 plus the four of the opaque stream passes" min="8" max="8">field_0x</stringmatch>
<stringmatch name="LIBCTYPES #16: off, the copy-relocated stream slot is an untyped word and its fields are raw offsets" min="1" max="1">return \*\(int8 \*\)\(stdout \+ 0x28\) - \*\(int8 \*\)\(stdout \+ 0x20\);</stringmatch>
<stringmatch name="LIBCTYPES #17: off, the GOT slot of a shared object is not even named" min="1" max="1">return \*\(int8 \*\)\(\*dat_3fd8 \+ 0x28\) - \*\(int8 \*\)\(\*dat_3fd8 \+ 0x20\);</stringmatch>
<stringmatch name="LIBCTYPES #18: on, the copy-relocated slot is the stream itself, so the reads are fields of it" min="1" max="1">return \*\(int8 \*\)&amp;stdout-&gt;field_0x28 - \*\(int8 \*\)&amp;stdout-&gt;field_0x20;</stringmatch>
<stringmatch name="LIBCTYPES #19: on, the GOT slot holds the stream's ADDRESS, and the name and the extra indirection both say so" min="1" max="1">return \*\(int8 \*\)&amp;\(\*stdout_ptr\)-&gt;field_0x28 - \*\(int8 \*\)&amp;\(\*stdout_ptr\)-&gt;field_0x20;</stringmatch>
<stringmatch name="LIBCTYPES #20: a store THROUGH the stream pointer still stores to the same address" min="1" max="1">\*\(unsigned long \*\)&amp;stdout-&gt;field_0x28 = a0;</stringmatch>
<stringmatch name="LIBCTYPES #21: and so does the GOT form" min="1" max="1">\*\(unsigned long \*\)&amp;\(\*stdout_ptr\)-&gt;field_0x28 = a0;</stringmatch>
<stringmatch name="LIBCTYPES #22: a read of the stream OBJECT stays the object -- `sink\(stdin\)`, never `sink\(\*stdin\)`" min="2" max="2">sink\(stdin\);</stringmatch>
<stringmatch name="LIBCTYPES #23: and in the library it is the one indirection the program performs, no more" min="1" max="1">sink\(\*stdin_ptr\);</stringmatch>
<stringmatch name="LIBCTYPES #24: a stream handed to a libc call the table already typed does not move" min="2" max="2">fputs\(a0,stdout\);</stringmatch>
<stringmatch name="LIBCTYPES #25: and the library's loses the cast the untyped slot needed" min="1" max="1">fputs\(a0,\*stdout_ptr\);</stringmatch>
<stringmatch name="LIBCTYPES #26: `datasyms off` still restores the raw address -- naming a data object stays that option's call" min="1" max="1">return \*\(int8 \*\)\(dat_4010 \+ 0x28\) - \*\(int8 \*\)\(dat_4010 \+ 0x20\);</stringmatch>
<stringmatch name="LIBCTYPES #27: off, an obstack entry point the image DEFINES says nothing, so its caller has neither arity nor a pointee" min="1" max="1">void grow\(void\)</stringmatch>
<stringmatch name="LIBCTYPES #28: on, that same definition -- gnulib's copy, linked in and exported -- names the pointer its caller holds" min="1" max="1">void grow\(obstack \*a0,uint8 a1\)</stringmatch>
<stringmatch name="LIBCTYPES #29: off, the imported `putspent` leaves both of its aggregate slots unsaid" min="1" max="1">void emit\(void\)</stringmatch>
<stringmatch name="LIBCTYPES #30: on, its declaration names the shadow record and the stream it is written to" min="1" max="1">void emit\(spwd \*a0,FILE \*a1\)</stringmatch>
</decompilertest>
