{
  "schema": "re-probe/1",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-project",
    "{{BIN}}",
    "-o",
    "{{TMP}}/proj",
    "--max-fn-seconds",
    "0",
    "--jobs",
    "2",
    "--jobs-chunk",
    "1"
  ],
  "cwd": "{{TMP}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 180,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/structsynthchain_x86_64",
    "binary_sha256": "6dd4ba2be931f9ee31ba0d636613aad25cf7e40646c2e2df0ade0dbb740501c7",
    "binary_size": 15792,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/structsynthchain_x86_64",
    "selector": null,
    "selector_kind": "none"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "wrote .*proj: structsynthchain_x86_64\\.c \\(\\d+ bytes\\), structsynthchain_x86_64\\.h"
    ],
    "stderr_matches": [
      "note: --jobs decompiles without the callee-first order \\(option protoorder, on by default\\)",
      "\\[kuna --jobs\\] structsynth: \\d+ function\\(s\\) with synthesized structures named as --jobs 1 --option protoorder off names them: \\d+ renamed, \\d+ decompile\\(s\\) again"
    ],
    "stderr_absent": [
      "worker shards recovered different user-defined types",
      "structsynth is off",
      "\\[kuna --jobs\\] note:"
    ]
  },
  "notes": "Workers used to number their own struct_N and the merged .h tore definitions apart. Now each gets the serial names and the workers rendering the .h hold the same replayed structures, so the merge warning never fires. That serial run is `--jobs 1 --option protoorder off`. Fixture moved off i386_pie_nl: libctypes names its one structure re_pattern_buffer."
}
