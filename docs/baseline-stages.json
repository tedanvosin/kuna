{
  "data_footer": [
    1262,
    1262
  ],
  "passing": [
    "data:ALIASOVERLAP #1: the 4-byte read at a0+7 is a local ahead of a store into it",
    "data:ALIASOVERLAP #2: the 8-byte indexed read is a local ahead of the element store",
    "data:ALIASOVERLAP #3: only the adjacent-byte stores keep the read folded",
    "data:ALIASOVERLAP #4: the adjacent-element store keeps the read folded",
    "data:ALIASOVERLAP #5: splitstore reads s+7 whole before the byte store into it",
    "data:ALIASOVERLAP #6: splitother reads s+7 whole before the store through t",
    "data:ALIASOVERLAP #7: splitcall reads s+7 whole before the call",
    "data:ALIASOVERLAP #8: with nothing in between, splitplain still splits into the return register",
    "data:ARRAYCOVERWIDTH #1: the vector zero-init of bank A reports sixteen bytes, not element zero",
    "data:ARRAYCOVERWIDTH #2: and so does bank B's",
    "data:ARRAYCOVERWIDTH #3: the swap's first leg carries the transfer width on both sides",
    "data:ARRAYCOVERWIDTH #4: a genuine one-byte in-element read KEEPS its subscript, in both passes",
    "data:ARRAYCOVERWIDTH #5: arraycoverwidth off restores the size-blind element-zero form (the filed bug), exactly once",
    "data:ARRAYSUBFIELD #1: an 8-byte read at offset 0 of char[16] is a sized sub-field, not element 0",
    "data:ARRAYSUBFIELD #2: an 8-byte read at offset 8 is a sized sub-field, not the ninth byte",
    "data:ARRAYSUBFIELD #3: the bug: no size-blind element subscript survives on either wide read",
    "data:ARRAYSUBFIELD #4: a genuine one-byte in-element access KEEPS its subscript",
    "data:ARRAYSUBFIELD #5: the reported reproducer writes the low half of the 16-byte return container at its real width",
    "data:ARRAYSUBFIELD #6: and the high half likewise",
    "data:ARRAYSUBFIELD #7: honest about what is NOT fixed: the whole-container shift is upstream-faithful and still not valid C",
    "data:BRANCHFLIP #1: default pipeline keeps the negated `== 0` guard (opt-in is default-off)",
    "data:BRANCHFLIP #2: `option branchflip on` flips to the positive `!= 0` guard (angr-style)",
    "data:BRANCHFLIP #3: the flip is logged so its effect is observable",
    "data:BRANCHFLIP-ARMSWAP #1: the swapped guard reads positively (the flip still happens)",
    "data:BRANCHFLIP-ARMSWAP #2: the else arm opens a real block, not an else-if collapse",
    "data:BRANCHFLIP-ARMSWAP #3: the second sibling if stays inside the arm in both passes",
    "data:BRANCHFLIP-ARMSWAP #4: branchflip off is the un-flipped reference, both siblings nested",
    "data:BRANCHFLIP-ARMSWAP #5: no else-if collapse of a statement-carrying arm",
    "data:BRANCHFLIP-ARMSWAP #6: no sibling if escapes to the outer statement indent",
    "data:CALLEEPRESERVES #1: off, the value that crossed the thunk is a local nothing assigns",
    "data:CALLEEPRESERVES #2: off, f loses the parameter that value was loaded from",
    "data:CALLEEPRESERVES #3: on, EDX survives the thunk and f regains its second parameter",
    "data:CALLEEPRESERVES #4: on, the third argument is the caller's own parameter, not an undefined local",
    "data:CALLEEPRESERVES #5: both passes, the second call is rendered identically (its callee writes no unaffected register)",
    "data:CALLEEPRESERVES #6: both passes, the thunk is still called and still returns nothing",
    "data:CALLEERETPRESERVES #1: off, the zero set before the cookie check is gone and f returns nothing",
    "data:CALLEERETPRESERVES #2: on, f returns the constant the caller placed in EAX",
    "data:CALLEERETPRESERVES #3: on, and that value gives f its own return type back",
    "data:CALLEERETPRESERVES #4: both passes, the checker is still called with the same argument",
    "data:CALLEERETPRESERVES #5: both passes, an endbr64/ret stub proves nothing and g is unchanged",
    "data:CALLEERETPRESERVES #6: both passes, the stub call keeps the convention's answer",
    "data:CALLEERETPRESERVES #7: a tailcallee RAX write never preserves the caller's seven",
    "data:CALLEERETPRESERVES #8: both direct- and tailcallee RAX writers keep the call in both passes",
    "data:CALLEESCRATCHBODY #1: off, the whole frame is indexed off the value __chkstk supposedly returns",
    "data:CALLEESCRATCHBODY #2: on, the probe is called for its side effect and the frame slot is a fixed offset",
    "data:CALLEESCRATCHBODY #3: on, the slot written and read back at that offset is the argument",
    "data:CALLEESCRATCHBODY #4: both passes, an endbr64/ret stub writes no memory and proves nothing",
    "data:CALLEESCRATCHBODY #5: both passes, so g keeps the convention's answer unchanged",
    "data:CALLOVERLAP #1: off, the call is emitted with no arguments at all (the CVTSI2SD result never reaches it)",
    "data:CALLOVERLAP #2: off, f loses its own parameter list because the argument chain dies with the trial",
    "data:CALLOVERLAP #3: off, the call result is read from a local that nothing ever assigns",
    "data:CALLOVERLAP #4: full, guardCallOverlappingInput recovers the truncated XMM0 argument and tryOutputOverlapGuard the return value",
    "data:CALLOVERLAP #5: full, f regains its own parameter",
    "data:CALLOVERLAP #6: full, the epilogue adds 1.0 to the recovered call result, not to a stale pre-call read",
    "data:CALLPOPRET #1: the default pass follows the call as a branch and recovers the pointer to the inline data",
    "data:CALLPOPRET #2: the two port reads decoded at the unreachable return address appear only in the callpopret-off pass",
    "data:CALLRETPAIR #1: off, the call is a bare statement and its JSValue is discarded",
    "data:CALLRETPAIR #2: on, the call gains the 16-byte join output the cspec output rule asked for",
    "data:CALLRETPAIR #3: on, both halves are SUBPIECEs of the call output instead of locals with no definition",
    "data:CALLRETPAIR #4: the unassigned rdx local survives in three of the four renderings, and is gone only where the pair formed",
    "data:CALLRETPAIR #5: the control keeps its bare call in BOTH passes, because the bounded callee decode proves no_payload never writes rdx",
    "data:CALLRETPAIR #6: on, the recovered return stops being the garbage pointer the discarded half left behind",
    "data:CALLTRAMPOLINE #1: the junk store decoded at the unreachable return address appears only in the calltrampoline-off pass",
    "data:CALLTRAMPOLINE #2: the default pass flows through the fragment and recovers the real continuation",
    "data:CANCELBYTEARITHMETIC #1: off keeps the computed byte between the two copies",
    "data:CANCELBYTEARITHMETIC #2: off keeps the fifteen-byte prefix copy",
    "data:CANCELBYTEARITHMETIC #3: off keeps the following five-byte suffix copy",
    "data:CANCELBYTEARITHMETIC #4: on preserves every byte as one complete copy",
    "data:CLEANUPCODE #1: the drop glue call is gone by default; only the cleanupcode-off pass shows it (exactly one occurrence across both passes)",
    "data:CLEANUPCODE #2: the deallocation call is gone by default; the plain (non generic) name arm of the matcher",
    "data:CLEANUPCODE #3: the drop's argument setup dies with the call (pre-SSA seam), so the constant is only in the cleanupcode-off pass",
    "data:CLEANUPCODE #4: the deallocation's argument setup dies with the call too",
    "data:CLEANUPCODE #5: the real call is untouched in BOTH passes, so the transform is surgical",
    "data:CNORM-BRACEELIDE #1: default renders the single-statement body braceless, indented (DIV-37)",
    "data:CNORM-BRACEELIDE #2: braceelide off restores the upstream braced body",
    "data:CNORM-INPLACE #1: default renders the self-add as += (DIV-35)",
    "data:CNORM-INPLACE #2: default renders the self-and as &=",
    "data:CNORM-INPLACE #3: default folds the negative addend to -= 3",
    "data:CNORM-INPLACE #4: inplaceops off restores the spelled-out add",
    "data:CNORM-INPLACE #5: inplaceops off restores the spelled-out and",
    "data:CNORM-INPLACE #6: inplaceops off restores the raw negative addend",
    "data:CNORM-NULLPRINT #1: default renders the zero pointer as NULL (DIV-34)",
    "data:CNORM-NULLPRINT #2: nullprinting off restores the upstream casted zero",
    "data:CNORM-PROTOGAP #1: default has the brace directly under the prototype (DIV-34)",
    "data:CNORM-PROTOGAP #2: braceformat function skip restores the upstream blank line",
    "data:CNORM-TRUTHY #1: default renders the nonzero test truthy (DIV-36)",
    "data:CNORM-TRUTHY #2: default renders the null test as a negation",
    "data:CNORM-TRUTHY #3: truthycond off restores the explicit != 0",
    "data:CNORM-TRUTHY #4: truthycond off restores the explicit NULL compare (DIV-34 form)",
    "data:CNORM-TRUTHY #5: a VALUE position keeps its comparison in BOTH passes (negate-absorption safety)",
    "data:CNORM-TRUTHY #6: the value-elision bug shape never appears",
    "data:CNORM-WARNSTYLE #1: default renders the terse end-of-line slug (DIV-38)",
    "data:CNORM-WARNSTYLE #2: warnstyle banner restores the upstream WARNING line",
    "data:CNORM-WARNSTYLE #3: banner WARNINGs appear only in the banner pass (indirect-jump + read-only)",
    "data:CNORM-WARNSTYLE #4: a condition-block warning rides to the header flush point after the brace",
    "data:CNORM-WARNSTYLE #5: the slug never fires inside the comma-separated header parens",
    "data:CONSTSELECTJUMP #1: the selected destination renders as a computed call (bug reproduced)",
    "data:CONSTSELECTJUMP #2: constselectjump on recovers the constant destination set as a switch",
    "data:CONSTSELECTJUMP #3: both arms behind the branch are decoded",
    "data:CORTEXMPRIV #1: OFF leaves the privilege user-op in the emitted C (pass 1 only)",
    "data:CORTEXMPRIV #2: OFF turns a straight-line special-register read into a branch (pass 1 only)",
    "data:CORTEXMPRIV #3: the real effect survives in BOTH passes",
    "data:CORTEXMPRIV #4: ON folds the guard away, leaving one unconditional read (pass 2 only)",
    "data:CTYPES #1: off, the return type is the Ghidra core-type name",
    "data:CTYPES #2: on, the same type spells as C",
    "data:CTYPES #3: off, an 8-byte unsigned cast is uint8",
    "data:CTYPES #4: on, LP64 spells an 8-byte unsigned `unsigned long`, not `unsigned long long`",
    "data:CTYPES #5: on, the 4-byte parameter is `unsigned int`",
    "data:CTYPES #6: off, that parameter carries the core-type name",
    "data:DECLARING-DOUBLE-SCORE #1: option off discards the log result",
    "data:DECLARING-DOUBLE-SCORE #2: default assigns the log result",
    "data:DECLARING-DOUBLE-SCORE #3: default returns both additive doubles",
    "data:DECLARING-DOUBLE-SCORE #4: preserving XMM0 does not remove any cookie call",
    "data:DECLARING-DOUBLE-SCORE #5: a body write vetoes preservation of the pre-call XMM0 value",
    "data:DECLARING-DOUBLE-SCORE #6: an exact cookie marker yields to a recovered XMM0 body write",
    "data:DECLARING-DOUBLE-SCORE #7: a wide XMM0 range retains the exact low double",
    "data:DECLARING-DOUBLE-SCORE #8: the overlapping upper XMM0 scratch lane remains killed",
    "data:DECODEHALT #1: off, the undecodable path leaves no statement at all and the prototype says nothing",
    "data:DECODEHALT #2: on, the halt names the decode failure and carries the truncation warning",
    "data:DECODEHALT #3: on, the function header says the body is incomplete",
    "data:DECODEHALT #4: exactly one of the two passes renders the pseudo-call",
    "data:DWARFVARIANTS #10: no variant name is ever synthesized",
    "data:DWARFVARIANTS #11: off, an overlaying Result payload is a raw byte offset",
    "data:DWARFVARIANTS #12: on, the overlaying Result keeps its recovered tag field",
    "data:DWARFVARIANTS #13: on, and its payload is spelled by OFFSET because Ok and Err both claim it",
    "data:DWARFVARIANTS #14: neither variant of the overlaying Result is ever named",
    "data:DWARFVARIANTS #15: on, an Option has exactly one payload-carrying variant, so Some is FORCED and survives",
    "data:DWARFVARIANTS #16: the suppression does not give back the phantom sret: the source signature still comes out",
    "data:DWARFVARIANTS #1: off, a field-less enum return is misclassified as a hidden-return buffer",
    "data:DWARFVARIANTS #2: on, the recovered layout gives the classifier a width and the source signature comes back",
    "data:DWARFVARIANTS #3: off, the discriminant is a raw byte offset",
    "data:DWARFVARIANTS #4: on, the discriminant is the named tag field DW_AT_discr pointed at",
    "data:DWARFVARIANTS #5: off, a variant payload is a raw byte offset",
    "data:DWARFVARIANTS #6: on, variant A's payload carries the SOURCE variant name, not a synthesized one",
    "data:DWARFVARIANTS #7: on, and so does variant B, so three variants are not two",
    "data:DWARFVARIANTS #8: a plain C-shaped struct in the same image renders identically in BOTH passes",
    "data:DWARFVARIANTS #9: and so does a fieldless enum, which rustc emits as an enumeration type this pass never sees",
    "data:ELFMAIN #1: off, the entry is an anonymous sub with a body-driven signature",
    "data:ELFMAIN #2: off, the argument vector has no element type, so the string it holds is an integer",
    "data:ELFMAIN #3: default, the libc-start argument is named and declared, in both images",
    "data:ELFMAIN #4: default, the declared element type reaches the call site",
    "data:ELFMAIN #5: off, an argument register past the third is recovered as a parameter",
    "data:ELFMAIN #6: off, the fabricated slots are what the body forwards",
    "data:ELFMAIN #7: default, the stored register beyond the third becomes a local nothing assigns",
    "data:ELFMAIN #8: default, the forwarded register beyond the third is dropped from the call",
    "data:EMPTYSTRCONST #1: the blob pointer keeps its address instead of becoming an empty literal",
    "data:EMPTYSTRCONST #2: an ordinary short literal beside it is untouched, in both passes",
    "data:EMPTYSTRCONST #3: a GENUINE empty literal, one that sits inside a run of string data, keeps its quotes in both passes",
    "data:EMPTYSTRCONST #4: emptystrconst off restores the empty-literal form for the blob (the filed bug), exactly once",
    "data:ENDPTRBOUND #10: both passes, a byte walk over an int array keeps the int array",
    "data:ENDPTRBOUND #11: both passes, that array keeps its int indexing",
    "data:ENDPTRBOUND #12: on, the byte walk's bound is rebuilt on the int array",
    "data:ENDPTRBOUND #1: off, the loop bound is the object that follows the buffer",
    "data:ENDPTRBOUND #2: off, the walking pointer is typed after that neighbour",
    "data:ENDPTRBOUND #3: on, the walked bytes are one 8-byte buffer",
    "data:ENDPTRBOUND #4: on, the bound is one past that buffer",
    "data:ENDPTRBOUND #5: on, the call argument that means the neighbour still names it",
    "data:ENDPTRBOUND #6: both passes, a store straddling the walk's end vetoes the buffer",
    "data:ENDPTRBOUND #7: both passes, a comparison that controls no loop exit keeps naming the neighbour",
    "data:ENDPTRBOUND #8: both passes, that comparison invents no buffer",
    "data:ENDPTRBOUND #9: both passes, a walk that reads past its end is not bounded on a buffer",
    "data:ENTRYRETDISPATCH #1: the default pass recovers the helper call while option off does not",
    "data:ENTRYRETDISPATCH #2: both passes retain one ordinary function return",
    "data:ENTRYTHUMBFLOW #1: off, the Thumb entry decodes as A32 and returns nothing",
    "data:ENTRYTHUMBFLOW #2: default, the entry is walked as Thumb and returns its constant",
    "data:ENTRYTHUMBFLOW #3: the constant the Thumb body returns appears exactly once, in the default pass",
    "data:EVALCURRENTPROTO #1: off, the __stdcall default model sees only the stack argument",
    "data:EVALCURRENTPROTO #2: off, the ECX argument survives as an undefined local",
    "data:EVALCURRENTPROTO #3: default, the spec's merged model resolves to __fastcall and recovers all three arguments",
    "data:EXCLUSIVEARGUSE #1: off, the slow-path call has an empty argument list",
    "data:EXCLUSIVEARGUSE #2: on, the write cursor and the source byte both reach the call",
    "data:EXCLUSIVEARGUSE #3: the loop control keeps its one argument in BOTH passes, so a back edge still counts the dereference as competing",
    "data:EXCLUSIVEARGUSE #4: append's fast path still writes the byte it read in BOTH passes, so only the argument list moved",
    "data:EXCLUSIVEARGUSE #5: the loop control's fast path is untouched in BOTH passes",
    "data:EXPLICIT-DOUBLE-SCORE #1: option off exposes the unassigned refined high return cell",
    "data:EXPLICIT-DOUBLE-SCORE #2: option on returns the complete double division",
    "data:EXPLICIT-DOUBLE-SCORE #3: an unlocked inferred output does not gain the unjustified cell",
    "data:FAST-FUNCDISC #1: loader-known entry is present in both inventories",
    "data:FAST-FUNCDISC #2: direct callee is absent off and present on",
    "data:FAST-FUNCDISC #3: the newly inventoried callee has a real decompiled body",
    "data:FASTFAILNORETURN #1: the parameter is returned directly once the interrupt ends the flow (default pass only)",
    "data:FASTFAILNORETURN #2: the unbalanced stack pointer becomes a tracked variable only in the fastfailnoreturn-off pass",
    "data:FASTFAILNORETURN #3: the parameter's own stack slot degenerates into an offset off that variable only in the fastfailnoreturn-off pass",
    "data:FLOATGROUPING #1: the right-grouped product keeps its grouping",
    "data:FLOATGROUPING #2: exactly one of the two products prints as a flat chain",
    "data:FLOATGROUPING #3: the right-grouped sum keeps its grouping",
    "data:FLOATGROUPING #4: exactly one of the two sums prints as a flat chain",
    "data:FMTSTATIC #10: ARM hard-float keeps the int parameter a %f prints",
    "data:FMTSTATIC #11: ARM hard-float prints the converted value",
    "data:FMTSTATIC #12: ARM hard-float prints a + b, not d0",
    "data:FMTSTATIC #13: full still types an ARM integer-only site",
    "data:FMTSTATIC #14: a format in writable data is not read",
    "data:FMTSTATIC #15: a %lc argument is not narrowed to char",
    "data:FMTSTATIC #16: x86-64 %lf passes the double and the int, no phantom parameter",
    "data:FMTSTATIC #17: x86-64 %lf prints the double",
    "data:FMTSTATIC #18: x86-64 %lf after an int keeps both arguments",
    "data:FMTSTATIC #19: scanf %lf stores a double, under static on x86-64 and full on both targets",
    "data:FMTSTATIC #1: off, and the default on AArch64, widen the %d argument to the register width",
    "data:FMTSTATIC #20: x86-64 scanf %lf returns the double it read",
    "data:FMTSTATIC #21: ARM hard-float declines a %lf printf, as off",
    "data:FMTSTATIC #22: ARM hard-float declines %lf after an int, as off",
    "data:FMTSTATIC #23: ARM scanf %lf keeps the phantom trailing argument unless full is asked for",
    "data:FMTSTATIC #24: no %lf site is typed as an integer",
    "data:FMTSTATIC #25: Win64 %zu prints the whole size_t, not its low half",
    "data:FMTSTATIC #26: Win64 %td prints the whole ptrdiff_t, not its low half",
    "data:FMTSTATIC #27: Win64 %zu and %td keep the 8-byte parameter",
    "data:FMTSTATIC #28: the default types no ARM site, so f_is renders as off",
    "data:FMTSTATIC #29: off leaves the version line's three phantom arguments",
    "data:FMTSTATIC #2: off, and the default on AArch64, leave the parameter it came from unsigned",
    "data:FMTSTATIC #30: the default and full print the version line with its three strings",
    "data:FMTSTATIC #31: the switch calls keep the arguments off gives them",
    "data:FMTSTATIC #32: no switch call takes the va_list's slots",
    "data:FMTSTATIC #33: a scanf destination in the outgoing argument slot is read back after the call",
    "data:FMTSTATIC #34: both %c destinations are read back after the call",
    "data:FMTSTATIC #35: the default and full type the %c destinations char *",
    "data:FMTSTATIC #36: read_int does not return the 5 it stored before the call, plus one",
    "data:FMTSTATIC #37: the default and full keep show9's ninth double, passed on the stack",
    "data:FMTSTATIC #38: the default and full give show9 its doubles and int",
    "data:FMTSTATIC #3: static on x86-64 and full on both targets type the varargs per the specifiers",
    "data:FMTSTATIC #4: static on x86-64 and full on both targets recover the %d parameter as int",
    "data:FMTSTATIC #5: a jump-table join keeps the parameter the %s argument came from",
    "data:FMTSTATIC #6: a jump-table join keeps both varargs",
    "data:FMTSTATIC #7: an alloca frame gains no argument past the format's one",
    "data:FMTSTATIC #8: Apple arm64 prints the computed value",
    "data:FMTSTATIC #9: Apple arm64 gains no phantom parameter",
    "data:FUNCBOUNDFLOW #1: default does NOT merge neighbor; only the funcboundflow-off pass shows its body (exactly one occurrence across both passes)",
    "data:FUNCBOUNDFLOW #2: both passes resolve the unnamed no-return call to fatal(), so the sole difference is the boundary truncation",
    "data:FUNCPTRALIGN #1: the SLEIGH ISA-mode bit-clear never reaches the emitted C",
    "data:FUNCPTRALIGN #2: the callee field recovers as code ** and the call needs no cast",
    "data:GH1243 #1: ADDC carry-out preserved, P1 = 2",
    "data:GH1243 #2: buggy truncated-carry result P1 = 0 absent",
    "data:GH1243 #3: already-correct case still holds, P0 = 1",
    "data:GH1276 #1: default pipeline keeps the (bool) << 7 s< 0 sign-bit idiom (bug reproduces, appears once)",
    "data:GH1276 #2: option flagcompare on folds it to a clean comparison (fix, appears once)",
    "data:GH1282 #1: booleanmask off restores the (bool << 7) >> 7 shift noise (old behavior, opt-in)",
    "data:GH1282 #2: default pipeline folds (b<<7)s>>7 (fix is DIV-2 default)",
    "data:GH1537 #1: memsetrecover off restores the element-store form (old behavior, opt-in)",
    "data:GH1537 #2: default pipeline reconstructs the single memset (fix is DIV-2 default; routing via the GH-9230 sub-phase)",
    "data:GH180 #1: the signed guard reads the PRE-add operands and the sum is a separate later expression",
    "data:GH180 #2: exactly one SCARRY8 in the transcript (the jo guard)",
    "data:GH180 #3: the collapsed self-update-then-test rendering does not appear (compound form)",
    "data:GH180 #4: the collapsed self-update-then-test rendering does not appear (the issue's literal shape)",
    "data:GH180 #5: the unsigned guard reads the PRE-add operands and the sum is a separate later expression",
    "data:GH180 #6: exactly one standalone CARRY8 in the transcript (the jb guard)",
    "data:GH180 #7: the unsigned collapse does not appear (compound form)",
    "data:GH180 #8: the unsigned collapse does not appear (plain form)",
    "data:GH1951 #1: rcall disassembles to the in-range 4KiW-wrapped target 0x100e",
    "data:GH1951 #2: lifted call targets the masked in-range word 0x0807",
    "data:GH1951 #3: the unmasked out-of-range word 0xf807 must NOT appear",
    "data:GH1951 #4: rcall must NOT render only as the bare relative operand -0xff2",
    "data:GH271 #1: the 15-byte alignment NOP decodes at 0x100d",
    "data:GH271 #2: the stream realigns onto the loop head at 0x101c (the NOP consumed exactly 15 bytes)",
    "data:GH271 #3: flow is not truncated at the NOP, so the loop past the padding survives",
    "data:GH271 #4: the store past the padding survives (it went missing with the rest of the body)",
    "data:GH271 #5: the early-exit guard keeps the JNC sense, not the inverted form the truncated CFG structured to",
    "data:GH275 #1: default (spillargtrial off) drops the first call's second argument to the caller-save spill",
    "data:GH275 #2: spillargtrial reload recovers the first call's second argument",
    "data:GH275 #3: spillargtrial reload binds the second call's argument to the register, not a spurious stack local",
    "data:GH2786 #1: nested float negation parenthesized (default-flip fix)",
    "data:GH2786 #2: the mis-parsing predecrement token is gone",
    "data:GH403 #1: the truncated conditional branch never renders the condition-less `} while ;`",
    "data:GH403 #2: the self-looping truncation still decompiles (both passes carry the loop)",
    "data:GH403 #3: the default pass truncates, so neighbor's body appears only in the funcboundflow-off pass",
    "data:GH403 #4: the conditional's surviving arm is unchanged by the truncation (both passes)",
    "data:GH4788 #1: disp17 Bcond decodes as bnc to 0x1963d8 (not ld.hu)",
    "data:GH4788 #2: the bytes are no longer mis-decoded as ld.hu",
    "data:GH510 #1: the function is decompiled instead of abandoned (no LOSS-131 record)",
    "data:GH510 #2: no parameterless error stub survives",
    "data:GH510 #3: the record loop is recovered",
    "data:GH510 #4: the first helper call is recovered",
    "data:GH510 #5: the second helper call is recovered",
    "data:GH558 #1: compareform canonical restores upstream x < 9 (old behavior, opt-in)",
    "data:GH558 #2: default pipeline keeps the source form x <= 8 (fix is DIV-2 default)",
    "data:GH558 #3: arraynotation off restores upstream pointer arithmetic (old behavior, opt-in)",
    "data:GH558 #4: default pipeline renders &p->r[i] (fix is DIV-2 default)",
    "data:GH5897 #1: reg-to-reg MOV renders UAL 'mov r12,r7'",
    "data:GH5897 #2: deprecated pre-UAL 'cpy' mnemonic is gone",
    "data:GH6389 #10: no undecoded bad bytes remain",
    "data:GH6389 #1: DSUB 0x08 decodes",
    "data:GH6389 #2: ARHL 0x10 decodes",
    "data:GH6389 #3: RDEL 0x18 decodes",
    "data:GH6389 #4: LDSI 0x38 decodes with imm8 operand",
    "data:GH6389 #5: SHLX 0xD9 decodes",
    "data:GH6389 #6: LHLX 0xED decodes",
    "data:GH6389 #7: RSTV 0xCB decodes",
    "data:GH6389 #8: JNK 0xDD decodes with addr16 operand",
    "data:GH6389 #9: JK 0xFD decodes with addr16 operand",
    "data:GH657 #1: both calls keep a statement of their own, ahead of the write to the global they read",
    "data:GH657 #2: the write to the global is emitted after that statement in both",
    "data:GH657 #3: target reads the spilled result, it does not re-evaluate the call",
    "data:GH657 #4: target2 does the same although its use is implied and the call would travel to the return",
    "data:GH657 #5: the folded form of the issue is never emitted",
    "data:GH657 #6: nor the folded form of the travelling case",
    "data:GH684 #1: the call keeps its own statement (gcc -O0 w1f and w4f, clang -O2 w4f)",
    "data:GH684 #2: gcc -O0 w1f reads the spill on the right of the or-or",
    "data:GH684 #3: gcc -O0 w4f reads the spill on the right of the and-and",
    "data:GH684 #4: clang -O0 w1f still folds the call on the left of the or-or",
    "data:GH684 #5: clang -O0 w4f still folds the call on the left of the and-and",
    "data:GH684 #6: clang -O2 w4f reads the spill on the right of the and-and",
    "data:GH684 #7: the call is never printed on the right of an or-or",
    "data:GH684 #8: the call is never printed on the right of an and-and",
    "data:GH6882 #1: post-call unimp renders as a computed call (bug reproduced)",
    "data:GH6882 #2: sparcstructret on recovers the function tail (the real return expression)",
    "data:GH6904 #1: r3/ptr compared as a plain 32-bit pointer (no widening)",
    "data:GH6904 #2: no spurious CONCAT44 over the 64-bit r3 parameter",
    "data:GH6904 #3: no spurious CONCAT44 widening of ptr at all",
    "data:GH6930 #1: inferfuncentry off restores the bare-literal form (old behavior, opt-in)",
    "data:GH6930 #2: default pipeline resolves 0x100000 to its function name (fix is DIV-2 default)",
    "data:GH6990 #1: the passive o1 join is dropped in BOTH passes (default already recovers the single-register return)",
    "data:GH6990 #2: no CONCAT44 join survives anywhere",
    "data:GH7139 #1: 16-bit disp16 rendered signed (BX + -0x804)",
    "data:GH7139 #2: the unsigned 0xf7fc displacement no longer appears",
    "data:GH7190 #1: ovlesssimplify off restores the OV-flag garbage condition (old behavior; 0<= form via DIV-2 compareform)",
    "data:GH7190 #2: default pipeline collapses to a clean signed byte compare (fix is DIV-2 default)",
    "data:GH7418 #1: CALL rel16 target masks EIP to low 16 bits (0x7004)",
    "data:GH7418 #2: the unmasked 0x17004 target no longer appears",
    "data:GH7451 #1: fli.s decodes (dest fa4, Zfa constant 0.25)",
    "data:GH7451 #2: no longer an unresolved constructor",
    "data:GH7890 #1: vcvt lifts to native INT2FLOAT (no pseudo-op)",
    "data:GH7890 #1b: clean human-readable float cast in C",
    "data:GH7890 #2: no VectorSignedToFloat pseudo-op remains",
    "data:GH7890 #3: no fpscr rounding-mode shift/read",
    "data:GH7890 #4: no unbound in_fpscr artifact in the C output",
    "data:GH8017 #1: default pipeline leaves the &pxVar2[-0x1000] probe-loop noise (bug reproduces)",
    "data:GH8017 #2: option stackprobeloop on removes all page noise (3 lines in default + 0 in fix = 3)",
    "data:GH8391 #1: Thumb mrrc2 decodes under v8-m (CDE)",
    "data:GH8391 #2: bytes no longer fail to decode",
    "data:GH8456 #1: MRS (Banked) decodes to 'mrs r0,r9_fiq'",
    "data:GH8456 #2: bytes no longer fail to decode",
    "data:GH8467 #1: dynamichashmax on lets the function decompile to a full body (fix)",
    "data:GH8467 #2: recovered control flow reaches the tail block (no abort)",
    "data:GH8471 #1: thumbfuncptr off restores the raw-hex form (old behavior, opt-in)",
    "data:GH8471 #2: default pipeline preserves the symbolic form, &fn[1] via DIV-2 arraynotation (fix is DIV-2 default)",
    "data:GH8500 #1: heritage deadcode-delay restart converges; no uninitialized stack local survives (bug fixed)",
    "data:GH8500 #2: both passes recover the store-through-pointer and return the parameter (deadcode-delay fix, option subsumed)",
    "data:GH8724 #1: default pipeline leaves the raw strided accumulator (+ 0x414), index lost",
    "data:GH8724 #2: option arraystride on recovers the array index as counter * 0x414",
    "data:GH8777 #1: default pipeline keeps the raw SBORROW2 N==V flag idiom (bug reproduces)",
    "data:GH8777 #2: option flagcompare on folds it to a clean signed compare (fix, no SBORROW2 left)",
    "data:GH8790 #1: 0f0dc0 (PREFETCH reg-form) decodes at 0x1000",
    "data:GH8790 #2: 0f18c0 (prefetch/NOP-reserved reg-form) decodes at 0x1003",
    "data:GH8790 #3: stream re-aligned onto the trailing NOP at 0x1006 (both reg-forms were 3 bytes)",
    "data:GH8817 #1: V850 jmp [reg] renders as a computed call (bug reproduced)",
    "data:GH8817 #2: v850indirectbranch on recovers the switch",
    "data:GH8844 #1: rjmp disassembles to the in-range 4KiW-wrapped target 0x102e",
    "data:GH8844 #2: the wrapped goto lands at the in-range block 0x0817 (function decompiles)",
    "data:GH8844 #3: the unmasked out-of-range word 0xf817 must NOT appear",
    "data:GH8844 #4: rjmp must NOT render only as the bare relative operand -0xfd2",
    "data:GH8913 #1: addcarrychain off leaks CONCAT11((0xNN < ...) (old behavior, both paths)",
    "data:GH8913 #1b: addcarrychain off leaks raw CARRY1 inside CONCAT11 (old behavior, both paths)",
    "data:GH8913 #2: default pipeline fuses path-3 first add into a wide add (fix is DIV-2 default)",
    "data:GH8913 #2b: default pipeline fuses path-5 first add into a wide add (fix is DIV-2 default)",
    "data:GH9001 #1: BRN consumes rel8 (2-byte) so 0x02 is the next instruction, not 0x01",
    "data:GH9001 #2: the 0x05 rel8 byte is no longer mis-decoded as JMP ...,+IX",
    "data:GH9184 #1: FMA accumulator is 256-bit (char [32] array), not truncated to [16]",
    "data:GH9184 #2: no 128-bit accumulator temp (char [16] array)",
    "data:GH9184 #3: no per-iteration ZEXT1632 truncation round-trip on the FMA result",
    "data:GH9184 #4: the vfmadd231ps_fma pcodeop is still emitted (sanity)",
    "data:GH9191 #1: modulo-bounded switch renders as a computed call (bug reproduced)",
    "data:GH9191 #2: switchmodbound on bounds the table and recovers the switch",
    "data:GH9203 #1: const COPY in loop body (= 0; present in default, gone after condexeplace on)",
    "data:GH9203 #2: loop structure preserved in both passes",
    "data:GH9218 #1: inputvarnodeadjust on recovers a single merged input parameter (no abort)",
    "data:GH9218 #2: overlapping stack inputs become SUBPIECEs of the new input (param_1._2_4_)",
    "data:GH9218 #3: the low-level abort no longer fires under the fix",
    "data:GH9230 #1: memsetrecover off restores the element-store form (old behavior, opt-in)",
    "data:GH9230 #2: default pipeline collapses the zero-fill into builtin_memset (fix is DIV-2 default)",
    "data:GHANGR-NORETURN-ERROR #1: option noreturn_error off parses and round-trips, and is inert on the XML path (no Listing built): the source form x <= 8 renders identically in both passes (default-on + flag-off parity)",
    "data:GUARDARM #1: OFF inverts the guard and nests the body one level deeper (pass 1 only)",
    "data:GUARDARM #2: ON keeps the guard flat with the fatal call as its clause (pass 2 only)",
    "data:GUARDARM #3: the no-return clause call is emitted in BOTH passes",
    "data:IMPLIEDREFS #1: at the shipped bound 2 a value with three readers is declared",
    "data:IMPLIEDREFS #2: the declared value is then read at all three uses",
    "data:IMPLIEDREFS #3: `option impliedrefs 4` drops the declaration and inlines the load at each use",
    "data:INDEXALIASGUARD #1: both passes recover the same four-word stack accumulator, so only its initialisation is in question",
    "data:INDEXALIASGUARD #2: both passes XOR it through the walking pointer, so the reads never moved",
    "data:INDEXALIASGUARD #3: both passes read the finished accumulator bit by bit",
    "data:INDEXALIASGUARD #4: load, the MOV [ESP+0x10],EAX at 0x401344 reaches element 0 (off emits no initialisation at all)",
    "data:INDEXALIASGUARD #5: load, the MOV [ESP+0x14],ECX at 0x40134d reaches element 1",
    "data:INDEXALIASGUARD #6: load, the MOV [ESP+0x20],EDX at 0x40135d reaches element 2 across the two intervening pushes",
    "data:INDEXALIASGUARD #7: load, the MOV [ESP+0x1c],EAX at 0x401355 reaches element 3",
    "data:INT3PAD #1: the four-byte pad is counted and named (warn and halt passes only)",
    "data:INT3PAD #2: the trap is the function's return value except when it ends the flow",
    "data:INT3PAD #3: int3pad halt renders the trap as a statement, not a returned call",
    "data:ISAMODE #1: no ISA-mode bookkeeping user-op survives into the emitted C",
    "data:ISAMODE #2: the four table-reachable tail-call arms are intact",
    "data:ISAMODE #3: the table-reachable f0 call sites survive the injection",
    "data:ISAMODE #4: flow still reaches the last jump-table entry",
    "data:KUNA-ASSERT #1: routed assertions take effect (compareform original + namelock rename)",
    "data:KUNA-ASSERT #2: kassert list reports the P3 config assertion with rewind scope",
    "data:KUNA-ASSERT #3: kassert list reports the cosmetic P9 assertion as HINT via HARD mechanism",
    "data:KUNA-CATALOG #10: fast whole-project discovery is default-off but live-toggleable",
    "data:KUNA-CATALOG #11: mapped ELF x86 flow recovery is default-on in P2",
    "data:KUNA-CATALOG #1: compareform documented with its post-DIV-2 default and P3 address",
    "data:KUNA-CATALOG #2: arraynotation documented as default-on, P9 pointer-notation",
    "data:KUNA-CATALOG #3: returnpair flagged destructive-as-default so an agent keeps it opt-in",
    "data:KUNA-CATALOG #4: v850indirectbranch is the other opt-in (off, destructive global)",
    "data:KUNA-CATALOG #5: each settable carries symptom-keyed use_when guidance for the operator",
    "data:KUNA-CATALOG #6: angr-derived features carry source_decompiler provenance (loweredswitch, namestyle, stackguard, branchflip, foldcallret, dedupvardecls, relocobjects, i386_pie_plt, noreturn_propagate, loopbreak_recovery, gotoreduce, crossjumprevert, taildup, dedupitetail, returndup, iteregion, iteexpr, switchguardbound, switchsharedcase, switchmultipred, unrolledguard, tailcalljump, regionstructure, regionlooprefine, regionedgeorder, ifelseflatten, noreturn_extern, noreturn_externmatch, earlyreturn, switchreturn, condfold, securitycheck, structsynth, protoorder)",
    "data:KUNA-CATALOG #7: change_kind structure-recovery tags the switch/array/loader/loop/tail-call reconstructors (loweredswitch, switchselector, constselectjump, arraystride, relocobjects, noreturn_propagate, loopbreak_recovery, gotoreduce, crossjumprevert, taildup, dedupitetail, returndup, iteregion, iteexpr, tailcalljump, regionstructure, regionlooprefine, regionedgeorder, ifelseflatten, earlyreturn, switchreturn, condfold, iteboolean, paramcopyhoist, itecondlist, guardarm, loopcondhoist, orchain, endptrbound, loweredswitchheads, structsynth)",
    "data:KUNA-CATALOG #8: the opt-in-tool change_kind aligns with the destructive options (returnpair counted twice; +switchguardbound, +switchsharedcase, +switchmultipred, +unrolledguard, +branchflip, +outline, +spillargtrial, +securitycheck, +msvcstackguard, +nulterminator)",
    "data:KUNA-CATALOG #9: every settable carries a machine-readable change_kind so an LLM can judge default-on eligibility",
    "data:KUNA-CONSOLE #1: phase list shows Band B membership",
    "data:KUNA-CONSOLE #2: phase list includes the P3 dead-definition gate sub-phase",
    "data:KUNA-CONSOLE #3: LATENT decisions are flagged in the catalog",
    "data:KUNA-CONSOLE #4: phase map resolves the analysis rule group to P3",
    "data:KUNA-CONSOLE #5: phase map routes force goto to the P7 edge-virtualization sub-phase",
    "data:KUNA-CONSOLE #6: phase map resolves the GH-558 sub-phase",
    "data:KUNA-CONSOLE #7: phase status reports the active pipeline variant",
    "data:KUNA-CONSOLE #8: phase status reports kuna sub-phase option defaults (DIV-2: original)",
    "data:KUNA-DECLAREDLIBCPROTO #1: the declared name renders in both passes",
    "data:KUNA-DECLAREDLIBCPROTO #2: so naming is not what moved, in either pass",
    "data:KUNA-DECLAREDLIBCPROTO #3: off, the arguments carry no type",
    "data:KUNA-DECLAREDLIBCPROTO #4: on, the parked signature types them",
    "data:KUNA-ENTRYMAINPROTO #1: the option parses and round-trips in both directions, and is inert on the XML path -- the source form x <= 8 renders identically in all three passes",
    "data:KUNA-ENTRYMAINPROTO #2: the catalog documents it as a default-on P1 prototype-source correctness fix, live-toggled back on",
    "data:KUNA-ENTRYMAINPROTO #3: the catalog row names the CRT accessors an agent would grep for",
    "data:KUNA-LISTING #1: option listing on parses and round-trips, and is inert: the source form x <= 8 renders identically in both passes (default-off + flag-on parity)",
    "data:KUNA-NORETURN-DISCSTRICT #1: option noreturn_discstrict off/on parses and round-trips, and is inert on the XML path (no Listing built): the source form x <= 8 renders identically in all three passes (default-on + flag-off + flag-on parity)",
    "data:KUNA-PICBASE #1: the option parses and round-trips in both directions, and is inert on the XML path -- the source form x <= 8 renders identically in all three passes",
    "data:KUNA-PICBASE #2: the catalog documents it as a default-on P1 code/data-partition option, live-toggled back on",
    "data:KUNA-PICBASE #3: the catalog row carries the analysis-enablement change_kind an agent reads to judge the flip",
    "data:KUNA-PIPELINE #1: pipeline list names the variants with decompile current",
    "data:KUNA-PIPELINE #2: reduced normalize variant produces no structuring",
    "data:KUNA-QUALITY #1: full pipeline reports the single if-goto edge (before and after the sub-query)",
    "data:KUNA-QUALITY #2: unstructured total is the goto-count quality signal",
    "data:KUNA-REGIONS #1: region tree finds the single-block self-loop as a cyclic region at 0x3e0",
    "data:KUNA-REGIONS #2: the region tree is rooted at the function entry 0x3c0",
    "data:KUNA-REGIONS #3: region blocks lists the single-block loop region on its own",
    "data:KUNA-REGIONS #4: the recursive walker visits the entry block exactly once",
    "data:KUNA-REGIONS #5: phase map routes region tree to P7 loop-refinement",
    "data:KUNA-REGIONS #6: the real-CFG top region keeps the CBRANCH entry, the nested region, and the early-out as three nodes",
    "data:KUNA-REGIONS #7: the loop's enclosing acyclic region pairs the cyclic self-loop with its 0x40a exit block",
    "data:KUNA-REGIONS #8: the cyclic self-loop region wraps exactly the single 0x3e0 block",
    "data:KUNA-RESTARTS #2: a function that never restarts reports none",
    "data:KUNA-STRUCTDEFS #1: the struct definition is printed exactly once, by the ON pass",
    "data:KUNA-STRUCTDEFS #2: every member of the referenced struct is spelled",
    "data:KUNA-STRUCTDEFS #3: the tag gets the forward declaration the project header gives it",
    "data:KUNA-STRUCTDEFS #4: the signature is unchanged by the flip",
    "data:KUNA-STRUCTDEFS #5: the body is unchanged by the flip",
    "data:KUNA-UNMAPPEDENTRY #1: the option parses and round-trips in both directions, and is inert on the XML path -- the source form x <= 8 renders identically in all three passes",
    "data:KUNA-UNMAPPEDENTRY #2: the catalog documents it as a default-on P1 code/data-partition correctness fix, live-toggled back on",
    "data:KUNA-UNMAPPEDENTRY #3: the catalog row carries the correctness-fix change_kind an agent reads to judge the flip",
    "data:LIBCTYPES #10: glibc off, a MINTED stream shell has no members, so the refill body reads an offset",
    "data:LIBCTYPES #11: glibc on, the same two accesses are the published field",
    "data:LIBCTYPES #12: glibc off, a MINTED stat shell reads its size through a cast at an offset",
    "data:LIBCTYPES #13: glibc on, that read is the field",
    "data:LIBCTYPES #14: and the value carries the FIELD's type, so the sentinel is a signed -1",
    "data:LIBCTYPES #15: and the lines carrying an offset-named field anywhere in this file are exactly the four of pass 4 plus the four of the opaque stream passes",
    "data:LIBCTYPES #16: off, the copy-relocated stream slot is an untyped word and its fields are raw offsets",
    "data:LIBCTYPES #17: off, the GOT slot of a shared object is not even named",
    "data:LIBCTYPES #18: on, the copy-relocated slot is the stream itself, so the reads are fields of it",
    "data:LIBCTYPES #19: on, the GOT slot holds the stream's ADDRESS, and the name and the extra indirection both say so",
    "data:LIBCTYPES #1: off, the stream parameter is an opaque void *",
    "data:LIBCTYPES #20: a store THROUGH the stream pointer still stores to the same address",
    "data:LIBCTYPES #21: and so does the GOT form",
    "data:LIBCTYPES #22: a read of the stream OBJECT stays the object -- `sink\\(stdin\\)`, never `sink\\(\\*stdin\\)`",
    "data:LIBCTYPES #23: and in the library it is the one indirection the program performs, no more",
    "data:LIBCTYPES #24: a stream handed to a libc call the table already typed does not move",
    "data:LIBCTYPES #25: and the library's loses the cast the untyped slot needed",
    "data:LIBCTYPES #26: `datasyms off` still restores the raw address -- naming a data object stays that option's call",
    "data:LIBCTYPES #2: on, the same parameter is a minted FILE *",
    "data:LIBCTYPES #3: off, the fopen result is an opaque void *",
    "data:LIBCTYPES #4: on, the fopen result is a minted FILE *",
    "data:LIBCTYPES #5: the DWARF layout of `stat` reads the same in both passes -- the named table adopted it instead of shadowing it",
    "data:LIBCTYPES #6: and neither aggregate a DWARF definition already described fell back to an offset-named field",
    "data:LIBCTYPES #7: on a binary whose DWARF DOES define the stream type, that definition is what the table points at",
    "data:LIBCTYPES #8: including the parameter the whole option exists for",
    "data:LIBCTYPES #9: with its real field layout, not a shell",
    "data:LINUXSYSCALL #1: both int 0x80 sites render as an indirect call through swi() only in the linuxsyscall-off pass",
    "data:LINUXSYSCALL #2: with the option on, the number in EAX names the call and the ABI registers become its arguments",
    "data:LINUXSYSCALL #3: the second site resolves independently of the first (EAX reloaded between them)",
    "data:LINUXSYSCALL #4: the pre-SSA seam is what keeps the argument setup alive - 0x1234 exists only in the on pass",
    "data:LINUXSYSCALL #5: arity comes from the syscall table, so exit's one argument does not absorb the stale ecx/edx",
    "data:LINUXSYSCALL-X64 #1: option off retains exactly the one compatibility interrupt as a raw swi call",
    "data:LINUXSYSCALL-X64 #2: option on uses i386 numbering and the low RBX lane to emit standalone compat exit",
    "data:LINUXSYSCALL-X64 #3: compat syscall 1 must never be interpreted as native x86-64 write",
    "data:LINUXSYSCALL-X64 #4: i386 exit arity remains one",
    "data:LINUXSYSCALL-X64 #5: the native SYSCALL control is unchanged across both linuxsyscall passes",
    "data:LOADGUARDRANGE #1: by default the guard bound sizes the array over all six real elements (filled to the next mapped local, like Ghidra)",
    "data:LOADGUARDRANGE #2: only the off pass caps the array at the min_items fallback, two short of the real extent",
    "data:LOADGUARDRANGE #3: only the off pass splits element 4 off as a separate never-assigned scalar",
    "data:LOADGUARDRANGE #4: only the off pass splits element 5 off as a separate never-assigned scalar",
    "data:LOADGUARDRANGE #5: the single indexed store writes the array in both passes (in bounds only under the default declaration)",
    "data:LOADGUARDRANGE #6: by default element 4 is read through the array, in bounds",
    "data:LOADGUARDRANGE #7: by default element 5 is read through the array, in bounds",
    "data:LOADGUARDRANGE #8: only the off pass stores the config bytes from the unassigned scalars",
    "data:LOOPCONDHOIST #1: OFF leaves the head test inside an infinite loop (pass 1 only)",
    "data:LOOPCONDHOIST #2: ON hoists the head test into the loop header (pass 2 only)",
    "data:LOOPCONDHOIST #3: the OFF pass needs a break to reach the match arm; the ON pass does not (pass 1 only)",
    "data:LOWEREDSWITCH-SCRUTINEE #1: the re-rolled switch dispatches on the compared parameter",
    "data:LOWEREDSWITCH-SCRUTINEE #2: the scrutinee is never a constant nor empty",
    "data:LOWEREDSWITCH-SCRUTINEE #3: neither pass drops the dispatched-on parameter from the prototype",
    "data:LOWEREDSWITCH-SCRUTINEE #4: case 0x1000 recovered",
    "data:LOWEREDSWITCH-SCRUTINEE #5: case 0x400b recovered",
    "data:LOWEREDSWITCH-SCRUTINEE #6: pass 1 keeps the compiler's chain over the same parameter",
    "data:LOWEREDSWITCH-SCRUTINEE #7: a case body is present in BOTH passes",
    "data:LOWEREDSWITCHEXACT #1: the shared-body case label appears only with the option on",
    "data:LOWEREDSWITCHEXACT #2: its shared partner is labelled in both passes",
    "data:LOWEREDSWITCHEXACT #3: pass 1 pick_table returns the register a deleted compare block zeroed",
    "data:LOWEREDSWITCHEXACT #4: both passes label g3's 129 unsigned, which only pass 2's cast makes reachable",
    "data:LOWEREDSWITCHEXACT #5: pass 2 casts the selector to the labels' signedness",
    "data:LOWEREDSWITCHEXACT #6: pass 1 loses the argument a deleted compare block loaded",
    "data:LOWEREDSWITCHEXACT #7: pass 2 keeps it for f, and both passes for the later-head rlater",
    "data:LOWEREDSWITCHEXACT #8: likewise the second body's argument",
    "data:LOWEREDSWITCHEXACT #9: rlater never switches on its selector with the load deleted",
    "data:LOWEREDSWITCHHEADS #1: the inlined cascade re-rolls into a switch over argc, in pass 2 only",
    "data:LOWEREDSWITCHHEADS #2: all fifteen case labels, in pass 2 only",
    "data:LOWEREDSWITCHHEADS #3: the default arm, in pass 2 only",
    "data:LOWEREDSWITCHHEADS #4: pass 1 keeps the fifteen gotos into the join",
    "data:LOWEREDSWITCHHEADS #5: the last case value is assigned in both passes",
    "data:LOWEREDSWITCHHEADS #6: the default value is assigned in both passes",
    "data:LOWEREDSWITCHVALUE #1: pass 2 dispatches on the compared first parameter",
    "data:LOWEREDSWITCHVALUE #2: pass 1 dispatches both functions on a local holding a clobbered register",
    "data:LOWEREDSWITCHVALUE #3: pass 1's local is the second parameter",
    "data:LOWEREDSWITCHVALUE #4: pass 2 keeps the dispatched-on parameter in the prototype",
    "data:LOWEREDSWITCHVALUE #5: a case body is present in both passes",
    "data:LOWEREDSWITCHVALUE #6: big_consts switches only in pass 1, on the second parameter",
    "data:LOWEREDSWITCHVALUE #7: pass 1 copies the second parameter for it",
    "data:LOWEREDSWITCHVALUE #8: pass 2 keeps the compare over the first parameter",
    "data:LOWEREDSWITCHVALUE #9: a case body is present in both passes",
    "data:MAPPED-FLOW #1: off loses both functions to staged padding",
    "data:MAPPED-FLOW #2: default retains both mapped returns",
    "data:MAPPED-FLOW #3: each missing fall-through is explicit",
    "data:MAPPED-FLOW #4: both recovered bodies warn about incomplete flow",
    "data:MAPPED-FLOW #5: the fall-through that ends exactly at the mapped end names its successor",
    "data:MAPPED-FLOW #6: the zero padding that is mapped is still lifted",
    "data:MAPPED-FLOW #7: the padding byte whose instruction runs past the mapped end is cut",
    "data:MSVCFPCONST #1: off, both operands of _scale are opaque -- the undefined x87 COMDAT and the defined IEEE-double one alike",
    "data:MSVCFPCONST #2: off, both operands of _fscale are opaque too",
    "data:MSVCFPCONST #3: on, _scale reads pi/4 from the undefined 20-hex x87 name and 0.1 from the defined 16-hex IEEE double",
    "data:MSVCFPCONST #4: on, _fscale reads 1.0 from the 20-hex x87 name at float width and 0.5 from the defined 8-hex IEEE float, the modern MSVC spelling",
    "data:MSVCFPCONST #5: no opaque operand survives the on pass -- every dat_ line belongs to the off one",
    "data:MSVCFPCONST #6: the recovery changes the operands only -- both passes agree on the signature",
    "data:MSVCFTOL #1: off yields two argument-less helper calls, on yields none",
    "data:MSVCFTOL #2: on, the first conversion casts the field it actually reads",
    "data:MSVCFTOL #3: on, the second conversion casts the field it actually reads",
    "data:MSVCFTOL #4: on, both conversions truncate at the helper's real 64-bit width",
    "data:MSVCFTOL #5: on, the call is replaced by an injection",
    "data:MSVCSTACKGUARD-RETURN #1: option off retains the raw checker call in both generic-policy passes",
    "data:MSVCSTACKGUARD-RETURN #2: option off retains the cookie read in both generic-policy passes",
    "data:MSVCSTACKGUARD-RETURN #3: option on keeps failure one and success zero with generic preservation off and on",
    "data:MSVCSTACKGUARD-RETURN #4: all passes retain both arm-specific side effects",
    "data:MSVCSTRAPPEND #10: kept in both passes - the heap pointer is loaded from another object",
    "data:MSVCSTRAPPEND #11: kept in both passes - the fast arm also writes a local",
    "data:MSVCSTRAPPEND #12: kept in both passes - the capacity is read from +8",
    "data:MSVCSTRAPPEND #13: kept in both passes - the heap pointer is loaded from +8 of the object",
    "data:MSVCSTRAPPEND #14: kept in both passes - the terminator is written to a local",
    "data:MSVCSTRAPPEND #15: kept in both passes - there is no terminator",
    "data:MSVCSTRAPPEND #16: kept in both passes - a two-byte append stores one byte and the terminator",
    "data:MSVCSTRAPPEND #17: kept in both passes - the room check is signed",
    "data:MSVCSTRAPPEND #18: kept in both passes - the fast arm runs when the string is full",
    "data:MSVCSTRAPPEND #19: kept in both passes - the index register is read after the join",
    "data:MSVCSTRAPPEND #1: on - the four constant appends are four push_back calls in order",
    "data:MSVCSTRAPPEND #20: kept in both passes - a flag the fast arm set is read after the join",
    "data:MSVCSTRAPPEND #21: kept in both passes - the fast arm writes a callee-saved register",
    "data:MSVCSTRAPPEND #22: kept in both passes - the grow call is handed another object",
    "data:MSVCSTRAPPEND #23: kept in both passes - the grow call is asked for two bytes",
    "data:MSVCSTRAPPEND #24: kept in both passes - the terminator skips a byte",
    "data:MSVCSTRAPPEND #25: kept in both passes - the heap path stores the character through the small buffer",
    "data:MSVCSTRAPPEND #26: kept in both passes - the two paths store different characters",
    "data:MSVCSTRAPPEND #27: kept in both passes - the size is stored four bytes wide",
    "data:MSVCSTRAPPEND #28: kept in both passes - the literal lives in writable memory",
    "data:MSVCSTRAPPEND #29: kept in both passes - the literal spells other bytes",
    "data:MSVCSTRAPPEND #2: on - the loop appends the byte it read",
    "data:MSVCSTRAPPEND #30: kept in both passes - the buffer pointer left in RAX is read after the join",
    "data:MSVCSTRAPPEND #31: kept in both passes - only the small-string path stores the size",
    "data:MSVCSTRAPPEND #32: kept in both passes - another branch enters the fast arm",
    "data:MSVCSTRAPPEND #33: kept in both passes - another branch enters the grow block",
    "data:MSVCSTRAPPEND #34: kept in both passes - the grow block stores the call result",
    "data:MSVCSTRAPPEND #35: kept in both passes - the outgoing count disagrees with the bytes stored",
    "data:MSVCSTRAPPEND #36: kept in both passes - one path after the join reads the index register low byte",
    "data:MSVCSTRAPPEND #3: on - the two-byte literal append returns the reference its caller uses",
    "data:MSVCSTRAPPEND #4: on - zeroing a changed register after the join does not block the collapse",
    "data:MSVCSTRAPPEND #5: seven synthesized calls, all in the on pass",
    "data:MSVCSTRAPPEND #6: off - four unrolled push_back grow calls, plus the two near misses in each pass",
    "data:MSVCSTRAPPEND #7: the register-count append keeps its grow call in both passes, the literal one only off",
    "data:MSVCSTRAPPEND #8: the 16-byte threshold diamond survives both passes",
    "data:MSVCSTRAPPEND #9: the mismatched-char diamond survives both passes",
    "data:MULBLOB #1: mulblob off declares a 16-byte local per operand (the bug)",
    "data:MULBLOB #2: mulblob off fills each blob in halves before the product is read",
    "data:MULBLOB #3: mulblob off reads the high half out of the blob product",
    "data:MULBLOB #4: default prints the high half over the operands themselves",
    "data:MULBLOB #5: default prints the low half the same way, with no blob left",
    "data:NULTERMINATOR #10: no array grows over the padding or the locals after its terminator",
    "data:NULTERMINATOR #11: gap_zero off declares [20]",
    "data:NULTERMINATOR #12: gap_zero on declares [21]",
    "data:NULTERMINATOR #13: gap_zero splits its terminator only with the option off",
    "data:NULTERMINATOR #14: gap_zero keeps the zero two elements past the end separate in both passes",
    "data:NULTERMINATOR #15: pad_zero off declares [26]",
    "data:NULTERMINATOR #16: pad_zero on declares [27]",
    "data:NULTERMINATOR #17: pad_zero splits its terminator only with the option off",
    "data:NULTERMINATOR #18: pad_zero keeps the byte after the padding separate in both passes",
    "data:NULTERMINATOR #19: read_later keeps its array in both passes",
    "data:NULTERMINATOR #1: canary_check off declares the 64-byte buffer one element short",
    "data:NULTERMINATOR #20: read_later keeps the zeroed local it reads later in both passes",
    "data:NULTERMINATOR #21: read_direct keeps its array in both passes",
    "data:NULTERMINATOR #22: read_direct keeps the directly read slot in both passes",
    "data:NULTERMINATOR #23: nonzero keeps its array in both passes",
    "data:NULTERMINATOR #24: nonzero keeps the non-zero store separate in both passes",
    "data:NULTERMINATOR #25: wide_elem keeps its int4 array in both passes",
    "data:NULTERMINATOR #26: wide_elem keeps the zeroed int separate in both passes",
    "data:NULTERMINATOR #27: wide_zero keeps its char array in both passes",
    "data:NULTERMINATOR #28: wide_zero keeps the zeroed two-byte slot separate in both passes",
    "data:NULTERMINATOR #29: addr_taken keeps its array in both passes",
    "data:NULTERMINATOR #2: canary_check on declares the whole 64-byte buffer",
    "data:NULTERMINATOR #30: addr_taken keeps the address-taken byte its own variable in both passes",
    "data:NULTERMINATOR #31: wide_str off declares [14]",
    "data:NULTERMINATOR #32: wide_str on declares [15]",
    "data:NULTERMINATOR #33: wide_str splits its terminator only with the option off",
    "data:NULTERMINATOR #34: exactly two uint2 arrays, so wide_str never grows past [15]",
    "data:NULTERMINATOR #35: two_literals keeps the merged range at [3] in both passes",
    "data:NULTERMINATOR #36: two_literals keeps the second literal's zero separate in both passes",
    "data:NULTERMINATOR #37: struct_field OFF keeps the 48-byte member correctly sized (the sibling active field stays its own variable)",
    "data:NULTERMINATOR #38: opt-in ON over-extends the member by one, absorbing the sibling active field -- the documented false positive that keeps the option off by default",
    "data:NULTERMINATOR #39: ON renders the absorbed active field as the array's 49th element",
    "data:NULTERMINATOR #3: canary_check on renders the terminator as the indexed store",
    "data:NULTERMINATOR #40: the uid field beyond active survives in both passes",
    "data:NULTERMINATOR #4: canary_check off splits the terminator into its own char",
    "data:NULTERMINATOR #5: both canary_check passes keep the bounded copy",
    "data:NULTERMINATOR #6: pad_after off declares [33]",
    "data:NULTERMINATOR #7: pad_after on closes the array one element after the terminator",
    "data:NULTERMINATOR #8: pad_after on renders buf[33] = 0",
    "data:NULTERMINATOR #9: pad_after off splits the terminator into its own char",
    "data:OVERLAPBRANCH #1: the branch target wins, so the real add-chain stream is recovered in the default pass only",
    "data:OVERLAPBRANCH #2: the out-of-image call the swallowed decode invents survives only in the overlapbranch-off pass",
    "data:OVERLAPBRANCH #3: the bogus global the swallowed decode invents survives only in the overlapbranch-off pass",
    "data:P1: TOTAL shared() epilogue count across both passes is 3 (off clones to 2, on dedups to 1)",
    "data:P1: default (crossjumprevert off) emits the condensed cross-jump goto",
    "data:P1: on recovers three constant early returns from the shared epilogue (off merges to none)",
    "data:P1b: default labels the shared condensed tail",
    "data:P2: on duplicates the cross-jump tail and logs the reversion",
    "data:P2: on duplicates the shared bare-epilogue return (3 in-edges -> split 2, leave 1) and logs it",
    "data:P2: on merges the duplicated if/else tail and logs the dedup",
    "data:P3: TOTAL goto-label count across both passes is 1 (off: 1, on: 0)",
    "data:P3: off keeps ONE merged variable return (the transform is fully option-gated)",
    "data:P3: the convergence sink post() is untouched (one per pass = 2 total)",
    "data:PDB-SIDECAR #1: with the pass off the function keeps the engine placeholder",
    "data:PDB-SIDECAR #2: the shipped default names it from the sidecar .pdb",
    "data:PDB-SIDECAR #3: the PE entry is renamed too",
    "data:PEBNAMES #10: PEB+0x68 changed meaning between Windows releases and stays offset-named",
    "data:PEBNAMES #11: no version-dependent PEB name is ever printed",
    "data:PEBNAMES #12: a variable displacement through the base is added as an integer, not as C pointer arithmetic",
    "data:PEBNAMES #13: FS is not the TEB in 64-bit mode, so fs:[0x58] stays an untyped register read under on",
    "data:PEBNAMES #14: the FS base keeps an untyped declaration in 64-bit mode",
    "data:PEBNAMES #15: a function that writes through the base keeps its PEB probe untyped under on",
    "data:PEBNAMES #16: the write itself stays a store through the untyped base",
    "data:PEBNAMES #17: a second decompile keeps the one named TEB Symbol instead of mapping an anonymous copy",
    "data:PEBNAMES #18: the jump-table cases read PEB fields by name in both decompiles",
    "data:PEBNAMES #1: BeingDebugged is read through the PEB the TEB points at (on pass only)",
    "data:PEBNAMES #2: the NtGlobalFlag heap-debug mask is read by name (on pass only)",
    "data:PEBNAMES #3: GS:[0x30] is TEB.Self and chains on to the PEB and its ProcessHeap (on pass only)",
    "data:PEBNAMES #4: an offset the table does not name prints offset-named, not as a neighbouring field (on pass only)",
    "data:PEBNAMES #5: the segment base is declared as the TEB pointer and keeps its storage comment (every GS reader under on)",
    "data:PEBNAMES #6: the magic-offset read of BeingDebugged, in the off pass and in the container-less auto pass",
    "data:PEBNAMES #7: the register storage comment survives in every pass",
    "data:PEBNAMES #8: a read past the largest TEB stays an integer addition, not a field of a phantom neighbour",
    "data:PEBNAMES #9: no read is split into an index of a neighbouring TEB or PEB",
    "data:PEBNAMES-X86 #10: fs:[0x1010] lies inside the Windows 11 WOW64 TEB32 and is offset-named, not a field of a phantom neighbour",
    "data:PEBNAMES-X86 #11: a read past the largest TEB32 stays an integer addition",
    "data:PEBNAMES-X86 #12: no read is split into an index of a neighbouring TEB or PEB",
    "data:PEBNAMES-X86 #13: PEB+3 and PEB+0x38 changed meaning between Windows releases and stay offset-named",
    "data:PEBNAMES-X86 #14: no version-dependent PEB name is ever printed",
    "data:PEBNAMES-X86 #15: a read past the largest PEB32 stays an integer addition",
    "data:PEBNAMES-X86 #16: GS is not the TEB on x86, so gs:[0x14] stays an untyped register read under on",
    "data:PEBNAMES-X86 #17: the GS base keeps an untyped declaration on x86",
    "data:PEBNAMES-X86 #18: the C++ EH registration record keeps its three-slot extent under on",
    "data:PEBNAMES-X86 #19: the C++ EH record's address is linked through the untyped base in both passes",
    "data:PEBNAMES-X86 #1: BeingDebugged is read through FS:[0x30] by name (on pass only)",
    "data:PEBNAMES-X86 #2: NtGlobalFlag sits at PEB+0x68 on x86 (on pass only)",
    "data:PEBNAMES-X86 #3: FS:[0x18] is TEB.Self and chains on to the ProcessHeap (on pass only)",
    "data:PEBNAMES-X86 #4: the magic-offset read of BeingDebugged (off pass only)",
    "data:PEBNAMES-X86 #5: the TEB pointer is declared for the reader and every near miss, never for the function that writes the base",
    "data:PEBNAMES-X86 #6: the function that links an SEH record keeps its PEB read untyped under on",
    "data:PEBNAMES-X86 #7: the SEH registration record stays one 12-byte object under on",
    "data:PEBNAMES-X86 #8: the record is linked and unlinked through the base's first member, not a TEB name, in both passes",
    "data:PEBNAMES-X86 #9: the register storage comment survives in every pass",
    "data:PEIMPORTCALL #1: off leaves the IAT-slot call unnamed",
    "data:PEIMPORTCALL #2: on binds the slot call to the import name",
    "data:PEIMPORTCALL #3: on gives the bound call its no-return flow effect",
    "data:PEIMPORTCALL #4: off leaves bail calling the raw slot pointer",
    "data:PEIMPORTCALL #5: on ends bail at the bound no-return import",
    "data:PEIMPORTCALL #6: off leaves a Mach-O non-lazy import slot anonymous",
    "data:PEIMPORTCALL #7: on binds the Mach-O import slot to objc_msgSend",
    "data:PEIMPORTCALL #8: ordinary ObjC message-reference data stays anonymous in both arms",
    "data:PUSHIMMEDIATERET #1: both arms preserve the unpacker call",
    "data:PUSHIMMEDIATERET #2: only option on emits the complete tail-transfer statement",
    "data:REALTYPES #1: signature uses real C types, not xunknown8",
    "data:REALTYPES #2: 1-byte stack buffer is char[8], not xunknown1[8]",
    "data:REALTYPES #3: 4-byte temp is unsigned int, not xunknown4",
    "data:REALTYPES #4: no xunknown placeholder remains",
    "data:REALTYPES #5: no undefined placeholder remains",
    "data:RETINPUTHALF #1: by default the returned RDX half that is a passthrough argument is kept, and the argument stays in the signature",
    "data:RETINPUTHALF #2: the passthrough half is written from the second argument",
    "data:RETINPUTHALF #3: with the option off the bug returns: the half is dropped AND the argument vanishes (a one-argument wide)",
    "data:RETINPUTHALF #4: w2, whose high half is arithmetic on the same argument, is identical in both passes (the rule only ever spoke about unwritten terminals)",
    "data:RETINPUTHALF #5: nothing ever recovers a one-argument w2",
    "data:RETPUSHEDHALF #1: by default the alignment push/pop leaves a one-argument function returning one register",
    "data:RETPUSHEDHALF #2: with the option off the bug returns: the pushed register becomes a fifth argument",
    "data:RETPUSHEDHALF #3: and the invented argument is used only as the high half of the return",
    "data:RETPUSHEDHALF #4: the same-register save/restore is unmoved in both passes, because the pop writes the register it pushed",
    "data:RETPUSHEDHALF #5: a plain register-to-register move into RDX is unmoved in both passes: no stack, no push, nothing to reject",
    "data:RUSTABI #10: and its call keeps no output in either pass",
    "data:RUSTABI #11: the one-byte discriminant write is a genuine element access and keeps its subscript",
    "data:RUSTABI #1: by default the producer collapses to the one-bit discriminant and the EDX payload is deleted",
    "data:RUSTABI #2: by default the consumer reads the payload register as a local nothing assigns",
    "data:RUSTABI #3: by default the call to prod has no output at all",
    "data:RUSTABI #4: with the option on the producer returns the whole pair",
    "data:RUSTABI #5: with the option on the payload half is written at the second register's offset",
    "data:RUSTABI #6: with the option on the call has an output and the payload is connected to it",
    "data:RUSTABI #7: with the option on the consumer reads the payload out of the call's result, not out of thin air",
    "data:RUSTABI #8: with the option on the discriminant guard reads the same call result",
    "data:RUSTABI #9: a callee proven never to write the second return register is not paired -- the reader is unchanged in BOTH passes",
    "data:SETLOCALE RETTYPE #1: NULL locale arg is typed char *",
    "data:SETLOCALE RETTYPE #2: no untyped indirect call",
    "data:SETLOCALE RETTYPE #3: call is the named setlocale",
    "data:SPLITSTOREKEEP #1: the seven-byte cell the store at src+8 was cut down to is a variable, at the default only",
    "data:SPLITSTOREKEEP #2: and so is its destination cell at dst+8",
    "data:SPLITSTOREKEEP #3: the one byte the two middle stores overlap on is its own cell",
    "data:SPLITSTOREKEEP #4: copy31 is five assignments at the default and one with the mark off, while the non-overlapping control copy32 is four in each pass",
    "data:SPLITSTOREKEEP #5: splitstorekeep off restores the filed bug, the head store with the middle fifteen bytes gone",
    "data:SPLITSTOREKEEP #6: the control's word at dst+0x18 is a plain eight-byte local in BOTH passes, so refinement never touched it",
    "data:STRUCTSYNTH #1: off leaves the offset-8 read as raw pointer arithmetic",
    "data:STRUCTSYNTH #2: the parameter is retyped to the synthesized structure",
    "data:STRUCTSYNTH #3: the offset-8 read renders as a field",
    "data:STRUCTSYNTH #4: the offset-0 read renders as a field",
    "data:STRUCTSYNTH #5: offset 0 keeps the pointer type its loaded value carries",
    "data:STRUCTSYNTH #6: offset 8 is a 4-byte field, not a padded 8-byte one",
    "data:STRUCTSYNTH-HOLE #1: off leaves the address as raw pointer arithmetic",
    "data:STRUCTSYNTH-HOLE #2: the parameter is retyped to the synthesized structure",
    "data:STRUCTSYNTH-HOLE #3: the body names the undereferenced offset",
    "data:STRUCTSYNTH-HOLE #4: and the layout declares a member there",
    "data:STRUCTSYNTH-HOLE #5: no part of the structure is padding the header would not name",
    "data:STRUCTSYNTH-OVERLAP #1: off leaves the offset-8 store as raw pointer arithmetic",
    "data:STRUCTSYNTH-OVERLAP #2: the parameter is retyped to the synthesized structure",
    "data:STRUCTSYNTH-OVERLAP #3: offset 0 is an 8-byte field",
    "data:STRUCTSYNTH-OVERLAP #4: offset 8 is an 8-byte field",
    "data:STRUCTSYNTH-OVERLAP #5: the interior read is a cast off field_0x0, not a field of its own",
    "data:STRUCTSYNTH-SHARED #10: a record with a member in the reader's alignment padding does not answer for it",
    "data:STRUCTSYNTH-SHARED #11: nor does it answer a reader with a typed pointer",
    "data:STRUCTSYNTH-SHARED #12: a reader with no typed pointer is not answered by a record with members past its last field",
    "data:STRUCTSYNTH-SHARED #1: option off leaves the offset-8 read raw in BOTH functions",
    "data:STRUCTSYNTH-SHARED #2: both readers name the same synthesized structure",
    "data:STRUCTSYNTH-SHARED #3: twelve readers, eleven records",
    "data:STRUCTSYNTH-SHARED #4: only the reader that claims offset 0xc renders it",
    "data:STRUCTSYNTH-SHARED #5: one definition serves both readers",
    "data:STRUCTSYNTH-SHARED #6: the four-claim reader mints its own record",
    "data:STRUCTSYNTH-SHARED #7: two integer words are not answered by a record that begins with them",
    "data:STRUCTSYNTH-SHARED #8: each interface table keeps its own definition",
    "data:STRUCTSYNTH-SHARED #9: the smaller table is not answered by the larger one",
    "data:STRUCTSYNTH-UNCLAIMED #1: each reader loads the word into a local ahead of its store",
    "data:STRUCTSYNTH-UNCLAIMED #2: no reader spells the load through the writer's byte member",
    "data:STRUCTSYNTH-UNCLAIMED #3: only the writer names the writer's structure",
    "data:SWITCHSELECTOR #1: the re-rolled switch dispatches on the message parameter, and only the off pass re-rolls",
    "data:SWITCHSELECTOR #2: the case labels exist only in the pass that re-rolled",
    "data:SWITCHSELECTOR #3: with the option on the cascade survives as the compiler lowered it, over the same parameter",
    "data:SWITCHSELECTOR #4: the switch never dispatches on a constant",
    "data:SWITCHSELECTOR #5: neither pass loses the dispatched-on parameter from the recovered prototype",
    "data:SWITCHSELECTOR #6: a case body is present in BOTH passes - the option changes the structure, not the content",
    "data:SYMBOLNAMEBOUND #1: the scope path keeps 255 components verbatim and folds the rest into one",
    "data:SYMBOLNAMEBOUND #2: the CALL resolves to the same folded name rather than sub_addr, so the resolve path applies the identical bound",
    "data:SYMBOLNAMEBOUND #3: the binary's ORIGINAL 1000-component name still addresses the function",
    "data:SYMBOLNAMEBOUND #4: the fold hash is deterministic, a written-out FNV-1a 64 over the dropped bytes and never a per-process DefaultHasher",
    "data:SYMBOLNAMEBOUND #5: no dropped scope component survives anywhere in the output",
    "data:SYMBOLNAMEBOUND #6: the bodies decompiled, so the load did not fail and the bound cost no analysis",
    "data:SYMBOLNAMECHARS #1: a comment-closing */ in a name cannot close the header comment",
    "data:SYMBOLNAMECHARS #2: a real newline byte no longer splits the declaration in two",
    "data:SYMBOLNAMECHARS #3: the 0x80 name keeps its own spelling",
    "data:SYMBOLNAMECHARS #4: the 0x81 name is a DIFFERENT one, not the same lossy string",
    "data:SYMBOLNAMECHARS #5: the sanitized names render at CALL sites too, not just on declarations",
    "data:SYMBOLNAMECHARS #6: the data half of the same symbol walk is sanitized with it",
    "data:SYMBOLNAMECHARS #7: no comment delimiter survives inside any emitted name",
    "data:SYMBOLNAMECHARS #8: nor the line-comment spelling",
    "data:SYMBOLNAMECHARS #9: the five bodies decompiled, so the load did not fail",
    "data:SYMBOLNAMEREPAIR #1: a top-level anonymous namespace is a named scope, not a leading empty one",
    "data:SYMBOLNAMEREPAIR #2: an anonymous namespace nested in a named one keeps both components",
    "data:SYMBOLNAMEREPAIR #3: a class inside an anonymous namespace nests one level deeper again",
    "data:SYMBOLNAMEREPAIR #4: the same scope path renders at a CALL site, not just on the declaration",
    "data:SYMBOLNAMEREPAIR #5: no empty scope component survives anywhere in the output",
    "data:SYMBOLNAMEREPAIR #6: the three bodies decompiled (the load did not fail)",
    "data:TAILCALLFRAME #1: default does NOT decode the callee into the caller; only the tailcallframe-off pass shows its store (exactly one occurrence across both passes)",
    "data:TAILCALLFRAME #2: only the default pass recovers the tail jump as a call to the undiscovered callee",
    "data:TAILCALLFRAME #3: both passes keep the caller's own return-42 path, so the sole difference is the tail-call boundary",
    "data:TAILCALLFRAME #4: the introduced call is attributed to the option that introduced it",
    "data:TAILCALLSAVED #1: only the tailcallsaved-off pass turns the jump into a call to the function's own join block",
    "data:TAILCALLSAVED #2: the false call is attributed to the rule that made it, in that pass only",
    "data:TAILCALLSAVED #3: only the default pass keeps the join block's own arithmetic inside the function",
    "data:TAILCALLSAVED #4: both passes keep the cdecl call whose arguments the add esp,8 discards, so the sole difference is the tail-call boundary",
    "data:TERMDUP #4: at the shipped bound 2 a three-term expression is forced explicit and gets its own statement",
    "data:TERMDUP #5: the explicit sum is then read at both multiplicand positions by name",
    "data:TERMDUP #6: `option termdup 3` drops the statement and duplicates the three-term sum at both uses",
    "data:TIEDPHITRIM #10: both passes keep upstream's merge for accum_loop's computed loop value",
    "data:TIEDPHITRIM #11: both passes keep xa11's upstream merge, the loop's x stored into buf[0] only after the loop",
    "data:TIEDPHITRIM #12: both passes keep xa13's upstream merge, the loop's x stored into buf[0] only after the loop",
    "data:TIEDPHITRIM #13: default, outparam_walk copies the filled head into its own loop variable",
    "data:TIEDPHITRIM #14: option off, outparam_walk stores each next pointer into the stack slot getnode filled",
    "data:TIEDPHITRIM #15: default, t8 keeps the loop variable out of buf[1] and stores the exit byte only after the loop",
    "data:TIEDPHITRIM #16: default, t1 stores the exit byte into buf[1] only after the loop",
    "data:TIEDPHITRIM #17: default, t1b stores the exit byte into buf[2] only after the loop",
    "data:TIEDPHITRIM #1: default, MSVC: buf[0] is read once into its own variable and the loop reloads that variable from buf[i]",
    "data:TIEDPHITRIM #2: option off, MSVC: the loop tests buf[0] and stores buf[i] into it",
    "data:TIEDPHITRIM #3: default, gcc -O2: the guarded byte is copied into the loop variable before the loop",
    "data:TIEDPHITRIM #4: option off, gcc -O2: each fetched byte is stored into the byte strncpy filled",
    "data:TIEDPHITRIM #5: default, clang-12 -O2: the guarded byte is copied into the loop variable before the loop",
    "data:TIEDPHITRIM #6: option off, clang-12 -O2: each fetched byte is stored into the byte strncpy filled",
    "data:TIEDPHITRIM #7: both passes print store_loop's live store of each fetched byte into buf[0]",
    "data:TIEDPHITRIM #8: both passes print store_join's store of the joined byte into buf[0]",
    "data:TIEDPHITRIM #9: both passes keep upstream's merge at outparam_join's if/else join",
    "data:UNSIGNED-BYTE-VM-SELECTOR #1: the selector declaration remains in both passes",
    "data:UNSIGNED-BYTE-VM-SELECTOR #2: recovered switch remains in both passes",
    "data:UNSIGNED-BYTE-VM-SELECTOR #3: low case remains in both passes",
    "data:UNSIGNED-BYTE-VM-SELECTOR #4: off reproduces the impossible negative label",
    "data:UNSIGNED-BYTE-VM-SELECTOR #5: default preserves the unsigned label",
    "data:VOIDTAILRETURN #1: the bare trailing return; is printed by EXACTLY ONE of the two passes - the shipped default keeps it, `option voidtailreturn on` elides it (2 would mean the option did nothing)",
    "data:VOIDTAILRETURN #2: both passes really emitted the function, so #1 cannot pass vacuously on a pass that produced no C",
    "data:ZEROIDIOMUSE #1: off, the watchdog call inside the loop has an empty argument list",
    "data:ZEROIDIOMUSE #2: on, both loaded registers reach the call as arguments",
    "data:ZEROIDIOMUSE #3: the zeroing idiom itself is folded to the constants reader is called with in BOTH passes, so the option moved no other call",
    "data:ZEROIDIOMUSE #4: both passes keep the loop counter and its two returns, so only the argument list moved",
    "data:angr-LoweredSwitch #1: exactly one switch (none in pass 1 off, one in pass 2 on)",
    "data:angr-LoweredSwitch #2: default present",
    "data:angr-LoweredSwitch #3: case 0x70 (p) recovered",
    "data:angr-LoweredSwitch #4: case 0x74 (t) recovered",
    "data:angr-LoweredSwitch #5: case 0x63 (c) recovered",
    "data:angr-LoweredSwitch #6: negative case -0x82 (-130) recovered",
    "data:angr-LoweredSwitch #7: negative case -0x83 (-131) recovered",
    "data:angr-StackGuard #1: bug -- __stack_chk_fail call (sub_10e0) rendered only with option off",
    "data:angr-StackGuard #2: bug -- a goto is emitted only with option off",
    "data:angr-StackGuard #3: bug -- label (goto target + definition) emitted only with option off",
    "data:angr-StackGuard #4: fix -- only the option-on pass inlines the deep match path as a direct return 1",
    "data:angr-StackGuard #5: fix (GH-183) -- the fs+0x28 canary accesses (entry init + reload) survive only in the option-off pass",
    "data:angr-StackGuard #6: fix (GH-183) -- the TLS-base fs_offset declaration survives only in the option-off pass",
    "data:angr-dedupvardecls #1: slot 0x58 declared 1x (off, now merge-coalesced per GH-181) + 1x (on) = 2 total",
    "data:angr-dedupvardecls #2: slot 0x3c declared 1x (off, now merge-coalesced) + 1x (on) = 2 total",
    "data:angr-gotoreduce #1 (bug, off): one residual goto to the shared return tail",
    "data:angr-gotoreduce #2 (bug, off): goto label reference + label definition present, none on",
    "data:angr-gotoreduce #3 (fix, on): return tail duplicated (1 return off + 2 returns on)",
    "data:angr-incorrect-duplication-chcon #1: the no-return call is present in both passes",
    "data:angr-incorrect-duplication-chcon #2: dead code after the call survives only with the option OFF (bug); dropped with ON (fix)",
    "data:angr-incorrect-duplication-chcon #3: with the option ON the call is recognized no-return (artificial halt planted)",
    "data:angr-loopbreak #1: nine loop-exit gotos (pass1 off only; pass2 on recovers all)",
    "data:angr-loopbreak #2: synthesized cleanup label present in pass1, suppressed in pass2",
    "data:angr-missing-function-call #1: fix recovers a switch (none in pass 1 off, one in pass 2 on)",
    "data:angr-missing-function-call #2: bug renders the unrecovered jump as a computed call (pass 1 off only)",
    "data:angr-missing-function-call #3: boundary case 0x36 (u/userlist) recovered, proving bound N==0x37 not 0x36",
    "data:angr-missing-function-call #4: case 0x26 (e/emaillist) recovered",
    "data:angr-missing-function-call #5: case 0x2e (m/maillist) recovered",
    "data:angr-mv #1: one switch (only pass 3, loweredswitchexact off)",
    "data:angr-mv #2: default present",
    "data:angr-mv #3: case 0x66 (102 f) recovered",
    "data:angr-mv #4: case 0x74 (116 t) recovered",
    "data:angr-mv #5: case 0x53 (83 S) recovered",
    "data:angr-mv #6: case 0x80 (128) recovered",
    "data:angr-mv #7: passes 1 and 2 keep the -n branch the legacy case map drops",
    "data:angr-taildup #1 (bug, off): one residual goto to the shared free();return; tail",
    "data:angr-taildup #2 (bug, off): goto label reference + label definition present, none on",
    "data:angr-taildup #3 (fix, on): free();return; tail duplicated (1 free off + 2 free on)",
    "data:argclobber #1: the caller's side is the shape the option exists for, a clobber on one path of the argument register's join and a dead idiv remainder on the other",
    "data:argclobber #2: and the argument survives with the option ON, because no callee was decompiled first and `target` stated no prototype for the rule to read",
    "data:argclobber #3: the quotient the caller really uses survives both passes",
    "data:argclobber-armreturn #1: the clobber is of r0 and the argument register is r3, so the trailing argument survives both passes",
    "data:argclobber-forward #1: the non-clobber path writes the caller's own parameter, so the trailing argument survives both passes",
    "data:argclobber-forward #2: and caller4 keeps the second parameter that argument reads",
    "data:argclobber-guards #1: the callee reads rdx before writing it, so the trailing argument survives both passes",
    "data:boolbyte #10: the slot both controls hand to the callee stays a character in both passes -- the INDIRECT a call leaves on it is not identity, and one byte of a bigger object is not a declaration of its own",
    "data:boolbyte #11: with the option off a truncation whose destination a comparison already made bool prints as the raw SUB41 intrinsic -- an undeclared identifier",
    "data:boolbyte #12: with the option on the same truncation prints as the cast it is",
    "data:boolbyte #13: with the option off a byte truncated from a value with bits above it is a character",
    "data:boolbyte #14: with the option on the truncation is kept explicit inside the bool cast",
    "data:boolbyte #15: and never prints as a bare bool cast, which would test bit 9 as well",
    "data:boolbyte #1: with the option off a byte that is only ever branched on is a character -- the bug",
    "data:boolbyte #2: with the option on the same parameter is a bool",
    "data:boolbyte #3: the witness function's body is the same in both passes -- here only the declaration moves",
    "data:boolbyte #4: a byte that is also widened and added is refused in both passes",
    "data:boolbyte #5: a byte tested for its low bit is a parity test, not a truth test, in both passes",
    "data:boolbyte #6: and that test keeps its shape, with no bool cast appearing around it",
    "data:boolbyte #7: a byte that is also stored through a pointer is refused in both passes",
    "data:boolbyte #8: the callfill control keeps its shape in both passes",
    "data:boolbyte #9: and so is a byte parameter copied into a slot the callee is handed the address of",
    "data:calleearity #1: option off drops the argument at the call site whose guard branches on it (bug reproduced)",
    "data:calleearity #2: option on gives both call sites of one callee the same arity",
    "data:calleearitycut #1: five of the six renderings are an empty argument list -- all three sites in pass 1 (bug reproduced) and the two discriminators again in pass 2",
    "data:calleearitycut #2: option on recovers the four arguments the callee's body reads, bounded by the register the run stops at",
    "data:calleearitycut #3: a callee that reads every argument register leaves the run no boundary, so its site is untouched in both passes",
    "data:calleearitycut #4: `caller3` writes the register the run stops at, so it is carrying a further argument and that site is untouched in both passes",
    "data:calleearityfwd #1: option off drops the argument at the call site that finalizes BEFORE its witness (bug reproduced)",
    "data:calleearityfwd #2: option on recovers the size the small arm leaves in the argument register",
    "data:calleearityfwd #3: the site that was already right keeps exactly one argument in both passes",
    "data:calleearitylive #1: option off drops the branched-on 4th argument at both callees (bug reproduced)",
    "data:calleearitylive #2: option on extends the short site to the sibling call's four arguments",
    "data:calleearitylive #3: the callee that also reads r8d contradicts a four-argument witness, so its short site is left alone in BOTH passes",
    "data:calleearitylive #4: the witness site keeps exactly four arguments in all four decompilations",
    "data:calleearityscratch #1: with the option off both scratch sites render an empty argument list -- the bug",
    "data:calleearityscratch #2: option on bounds the run at the 64-bit scratch register and recovers the one argument the callee reads",
    "data:calleearityscratch #3: the witness's own byte-scratch shape recovers the same way",
    "data:calleearityscratch #4: a boundary the caller uses for nothing but the call scores active and the site recovers it on its own, in both passes",
    "data:calleearityscratch #5: a constant boundary is never scratch, so that site is untouched in both passes",
    "data:calleedeadarg #1: option off passes the previous call's return value to a callee that never reads it (bug reproduced)",
    "data:calleedeadarg #2: option on drops the argument the callee's own body proves it never reads",
    "data:calleedeadarg #3: with the argument gone the producer call stands on its own (only the fixed pass)",
    "data:callretmulti #1: one helper assignment in each pass",
    "data:callretmulti #2: helper is never duplicated into the low-word sinks",
    "data:callretmulti #3: same-block fan-out evaluates helper2 once per pass",
    "data:callsitestackargs #1: option off truncates the call at the six register arguments (bug reproduced)",
    "data:callsitestackargs #2: option on recovers both stack-passed arguments",
    "data:charbyte #10: and the call needs a cast back (dashlen off; argbyte and sibling, whose pointers stay uint1 *, in both passes)",
    "data:charbyte #11: with the option on the pointer stays char *",
    "data:charbyte #12: and the call takes it as it is",
    "data:charbyte #13: the dash compare in dashlen follows the byte",
    "data:charbyte #14: a byte passed straight to a call keeps uint1 and its pointer uint1 * in both passes (argbyte)",
    "data:charbyte #15: the one-byte argument prints as the uint1 it is, so C zero-extends it as the binary does",
    "data:charbyte #16: a sibling byte passed to a call keeps the shared pointer uint1 * in both passes (sibling)",
    "data:charbyte #17: and the sibling argument is read through that uint1 *",
    "data:charbyte #18: a counter whose own sum is stored back keeps uint1 * in both passes (counter)",
    "data:charbyte #19: and its increment prints with a number, not a char literal",
    "data:charbyte #1: with the option off a string byte compared with a dash is uint1 and the dash a number (isdash) -- the bug; the unsigned-char-table control keeps that form in both passes",
    "data:charbyte #20: a byte that selects a switch keeps uint1 and its pointer uint1 * in both passes (highcase)",
    "data:charbyte #21: so the case labels past 0x7f still match the unsigned switch variable",
    "data:charbyte #2: with the option on the same byte is char and the dash a character literal",
    "data:charbyte #3: the widened copy of a char byte prints as the zero-extension it is (isdash, ishigh with the option on)",
    "data:charbyte #4: a byte also compared unsigned stays uint1 in both passes",
    "data:charbyte #5: uint1 bytes: all eight functions with the option off; the table, unsigned-compare, call-argument, sibling and counter controls with it on",
    "data:charbyte #6: char bytes: isdash, ishigh and dashlen with the option on",
    "data:charbyte #7: with the option off a byte compared with 0xe9 is uint1 against a number",
    "data:charbyte #8: with the option on the constant takes char with the byte, so the comparison stays true for 0xe9",
    "data:charbyte #9: with the option off the byte pushes uint1 * over the pointer strlen made char *",
    "data:charptr #1: with the option off the two string parameters the body walks a byte at a time are void * - the bug",
    "data:charptr #2: with the option on the byte walk names the element type",
    "data:charptr #3: a parameter shifted before it reaches the declaration is refused in both passes",
    "data:charptr #4: a one-byte read at a fixed offset is a struct field, so the pointer keeps its placeholder in both passes",
    "data:charptr #5: a value also read four bytes wide is not a character pointer in either pass",
    "data:charptr #6: a callee that declares a pointer at something else contradicts a character pointer in both passes",
    "data:charptr #7: the byte-at-a-time compare is already char * without the option, and the option does not move it",
    "data:charptr #8: a declared char * reached at a fixed non-zero offset is about the field, not the base, in both passes",
    "data:checker-stack-aggregate #1: the partial-backed scalar is declared exactly once",
    "data:checker-stack-aggregate #2: the low piece keeps the shared name",
    "data:checker-stack-aggregate #3: the high piece keeps the shared name",
    "data:checker-stack-aggregate #4: the address reference that triggered the false sibling remains",
    "data:codescalar #1: with the option off the dereferenced byte is typed by the size-1 code pointee -- the bug",
    "data:codescalar #2: and the value stored back through the function pointer is cast to that widthless type",
    "data:codescalar #3: with the option on the byte carries the width the access has, in the witness and in the control",
    "data:codescalar #4: the cast is gone with it, so the store is a plain assignment",
    "data:codescalar #5: the POINTER keeps its code type in both passes, so the indirect call still renders",
    "data:codescalar #6: the data control is untouched in both passes -- its pointer was never a code pointer",
    "data:condfold #1: OFF, the fold is declined and the sibling's test becomes a crossing goto (pass 1 only)",
    "data:condfold #2: OFF, the crossing goto needs a label on the shared continuation (pass 1 only)",
    "data:condfold #3: ON, the two conditions fold into one short-circuit OR whose right operand carries the sibling prefix as a comma expression (pass 2 only)",
    "data:condfold #4: ON, the folded node then chains with the shared clause's own condition as an AND (pass 2 only)",
    "data:condfold #5: the sibling prefix stores survive verbatim in both renderings, only their placement changes (off plus on = 2 each)",
    "data:condfold #6: the shared clause body is unchanged by the fold (off plus on = 2)",
    "data:condfold #7: the untouched tail arms are identical in both passes (off plus on = 2)",
    "data:condfold-newbury #1: OFF virtualizes one of the two edges into the shared body to a goto; WIDE folds the cascade so no goto is emitted",
    "data:condfold-newbury #2: OFF must also emit the goto's target label inside the sibling arm; WIDE emits no label",
    "data:condfold-newbury #3: WIDE folds the two equality guards into one inverted short-circuit condition guarding the terminating arm",
    "data:condfold-newbury #4: OFF keeps the guards positive and nested (the un-inverted form appears only in the OFF pass, the WIDE pass has none)",
    "data:condfold-ruleb #1: ON, the whole three-guard cascade folds into ONE short-circuit condition whose right operand is the comma expression (pass 2 only; Rule A cannot produce this - the middle guard has two statement-root calls and the outer fold's sibling is a nested BlockCondition)",
    "data:condfold-ruleb #2: OFF, the fold is declined and the sibling's void call stays a statement of its own ahead of the inner test (pass 1 only)",
    "data:condfold-ruleb #3: OFF, so does the second statement-root call - the one that puts this block over Rule A's single-call cap (pass 1 only)",
    "data:condfold-ruleb #4: OFF, the declined cascade nests the last two guards inside the first one's arm (pass 1 only)",
    "data:condfold-ruleb #5: the failure arm consumes the escaping call result in both renderings, which is what keeps that result non-implied and printed (off plus on = 2)",
    "data:condfold-ruleb #6: the guarded body is untouched by the fold (off plus on = 2)",
    "data:constspaceload #1: the non-zero immediate vmov.i32 q8,#1 folds to all four of its 32-bit lanes, default pass only",
    "data:constspaceload #2: the zero immediate vmov.i32 q9,#0 becomes two whole 8-byte zero slots, default pass only",
    "data:constspaceload #3: only the option-off pass reads near-null memory for the lanes that need an offset",
    "data:constspaceload #4: only the option-off pass leaves the immediate as a 16-byte pointer-shaped value",
    "data:constspaceload #5: the buffer still escapes to the callee in both passes",
    "data:cookiescramble #1: the /GS cookie mix drops both stack-passed arguments (bug reproduced)",
    "data:cookiescramble #2: option off leaves the dropped arguments as dead stores to locals",
    "data:cookiescramble #3: option on recovers both stack-passed arguments",
    "data:cspecprotos #1: default model, the ECX this-pointer is not a parameter",
    "data:cspecprotos #2: default model, the this-pointer degrades to an uninitialized local",
    "data:cspecprotos #3: default model, only the stack argument becomes a parameter",
    "data:cspecprotos #4: the registered __thiscall model resolves and recovers ECX as the first parameter",
    "data:cspecprotos #5: the member access is now a parameter dereference",
    "data:cspecprotos #6: the stack argument shifts to the second slot behind the this-pointer",
    "data:earlyreturn #1: OFF keeps the diamond's else-arm, ON hoists it away (else appears once, in the OFF pass only)",
    "data:earlyreturn #2: ON logs the const-guard hoist exactly once",
    "data:finalorder #1: no statement is emitted after an unconditional goto",
    "data:finalorder #2: the body does not open with a label",
    "data:finalorder #3: the body opens with an indented statement",
    "data:finalorder #4: the entry component prints first",
    "data:finalorder #5: the mid-function label the buggy order hoisted is still emitted",
    "data:finalorder #6: the entry falls through into the loop head label",
    "data:foldcallret #1: spilled call return (only pass 1, off)",
    "data:foldcallret #2: call folded into condition (only pass 2, on)",
    "data:foldcallretphi #1: call result spilled to its own statement (pass 1 default with a narrowed output, and pass 5 off)",
    "data:foldcallretphi #2: the spill is read back by the read-modify-write (passes 1 and 5)",
    "data:foldcallretphi #3: call folded into the read-modify-write (only pass 2, the default, helper declared int4)",
    "data:foldcallretphi #4: a global write between the call and its use still blocks the fold (pass 3, default)",
    "data:foldcallretphi #5: and the folded form is never emitted for it (pass 3, default)",
    "data:foldcallretphi #6: a call feeding a short-circuit operator keeps its spill (pass 4 default, pass 6 off)",
    "data:foldcallretphi #7: the call is never printed inside the and-and (pass 4, default)",
    "data:foldcallretphi-join #1: an undeclared call's eax output keeps its spill (pass 1 default, pass 3 off)",
    "data:foldcallretphi-join #2: the call folds into the comparison only once big is declared int4 (pass 2, default)",
    "data:hideshadow #1: the same value is copied into the variable twice (only pass 1, off)",
    "data:hideshadow #2: the redundant copy is consolidated away (only pass 2, on)",
    "data:hideshadow #3: two copies on exclusive branches are left alone (pass 3, on)",
    "data:ifelseflatten #1: the else-bearing 3-component BlockIf is present (off plus on = 2)",
    "data:ifelseflatten #2: the else-arm constant body survives identically (no-op, not corrupted; off plus on = 2)",
    "data:ifelseflatten #3: enabling the option is byte-identical, the loop tail is unchanged (off plus on = 2)",
    "data:indirectonly #1: off assigns the remainder across into the escaped slot",
    "data:indirectonly #2: on folds the remainder back into that same slot",
    "data:indirectonly #3: on computes the call result straight into the slot",
    "data:indirectonly #4: the call result spills to a register local in take pass 1 and in both passes of keep",
    "data:indirectonly #5: that register local is declared three times, never four",
    "data:indirectonly #6: keep reads the slot's entry value for real, in both passes",
    "data:indirectonly #7: so keep's remainder is assigned across in BOTH passes; a directly-read illegal input is never marked",
    "data:indirectonly-escape #1: by default the count lives in a register local",
    "data:indirectonly-escape #2: and the escaped slot is read, not written",
    "data:indirectonly-escape #3: that slot is its own variable by default",
    "data:indirectonly-escape #4: with the option on the register is merged into the slot",
    "data:indirectonly-escape #5: so the decrement is written to memory twice instead of once",
    "data:indirectonly-escape #6: and both arms still hand the object to the call that reads it",
    "data:inferfuncentry #1: the bounded value keeps its integer type",
    "data:inferfuncentry #2: an integer bound that lands on a function entry stays a number",
    "data:inferfuncentry #3: a callback argument at the same address still prints its name",
    "data:inferfuncentry #4: a function pointer the body never orders still prints its name",
    "data:inferfuncentry #5: inferfuncentry off restores the bare literal for the argument too",
    "data:inputparamgap #1: option off drops the live-in registers sitting behind a 3-register gap (bug reproduced)",
    "data:inputparamgap #2: option off leaves r9d read with nothing assigning it (bug reproduced)",
    "data:inputparamgap #3: option on recovers the callback's full ABI signature, filler parameters and all",
    "data:inputparamgap #4: option on renders the incoming key/state registers as the parameters they are",
    "data:inputparamgap #5: a 2-register gap already recovered and both passes agree (the option moves only the limit)",
    "data:inputtile-overlap #1: the function is no longer abandoned",
    "data:inputtile-overlap #2: no overlapping-input error text survives",
    "data:inputtile-overlap #3: the wrapper is not an empty shell",
    "data:inputtile-overlap #4: every switch arm reaches its fcntl call",
    "data:inputtile-overlap #5: the F_DUPFD_CLOEXEC command is recovered",
    "data:inputtile-overlap #6: the EINVAL retry probe is recovered",
    "data:inputtile-overlap #7: the errno save/restore around close is recovered",
    "data:inputtile-overlap #8: the failure path closes the descriptor",
    "data:iteboolean #1: option off leaves the explicit 0/1 constant diamond (gap reproduced)",
    "data:iteboolean #2: option off emits the else arm storing the 0 constant",
    "data:iteboolean #3: option on re-rolls it into one boolean assignment, short-circuit condition intact",
    "data:iteboolean #4: the re-roll is logged on the function header",
    "data:itecondlist #1: option off leaves the middle diamond as an explicit if/else true arm (gap reproduced)",
    "data:itecondlist #2: option off leaves that diamond's else arm too",
    "data:itecondlist #3: option off recovers only ceil(3/2) = 2 of the 3 identical diamonds (the header count carries into pass 2, so it appears twice)",
    "data:itecondlist #4: option on recovers all 3 of them",
    "data:itecondlist #5: every recovered diamond renders as the same ternary statement (2 off + 3 on)",
    "data:itecondlist #6: the surrounding indirect calls are untouched in both passes",
    "data:iteexpr on folds a computed-arm diamond to one ?: ternary (off = if/else)",
    "data:iteregion #1: bug (off) -- the assignment diamond renders as a literal if/else (else-arm only, off pass)",
    "data:iteregion #2: bug (off) -- the else-arm constant assignment is a separate statement (off pass only)",
    "data:iteregion #3: fix (on) -- the diamond collapses to a single ?: ternary (on pass only)",
    "data:iteregion #4: fix (on) -- the rewrite is logged",
    "data:iteregion #5: p-code untouched -- the merged variable still feeds the call in BOTH passes",
    "data:jtsharepartial #2: all three passes are in the scanned stream (one memcpy_opt definition each), so the counts above are not one dropped pass",
    "data:kuna-msvcstackguard #1: the entry-side cookie scramble is emitted only with the option off",
    "data:kuna-msvcstackguard #2: the checker call is emitted only with the option off",
    "data:kuna-msvcstackguard #3: the computation the function actually performs survives both passes",
    "data:mode aggressive enables returndup (warning appears once); mode reliable does not",
    "data:namestyle #1 default: argument renders as aN",
    "data:namestyle #2 default: local renders as vN with a register source-location comment",
    "data:namestyle #3 default: unnamed call target renders as sub_<addr>",
    "data:namestyle #4 default: global data renders as dat_<addr> (lowercase, no leading zeros)",
    "data:namestyle #5 ghidra restores: argument renders as param_N",
    "data:namestyle #6 ghidra restores: local renders as iVar with no location comment",
    "data:namestyle #7 ghidra restores: unnamed call target renders as func_0x<addr>",
    "data:noreturn_extern #1: neighbour call leaks past the no-return call (pass 1 off only)",
    "data:noreturn_extern #3: __stack_chk_fail is the bounding call (both passes)",
    "data:opcode-seam #1: the function emits a real body (was: no output, seam panic)",
    "data:opcode-seam #2: RuleBitUndistribute pulls the common INT_SRIGHT out of the AND",
    "data:opcode-seam #3: no residual per-operand shift is left behind",
    "data:orchain #1: option off opens the cascade with the first operand as its own guard (gap reproduced)",
    "data:orchain #2: option off gives the second operand a private return too",
    "data:orchain #3: and the third",
    "data:orchain #4: and the fourth, so one source expression is four guards plus a trailing return",
    "data:orchain #5: the splits are returndup's (the header slug carries into pass 2)",
    "data:orchain #6: option on (the default) folds the whole operand chain back into one short-circuit condition",
    "data:orchain #7: three of the four `return 1;` are the cascade's, one is the folded form's",
    "data:orchain #8: two of the three `return 0;` are the cascade's, one is the folded form's guarded return",
    "data:outlang #1: pass 1 keeps the C switch statement",
    "data:outlang #2: pass 1 keeps C case labels",
    "data:outlang #3: pass 1 keeps the C default label",
    "data:outlang #4: pass 2 emits the Rust function shell",
    "data:outlang #5: pass 2 emits the allow attribute",
    "data:outlang #6: pass 2 emits let-mut declarations",
    "data:outlang #7: pass 2 spells types as Rust primitives",
    "data:outlang #8: pass 2 emits a match with the synthesised wildcard arm",
    "data:outlang #9: pass 2 emits a postfix as-cast",
    "data:outline #1: OFF is the bug - the inlined body is spliced into the caller (pass 1 only)",
    "data:outline #2: OFF - the body's own branch is present too (pass 1 only)",
    "data:outline #3: ON - the whole region becomes one call named after the region head (pass 2 only)",
    "data:outline #4: ON - the report names the block count and the recovered signature (pass 2 only)",
    "data:outline #5: ON - the excised body is gone, so its multiply appears in pass 1 only",
    "data:outline #6: the surrounding function is untouched in both passes (off plus on = 2)",
    "data:outline #7: the guard that reaches the region is untouched in both passes (off plus on = 2)",
    "data:outline-proto #1: OFF is the bug - the region is inline (pass 1 only)",
    "data:outline-proto #2: ON - the region becomes one call, and the RETURN VALUE is recovered as its result, not dropped (pass 2 only)",
    "data:outline-proto #3: ON - the report shows one argument AND one result; a 0-result banner here is defect 2 (pass 2 only)",
    "data:outline-proto #4: the call AFTER the region keeps exactly one argument in both passes - defect 1 gave it stack junk (off plus on = 2)",
    "data:outline-proto #5: no raw stack pointer leaks into the C in either pass - defect 1 emitted `&Stack...`",
    "data:outline-proto #6: no rsp-typed local is introduced in either pass - defect 1 emitted `// rsp`",
    "data:oxidizer-SecurityCheck #1: the panic helper call is emitted only with the option off",
    "data:oxidizer-SecurityCheck #2: the bounds guard is emitted only with the option off",
    "data:oxidizer-SecurityCheck #3: the guarded load itself is untouched (emitted by both passes)",
    "data:paramcopyhoist #1: option off sinks the second parameter's copy-shadow below the first guard (gap reproduced)",
    "data:paramcopyhoist #2: option on anchors both copy-shadows in the entry block, ahead of every guard",
    "data:paramcopyhoist #3: both passes keep the guard writing NULL through each parameter",
    "data:paramrefdecl #1: option off keeps the distinct body local under a collision-free suffix",
    "data:paramrefdecl #2: each pass references the identifier selected for its address-taken object",
    "data:paramrefdecl #2b: the guarded pass resolves the address to the signature parameter",
    "data:paramrefdecl #3: the parameter keeps its place in the signature in both passes",
    "data:ptrdepthcap #1: option off leaves the unbounded escalation (bug reproduces: 6 four-star decls + 1 cast)",
    "data:ptrdepthcap #2: option on settles the lattice (7 in the off pass + 0 in the on pass = 7; any leak back raises the count)",
    "data:ptrfromuse #10: and the named global it indexes survives in all three, instead of becoming a raw address",
    "data:ptrfromuse #11: an address-like constant that names nothing is ambiguous, so that parameter is refused too in all three passes",
    "data:ptrfromuse #1: with the option off a parameter that is only ever dereferenced is an integer -- the bug",
    "data:ptrfromuse #2: and every field read is pointer arithmetic on that integer",
    "data:ptrfromuse #3: with byte the same parameter is a byte pointer",
    "data:ptrfromuse #4: and the field reads become indexing, which is what makes this body-changing",
    "data:ptrfromuse #5: with void the parameter is a void pointer instead",
    "data:ptrfromuse #6: and the arithmetic stays, carrying a cast",
    "data:ptrfromuse #7: a parameter that is shifted before it is dereferenced is refused in all three passes",
    "data:ptrfromuse #8: and a parameter that upstream already typed is untouched in all three",
    "data:ptrfromuse #9: a parameter that subscripts a global byte table is refused in all three passes",
    "data:realtypes-pointee #1: the callback-struct parameter declares its 8-byte pointee, not void",
    "data:realtypes-pointee #2: no unknown pointee collapses to void in either pass",
    "data:realtypes-pointee #3: the 4-byte errno store keeps its access width",
    "data:realtypes-pointee #4: the 4-byte field store keeps its access width",
    "data:realtypes-pointee #5: the declared stride agrees with the index expression in both passes",
    "data:realtypes-pointee #6: the untouched off path keeps the upstream placeholder spelling",
    "data:realtypes-pointee #7: the off path shows the same width the relabel now preserves",
    "data:regionstructure #1: the if/else diamond is structured (true clause), both passes",
    "data:regionstructure #2: the else clause is structured, both passes",
    "data:regionstructure #3: the join sequence + tail return is recovered, both passes",
    "data:regionstructure #4: ZERO gotos  -  region structurer matches Ghidra's acyclic structuring (parity)",
    "data:regionstructure shortcircuit #1: the cascading b==0xc9 / a<=299 conditions fold to one compound and (both passes)",
    "data:regionstructure shortcircuit #2: the inner a<=299 condition is NOT left as a separate nested if (both passes)",
    "data:regionstructure shortcircuit #3: an unrelated single-condition else-if is unchanged (both passes)",
    "data:regionstructure-edgeorder #1: else-arm constant assignment recovered, both passes",
    "data:regionstructure-edgeorder #2: then-arm constant assignment recovered, both passes",
    "data:regionstructure-edgeorder #3: post-test guard recovered, both passes",
    "data:regionstructure-edgeorder #4: function returns the accumulator, both passes",
    "data:regionstructure-edgeorder #5: regionedgeorder on is byte-identical to off (parity invariant)",
    "data:regionstructure-loop #1: do-while recovered (BlockDoWhile), both passes",
    "data:regionstructure-loop #2: for/while recovered (BlockWhileDo), both passes",
    "data:regionstructure-loop #3: inf-loop-with-break recovered (BlockInfLoop), both passes",
    "data:regionstructure-loop #4: ZERO gotos - region structurer matches Ghidra on reducible loops (parity)",
    "data:regionstructure-loop-refine #1: multi-exit loop body is a do-while (BlockDoWhile), both passes",
    "data:regionstructure-loop-refine #2: loop-exit condition recovered structurally, both passes",
    "data:regionstructure-loop-refine #3: early-return secondary exit preserved, both passes",
    "data:regionstructure-loop-refine #4: break secondary exit + post-loop return both recovered (2 sites x 2 passes), both passes",
    "data:regionstructure-loop-refine #5: ZERO gotos - regionlooprefine on is byte-identical to off (parity invariant)",
    "data:regionstructure-switch #1: the switch head is recovered, both passes",
    "data:regionstructure-switch #2: case 0, both passes",
    "data:regionstructure-switch #3: case 3 (middle of the dense table), both passes",
    "data:regionstructure-switch #4: case 6 (last case), both passes",
    "data:regionstructure-switch #5: the default case is recovered, both passes",
    "data:regionstructure-switch #6: ZERO gotos  -  region structurer recovers the switch natively (parity with Ghidra)",
    "data:retsplitglobal #1: the shared epilogue is cloned into all three predecessors with the option off and emitted once with it on",
    "data:retsplitglobal #2: every store of the epilogue is cloned, not just its head (the last one counts the same)",
    "data:retsplitglobal #3: the vectorised store loop the epilogue reads back is untouched by the split decision (three unrolled sites x two passes)",
    "data:returncopysplit #1: fix -- no per-element store into the read-only literal in either pass",
    "data:returncopysplit #2: fix -- no raw SUB81 truncation of the literal survives",
    "data:returncopysplit #3: the real copy site still reads the literal's low half per byte",
    "data:returncopysplit #4: the real copy site still reads the literal's high half per byte",
    "data:returncopysplit #5: the rule-off pass pins the unsplit whole-half rendering",
    "data:returncopysplit #6: every reference to the literal in the transcript is a read (16 in pass 1, 2 in pass 2)",
    "data:rodatastring #1: default pipeline recovers the read-only block copy as one builtin_strncpy",
    "data:rodatastring #2: rodatastring off restores the invalid array-typed cast (the filed bug)",
    "data:short UTF-16 #1: explicit wchar_t mapping replaces narrow exact-address data",
    "data:short UTF-16 #2: the one-byte rendering is gone",
    "data:short UTF-16 #3: adjacent wide literal is preserved",
    "data:short UTF-16 #4: ordinary ASCII literal is preserved",
    "data:signedness #1: the masked loop bound is declared unsigned under option signedness upstream, the declaration type inference produces (bug reproduces, once)",
    "data:signedness #2: and every signed comparison on it carries a cast back to int4 (bug reproduces, once)",
    "data:signedness #3: the shipped default, option signedness auto, declares it at the signedness its operations demand (fix, once)",
    "data:signedness #4: and the cast the declaration makes a no-op is dropped (fix, once)",
    "data:signedness #5: a local narrower than int keeps its declaration in BOTH passes, because C promotes it before every operator (once per pass)",
    "data:signedness #6: while the int-wide locals in that same function do flip, so #5 is the width rule declining and not an inactive option (fix, once)",
    "data:signedness-int16 #1: the 2-byte local keeps its declaration under both arms, because the reader promotes it before every operator (once per pass)",
    "data:signedness-int16 #2: nothing 2 bytes wide is ever re-signed on this target, whose own int is 2 bytes",
    "data:signedness-int16 #3: the 4-byte control is declared signed under option signedness upstream (once)",
    "data:signedness-int16 #4: and prefer-unsigned does re-sign it on this same target, so #1 is the width rule declining and not an inactive option (once)",
    "data:simdlane #1: the option-off pass keeps all 32 opaque lane extractions (and the option-on pass has none)",
    "data:simdlane #2: the option-off pass declares the 16-byte shuffle result as an array temporary",
    "data:simdlane #3: the option-off pass declares the last byte-lane temporary of the second register",
    "data:simdlane #4: the option-on pass re-anchors every lane on the broadcast source byte (49 of the 50 occurrences)",
    "data:simdlane #5: neither pass invents a lane the shuffle does not select",
    "data:snipreads-indirect #1: the out-parameter copy directly follows the call statement in both passes",
    "data:snipreads-indirect #2: no slot copy immediately precedes the call text (the hoisted shape is gone)",
    "data:snipreads-indirect #3: freecon consumes the copied post-call value in both passes",
    "data:spacebase-unnamed #1: no internal PTRSUB operator reaches the C body",
    "data:spacebase-unnamed #2: no raw ESP register leaks into an expression",
    "data:spacebase-unnamed #3: every statement touching the untracked frame renders a stack-location leaf",
    "data:spacebase-unnamed #4: positive frame offset renders as its storage",
    "data:spacebase-unnamed #5: zero frame offset renders as its storage",
    "data:spacebase-unnamed #6: negative frame offset wraps in the space address size",
    "data:stackarggap #1: option off invents a fourth and fifth argument to reach a stack slot (bug reproduced)",
    "data:stackarggap #2: option on ends the argument list at the register the caller never wrote",
    "data:stalejumptable #1: the real jump-table switch survives on BOTH passes (0 before the fix: pass 1 panicked)",
    "data:stalejumptable #2: case 0x7a ('z') on both passes",
    "data:stalejumptable #3: case 0x80 (CHECK_ORDER) on both passes",
    "data:stalejumptable #4: case 0x83 (TOTAL) on both passes",
    "data:stalejumptable #5: no pipeline failure on either pass",
    "data:subright #1: the top-byte truncation lowers to a shift and a cast",
    "data:subright #2: the middle-byte truncation lowers to a shift and a cast",
    "data:subright #3: the one-byte-offset truncation lowers to a shift and a cast",
    "data:subright #4: the least significant truncation is untouched in both passes (the c==0 bail)",
    "data:subright #5: a lone constant right-shift reader is lumped into the synthesized shift (32+3)",
    "data:subright #6: every raw SUBnn operator in the transcript comes from the rule-off pass",
    "data:subright #7: the rule-off pass pins the pre-fix truncation rendering",
    "data:subright #8: the rule-off pass pins the pre-fix un-lumped rendering",
    "data:switchmultipred #1: multi-predecessor switch renders as a computed call (bug reproduced)",
    "data:switchmultipred #2: switchmultipred on bounds the table and recovers the switch",
    "data:switchmultipred #3: switchmultipred on recovers the first table case (index 0)",
    "data:switchmultipred #4: switchmultipred on recovers the last table case (index 16)",
    "data:switchmultipred #5: the recovered switch has no default case (matches angr's 17-case-no-default)",
    "data:switchreturn #1: the switch dispatch head survives in BOTH passes (only the case bodies change)",
    "data:switchreturn #2: ON hoists each const case into its own return, so the default 0xffffffff becomes a return exactly once (ON pass only)",
    "data:switchreturn #3: ON logs the const-case return hoist exactly once",
    "data:switchsharedcase #1: option off downgrades the dispatch to an indirect call (bug reproduced)",
    "data:switchsharedcase #2: option on bounds the table and recovers the switch (recovered exactly once, on-pass only)",
    "data:symbol-keyed-local-decls #1: the overlap group has one whole declaration in each pass",
    "data:symbol-keyed-local-decls #2: the surviving declaration is as wide as the widest access, in both passes",
    "data:symbol-keyed-local-decls #3: even the off path cannot emit a narrow same-name shadow",
    "data:symbol-keyed-local-decls #4: the adjacent slot is untouched in both passes",
    "data:symbol-keyed-local-decls #5: the body still reads the collapsed name in both passes",
    "data:symbol-keyed-local-decls #6: the 4-byte use that outgrows the 2-byte ScopeLocal symbol, checksum call sequenced before it (GH-181)",
    "data:symbol-keyed-local-decls #7: Ghidra style keeps the same single whole declaration",
    "data:symbol-keyed-local-decls #8: Ghidra-style body references bind to that whole",
    "data:tee-O2 tail-jumps #1: indirect-jump-as-call marker (pass 1 off only)",
    "data:tee-O2 tail-jumps #2: unresolved GOT-pointer indirect call (pass 1 off only)",
    "data:tee-O2 tail-jumps #3: tail call resolves to named setlocale (pass 2 on only)",
    "data:tee-O2 tail-jumps #4: the recovered tail call is logged (pass 2 on only)",
    "data:truncarg #10: a sum that provably fits its byte gets no cast",
    "data:truncarg #11: a narrow parameter forwarded untouched keeps its promoted form",
    "data:truncarg #12: a byte nothing zero-extended gets no unsigned cast",
    "data:truncarg #1: a narrowed product keeps its 16-bit truncation",
    "data:truncarg #2: a narrowed byte sum keeps its 8-bit truncation",
    "data:truncarg #3: a narrowed negation keeps its 16-bit truncation",
    "data:truncarg #4: the promoted forms are never printed",
    "data:truncarg #5: an input whose promotion zero-extends gets no cast",
    "data:truncarg #6: a sign extension is untouched",
    "data:truncarg #7: a declared narrow parameter converts the argument itself",
    "data:truncarg #8: a trimmed byte load printed as char keeps its zero-extension",
    "data:truncarg #9: an AND of unsigned byte loads gets no cast",
    "data:undefname #1: no buildUndefinedName placeholder survives the naming pass, in either pass",
    "data:undefname #2: the va_arg slot is declared once per pass, at the width the body uses",
    "data:undefname #3: the va_arg guard reads the name the loop actually writes",
    "data:undefname #4: the gp_offset bump reads and writes one identifier",
    "data:undefname #5: that identifier is the one the prologue initialises",
    "data:undefname #6: the ifreq union slot no high covers wholly is named by the wired assignDefaultNames",
    "data:undefname #7: and it takes the highest index in the function, after every other local",
    "data:undefname #8: the body writes that slot through the same name",
    "data:unrolledguard #1: both gates off, the interleaved dispatches degrade to computed calls (bug reproduced); neither fix adds any (7 pass1 + 0 pass2 + 0 pass3)",
    "data:unrolledguard #2 / jtsharepartial #1: both fixes recover every interleaved table as a switch, independently (1 pass1 + 16 pass2 unrolledguard + 16 pass3 shipped default = 33)",
    "data:varargstackargs #1: option off drops the stack-passed variadic argument (bug reproduced)",
    "data:varargstackargs #2: option on recovers the variadic argument the ABI passes on the stack",
    "data:whiledo-complex #1: compound while condition forces overflow syntax (valid C)",
    "data:whiledo-complex #2: no statement is inlined inside a while-parens condition (plain form absent)",
    "data:whiledo-complex #3: the bound check is a proper if statement in the loop body",
    "data:whiledo-complex #4: the loop exit is a proper break statement",
    "data:whiledo-complex #5: the bound-check return is a statement, not condition text",
    "data:x64syscall #1: with the option off the user-op reads nothing, so it prints with empty parens in both syscall-bearing functions -- the bug",
    "data:x64syscall #2: and nothing redefines RAX, so kernel_call returns the RAX:RDX pair it was handed instead of a result",
    "data:x64syscall #3: on, kernel_call recovers the number and all three arguments, and returns the syscall's own result",
    "data:x64syscall #4: on, split_setup keeps RAX alone -- its RDI is set two blocks back, which the in-block walk does not claim",
    "data:x64syscall #5: abi takes all six argument registers, recovering split_setup's RDI at the price of the ones nothing writes",
    "data:x64syscall #6: the no-SYSCALL control is byte-identical in all three passes"
  ],
  "returncode": 0,
  "unit_footer": null
}