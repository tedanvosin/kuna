[
  {"option": "compareform", "values": ["canonical", "original"], "default": "original", "destructive_as_default": false, "phase": "P3", "subphase": "comparison-canonicalization", "strength": "HARD", "rewind": "P3", "issue": "GH-558", "summary": "Whether comparisons keep their source form (V <= c) or the analysis-canonical rewrite (V < c+1).", "use_when": "Flip to canonical only to reproduce upstream Ghidra output; original (default) is more faithful to source.", "example": "option compareform canonical", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra upstream comparison rendering (GH-558 prototype)", "change_kind": "presentation-default", "tier": "core", "symptoms": ["comparison constant off by one versus upstream ghidra (x <= 9 vs x < 10)", "need the analysis-canonical compare form to diff against upstream ghidra output"]},
  {"option": "arraynotation", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "pointer-notation", "strength": "HARD", "rewind": "P9", "issue": "GH-558", "summary": "Render standalone pointer arithmetic as &base[index] (on) vs base + index (off).", "use_when": "Flip off if the consumer expects raw pointer-arithmetic rendering; on (default) is more readable.", "example": "option arraynotation off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra upstream pointer-arithmetic rendering", "change_kind": "presentation-default", "tier": "core", "symptoms": ["&base[index] rendering unwanted; consumer expects raw pointer arithmetic", "indexed array form where base + offset arithmetic is desired for diffing"]},
  {"option": "truthycond", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "condition-form", "strength": "HARD", "rewind": "P9", "issue": "kuna-cnorm-fmt", "summary": "Render a boolean-context zero comparison in truthy form (if (x) / if (!p)) vs the explicit comparison (if (x != 0) / if (p == NULL)).", "use_when": "On by default (DIV-36): idiomatic C for if/while/for/ternary conditions and &&/||/! operands. Flip off to reproduce upstream Ghidra's explicit comparisons or to diff against Ghidra output. Float compares, enum-typed zeros, and equate-named zeros always keep the explicit form; value uses (v = (x != 0)) are never rewritten.", "example": "option truthycond off", "source_decompiler": "kuna", "inspiration": "user-reported readability issue on the angr fmt corpus binary (if (v8 != (char *)0x0) vs if (v8))", "change_kind": "presentation-default", "tier": "core", "symptoms": ["if (x != 0) where idiomatic C reads if (x)", "if (p == NULL) where if (!p) is wanted", "explicit zero comparison in conditions unwanted", "truthy condition rendering needs to match upstream ghidra for diffing"]},
  {"option": "braceelide", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "brace-form", "strength": "HARD", "rewind": "P9", "issue": "kuna-cnorm-fmt", "summary": "Render a single-statement if body braceless with the statement indented on the next line (on) vs always braced (off).", "use_when": "On by default (DIV-37): idiomatic C for one-statement then-bodies. Flip off to reproduce upstream Ghidra's braced form or to diff against Ghidra output. Only plain single-statement bodies elide: labels, comments, and multi-statement bodies keep their braces; the if (cond) goto L; one-liner and else-if collapse are unaffected.", "example": "option braceelide off", "source_decompiler": "kuna", "inspiration": "user-reported readability issue on the angr fmt corpus binary (if (expr) { single-statement; })", "change_kind": "presentation-default", "tier": "core", "symptoms": ["if (x) { stmt; } where braceless if (x) stmt; is wanted", "single-statement if body keeps braces", "braced one-liner bodies unwanted", "brace style needs to match upstream ghidra for diffing"]},
  {"option": "arraycoverwidth", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "array-cover-width", "strength": "HARD", "rewind": "P9", "issue": "repipe-16-byte-vm-state", "summary": "Render an access that spans more than one element of a mapped array Symbol as the width-carrying `name._<off>_<size>_` field instead of an element-zero subscript. `PrintC::pushSymbolDetail` sends a symbol-mapped access into `pushPartialSymbol`, whose walk breaks out at the top on a whole-symbol cover and leaves the caller to render; kuna's caller has a dedicated whole-array `name[index]` branch computing `index = symboloff / elementAlignSize` that never consults the access size. So a sixteen-byte `movaps` transfer through a `char v30[16]` VM register bank printed `v30[0] = v32[0];` -- a one-byte lvalue for a sixteen-byte copy -- and the vector zero-init printed `v30[0] = 0;`. A PARTIAL multi-element access was already repaired, because it misses the break and reaches the walk's artificial `._<off>_<size>_` field, so `v30._0_4_` and `v30[0]` appeared side by side in one function for accesses four times apart in width. On, the top-of-walk break is suppressed for a TYPE_ARRAY whose element stride is smaller than the access, so the cover falls to that same artificial field and both spellings agree. An access that fits inside one element still renders its subscript (`g[3]`), and scalars, structs and unions keep the upstream break untouched.", "use_when": "On by default: the element-zero subscript is a false statement about the access width, and every changed line is a token-for-token substitution -- 0/675 datatest assertions move, and a whole-binary sweep over 40 images changed only the accesses whose width the subscript could not carry, with no statement added, deleted or re-anchored. Flip off to reproduce upstream Ghidra's bare whole-symbol name (and kuna's pre-existing element-zero form) when diffing against Ghidra, or when a consumer parses the subscript spelling.", "example": "option arraycoverwidth off", "source_decompiler": "kuna", "inspiration": "repipe round 4 (crackmes.one 605443e333c5d42c3d016f59, KataVM_L1): a VM interpreter's two four-word register banks are swapped with movdqa/movaps at 0x1410-0x1428 and the swap emitted as `v30[0] = v32[0];`", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a 16-byte movaps/movdqa transfer emits as v1[0] = v2[0]", "a vector zero store emits as arr[0] = 0", "an array-typed local is assigned at element zero for a whole-array copy", "the same variable shows both v1._0_4_ and v1[0] for accesses of different width", "declared char v[16] but every whole-bank access reads as one byte"]},
  {"option": "emptystrconst", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "empty-string-constant", "strength": "HARD", "rewind": "P9", "issue": "repipe-binary-maze-pointer-becomes", "summary": "Decline the quoted string literal for a constant pointer whose readonly target spells a ZERO-character string AND does not sit in string data, and print the address instead. `PrintC::pushConstant`'s pointer arm hands a character-printable pointer constant to `pushPtrCharConstant`, which replaces the address with the literal at it -- a good trade when the literal spells something. `StringManager::isString` answers yes for ANY readonly location whose first byte is a NUL, because `checkCharacters` walks only to the first terminator and therefore validates zero characters, so a pointer into a binary blob that opens with a zero row is accepted as a string and rendered `p = \"\"`. That token names no byte of the image and the address it displaced was the only thing in the statement a reader could follow -- a 585-byte packed maze at 0x403550 disappeared from the emitted C entirely, while the 0x403511 and 0x403799 constants around it survived. Not every empty literal is wrong, though: `setlocale(6,\"\")` is idiomatic C, and a linker that merges string constants stores the program's only `\"\"` as the tail NUL of another literal. The two separate on the bytes PAST the terminator, which is the evidence the emptiness test never looked at: sixteen bytes are read, the terminator run is skipped, and the next run is walked to its NUL -- if any byte in it is outside printable ASCII and tab/newline/return, this is not a string table and the literal is declined. It is a falsification test on purpose, so everything it cannot judge keeps the upstream literal: a window of nothing but NULs (section padding), a run still spelling text at the window's end, an unreadable neighbourhood, and any literal with a character of its own. Only the first run after the terminator is judged, so `du`'s `fts_alloc(sp,\"\",0)` -- whose `\"\"` opens the table `\"\" \".\" \"..\"` and is followed by relocation bytes four bytes later -- keeps its quotes.", "use_when": "On by default: an empty literal that is not backed by string data is strictly less informative than the address it replaces, and the rule reaches nothing else -- 0/675 datatest assertions move. Flip off to reproduce upstream Ghidra's (and kuna's pre-existing) `\"\"` spelling when diffing against Ghidra, or when a consumer relies on a pointer to a NUL byte printing as an empty string.", "example": "option emptystrconst off", "source_decompiler": "kuna", "inspiration": "repipe round 4 (crackmes.one 60be2ad433c5d410b8842c95, Sabloom Text 6.exe): sub_402020 walks a 585-byte packed maze at 0x403550 whose first row is all zeroes, and both pointers into it printed as empty string literals", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a pointer variable is initialised to an empty string literal and then indexed past its terminator", "a data address visible in the disassembly appears nowhere in the emitted C", "a blob or table pointer renders as = \"\" instead of its address", "two locals are both assigned \"\" and then walked as buffers"]},
  {"option": "structdefs", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P9", "subphase": "type-definition-preamble", "strength": "HARD", "rewind": "P9", "issue": "kuna-structdefs", "summary": "Print the definitions of the composite, enum and typedef types a function's C names ABOVE the function. kuna recovers a layout and then does not show it: a body that reads `f->_flags` names `FILE` in its signature and nothing on the surface says what a `FILE` is, so the struct the decompiler recovered is invisible to the one surface an agent reads. The renderer has existed since `kuna decompile-project` -- `PrintC::doc_type_definitions` writes every user-defined type into the export's `.h` -- but nothing called it for a single function. When on, `kuna_structdefs::referenced_types` collects the function's semantic type surface (the prototype's return and parameter types, every Varnode data-type, every mapped Symbol type behind a HighVariable), descends through typedef bases and component types -- a pointer's pointee, an array's element, a struct's fields -- and pushes each definable type in postorder, so the list is definition-before-use over exactly the referenced subset rather than the whole factory. Those roots are a superset of the type names the printer can spell (a declaration is read off a high or its symbol, the signature off the prototype, a cast off the Varnode type it casts to), so the preamble cannot miss a definition the body refers to -- and it catches the struct behind `p->field_0x8`, whose tag name the C never prints at all. The text is the SAME `render_type_definitions` the `.h` type block is built with: the forward-declaration block, then bodies, an incomplete struct printing as `typedef struct FILE FILE; /* opaque */`, core types never. A `kuna decompile-project` export's bodies suppress the preamble, because they include the header that already carries it. `kuna decompile-all --json` carries the same set as an additive per-function `types` array (name, definition text, size) so a consumer reads the layout without parsing C.", "use_when": "Flip on when you are reading a function whose arguments are pointers to composites and want the layout in front of you -- `structsynth`'s synthesized `struct_N`, a DWARF struct on an unstripped binary, `libctypes`' opaque `FILE`/`DIR` shells -- instead of opening the `decompile-project` header to find it. Default OFF: it is a presentation default, and a preamble in front of every function is noise for a caller diffing bodies or feeding one function to a matcher. It changes nothing but the text above the function -- the body, its line contents and every recovered type are identical either way -- but it DOES move line numbers, so a consumer pinning a line number should read `line_mappings` rather than assume the first line is the signature. The preamble is documentation, not a compilable unit: a padding member spelled `undefined1` needs the export's recompile prelude to compile, which is one more reason the project surface keeps the definitions in the header.", "example": "option structdefs on", "source_decompiler": "angr", "inspiration": "angr's decompiler prints the typedefs a function references above it (`analyses/decompiler/structured_codegen/c.py`), which is a large part of why its output reads struct-aware; kuna had the same renderer and only a whole-project caller for it.", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["the signature says FILE * or struct_0 * and nothing says what the fields are", "a field access like f->_flags with no layout anywhere in the output", "you have to run decompile-project just to read the .h for one function's struct", "a synthesized struct_N appears in a parameter type with no definition"]},
  {"option": "warnstyle", "values": ["inline", "banner"], "default": "inline", "destructive_as_default": false, "phase": "P9", "subphase": "warning-style", "strength": "HARD", "rewind": "P9", "issue": "kuna-cnorm-fmt", "summary": "Render analysis warnings as terse end-of-line // slugs on the line they describe (inline) vs full /* WARNING: ... */ banner lines (banner).", "use_when": "Inline by default (DIV-38): usage(1); // no-return, if (v8) { // branch-flip, prototype-line // early-return x3. Flip to banner to reproduce upstream Ghidra's full warning text or to grep for the long-form messages. Unknown warning texts keep their full text behind a warn: marker; user/dwarf_lines comments always keep banner lines.", "example": "option warnstyle banner", "source_decompiler": "kuna", "inspiration": "user-reported readability issue on the angr fmt corpus binary (obtrusive full-line WARNING banners)", "change_kind": "presentation-default", "tier": "core", "symptoms": ["full-line WARNING banner comments clutter the output", "warning text wanted inline at the end of the statement it describes", "terse warning slugs unwanted; full upstream warning text needed", "warning style needs to match upstream ghidra for diffing"]},
  {"option": "thumbfuncptr", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "const-pointer", "strength": "HARD", "rewind": "P5", "issue": "GH-8471", "summary": "Preserve a Thumb function pointer (fn|1) as a symbolic &fn[1] rather than a raw hex literal.", "use_when": "Flip off only to see the raw constant; on (default) recovers the function symbol on ARM/Thumb.", "example": "option thumbfuncptr off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-8471 (ARM/Thumb function-pointer recovery)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["thumb function pointer renders as symbolic &fn[1] where the raw odd-address constant is wanted", "callback constant on arm resolves to a function symbol plus one instead of a bare hex literal"]},
  {"option": "inferfuncentry", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "const-pointer", "strength": "HARD", "rewind": "P5", "issue": "GH-6930", "summary": "Infer a function pointer when a constant equals an exact function entry at a single-bit image base (e.g. 0x100000).", "use_when": "Flip off only to reproduce the bare-literal form; on (default) names the function.", "example": "option inferfuncentry off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-6930 (function-entry constant inference)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a bare constant equal to a function entry renders as the named function pointer", "hex literal at a single-bit image base unexpectedly named as a function (flip off for the raw constant)"]},
  {"option": "booleanmask", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "GH-1282", "summary": "Fold the (b<<k) s>>k boolean sign-extension-mask idiom (flag-as-high-bit lowering) into a clean comparison.", "use_when": "Flip off only to see the raw shift idiom; on (default) cleans flag-modelled comparisons (8051 etc.).", "example": "option booleanmask off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-1282 (boolean sign-extension-mask idiom)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["want the raw (b<<k) s>> k sign-extension shift idiom visible instead of the folded comparison", "flag-as-high-bit lowering (8051 style) hidden by the cleaned boolean compare"]},
  {"option": "mulblob", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "wide-multiply-operand-blobs", "summary": "Print a widened 64x64->128 multiply operand as the value it is (`SUB168(ZEXT816(x) * ZEXT816(y),8)`) instead of materialising a 16-byte local per operand and filling it in halves.", "use_when": "On by default. x86-64 `MUL r64` lowers (SLEIGH `ia.sinc:4128`) to `tmp:16 = zext(RAX) * zext(rm64)`, and a 16-byte varnode has no primitive data-type, so the type factory hands it the width fallback `undefined1[16]` - an array, which `Datatype::isPieceStructured` accepts. `RulePieceStructure` (ruleaction.cc:7488) therefore rewrites each zero-extension into a `PIECE` rooted at a partial-root Varnode, and P6 gives that root a declaration of its own: every unsigned wide multiply costs two `char vN [16]` locals whose entire content is the operand (`v8._8_8_ = 0; v8._0_8_ = v29;`) before a single `SUB168` reads the product back out. The signed sibling never had this: `IMUL` lowers through `sext`, `INT_SEXT` is not in `RulePieceStructure`'s op list, and kuna already prints `SUB168(SEXT816(x) * SEXT816(y),8)`. On, the unsigned form prints the same way. The decline is narrow: the extension's output must be an anonymous unique, not addr-tied, not mapped, not covered by a Symbol, 9..16 bytes wide, typed with the width fallback (an array of exactly that many one-byte `undefined` elements) and read only by same-width integer arithmetic whose own result is read only through `SUBPIECE`. A Varnode backed by real storage or a recovered array keeps upstream's structuring in either arm. Flip off to restore the upstream `undefined1 auVarN [16]` rendering - Ghidra prints those blobs too, so this is a deliberate divergence, not a fidelity fix.", "example": "option mulblob off", "source_decompiler": "kuna", "inspiration": "coreutils fmt -O2 xstrtoumax (10 `MUL RCX` sites): 20 of the function's 28 declarations were the operand blobs, 23 of the binary's 298 non-thunk declarations (7.7%); IDA prints the same expression as one `__int128` multiply", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["a 16-byte local (`char vN [16]` / `undefined1 auVarN [16]`) is declared for every operand of an unsigned multiply", "`vN._8_8_ = 0; vN._0_8_ = x;` pairs appear in front of a `SUB168(... * ...,0)`", "a function with several `mul` instructions declares twice as many locals as it has values", "the high half of a 64x64 product is read out of a blob instead of off the multiply"]},
  {"option": "cancelbytearithmetic", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "repipe-cancelling-byte-arithmetic-splits", "summary": "Fold an exact one-byte multiply/left-shift pair whose coefficients cancel modulo 256.", "use_when": "On by default (DIV-174). Keep it on when byte arithmetic retains matching multiply and left-shift terms that provably cancel modulo 256. Flip off to retain the unsimplified arithmetic.", "example": "option cancelbytearithmetic off", "source_decompiler": "kuna", "inspiration": "repipe:cancelling-byte-arithmetic-splits, 69d7f15a8afd9d6c48b48871 crackme_shroud.exe sub_1406fa160; upstream RuleSubCommute's conservative INT_LEFT input gate", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["byte arithmetic retains -2*x + (x << 1) instead of cancelling modulo 256", "a string initializer is split by one computed byte even though its coefficients sum to zero", "an exact low-byte multiply/left-shift cancellation remains unsimplified"]},
  {"option": "simdlane", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "repipe-simd-constant-string-initializer", "summary": "Resolve a one-byte lane read of a CONSTANT-mask `pshufb` byte shuffle to the source lane the mask actually selects. `pshufb` has no p-code semantics: the x86 SLEIGH spec models it as an opaque CALLOTHER user op over a 16-byte value. `ActionLaneDivide` then splits the vector consumers into byte lanes, so every lane of the result becomes its own SUBPIECE of something nothing downstream can see through, and neither RuleSubExtComm/RuleSubZext nor copy propagation can collapse them -- even though the shuffle mask is a compile-time constant sitting in the operand. The instruction is a pure permutation with an exact per-lane definition once the mask is known, `dst[i] = (mask[i] & 0x80) ? 0 : src[mask[i] & (N-1)]`, so a one-byte `SUBPIECE(pshufb(src,mask), i)` over a constant mask IS `SUBPIECE(src, mask[i] & (N-1))` or the constant 0. The rewrite is that identity, not a heuristic; with a non-constant mask the rule declines. Once every lane read is re-anchored on the source the CALLOTHER loses its last reader, and for the standard byte-broadcast idiom `pxor xmm2,xmm2; pshufb xmm0,xmm2` all sixteen lanes resolve to `SUBPIECE(src, 0)` and collapse into one value. Bounded: only a user op the architecture registered under a shuffle name (`pshufb`, `vpshufb` -- resolved once per program into the ArchSeam), only the three-input form whose two operands and output all have the vector width, only widths 8 (MMX) and 16 (SSE), and only a ONE-BYTE lane read -- a wider SUBPIECE of a shuffle is a concatenation of lanes, not another SUBPIECE, so it is declined rather than approximated. A mask wider than eight bytes cannot be decoded out of a `uintb` offset and is accepted only at value 0, where the offset IS the whole value and every lane byte is provably zero; that is the broadcast mask, and it is the only wide constant mask the engine constructs.", "use_when": "On by default. Keep it on when a function that touches SSE renders a wall of `SUBnn(v,k)` lane temporaries off one `pshufb(...)` result -- typically a compiler-vectorised byte loop, where a single scalar value was broadcast across a register and every one of the sixteen lanes then reads back the SAME byte. The tell is a declaration block of `char vN;` entries each assigned once from `SUBxx(vM,k)` for consecutive k. Flip OFF to see the individual lane extractions and the shuffle result they come from -- e.g. when checking by hand which source byte a hand-written or obfuscated non-broadcast shuffle mask selects. Inert on any binary with no constant-mask pshufb, so output is byte-identical either way.", "example": "option simdlane off", "source_decompiler": "kuna", "inspiration": "repipe:simd-constant-string-initializer, 653d88600f4238b24302b0ec (macOS x86-64 Crackme0x04) sub_100003790; Intel SDM PSHUFB; upstream ActionLaneDivide (coreaction.cc) leaves a CALLOTHER lane source opaque", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a wall of SUB161( lane temporaries read off one pshufb( result", "dozens of char vN; declarations each assigned once from a consecutive SIMD byte lane", "a vectorised byte loop needs SSE lanes simulated by hand to see that every lane holds the same broadcast byte", "a constant-mask pshufb result is never folded even though the mask is an immediate zero"]},
  {"option": "constspaceload", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "repipe-arm-neon-zero-initialization", "summary": "Resolve a SLEIGH dynamic constant-space export to the value it exports instead of leaving it as a pointer-shaped LOAD. A SLEIGH constructor that ends in `export *[const]:N v` lowers to `LOAD(const, v)`, and the defining property of the constant space is that an address IS its own value -- which is exactly the identity `RuleLoadVarnode` applies when `v` is already a constant Varnode. That identity does not stop holding when `v` is a temporary, but no rule applied it there, and `RuleCollapseConstants` cannot turn `v` into a constant when it is wider than a `uintb` (`PcodeOp::isCollapsible` rejects any output over eight bytes), so a 16-byte SIMD immediate such as ARM `vmov.i32 q8,#0` (`simdExpImm_16`, whose body is `tmp:16 = 0; export *[const]:16 tmp`) survives as a LOAD through a pointer. `ActionLaneDivide` then splits that LOAD into one lane LOAD per word at `v`, `v+4`, `v+8`, `v+0xc` -- a split that is invalid in the constant space, because offsetting an address there changes the value rather than selecting a word of it. Lane 0 needs no offset and folds correctly; the rest are left reading near-null memory, and folding them where they stand would print the literal offsets instead. This option rewrites `out:N = LOAD(const, p:N)` to `COPY p`, at equal widths only. Applied before `ActionLaneDivide`, so a vector immediate is an ordinary value by the time the lanes are cut and each lane is split off the value. Bounded: only the constant space is matched; a width mismatch is declined, because a dynamic `export *[const]:N tmp` gives the operand and `tmp` the same size, so N != S is a different shape and resizing it would invent a truncation rather than apply an identity -- a `SUBPIECE`/`INT_ZEXT` version of this rewrite was measured to re-render live AVX-512 `k` mask registers on statically linked glibc, and in `__strlen_evex`-shaped code one mask lost its reaching definition at a shared label, so the scan loop read a stale earlier compare; a constant pointer is left to `RuleLoadVarnode` (which also resolves the spacebase-placeholder tail a bare COPY would drop), and a free pointer is declined.", "use_when": "On by default. Keep it on when a function that sets up a structure or buffer with one SIMD immediate renders the initialization as reads through a null pointer -- `*(unsigned int *)(0 + 4)`, `(0 + 8)`, `(0 + 0xc)` -- with the word at offset 0 correctly showing as a plain `0`. The unfolded `0 + k` address expressions are the tell: ordinary constant folding would have printed `4`. Flip OFF to see the upstream rendering, i.e. the const-space LOAD and the lane reads `ActionLaneDivide` cuts from it. Inert on any binary whose SLEIGH constructors emit no dynamic const-space export, so output is byte-identical either way.", "example": "option constspaceload off", "source_decompiler": "kuna", "inspiration": "repipe:arm-neon-zero-initialization, 68d40081224c0ec5dcedc2d2 (ARM 32-bit ELF `trap`) _start_main; ARMneon.sinc:549 simdExpImm_16; upstream RuleLoadVarnode (ruleaction.cc:4258) applies the same identity only to a constant pointer", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a zeroed struct or buffer renders as reads from (0 + 4), (0 + 8), (0 + 0xc)", "a SIMD immediate becomes a near-null memory read instead of a constant", "address expressions print as an unfolded 0 + k instead of the constant k", "only the word at offset 0 of a vector initialization is correct"]},
  {"option": "ovlesssimplify", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "GH-7190", "summary": "Simplify the explicit S/OV-flag compare idiom into a direct signed comparison.", "use_when": "Flip off only to see the raw overflow-flag arithmetic; on (default) cleans V850-style compares.", "example": "option ovlesssimplify off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-7190 (S/OV-flag compare idiom)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["want the raw S/OV overflow-flag arithmetic visible instead of the folded signed compare", "v850-style explicit flag compare collapsed to a clean signed less-than"]},
  {"option": "addcarrychain", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P5", "issue": "GH-8913", "summary": "Recover an 8-bit carry-chain (ADC) pair into a single wide add instead of CONCAT11(CARRY1(...)).", "use_when": "Flip off only to see the raw carry intrinsics; on (default) recovers wide arithmetic (6502 etc.).", "example": "option addcarrychain off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-8913 (8-bit ADC carry-chain recovery)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["want the raw CONCAT11(CARRY1(...)) intrinsics visible instead of one wide add", "8-bit adc carry-chain pair (6502 style) folded into a single 16-bit addition"]},
  {"option": "rodatastring", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "constsequence", "strength": "HARD", "rewind": "P5", "issue": "repipe-68149b8a-stack-string-invalid-cast", "summary": "Collapse a read-only string block copy (wide rodata loads re-stored into the frame) into a single builtin_strncpy.", "use_when": "Flip off to keep the raw partial-symbol slice assignments; on (default) recovers the literal the copy spells.", "example": "option rodatastring off", "source_decompiler": "kuna", "inspiration": "Ghidra RuleStringCopy (constant stack strings) extended to read-only block copies", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["stack string initialisation renders as an array-typed cast such as (char[8])s_addr._0_8_", "partial-symbol slice assignments like buf._0_9_ = s_addr._16_9_ instead of the string", "a recognized rodata literal never appears in the function that copies it onto the stack", "builtin_strncpy call where the individual slice assignments are wanted (flip off)"]},
  {"option": "memsetrecover", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "constsequence", "strength": "HARD", "rewind": "P5", "issue": "GH-9230/1537", "summary": "Collapse a run of constant-fill stores (incl. inlined/SIMD bzero) into a single builtin_memset.", "use_when": "Flip off only to see the individual element stores; on (default) reconstructs the memset.", "example": "option memsetrecover off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-9230/1537 (constant-fill store run -> builtin_memset)", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["long run of per-element constant stores instead of a single memset", "unrolled or SIMD zeroing rendered as dozens of assignments", "builtin_memset call where the individual element stores are wanted (flip off)"]},
  {"option": "returnpair", "values": ["pair", "single"], "default": "pair", "destructive_as_default": true, "phase": "P4", "subphase": "trial-budget", "strength": "HARD", "rewind": "P4", "issue": "GH-6990", "summary": "Whether a passively-active second return register may be joined into a wide return (pair) or dropped (single).", "use_when": "Set single PER FUNCTION when a void/single-register function shows a spurious CONCAT44 return (e.g. SPARC); DESTRUCTIVE as a global default (truncates real multi-register returns).", "example": "option returnpair single", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-6990 (wide return-register joining)", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["spurious CONCAT44 return value in a void or single-register function", "two return registers joined into one double-width return (e.g. sparc o0/o1)", "return type twice the natural width built from a register concat"]},
  {"option": "v850indirectbranch", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "GH-8817", "summary": "Reclassify a V850 jmp [reg] CALLIND to BRANCHIND so switch-table recovery runs.", "use_when": "Set on PER V850 PROGRAM to recover jump-table switches; DESTRUCTIVE as a global default (matches register-indirect calls on other architectures).", "example": "option v850indirectbranch on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-8817 (V850 jmp[reg] flow classification)", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["v850 jmp [reg] rendered as an indirect call instead of a recovered switch", "jump table on v850 never recovered because the dispatch decodes as a computed call"]},
  {"option": "msvcftol", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "kuna-msvcftol", "summary": "Lower the MSVC x86-32 float-to-integer CRT helpers (`__ftol`, `__ftol2`, `__ftol2_sse`) to p-code via a synthesized `<callfixup>`, so the conversion prints as a C cast instead of an argument-less `__ftol()` call. MSVC passes the value in the x87 stack top ST0 and returns the __int64 in EDX:EAX, but no vendored x86 prototype model has an `<input>` pentry naming an x87 register (ST0 appears only as an `<output>` pentry), so the call characterizes ST0 as NoContainment, no argument trial is created, and the feeding FLD chain is dead-code eliminated together with every register it was based on -- including the ECX `this` pointer of a __thiscall method. The fixup body pops the CALL's pushed return address, truncates ST0 at the full 64-bit width into EDX:EAX, and pops the x87 stack.", "use_when": "An x86-32 MSVC binary shows bare `__ftol()` / `__ftol2()` calls with no arguments and the floating-point computation that fed them is missing (often together with a lost `this` pointer). On by default; flip OFF to restore the un-fixed call rendering.", "example": "option msvcftol off", "source_decompiler": "ida", "inspiration": "Hex-Rays ships a name-keyed compiler-helper table (ftol/ftol2/ftol2_sse/...) that lowers these to plain casts; upstream Ghidra ships the same mechanism for alloca_probe/mcount but no ftol entry", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["__ftol() / __ftol2() called with no arguments", "the float computation feeding a float-to-int conversion is missing from an MSVC x86-32 binary", "a __thiscall method lost its `this` pointer and reads its fields off a stack argument instead"]},
  {"option": "cortexmpriv", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P2", "subphase": "inline-inject", "strength": "HARD", "rewind": "P2", "issue": "kuna-cortexmpriv", "summary": "Assume the Cortex-M core is privileged, so the `isCurrentModePrivileged()` guard the vendored ARM SLEIGH wraps around every VERSION_7M special-register move folds away. Twelve `ARMTHUMBinstructions.sinc` constructors (`mrs`/`msr` against ipsr, primask, basepri, basepri_max, faultmask and control) model the move as a RUNTIME privilege test -- `b:1 = isCurrentModePrivileged(); if (!b) goto <notPriv>; <the real effect>` -- so kuna, lowering the model literally, gives every MRS/MSR one extra basic block and two extra CFG edges that exist in no source. A four-instruction `irq_disable`/`irq_restore` pair therefore lands four phantom branches in the middle of an otherwise straight-line function. On, a synthesized `<callotherfixup>` makes the user-op return the constant 1: the condition constant-folds, the guard block and its edges die, and the real effect survives unchanged. Registered at architecture bootstrap on any language that declares the user-op (i.e. ARM) and gated at the CALLOTHER consumption seam, so off the emitted C is byte-identical to a build without the fixup. IDA and Binary Ninja both model MRS/MSR as a plain intrinsic with no test at all; upstream Ghidra shares kuna's defect.", "use_when": "Cortex-M firmware whose C is broken into extra `if (isCurrentModePrivileged())` blocks around every interrupt-mask or special-register access, or whose CFG carries branches the source has no `if` for. ON in the `aggressive` preset, which `auto` selects for any binary under 500 KiB -- essentially all Cortex-M images -- so this is the default rendering of `kuna decompile`, `decompile-all` and the web front-end on firmware. The shipped catalog default is OFF because the assumption is a MODELLING JUDGEMENT, not a proof: Cortex-M Thread mode really can run unprivileged (CONTROL.nPRIV = 1) and those moves really do read as zero there. Flip off when auditing code that is meant to run unprivileged, or to see the hardware model exactly as the SLEIGH spells it. Inert on every non-ARM target and on any ARM function with no MRS/MSR.", "example": "option cortexmpriv on", "source_decompiler": "ida", "inspiration": "decbench sample set: RIOT `thread_sleep` and NuttX `nxsched_process_scheduler` score GED 24 and 26 against straight-line source, entirely from the guard blocks; IDA scores 6 and Binary Ninja 0 on the same functions because they lower MRS/MSR as an intrinsic", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["an `if (isCurrentModePrivileged())` block wraps every interrupt-mask or special-register access in Cortex-M firmware", "`isCurrentModePrivileged()` appears in the emitted C at all", "a Cortex-M function has more basic blocks and branches than its source", "`getBasePriority()`/`isIRQinterruptsEnabled()`/`getCurrentExceptionNumber()` are each read under their own conditional"]},
  {"option": "tailcalljump", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "angr-tee-O2-tail-jumps", "summary": "Recover an -O2 tail jump (a direct `jmp` to another function's entry, e.g. `jmp setlocale@plt`) as a tail call (CALL + RETURN) so the callee resolves by name and its return value flows out, instead of flowing into the callee (which inlines a PLT thunk and mis-renders it as a `(*dat_...)(...)` indirect call with a 'Treating indirect jump as call' warning). When it fires it logs a `tailcalljump: recovered tail call` warning at the branch site so the introduced call is observable.", "use_when": "A leaf function ends in `jmp <func>@plt` and kuna would emit `(*dat_...)(...)` + a 'Treating indirect jump as call' marker instead of `func(...)`. On by default (DIV-14) = the named call plus a `tailcalljump: recovered tail call` WARNING; flip OFF to restore the upstream flow-into-callee rendering (the two affected datatests, Long double #1/#2, opt out per-test).", "example": "option tailcalljump on", "source_decompiler": "angr", "inspiration": "test_decompiling_tee_O2_tail_jumps; angr tail-call handling (direct jmp to a function/PLT thunk); setlocale_null_androidfix", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["leaf function ends in a (*dat_...)(...) computed call with a 'Treating indirect jump as call' warning", "jmp to a plt stub inlined into the caller instead of a named tail call", "plt thunk body absorbed where func(...) is expected"]},
  {"option": "tailcallframe", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "direct-address-keyboard-handler", "summary": "REMOVES CODE: recover a direct `jmp` as a tail call when the instructions ending at it tear down EXACTLY the frame the entry block built, even though the target is not a known function. `tailcalljump` asks `query_call(dest).is_some()`, so it only fires on a callee some discovery oracle already found; a callee reached only through a code pointer in initialized data satisfies none of them (the recursive-descent walk never enters the callback, so it never sees the callback's `call`), and the tail `jmp` is then followed as ordinary intraprocedural flow and the whole callee is decoded into the caller. This measures two constant stack-pointer deltas over the raw p-code -- forward from the function entry over the run that establishes the frame, backward from the branch over the run that ends at it -- and fires when the second is a strictly positive teardown of exactly the first. Both scans stop at the first control-flow op, at a stack-pointer write that is not `SP = SP +/- <const>` (a `leave`-style `SP = FP` restore declines), at a non-adjacent instruction address, and after 24 instructions. A frameless leaf can never match (no frame torn down = no evidence, and an intraprocedural jump would be indistinguishable), nor can a target this function has already decoded, nor the function's own entry. Logs a `tailcallframe: recovered tail call` WARNING at the branch site.", "use_when": "A function decompiled by address runs on into an unrelated function's body -- the output starts with the code you asked for and continues through a second routine's logic, strings and callees -- and the disassembly shows an unconditional `jmp` to that routine right after a stack-frame teardown (`add rsp,N; pop reg; jmp X`). Typical of an indirect-only callback (an event/listener/vtable entry) in a stripped binary, where the callee is not in `kuna functions` and so `tailcalljump` cannot see it. On by default; flip OFF to restore the flow-into-the-callee decode.", "example": "option tailcallframe off", "source_decompiler": "ida", "inspiration": "round-2 RE-friction crackme lugosiii: the keyboard callback at 0x6500 tail-jumps to the renderer at 0x4610 (0x66e2), which no oracle discovers because 0x6500 is itself only a wl_keyboard_listener pointer; 1555 lines instead of ~440. IDA and Ghidra both end the function at the tail jump.", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a function decompiled by address continues into an unrelated function's body", "an event/listener callback emits a second routine's UI strings and callees", "the same callee is emitted twice in one function, once as a named call and once inlined", "an unconditional jmp after add rsp,N; pop reg is followed instead of ending the function", "kuna emits thousands of lines for a function the disassembly shows is a few hundred bytes"]},
  {"option": "tailcallsaved", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "argument-cleanup-creates-false", "summary": "PRESERVES CODE: narrow `tailcallframe` so a stack-pointer run that gives back nothing the entry block saved is read as argument cleanup rather than as a frame teardown. `tailcallframe` fires when the straight-line run ending at a direct `jmp` raises the stack pointer by exactly what the run leaving the entry address lowered it by; the backward scan stops at the first control-flow op, so on a cdecl caller (`push ebx; push esi; ...; push arg; push arg; call f; add esp,8; jmp L`) it measures the `add esp,8` that discards the call's two arguments and never sees the two pushes that put them there. The saved registers are still on the stack and the jump target is an ordinary block of the same function, so the recovered call truncates the function at an internal join and the rest of its body is emitted as a separate callee. A tail jump runs with the stack as `ret` would find it, so every callee-saved register the entry block pushed must have been popped back before the branch. This asks each scan for a second number beside its delta: the bytes it moved THROUGH the stack pointer -- an instruction that lowers the stack pointer and stores through it is saving, one that raises it and loads through it is restoring, each capped at that instruction's own stack-pointer motion -- and requires `restored >= saved`. A frame built purely by `sub rsp,N` saves nothing and demands nothing, so the `sub rsp,8; call f; add rsp,8; jmp g` alignment shape gcc emits at -O2 is decided exactly as before. One-directional: it can only decline a recovery `tailcallframe` would have made.", "use_when": "A function decompiled by address ends in a `sub_X()` tail call to an address the disassembly shows is a block INSIDE the same function -- typically the join both sides of an if/else reach -- with a `tailcallframe: recovered tail call` warning on it, and the code after that block is missing from the output. Look for `add esp,N` immediately after a `call` in front of the `jmp`: that is cdecl argument cleanup, not a teardown. On by default; flip OFF to restore the delta-only test.", "example": "option tailcallsaved off", "source_decompiler": "ida", "inspiration": "round-8 RE-friction crackme sddecoder: the character classifier at 0x4010b0 pushes EBX and ESI, then `call 0x40167d; add esp,8; jmp 0x401102`, and the jump target is the TEST EAX,EAX both sides of the branch at 0x4010e2 reach. IDA ends neither function there.", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a recovered tail call whose target is an internal block of the same function", "`tailcallframe: recovered tail call` on a `jmp` that follows `call f; add esp,N`", "the tail of a cdecl function is emitted as a separate callee instead of inline", "a function decompiled by address stops at an if/else join and calls sub_<join address>"]},
  {"option": "calltrampoline", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "beria-call-fragments-produce", "summary": "RESTORES CODE: flow a direct `call` through a callee that throws the pushed return address away and jumps back into the instruction stream, instead of decoding a fall-through at the return address. `CALL` is a fall-through op, so kuna decodes the return address as the next instruction; a protector fragment of the form `lea esp,[esp+4]; <one instruction>; jmp <back into the caller>` makes that address unreachable, and the byte there is free to be junk. The junk decodes into the emitted C (on the round-9 witness, a store to a global that exists nowhere) and leaves everything after it one byte out of phase with the real stream. This decodes the callee's raw p-code out of band and fires when the stack pointer passes through exactly `entrySP + <pointer size>` -- the instant the return address stops existing -- and the run then ends in a direct unconditional branch, with no call, conditional branch, indirect branch or return in between and no unaccountable stack-pointer write. The re-entry point is read off the fragment's own `jmp`, never assumed to be `return address + 1`. The branch target must NOT be a known function entry, which is what separates the fragment from an ordinary tail-call thunk (`add esp,4; jmp printf`); those stay a `CALL`. Logs a `calltrampoline:` WARNING at the call site.", "use_when": "The emitted C assigns a global whose name is nonsense (`dat_8364c783 = f();`) right after a call, and the disassembly shows the callee starts by discarding the return address (`lea esp,[esp+4]` / `add esp,4` / `pop reg`) and ends in a `jmp` back into the caller -- so the store was lifted from a byte control never reaches. Typical of the Beria/PE protector family, where one table of such fragments is called from every protected function. On by default; flip OFF to restore the fall-through decode at the return address.", "example": "option calltrampoline off", "source_decompiler": "kuna", "inspiration": "round-9 RE-friction crackme (Beria-protected PE): sub_40d120 calls five fragments in the 0x44ac2f table; the one at 0x44ac6b re-enters at 0x40d1b4, one byte past the return address, and the skipped `a3 83 c7 64 83` lifts as `MOV [0x8364c783],EAX`", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a store to a nonsense global (dat_<huge hex>) appears right after a call", "the callee of a call starts with lea esp,[esp+4] / add esp,4 / pop reg and ends in a jmp back into the caller", "the body after a call is decoded one byte out of phase with the disassembly", "a protected PE emits garbage arithmetic on undefined registers after each call fragment"]},
  {"option": "callpopret", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "call-pop-pointer-helper", "summary": "RESTORES CODE: flow a direct `call` through a callee that pops the pushed return address off the stack and `ret`s through the word above it, instead of decoding a fall-through at the return address. This is the oldest position-independent idiom on x86 -- `call` over your own inline data, `pop` the address of it, `ret` to the grandparent -- and `CALL` being a fall-through op, kuna decodes the data as instructions. On the round-12 witness (a packed PE) the data is an embedded NUL-separated `kernel32.dll` / `GetProcAddress` / ... name table and it lifts as port-input operations and stores through registers that were never set, while the pointer the helper exists to hand back appears nowhere. Section flags are not the discriminator: the whole packer stub sits in one non-executable-flagged section, so a rule keyed on executability would refuse the `call` as readily as the string. This decodes the callee's raw p-code out of band and fires only when the stack pointer passes through exactly `entrySP + <pointer size>` -- the instant the pushed return address stops existing -- AND the run ends in a `RETURN` through the untouched word at `entrySP + <pointer size>`, with no call, branch or store in between and no unaccountable stack-pointer write. That second clause is what keeps the `__x86.get_pc_thunk` family (`mov ebx,[esp]; ret`, which reads the return address without consuming it and DOES come back to the call site) an ordinary `CALL`. On a match the `call` is rewritten to a branch and the callee's bytes are followed into the caller, so the push, the pop and the `ret` are all one function and the popped constant is recovered by ordinary dataflow. Logs a `callpopret:` WARNING at the call site. The `jmp` flavour of the same idiom belongs to `calltrampoline`.", "use_when": "A function decompiled by address emits port-input operations (`in(...)`), stores through registers that were never assigned, or arithmetic on undefined locals, and the disassembly shows the body is a single `call` followed by ASCII -- library names, a format string, a key table. Check the callee: `pop reg; ret` or `add esp,4; ret` means it never returns to the call site, and everything after the call is data. On by default; flip OFF to restore the fall-through decode at the return address.", "example": "option callpopret off", "source_decompiler": "kuna", "inspiration": "round-12 RE-friction crackme (packed PE CryptoME.exe): the helper at 0x4f7036 is `call 0x4f708d` followed by the NUL-separated names `kernel32.dll`, `GetProcAddress`, `LoadLibraryA`, ..., and 0x4f708d is `pop eax; inc eax; ret` -- so the truth is `return \"kernel32.dll\";` and the default emitted three `in(...)` sites instead", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["port-input operations (`in(...)`) appear in a function that does no I/O", "the body after a call is ASCII decoded as instructions", "a one-line pointer helper decompiles to dozens of statements over undefined registers", "the callee of a call is `pop reg; ret` or `add esp,4; ret` and the bytes after the call are a string table"]},
  {"option": "entryretdispatch", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "entry-point-ret-dispatch", "summary": "RESTORES CODE: recognize a function-entry chain encoded as repeated `push <continuation>; push <callee>; ret` links and classify each proven RET site as a call before flow following. Without the derived flow overrides, the first RET is treated as the function return and every later link is dead, so an entire dispatcher entry decompiles to `return;`. The recognizer follows a bounded straight-line raw-p-code walk and accepts a link only when the RET destination was loaded from a slot written during that run and the adjacent slot contains that RET instruction's exact fall-through. Affine addresses sign-extend p-code constants by operand width; overlapping register writes and overlapping stores invalidate facts; conditional control flow, real calls, indirect control flow, opaque stack writes and unproven destinations decline. Explicit caller flow assertions always take precedence, and an explicit `flow <site> return` cleanly vetoes the automatic call at that site. Logs no extra output: the recovered calls appear through the normal decompile pipeline.", "use_when": "A function entry is a stack-built dispatcher and decompiles as an empty `return;`: disassembly shows `push` of the instruction after a RET, then `push` of a callee address, then `ret`, repeated after each callee returns. On by default; flip OFF to restore the first-RET termination while keeping explicit `flow <address> call` assertions available.", "example": "option entryretdispatch off", "source_decompiler": "kuna", "inspiration": "entry-point-ret-dispatch RE need: an i386 entry routine has three consecutive push/push/ret links, but the CLI previously needed one manual flow assertion per RET and otherwise emitted only `return;`", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a function entry containing push continuation; push target; ret decompiles to only return", "only the first helper in a stack-built RET dispatcher appears after one manual flow override", "multiple flow <address> call assertions are required to recover one entry routine", "a packer or crackme dispatch chain is truncated at its first ret"]},
  {"option": "pushimmediateret", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "push-immediate-ret-transfer", "summary": "RESTORES A TERMINAL TRANSFER: recognize a function run ending in `push <immediate>; ret` and classify the proven RET as a branch before flow following. Without this fact, the RETURN ends the function and dead-store removal erases the pushed destination, so an unpacking stub emits its unpacker call but not the transfer to the original entry point. The bounded straight-line scanner uses raw-p-code affine stack provenance: RETURN must load a current-run stack slot, the last exact store to that slot must be a constant, and RET must advance the same stack base by the loaded width. Any adjacent in-run stack store declines, keeping `push continuation; push target; ret` call emulation under `entryretdispatch`. Ordinary returns, calls after argument pushes, stack adjustments or overwrites, computed targets, conditional/indirect flow, opaque writes, and unsupported userops decline. The derived fact is BRANCH, never CALL, so there is no fall-through; caller-supplied flow assertions always take precedence. No target function is synthesized or decoded.", "use_when": "On by default (DIV-170). A packer/unpacker entry calls a decoder and then disassembly ends `push <original-entry-address>; ret`, but emitted C contains only the decoder call and silently loses the final transfer. The default preserves the address as a terminal indirect transfer without treating encrypted on-disk target bytes as a function; use `option pushimmediateret off` to restore the bare RETURN.", "example": "option pushimmediateret off", "source_decompiler": "kuna", "inspiration": "RE-friction duplicate needs entry-point-push-return and push-immediate-ret-transfer: CryptoME.exe 0x4f7001 executes call 0x4f7990; push 0x40365a; ret, while default C omitted 0x40365a", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a push immediate followed by ret disappears from emitted C", "an unpacker call remains but the original-entry transfer after it is missing", "a manual flow branch assertion on a RET restores an indirect transfer to a constant address", "a packed entry stub ends at ret even though it pushed a destination first"]},
  {"option": "mappedflowboundary", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "default-entry-decompilation-aborts", "summary": "Retain decoded paths when another path falls through the mapped edge of a linked little-endian ELF x86 image. The resolved SLEIGH processor, endianness, and width must match the ELF class and machine. Check every actual instruction byte against the live image map before committing context or emitting p-code; staged prefetch padding is not evidence. Cut only an unmapped effective fall-through, emit the existing unmapped-flow warning and missing halt, and continue other queued paths. Explicit branches may cross gaps to mapped destinations. Touching mappings and the mapped zero tails (p_memsz excess) of non-executable segments remain valid, so flow that reaches such a tail lifts its zeros up to the mapped end. An instruction that decoded flow reaches on a mapped byte but whose encoding runs past the mapped bytes ends that path the same way; the entry instruction, in-lined callee flows, mapped read failures and genuine decoder errors keep their errors. Other formats, relocatable objects, and mismatched or non-x86 targets abstain.", "use_when": "On (default) recovers meaningful C from mapped paths when another decoded path leaves the image. Flip off to restore legacy staged-byte flow. Recovery reports incomplete flow rather than proving the missing path returns or inferring another ISA.", "example": "option mappedflowboundary off", "source_decompiler": "kuna", "inspiration": "A synthetic ELF conditional return is lost when another path falls through the mapped segment end; the authoritative ImageBytes map already distinguishes loader padding from mapped memory.", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["mapped return lost after another path leaves the image", "decompile exits with Unable to load bytes at a segment edge", "JSON code is null although a mapped branch returns", "zero-padded instructions decoded beyond mapped memory"]},
  {"option": "funcboundflow", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "interp-bee-func-merge", "summary": "REMOVES CODE: bound a function's fall-through at the entry of another known function. A kuna FunctionSymbol has no extent and CALL is fall-through, so a function whose last act is a `call` to a no-return routine kuna cannot prove no-return (in a stripped static binary: the unnamed `exit`/`abort`/`__stack_chk_fail` bodies and the app-level `die()`/`throw` wrappers built on them) runs its flow past the compiler's inter-function padding straight into the next function's entry and decodes THAT function's body into itself (the merge bug). When a fall-through reaches a known function entry (`query_call(next).is_some()`), plant a no-return artificial RETURN and stop, emitting a `funcboundflow` truncation WARNING. Excludes the function's own entry and admits one safe shared-epilogue shape: if the complete instruction at the foreign entry is an unconditional CPUI_RETURN with no other control transfer, decode that instruction because it has no successor and therefore cannot consume the function after it. Branch, computed-branch, and conditional-return entries stay bounded, and a failed entry decode is conservative.", "use_when": "A function shows a garbage tail that is really the body of the FOLLOWING function (dead code after a call to a die()/throw/exit wrapper, uninitialized reads, a second unrelated function inlined after an error call). On by default; flip OFF to restore the upstream flow-into-the-next-function behavior.", "example": "option funcboundflow off", "source_decompiler": "ida", "inspiration": "interp-bee/jit-bee static-pie study: 70/195 app functions end in a call to an unnamed no-return and absorb the next function; IDA/Ghidra bound decompilation to the function body", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a function's tail is really the body of the NEXT function", "dead/garbage code after a call to a die()/fatal()/throw wrapper that never returns", "two adjacent functions merged into one, the second also decompiled on its own", "uninitialized reads in the tail after an unrecognized no-return call"]},
  {"option": "overlapbranch", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "overlapping-anti-disassembly-sequence", "summary": "REMOVES CODE: when a conditional branch's own target lies STRICTLY INSIDE the encoding of the instruction at its fall-through, the fall-through decode has swallowed the branch target -- the classic anti-disassembly junk-lead-byte overlap (`75 01 e8` : a `JNZ +1` over an `e8` byte that turns the real stream into a bogus 5-byte CALL). kuna's flow follower pops the fall-through before the branch target, so the bogus decode wins by arrival order and desynchronises everything downstream; `set_fallthru_bound` only notices afterwards, when the losing stream is already built. This option makes the explicitly encoded branch target win: the ops of the overlapping fall-through decode are dropped, an artificial halt is planted at its own address (the conditional and its fall-through EDGE both survive), and the target is decoded on its own boundary. Two real instruction starts cannot sit at `next` and strictly inside `next`, so whenever it fires at least one of the two decodes was already wrong. ONE legitimate overlap is excluded first, and it is compiler-generated: glibc's conditional-`LOCK` idiom (`74 01` hopping over an `f0` prefix byte, as in a statically linked `_int_free`/`_int_malloc`) is the SAME instruction with its prefix taken or skipped, so the two decodes END AT THE SAME ADDRESS and both streams are real. The branch target's own instruction length is therefore decoded and the rule declines whenever `target + target_len == curaddr + step`, and likewise whenever the target does not decode at all. Ablating that one exclusion deletes the atomic store on a live path, so it is not decoration.", "use_when": "A hand-written / obfuscated / anti-disassembly binary decompiles with a call to an address outside the image, stores through never-assigned pointers, or bogus `dat_<hex>` globals right after a short conditional jump. Off restores the upstream fall-through-wins decode. Inert on ordinary compiler output: the trigger needs a branch target strictly inside its own fall-through instruction, and the one compiler idiom that produces that (a conditional jump over a `LOCK` prefix) is excluded because both decodes end at the same address.", "example": "option overlapbranch off", "source_decompiler": "ida", "inspiration": "round-2 RE-friction crackme pcme0: JNZ 0x8048838 followed by a linear CALL at 0x8048837 whose target 0xb10cdc7 is outside the image; IDA follows the branch target and recovers the real checker", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a call to a sub_<hex> address that is not in the binary", "writes through pointers that are never assigned right after a short conditional jump", "a bogus dat_<hex> global incremented once and never read", "a comparison whose operands are unreadable CONCAT/subpiece salad in hand-written or obfuscated code"]},
  {"option": "cleanupcode", "values": ["on", "off"], "default": "on", "destructive_as_default": true, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "oxidizer-CleanupCodeRemover", "summary": "REMOVES CODE: delete the Rust drop/deallocate call sites. Rust emits a drop-glue call at every scope exit and every `?` early return, so decompiled Rust is dominated by `core::ptr::drop_in_place<...>(v)` lines that appear nowhere in the source and carry no meaning for a reader. A direct CALL whose recovered callee name normalizes to one of `core::ptr::drop_in_place`, `core::ops::drop::Drop::drop`, `alloc::raw_vec::RawVecInner::deallocate`, `__rust_dealloc` or `__rustc::__rust_dealloc` is destroyed in the PRE-SSA window (the top of mainloop, gated on heritage pass 0), which is the point of the timing: the register/stack writes that existed only to set up the drop's arguments lose their last reader and are collected by the ordinary ActionDeadCode fixpoint, so the argument setup goes with the call instead of being left behind as unexplained assignments. Normalization reproduces the Oxidizer matcher: un-escape the legacy rustc `$LT$`/`$GT$`/`$u20$` mangling, then collapse the innermost angle-bracket group repeatedly - deleting a plain generic argument list and replacing a `<T as Trait>` qualified path with the TRAIT name - so `_$LT$alloc..vec..Vec$LT$T$C$A$GT$$u20$as$u20$core..ops..drop..Drop$GT$::drop` matches and `FakeCrypt::fileops::drop_ransom_note` does not. Oxidizer's `free`/`close`/`_close` entries are deliberately NOT carried: kuna's primary corpus is C binaries and deleting `free()` would be a catastrophically wrong answer. The remaining names cannot occur in a C program, which is what makes the pass structurally inert on a C binary and lets it default ON with no compiler-detection channel from the loader.", "use_when": "On by default (DIV-81). A Rust binary whose functions are dominated by drop_in_place/dealloc glue that is not in the source. Flip OFF to keep every drop and deallocation call site - e.g. when auditing a use-after-free, reasoning about when a resource is actually released, or matching the binary instruction-for-instruction. Inert on any binary with no Rust drop symbols, so C/C++ output is byte-identical either way.", "example": "option cleanupcode off", "source_decompiler": "oxidizer", "inspiration": "SEFCOM Oxidizer CleanupCodeRemover (angr/rust/optimization_passes/cleanup_code_remover.py), STAGE = BEFORE_VARIABLE_RECOVERY; FakeCrypt::fileops::encrypt_file", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["core::ptr::drop_in_place(...) calls all over a decompiled rust function", "drop glue and deallocation calls that are nowhere in the rust source", "Drop::drop / __rust_dealloc / RawVecInner::deallocate noise at every scope exit", "rust output is twice as long as the source because of automatic resource management", "flip off to see when a rust value is actually dropped or freed"]},
  {"option": "linuxsyscall", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "kuna-linux-int80-syscall", "summary": "Render a 32-bit Linux `int 0x80` as the syscall it is instead of an indirect call through the `swi` userop. x86 SLEIGH lowers `INT imm8` to `intloc = swi(imm8); call [intloc]` (ia.sinc:3671) - the honest lifting of a general software interrupt, since SLEIGH cannot know which operating system is behind the vector - and the decompiler renders that as `(*(void *)swi(0x80))();`: a call through a pointer nothing assigns, with the syscall number and every argument erased, because the register writes that set them up have no reader left and the dead-code fixpoint collects them. On 32-bit Linux the vector is fixed and so is the ABI: the number is in EAX and the arguments are EBX, ECX, EDX, ESI, EDI, EBP, with the result back in EAX. This pass recognizes the lowering structurally (a two-input CALLOTHER whose second input is the one-byte vector constant 0x80, feeding a CALLIND at the same instruction address), reads the number from a backward walk of the basic block that accepts only a full-width `EAX = <constant>`, and retargets the CALLIND in place onto a call spec named `sys_<name>` whose input prototype is LOCKED to the first N argument registers - so the later ActionFuncLink materializes the argument Varnodes itself and the `mov $1,%ebx` that feeds the call keeps a reader. The synthesized spec also sets extrapop = 0: `int 0x80` pushes no return address, where the default __cdecl extrapop of 4 (what the indirect call gets today) hands ESP back four bytes it never lost and shifts every later ESP-relative reference in the function. Three refusals leave the old rendering untouched rather than guess: a number that is not a locally-resolvable constant, a number with no vetted entry in the syscall table (332 of the 438 `__NR_` names - the arity has to be known, because printing a name with the wrong argument count is a false statement about the call, not a partial one), and any language that is not x86-32.", "use_when": "A 32-bit Linux binary - typically hand-written assembly, a static `_start`, shellcode or a packer stub - whose output contains `(*(void *)swi(0x80))();` and in which the syscall arguments have vanished. Off by default: naming the call asserts that the target OS behind vector 0x80 is Linux, which the vector alone does not prove, and it locks a prototype the recovery would otherwise keep open. Flip on when you are reading i386 syscall code; the pass is inert on every other language and on any int 0x80 site whose number it cannot resolve.", "example": "option linuxsyscall on", "source_decompiler": "kuna", "inspiration": "x86 ia.sinc:3671 INT imm8 lowering vs the i386 Linux syscall ABI (asm/unistd_32.h + the section-2 manual pages); crackmes.one 66fd63059b533b4c22bd0b91 `_start`", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["a syscall renders as (*(void *)swi(0x80))() - a call through a pointer nothing assigns", "the mov $N,%eax before an int 0x80 has vanished from the output", "an int 0x80 site shows no arguments even though the disassembly loads ebx/ecx/edx", "32-bit linux _start or shellcode where every system call is an unnamed indirect call", "esp-relative locals shift by four bytes after an int 0x80"]},
  {"option": "msvcstrappend", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "GH-468", "summary": "REMOVES CODE: collapses each inlined MSVC x64 `std::string` character append into one call. MSVC inlines `push_back` (which `operator+=(char)` is) and the literal form of `append(const char *, size_t)` at every call site as a capacity diamond over the string object: load `_Mysize` (+16) and `_Myres` (+24), and on the fast arm store `_Mysize + n`, pick the buffer with `_Myres > 15 ? _Bx._Ptr : _Bx._Buf`, and write the bytes plus a terminating zero at `ptr[_Mysize]`; on the slow arm call `_Reallocate_grow_by`. Every such append prints as the same five-to-ten-line `if`/`else` block, so a run of appends is a run of near-identical blocks whose count carries the meaning. The pass proves the diamond on the raw p-code before heritage, by symbolic execution of the head, every fast-arm path and the grow block: the head branches to the fast arm exactly when `_Mysize < _Myres` (or `_Myres - _Mysize >= n`), decided exactly over a test set complete for the comparisons it admits; each fast path stores `_Mysize + n` to the size slot and exactly `n` bytes and a zero at `ptr[_Mysize]` and nothing else, with `ptr` the object on every path taken only when `_Myres <= 15` and the loaded pointer on every path taken only when `_Myres > 15`, both kinds present; the grow block is one direct call receiving the object in `RCX` and the same character in `R9B` (or the count in `RDX` and, in `R9`, a pointer into read-only memory whose image bytes are the stored bytes); no register either arm writes is one the default model preserves; and no op after the join reads a register byte the rewrite changes before overwriting it, up to the next call or return. It then removes the fast-arm edge, replaces the grow block's argument setup, and gives its call a locked `void std::string::push_back(void *this, char c)` or `std::string::append(void *this, char *s, size_t n)` prototype that returns `this` only when every fast path left the object in `RAX`; a register the model kills at a call but no fast path writes is declared preserved by it, so later calls keep the argument evidence the fast path carried. The grow callee's body is never read: a call handed exactly the append's arguments on the capacity-exhausted edge is taken to be the reallocating append. Any unproven element leaves the diamond untouched: a small-string threshold other than 15, a grow call handed a different character, an extra store, a count the grow arm takes from a register set outside the head, a scratch register or flag read after the join. x86-64 under a Win64-shaped model (`RSI`/`RDI` preserved) only; inert on every other language and ABI.", "use_when": "Flip ON when reading an MSVC x64 C++ binary whose functions build strings character by character: each `s += c` / `s.push_back(c)` otherwise prints as its own capacity check, `// branch-flip` marker and grow call, and a password or format string assembled from several appends reads as a stack of identical blocks. Default OFF because it is destructive -- it deletes the inlined fast path's stores and renames the grow call to a library name the binary does not contain, trusting the grow call's argument shape rather than its body -- and because no MSVC C++ corpus in the tree can measure it. Off also restores the raw rendering when auditing the inlined code itself.", "example": "option msvcstrappend on", "source_decompiler": "kuna", "inspiration": "GH-468 finding 6: an MSVC C++ crackme whose `main` builds its password with four inlined `std::string::operator+=(char)` calls, miscounted as three from the C", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["several near-identical if/else blocks in a row, each comparing two adjacent stack slots and either calling a helper with the same object or storing one character through a 16-byte-threshold pointer select", "an msvc c++ function whose string-building code shows a `0x10 <= v` buffer select before every character store", "a loop body that is a whole capacity check plus a grow call to append one byte to a std::string", "counting inlined std::string appends from the c output gives the wrong number"]},
  {"option": "switchselector", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "kuna-loweredswitch-parameter-selector", "summary": "Record a lowered-switch cascade only when the switch value is computed inside the function; a cascade that dispatches on a parameter is left exactly as the compiler lowered it. `loweredswitch` re-rolls a compiler-lowered comparison cascade into a `switch`, and both renderings read the same value, so which one is better is a judgement call, not a correctness question. On a callback shape - a Win32 window or dialog procedure dispatching on its message parameter - the chain is often the more faithful rendering: it names the parameter at every arm and keeps the compiler's own ordering, where the re-rolled `switch` presents the dispatch as if the function had derived the selector itself. With this on, only a cascade whose switch variable has a defining op here - a call return, a load, a computed value - is re-rolled. Detection runs on the fully simplified, SSA'd graph, so `computed here` is exactly `the Varnode is written`. `loweredswitch off` remains the whole-pass escape hatch.", "use_when": "You want the compiler's own if/else-if chain back for a cascade that dispatches on a parameter, without turning `loweredswitch` off for the whole binary - typically a callback, window procedure or dialog procedure whose message parameter is the switch value. A declined cascade renders exactly as `option loweredswitch off` renders it, and every cascade over a value the function computes (a getopt return, a table load) still re-rolls. MEASURED: byte-identical on the 675-assertion datatest corpus with the guard forced on.", "example": "option switchselector on", "source_decompiler": "kuna", "inspiration": "crackmes.one 5ab77f5433c5d40ad448c14f SCORPiON.exe sub_401350 (Win32 DialogProc): the message-parameter dispatch reads as the compiler's chain", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["a re-rolled switch dispatches on a function parameter and the compiler's if/else-if chain would read better", "a callback or window/dialog procedure's message dispatch reads better as the chain the compiler emitted", "option loweredswitch off gives the rendering you want, but you want it for one cascade and not for the whole binary"]},
  {"option": "fastfailnoreturn", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "kuna-fastfail-stack-unbalance", "summary": "REMOVES CODE: ends the flow at a Windows `int 0x29`, so the fall-through past `__fastfail` is dropped as unreachable. x86 SLEIGH lifts `INT imm8` to `intloc = swi(imm8); call [intloc]` (`ia.sinc:3671`) - a `call` with NO matching push, unlike every other x86 `CALL`, which lifts as `RSP = RSP - 8; push &next; call target`. The call is an ordinary modelled call to the rest of the pipeline, so `x86-64-win.cspec`'s default `__fastcall` (`extrapop=\"8\" stackshift=\"8\"`) hands 8 bytes back after it and the stack pointer rises by 8 at every interrupt: `(*(void *)swi(0x29))(5);` is printed followed by `v62 = &v61[8];`. The damage is not local - once two paths join carrying stack-pointer values 8 apart, the frame stops being a constant offset from the spacebase, stack locals degenerate into offsets off a `char *`, and each `CALL`'s return-address push (normally a dead store into an unmapped slot) survives as an explicit `*(unsigned long long *)&v66[-8] = 0x140003e16;`. On Windows `int 0x29` is `__fastfail`, the MSVC `/GS` and STL `_STL_VERIFY` failure path, which terminates the process and by contract never returns; this option marks that call spec no-return so the block ends at the interrupt and the unbalanced stack pointer reaches no join.", "use_when": "On by default. A modern MSVC C++ binary whose stack locals render as offsets off a `char *` (`*(unsigned int *)&v66[0x30] = 0;`), whose calls are preceded by a store of their own return address (`*(unsigned long long *)&v66[-8] = 0x140003e16;`), or whose Win32 call arguments are stack blobs rather than values - all downstream of `(*(void *)swi(0x29))(5);` followed by a stack-pointer bump. Flip OFF to restore the upstream fall-through past the interrupt. MEASURED on a 240 KB MSVC PE32+ (crackme `selam.exe`): the checker at 0x140002d50 holds ELEVEN `int 0x29` and goes 1267 -> 1171 lines, 63 -> 0 return-address stores, 159 -> 144 declarations, and `BCryptGenerateSymmetricKey(*(unsigned long long *)&v66[0x50],&v53,v60,v51)` becomes `BCryptGenerateSymmetricKey(v52,&v55,v62,v54)`; across all 24 functions in that image that contain one, 2802 -> 2583 lines and 85 -> 0 return-address stores. Gated on the Windows compiler spec (`windows`/`clangwindows`) and on the exact lifted shape - the CALLIND must read the storage a `swi` CALLOTHER with the 1-byte constant `0x29` wrote in the same instruction - so `int 0x80` (Linux, see `linuxsyscall`), `int1`/`int3`/`into` (which carry their own `return` and genuinely do return) and every non-Windows image are untouched. Byte-identical (0/675) on the datatest corpus.", "example": "option fastfailnoreturn on", "source_decompiler": "kuna", "inspiration": "crackmes.one 6a3822888a86e4c2c55254fe selam.exe 0x140002d50: 1267 lines, 63 synthetic stack-address writes, BCrypt arguments as stack blobs", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["stack locals render as offsets off a char * instead of named locals", "explicit stores of a call's own return address, like *(unsigned long long *)&v66[-8] = 0x140003e16", "Win32/API call arguments come out as *(unsigned long long *)&v66[0x50] stack blobs instead of values", "(*(void *)swi(0x29))(5); is followed by a stack-pointer bump such as v62 = &v61[8]", "an MSVC C++ function emits hundreds of lines and a hundred-plus anonymous variables"]},
  {"option": "int3pad", "values": ["off", "warn", "halt"], "default": "warn", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "GH-468", "summary": "Name the `int3` pad control ran into, instead of printing it as a call through a pseudo-op. x86 SLEIGH lowers `INT3` to `intloc = swi(3); call [intloc]; return [0:1]` (ia.sinc:3670) - honest lifting, since SLEIGH cannot know what sits behind the vector - and the printer renders that as `v1 = (void *)swi(3); return (*v1)();`, which reads as an ordinary call through a function pointer. Nothing in the C says the byte at that address is `0xcc`, that it is one of a run of them, or that control got there by running off the end of the decoded code; the pad is visible only in `kuna disassemble`. `warn` (the default) recognizes the lowering structurally - a CALLIND reading the storage a `swi` CALLOTHER with the 1-byte constant vector 3 wrote in the same instruction - counts the `0xcc` bytes at the site, and buffers a warning the P9 slug table renders as `// int3-pad xN`. `halt` additionally marks the call spec no-return and plants the artificial halt, so the pad ends the flow instead of contributing the fabricated return value of `return (*v1)();`.", "use_when": "On (`warn`) by default: it adds a comment and changes nothing else, so a decoded `int3` is named wherever one is reached. Set `halt` when reading anti-debug code whose clean-run path falls into a pad and the fabricated `return (*(void *)swi(3))();` is in the way - it is not the default because a debugger attached to the process can step past an `int3`, so ending the flow is a judgement about the pad rather than a fact about the encoding. Set `off` to restore the unannotated upstream rendering. Shape-gated on the `swi` vector 3, so `int 0x29` (Windows __fastfail, see `fastfailnoreturn`), `int 0x80` (Linux, see `linuxsyscall`) and every non-x86 language are untouched; structurally inert wherever no `int3` is decoded, and byte-identical (0/675) on the datatest corpus. The pad length is a loader read: when it fails, or when the encoding is the two-byte `INT 0x03` rather than `0xcc`, the warning is emitted without a count rather than with a guessed one.", "example": "option int3pad halt", "source_decompiler": "kuna", "inspiration": "GH-468 finding 8: an anti-debug crackme whose no-debugger path jumps into an `int3` pad at the function tail, rendered as `(*(void *)swi(3))();` with no warning at all", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["an int3 pad renders as (*(void *)swi(3))() - a call through a pointer nothing assigns", "a function returns the result of a call through swi(3)", "nothing in the C says control ran into a run of 0xcc padding bytes", "an anti-debug binary's clean-run path ends in an indirect call that only the disassembly explains"]},
  {"option": "x64syscall", "values": ["off", "on", "abi"], "default": "off", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "kuna-x64syscall", "summary": "Give the x86-64 `SYSCALL` user-op the Linux kernel ABI's register effects, so a syscall wrapper stops returning its own first argument. `ia.sinc:4836` lowers the instruction as `RCX = inst_next; R11 = rflags; syscall();` - a CALLOTHER with NO inputs and NO output - and that one fact produces both halves of the damage. No inputs: the register writes that set the call up have no reader, the dead-code fixpoint collects them, and the number and every argument are erased, so the op prints as `syscall()` with empty parens. No output: nothing redefines RAX across the instruction, so the RAX a wrapper stores afterwards is still the one it loaded from its own `number` parameter and the function is emitted as `syscall(); return number;` - correct copy propagation over an operation kuna has been told writes nothing, which makes the emitted C a false statement about the program. On, the CALLOTHER is rewritten in place before heritage to read RAX and the argument registers and to write RAX; the reads keep the setup alive and the write retires the false return. `linuxsyscall` does not cover this and cannot be extended to it: there is no CALLIND here to retarget (a bare CALLOTHER, not the `int 0x80` `intloc = swi(0x80); call [intloc]` pair), the number is a runtime parameter rather than a constant it could look up, and it is gated to x86-32. RCX and R11 are already written by the SLEIGH constructor and are left alone; a CALLOTHER a compiler spec has specialized with its own `<callotherfixup>` is declined, so a spec-declared model always wins.", "use_when": "Any x86-64 function containing a `SYSCALL`: hand-written asm wrappers, static binaries, shellcode, a libc `syscall()` - anywhere the C reads `syscall();` with empty parens, or returns a value that is really the incoming syscall number. ON in the `aggressive` preset, which `auto` selects for any binary under 500 KiB, so it is the default rendering of `kuna decompile` there; the shipped catalog default is OFF because with a RUNTIME number nothing can say how many of the six argument registers the callee reads, which makes the read set a modelling judgement rather than a fact. The two values are that judgement: `on` takes an argument register only where a bounded backward walk of the CALLOTHER's own basic block finds it written before the instruction, so every read is of a value the function demonstrably places there and no undefined register is ever introduced - the cost is that a wrapper whose setup sits in a predecessor block keeps the old rendering for that argument. Set `abi` for such a wrapper: it takes all six unconditionally, which is what the kernel contract permits the callee to read, at the price that a register the function never writes reads as an undefined input and can add a parameter the source has not got. Inert on every language but x86-64 (RAX and all six argument registers must resolve at 8 bytes and the default code space must be 8 bytes wide) and on any function with no SYSCALL.", "example": "option x64syscall abi", "source_decompiler": "kuna", "inspiration": "re-needs x86-64-syscall-wrapper: a crackme's three-argument kernel_call wrapper decompiled to `syscall(); return number;` even with an explicit prototype and every argument-recovery option on", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["an x86-64 syscall renders as syscall() with empty parens, with the number and every argument erased", "a syscall wrapper returns its own first parameter, the syscall number, instead of the syscall's result", "the register writes feeding a SYSCALL are all missing from the C while kuna disassemble shows them", "a SYSCALL's result is never assigned to anything"]},
  {"option": "decodehalt", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "undecodable-encrypted-code-becomes", "summary": "A `RETURN` kuna planted because it could NOT decode the bytes says so, instead of printing as an ordinary `return;`. `FlowInfo` ends a path it cannot follow with an ARTIFICIAL HALT - a `CPUI_RETURN` carrying the constant 1 and one of the `getHaltType()` bits (`artificial_halt`, flow.cc:483). Three of those bits are decode failures rather than program returns: `badinstruction` (the bytes at the address are not an instruction), `unimplemented` (SLEIGH decoded them but has no semantics) and `missing` (the address was never decoded at all, e.g. a branch that leaves a caller-declared `--define-function` extent). Upstream `PrintC::opReturn` (printc.cc:774) renders those three as the pseudo-calls `halt_baddata()` / `halt_unimplemented()` / `halt_missing()`, and `FlowInfo::processInstruction` buffers `Bad instruction - Truncating control flow here` at the failing address plus a `Control flow encountered bad instruction data` header on the function (flow.cc:441-475). kuna ported the halt but neither the rendering nor the warnings, so a function that ran off into bytes it could not read ended with a bare `return;`, `error: null` in `--json`, and nothing anywhere saying the body is incomplete - which reads as `analysis complete, nothing here`. The fourth halt bit, `noreturn`, is deliberately NOT renamed: it is not a decode failure, kuna already reports it as `// no-return` on the call, and its rendering is pinned by the whole-corpus measurements behind `noreturn_propagate` and `guardarm`. `voidtailreturn` additionally stops eliding a trailing `RETURN` marked with one of those three causes while this option is on, because that statement is the truncation marker and not the source falling off the end of a void body - eliding it deletes the marker and leaves its warning attached to whatever decoded before it. That half is gated together with the rendering, so `decodehalt off` restores both and a `noreturn` halt is untouched in either arm.", "use_when": "On by default (DIV-151): kuna cannot honestly assert a normal return at an address it could not read, so the flip is strictly more truthful and there is no source shape it reports wrongly. Reachable only through the three decode-failure halt types, so it is structurally inert on code that decodes - byte-identical (0/675) on the datatest corpus. SWEPT `decompile-all` in both arms over 82 binaries / 31,862 functions (27 decbench ELFs across the O0/O2/O2-noinline slices spanning x86-64/i386/ARM/AArch64/MIPS/PPC64/RISC-V, plus 55 crackme PE/ELF images): 89 functions in 19 binaries change, +178/-154 lines, 61 `halt_baddata()` and 35 `halt_missing()` (no `halt_unimplemented()` in this corpus) under 62 warned prototypes. **24 functions GAIN a statement and none loses one**, and with comments stripped every single hunk is `return;` -> `halt_<cause>();` - no other code moves. The 24 gains are the elision half doing its job: in `bustme1.exe` an anti-debug stub read `if (rdtsc-check) return; // warn: Function flow reaches unmapped memory`, blaming a NORMAL early return for the unmapped jump, because voidtailreturn had deleted the halt and orphaned its warning onto the statement above; on, that reads `if (rdtsc-check) return;` then `halt_missing(); // warn: ...`. MEASURED on the crackmes.one self-decrypting stub `Defender.exe` (`5ab77f5c33c5d40ad448c65c`) `sub_401746`, a packer that XORs 0x30a bytes in place and jumps into the plaintext through `PUSH 0x40190e / POP EBX / JMP EBX`: off, the decrypt loop is followed by `switch(0x40190e) { case 0x40190e: return; }` and nothing else; on, the same body ends `halt_baddata(); // warn: Bad instruction - Truncating control flow here` under a `// warn: Control flow encountered bad instruction data` prototype. One residue in the sweep, reported rather than hidden: in `Crackme#7.exe sub_403b64` the halt sits inside a degenerate `switch(0x403bf3) { }` that prints no arm, so its truncation warning attaches to the decoded call above it instead of to a `halt_baddata()` statement - a comment placement, no code difference. Set off to restore the silent `return;`.", "example": "option decodehalt off", "source_decompiler": "ghidra-upstream", "inspiration": "RE-friction round 12, need undecodable-encrypted-code-becomes: a tester read `case 0x40190e: return;` with error null as `the stub does nothing` and stopped; upstream Ghidra prints `halt_baddata()` there", "change_kind": "presentation-default", "tier": "core", "symptoms": ["a function that runs into undecodable, encrypted or packed bytes ends with a plain return; and no warning", "a self-decrypting stub decompiles to case 0x<hex>: return; with error null in --json", "nothing in the emitted C says the decompiled body is incomplete", "a branch that leaves a --define-function extent renders as an ordinary return", "kuna disassemble reports .byte at an address the decompiled C returns from"]},
  {"option": "noreturn_extern", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "angr-tail-tail-bytes-ret-dup", "summary": "REMOVES CODE: marks a matched extern call no-return, so code after the call (the fall-through) is dropped as unreachable. Treat a direct CALL whose resolved callee display name matches a known ELF no-return name (`__stack_chk_fail`, `abort`, `exit`, `__assert_fail`, `pthread_exit`, `longjmp`, `_Unwind_Resume`, the C++ terminate/throw family, ...) as no-return at decompile flow time - planting the artificial halt so flow stops at the call - even when the address-keyed no-return flag is unset. This is the undefined-extern case the analysis-tier `noreturn_known` pass cannot reach: in an ELF relocatable object (ET_REL `.o`) a libc no-return is an `UND` symbol with no definition/address, so the address-keyed pass never marks it, and flow runs off the function's end into the next function.", "use_when": "On by default (DIV-14). Flip OFF only to restore the upstream fall-through past a matched extern no-return call (e.g. to study the raw flow). On = a direct call to a known ELF no-return name is treated as no-return, so the function is bounded at it and the post-call dead code is dropped (e.g. an ET_REL `.o` tail.o::tail_bytes renders 87 lines instead of ballooning to 326 lines / 9 gotos by swallowing adjacent `tail_lines`/`die_pipe` after `__stack_chk_fail()`). The name match overlaps `noreturn_known`'s for defined/imported symbols; default-on is byte-identical over the 675 datatests (no datatest call resolves to a no-return name).", "example": "option noreturn_extern on", "source_decompiler": "angr", "inspiration": "test_tail_tail_bytes_ret_dup; angr CFG call-graph no-return analysis (undefined-extern `__stack_chk_fail`); tail_bytes", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["garbage code after a call to __stack_chk_fail or abort in a relocatable .o", "function balloons by swallowing the next function after an extern no-return call", "dead fall-through after exit or assert-fail in an object file"]},
  {"option": "flagcompare", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "GH-1276/8777", "summary": "Fold flag-modelled comparison idioms into clean compares: a boolean shifted into the sign bit ((b<<k) s< 0) and the N==V signed-overflow idiom (bra ge).", "use_when": "On by default (DIV-3): cleans flag-as-bit comparisons on architectures that model condition flags explicitly (8051, PIC24, etc.); flip off to restore the upstream raw flag-arithmetic rendering.", "example": "option flagcompare on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-1276/8777 (flag-modelled comparison idioms)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["boolean shifted into the sign bit tested with a less-than-zero compare", "N==V signed-overflow branch idiom rendered as raw flag arithmetic", "want the explicit condition-flag math visible (flip off)"]},
  {"option": "switchmodbound", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "GH-9191", "summary": "Bound a LOAD-table jumptable by a modulo (index % N) or and-mask on its index when no guard bounds it.", "use_when": "Set on PER PROGRAM when a switch reports 'Could not recover jumptable ... Too many branches' and renders as a computed call; DESTRUCTIVE as a global default (may over-bound an unrelated indirect jump).", "example": "option switchmodbound on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-9191 (modulo/and-mask jumptable bound)", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["switch reports 'Could not recover jumptable ... Too many branches' and renders as a computed call", "indirect jump bounded only by a modulo or and-mask on its index never becomes a switch"]},
  {"option": "constselectjump", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "re-conditional-indirect-branches-hide", "summary": "Recover an indirect branch whose destination is selected between constant addresses (a CMOV/predicated dispatch) as a jump table over that constant set, instead of truncating it to a call.", "use_when": "An indirect jump renders as a computed `(*v)()` call carrying a 'Treating indirect jump as call' warning while the destinations are already visible as address constants in the emitted C, and the code behind them is missing. DESTRUCTIVE as a global default: a conditional TAIL CALL selecting between two nearby function entries is the same shape, and recovering it splices both callee bodies into the caller.", "example": "option constselectjump on", "source_decompiler": "kuna", "inspiration": "RE-friction need conditional-indirect-branches-hide: a Mach-O decoder dispatches through `CMOVZ R11,R10; JMP R11` over two LEA-loaded code addresses", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["indirect jump renders as a computed (code *)() call whose destinations are already constants in the emitted C", "'Treating indirect jump as call' on a CMOV/predicated dispatch", "the body behind a conditional indirect branch is never decoded"]},
  {"option": "switchguardbound", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "angr-missing-function-call", "summary": "Bound a LOAD-table jumptable by an out-of-band CBRANCH range guard (idx LEu N) when the basic model's guard analysis could not (e.g. a GCC `sub LOW; ja DEFAULT` dispatch with the index spilled to the stack and the flag idiom not yet simplified).", "use_when": "Set on PER PROGRAM when a switch reports 'Could not recover jumptable ... Too many branches' and renders as a computed (code *)() call AND the dispatch is range-guarded (vs the modulo form switchmodbound handles); DESTRUCTIVE as a global default (the guard-to-index correspondence is asserted across a stack spill it cannot prove in dataflow, so it may over-bound an unrelated indirect jump).", "example": "option switchguardbound on", "source_decompiler": "angr", "inspiration": "test_decompiling_missing_function_call; angr jump-table / VSA guard bounding; main", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["'Too many branches' jumptable failure where a cbranch range guard bounds the index", "computed (code *)() call at a gcc sub/ja guarded dispatch with the index spilled to the stack"]},
  {"option": "switchsharedcase", "values": ["on", "off"], "default": "on", "destructive_as_default": true, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "angr-switch-case-shared-case-node", "summary": "Recover a GCC PIC relative-offset jump table (target = base + sext(load4(base + idx*4))) whose table-base register is a loop-carried MULTIEQUAL: the `lea .rodata` base is set before a getopt-style loop while the BRANCHIND is inside it, so the path-meld collapses to the final base+offset add and the CBRANCH range guard on the load index never bounds the table. Rebuilds the meld as a clean single path down to the guarded index so the table resolves and the switch structures. On by default (DIV-14): SLOWER on the functions whose loop-carried switch it recovers (the extra meld-rebuild + table resolution walk runs there), but kept on for the better recovery quality; byte-identical (0/675) on every datatest, which has no such loop-carried PIC switch.", "use_when": "On by default (DIV-14): recovers a getopt/dispatch switch inside a loop that would otherwise report 'Could not recover jumptable ... Too many branches' and render as a computed (code *)() call with goto spaghetti. Flip OFF only if the heuristic mis-sizes or accepts an unrelated indirect jump on a specific program (it reads a read-only table at a base it recovers across a loop phi) — it is marked destructive for that reason, but it is byte-identical over the 675 datatests and kept on for quality despite being slower on the functions it recovers.", "example": "option switchsharedcase on", "source_decompiler": "angr", "inspiration": "test_switch_case_shared_case_nodes_b2sum_digest; angr CFGFast indirect-jump resolution across the loop phi; main", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["getopt-style switch inside a loop degrades to a computed call with goto spaghetti", "pic relative-offset jump table with a loop-carried base register never recovered", "'Too many branches' on a dispatch whose table base is set before the loop"]},
  {"option": "switchmultipred", "values": ["on", "off"], "default": "on", "destructive_as_default": true, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "angr-abnormal-switch-case-case3", "summary": "Recover an image-base-relative jump table (target = imagebase + load4(table + idx*4)) whose bound guard is 'unrolled' / duplicated across MULTIPLE predecessors of the dispatch block: the BRANCHIND's parent has sizeIn() > 1, each incoming path ending in its OWN (often semantically different) bound CBRANCH, the per-path switch indices meeting in a MULTIEQUAL. The single-predecessor guard walk short-circuits to checkUnrolledGuard, so no dominating guard bounds the table and the dispatch is truncated to a computed (code *)() call. When on, finds the dispatch MULTIEQUAL, evaluates each predecessor guard as a function of its MULTIEQUAL input, and re-bounds the table to [0, max-over-paths) so the readonly-LOAD table model recovers the switch (angr's 'abnormal switch case').", "use_when": "Set on PER PROGRAM when an image-base-relative switch (e.g. an MSVC memmove/memcpy small-count tail dispatch) reports 'Could not recover jumptable ... Too many branches' and renders as a computed (code *)() call AND the dispatch block has multiple predecessors each with its own bound guard (vs the single-guard form switchguardbound handles); DESTRUCTIVE as a global default (it unions per-path guard prefixes across a MULTIEQUAL and re-binds the table from a heuristic max bound, so it may over-bind an unrelated multi-entry indirect jump).", "example": "option switchmultipred on", "source_decompiler": "angr", "inspiration": "test_decompiling_abnormal_switch_case_case3; JumpTableResolver / abnormal switch case; 0x18003c330", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["image-base-relative switch whose bound guard is duplicated across multiple predecessors renders as a computed call", "'Too many branches' where the dispatch block has several guarded incoming paths", "msvc memmove small-count tail dispatch truncated to an indirect call"]},
  {"option": "unrolledguard", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "angr-optimized-memcpy-6301a9", "summary": "Recover the interleaved jump tables of an MSVC optimized memcpy/memmove (Duff's device) at the partial-flow stage. The function holds several image-base-relative jump tables whose case bodies are reachable only as one another's case targets; kuna recovers them one at a time, each in its OWN fresh partial-flow clone. A later table's clone re-clones an already-recovered sibling table into its jumpvec, and that partial's collectEdges then calls target() on a sibling case body that was never decoded into THIS partial's visited snapshot (it is only decoded into the parent flow after the recovery pass returns), throwing 'Could not find op at target address' so the dispatch is truncated to a computed (code *)() call. Upstream avoids this by building one shared partial and running collectEdges once while the sibling tables are still empty. When on, the partial-clone collectEdges SKIPS an unresolvable recovered-table case-target edge (the same 'assume no branches out' shape the findJumpTable==0 partial path already uses) instead of throwing, so the interleaved tables all recover and the dispatches render as switches (angr's optimized memcpy).", "use_when": "Set on PER PROGRAM when a function holding several interleaved jump tables (e.g. an MSVC optimized memcpy/memmove Duff's-device tail dispatch) recovers ONE switch but degrades the OTHERS to 'Treating indirect jump as call' computed (code *)() calls, and the failures are flow-stage 'Could not find op at target address' on a sibling table's case body (NOT a guard-bound failure switchguardbound/switchmultipred would handle); DESTRUCTIVE as a global default (it suppresses an unresolved-case-target edge during partial-flow recovery, so on an unrelated truly-malformed table it could mask a real missing target instead of declining the table).", "example": "option unrolledguard on", "source_decompiler": "angr", "inspiration": "test_decompiling_optimized_memcpy; JumpBasic::checkUnrolledGuard / angr JumpTableResolver; 0x42cca0", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["one switch recovered but sibling interleaved jump tables degrade to computed calls", "'Could not find op at target address' during recovery of an msvc optimized memcpy dispatch", "duff's-device tail dispatch rendered as 'Treating indirect jump as call'"]},
  {"option": "jtsharepartial", "values": ["on", "off"], "default": "on", "destructive_as_default": true, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "decompiling-3396-byte-main", "summary": "Run the jump-table partial sub-decompilation ONCE per `recoverJumpTables` batch and share it across every table in that batch, the shape the C++ has (`Funcdata::stageJumpTable` guards the clone and the reduced pipeline behind `if (!partial.isJumptableRecoveryOn())`). Recovering a BRANCHIND means cloning the function's raw p-code into a partial Funcdata, building its blocks and running the reduced `jumptable` action set over it, so that the switch's index calculation simplifies to something the emulator can walk; kuna did that once PER TABLE, paying a whole sub-decompilation for every switch in the function. Sharing pays it once. Off restores the per-table re-clone, which is what `option unrolledguard` needs: its recovery of MSVC interleaved (Duff's-device) tables works precisely because a later table's fresh clone re-clones the siblings recovered before it, and a shared partial is built before any sibling has recovered.", "use_when": "Leave on. Flip OFF together with `option unrolledguard` on a function holding several INTERLEAVED jump tables whose case bodies are reachable only as one another's case targets (an MSVC optimized memcpy/memmove tail dispatch), where the per-table re-clone is what recovers the siblings; DESTRUCTIVE as a default in exactly that shape, since a shared partial cannot see an already-recovered sibling table. On a function with one jump table the two settings are structurally identical, and on an ordinary multi-table function sharing removes one full sub-decompilation per extra table.", "example": "option jtsharepartial off", "source_decompiler": "ghidra", "inspiration": "round-2 RE-friction crackme nikos_crack_me.exe: `sub_140023350` holds two jump tables and paid two partial sub-decompilations, 3.1 s of action time plus 0.5 s of cloning out of an 11 s decompile; C++ `Funcdata::stageJumpTable` (funcdata_block.cc:512) pays it once.", "change_kind": "speed", "tier": "transform", "symptoms": ["decompiling one function with several switch statements takes far longer than its size suggests", "per-function latency scales with the NUMBER of jump tables rather than their size", "profile shows the reduced jumptable action set running once per switch"]},
  {"option": "noreturn_externmatch", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "flow-follow", "strength": "HARD", "rewind": "P2", "issue": "angr-incorrect-duplication-chcon", "summary": "Treat a direct CALL whose callee NAME matches the vendored ELF known-no-return list (exit/abort/__stack_chk_fail/...) as no-return at the flow `query_call_no_return` seam, even when the address-keyed noreturn_known scan emitted no fact. Closes the ET_REL `.o` gap: an undefined extern has symtab address()==0 and no PLT, so noreturn_known never marks it, and flow runs past a `__stack_chk_fail()` call into the inter-function alignment padding (00 00 -> `add [rax],al`), emitting dozens of garbage `*v = *v + c;` lines.", "use_when": "On by default (DIV-13): applies the SAME vendored name list and global/std namespace guard as the already-default-on noreturn_known, just at the flow query seam to reach the ET_REL `.o` undefined extern the address-keyed scan structurally misses; a no-op on a normal dynamically-linked ELF (the proto flag is already set). Set OFF to restore the prior byte-identical rendering (dead `add`-padding after a `__stack_chk_fail`/`abort`/`exit` call reappears).", "example": "option noreturn_externmatch off", "source_decompiler": "angr", "inspiration": "test_decompiling_incorrect_duplication_chcon_main; angr CLE/CFGFast extern no-return tagging; main", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["dozens of garbage *v = *v + c; lines after a __stack_chk_fail call in a .o", "inter-function alignment padding decoded as add [rax],al style instructions", "flow runs past an undefined-extern abort or exit call in a relocatable object"]},
  {"option": "stackprobeloop", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "stack-pointer-normalization", "strength": "HARD", "rewind": "P2", "issue": "GH-8017/6858", "summary": "Resolve a gcc -fstack-check / stack-clash probe loop's stack-pointer MULTIEQUAL to a fixed offset from the input SP, so the frame and call arguments recover cleanly.", "use_when": "Set on when a large-frame function shows &pxVar[-0x1000] page-probe noise or argument-less calls; shape-gated, so it is inert on functions without a probe loop.", "example": "option stackprobeloop on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-8017/6858 (stack-probe loop SP normalization)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["&pxVar[-0x1000] page-probe noise in a large-frame function", "calls rendered argument-less because the stack pointer never resolved to a constant offset", "gcc stack-clash probe loop leaves the frame layout unrecovered"]},
  {"option": "calleeprotostack", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "callee-prototype-stack-contract", "strength": "HARD", "rewind": "P3", "issue": "re-needs-getlasterror-consumes-arguments-prepared", "summary": "Read a declared callee's stack contract off its locked prototype: how much it pops, and how much of the CALLER's stack it can reach. A locked parameter list under a model whose `<prototype extrapop=\"unknown\">` declines to state its own cleanup (`x86win.cspec`'s `__stdcall`) implies `4 + <stack argument bytes>`; `FuncProto::resolveExtraPop` computes exactly that and nothing in the tree called it, so a Win32 import with a signature still reached `StackSolver` as extrapop-unknown and `calleepop` guessed its cleanup from the push run in front of the call. That run is not always the callee's: MSVC stages `FormatMessageA`'s last four arguments, calls the argument-less `GetLastError` in the middle of the run, and pushes the first three afterwards -- so the four staged pushes were credited to `GetLastError`, the solver latched a stack pointer 16 bytes too high into every later reference, and `FormatMessageA` rendered with three of its seven arguments. The second half is `Heritage::guardCalls` (heritage.cc:1444), which asks the prototype what the call does to every heritaged range and gets `unknown_effect` for the whole stack: honest for an unknown callee, but a locked non-variadic one owns the return-address slot and its own parameter area and nothing above them. A caller slot above that floor is left unguarded -- unless `AliasChecker` says a pointer in the caller could reach it, which is the `ReadFile(h,&buf,...)` case where the guard is what models the callee's write through the pointer and it stays. Both halves are the same claim, that the declared prototype is the truth about this callee's stack, and both are declined wherever a model states its own extrapop, wherever the prototype is unlocked or variadic, and wherever the range is aliased.", "use_when": "On by default: the flip moves 0 of 675 datatest assertions -- both parity corpora are symbol-less byte chunks whose callees carry no declared prototype, so nothing in them reaches the locked branch. Flip OFF to restore the pre-signature behaviour. With it off, a Win32 API whose arguments are staged across an intervening call renders with only the arguments pushed after that call, the staged ones are dropped, and the buffer whose address was among them decays into an incoming parameter -- so the call that fills it and the calls that read it disagree about which object they are talking about. Inert on any target whose callees carry no signature, and on any spec that states its own extrapop (x86gcc `__cdecl extrapop=\"4\"`, x86-64, every RISC spec).", "example": "option calleeprotostack off", "source_decompiler": "kuna", "inspiration": "upstream FuncProto::resolveExtraPop (fspec.cc) + Heritage::guardCalls stack effect query (heritage.cc:1444)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a win32 api renders with only the arguments pushed after an intervening call", "GetLastError() or another argument-less import eats the pushes staged for the call after it", "a buffer whose address is passed to an api is read back as an incoming parameter instead of the local", "an import with a known signature still has its cleanup guessed from the caller's push run"]},
  {"option": "calleepop", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "stack-pointer-flow", "strength": "HARD", "rewind": "P6", "issue": "re-needs-pixel-reader-uses-undefined", "summary": "Read the argument bytes a callee pops off the caller's own push run, instead of guessing that an unknown extrapop pops nothing. StackSolver (coreaction.cc:238) contributes the guessed equation `extrapop = 4` for every call whose prototype model declares `extrapop=\"unknown\"` -- on x86 Windows that is EVERY call, because `x86win.cspec`'s `<default_proto>` is `__stdcall extrapop=\"unknown\"` and the convention is decided per callee. The guess says the callee pops none of its arguments, so each `__stdcall` call leaves the modelled stack pointer 4*nargs too low, and the error accumulates: the solver latches the solution into `INT_ADD sp, #c` at every reference, so every stack slot after the first call is displaced by the running total. With the option on, a call whose result is consumed by an `INT_ADD sp, #+k` is read as caller cleanup (`__cdecl`) and keeps the upstream guess; otherwise the argument pushes are counted back from the return-address slot in pointer-sized steps, stopping at the first push that stores a register's own input Varnode (a prologue callee-save) or at a step with no spacebase reference (a bare `sub esp,n`).", "use_when": "On by default: the flip moves 0 of 675 datatest assertions and is inert on any spec that states its own extrapop. Flip OFF to restore upstream's constant guess. With it off, an MSVC x86 PE that calls a stdcall API and then reads the buffer that API filled splits ONE stack slot into two variables -- the address handed to the callee and the slot read afterwards land at different offsets, the read half has no reaching definition, and the emitted C computes over a constant instead of over the bytes the callee wrote. The displaced locals also drift onto an earlier call's outgoing-argument slots, so a declared prototype for that callee makes the local unmappable and it prints as a raw `&Stackffffffe0`. Inert wherever the compiler spec states its own extrapop (x86gcc `__cdecl extrapop=\"4\"`, x86-64, every RISC spec): those calls contribute an exact equation and never reach the guess.", "example": "option calleepop off", "source_decompiler": "kuna", "inspiration": "upstream StackSolver::build (coreaction.cc:162) guessed equation + x86win.cspec `__stdcall extrapop=\"unknown\"`", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["an x86 windows pe passes `&Stackffffffe0` to an api and reads a DIFFERENT variable afterwards", "a buffer filled by ReadFile/RegQueryValueEx is checksummed as a constant zero", "declaring a prototype for one callee turns another function's locals into raw `&Stack` references", "stack locals in an msvc x86 pe sit at offsets that drift further from the truth after every call"]},
  {"option": "cookiescramble", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "alias-facets", "strength": "HARD", "rewind": "P4", "issue": "re-needs-variadic-prototype-still-drops", "summary": "Treat an INT_XOR of the stack pointer -- MSVC's /GS cookie mix -- as a non-address use, so it does not record a local-alias escape site at the bottom of the frame.", "use_when": "On by default (DIV-126). With it OFF, `xor rax,rsp` in an MSVC /GS prologue makes the raw stack pointer an escape site at frame offset 0, so `hasLocalAlias` answers yes for every stack location in the function and `checkInputTrialUse` scores every stack-passed call argument no-use: calls in /GS-protected functions truncate at the register budget (x86-64 Windows: four arguments), the variable tail of a `...` prototype never appears, and the dropped argument's computation is dead-code eliminated. GCC/Clang read the cookie from %fs:0x28 and never touch the stack pointer, so the flip is inert on ELF corpora. Flip OFF to restore upstream `gatherAdditiveBase` for a bisect or an ablation.", "example": "option cookiescramble off", "source_decompiler": "ghidra-upstream", "inspiration": "upstream AliasChecker::gatherAdditiveBase (varmap.cc:733) + FuncCallSpecs::checkInputTrialUse (fspec.cc:5616)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["an msvc /GS binary drops the stack-passed arguments of every call in a function", "a variadic call renders with only its register arguments after the format string", "a value stored to [rsp+0x20] right before a call never reaches the call", "an argument-producing store survives as a dead assignment to a stack local"]},
  {"option": "nulterminator", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P6", "subphase": "stack-frame-layout", "strength": "HARD", "rewind": "P6", "issue": "GH-468", "summary": "Keep the zero element that terminates an open stack character array inside the array, and close the array right after it (opt-in). ScopeLocal::restructure (varmap.cc:1294) extends an open RangeHint over a following hint only through RangeHint::attemptJoin (varmap.cc:170): within its highind, or a constant COPY no further than highind (isConstAbsorbable, varmap.cc:30); otherwise the open range ends where the next hint starts, so `strncpy(buf, s, sizeof(buf) - 1); buf[sizeof(buf) - 1] = 0;` prints `char buf[63]` beside a separate never-read char. With the option on, when attemptJoin declines the hint an open, unlocked char/UTF-16 range would end at and that hint is a plain constant-COPY slot of one element at a whole-element offset holding only unread zeros, the range becomes a fixed range ending one element after the slot (provided adjustFit keeps the extent and no plain frame address starts inside the array). DEFAULT OFF: the same frame shape -- an open char array followed by a lone unread zero at the next whole-element offset -- is produced by the struct-first-member idiom `struct { char name[32]; bool active; ... } u; strncpy(u.name, s, 32); u.active = 0; ...; f(&u);`, where the zero is a sibling FIELD read only through the base pointer, not the array's terminator. A spilled scalar whose value is read from a register looks identical too, so there is no frame-level way to tell a genuine terminator from a following field, and on by default this over-extends a correctly-sized array by one element. Flip on when reverse-engineering a string-heavy target where the buffers are known to be NUL-terminated strings, not struct members.", "use_when": "Off by default. Flip ON on a string-heavy target (an MSVC /GS function, a bounded strncpy/snprintf/read followed by an explicit NUL) to fold the terminator into the array: `char v3 [63]; char v8; // stack - 0x19` with `strncpy(v3,a0,0x3f); v8 = 0;` becomes `char v3 [64];` with `v3[0x3f] = 0;`. Keep it OFF on code with structs or records: the identical frame shape is also `struct { char name[N]; T field; }` initialized field by field then passed by pointer, and turning it on absorbs `field` into `name` (`char name[32]` -> `[33]`), inventing an element. gcc/clang and MSVC, every optimisation level. Inert unless a zero is stored at an open char array's next whole-element offset and never read directly.", "example": "option nulterminator on", "source_decompiler": "kuna", "inspiration": "GH-468 (MSVC /Od and /O2 canary_check: `char buf[64]` printed as `unsigned char v3 [63]` plus a separate never-read `char v8 = 0`); the struct-first-member false positive (`strncpy(u.name, s, sizeof u.name); u.active = 0` over-extends `name` by one) is why it is opt-in; upstream RangeHint::attemptJoin (varmap.cc:170), isConstAbsorbable (varmap.cc:30), MapState::gatherOpen min_items (varmap.cc:1233)", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["a stack char buffer is declared one element short, e.g. [63] where the source has [64]", "the NUL terminator after strncpy renders as an assignment to a separate char local", "a stack char local is assigned 0 and never read, right after the end of an array", "the buffer's last element is missing from the array and declared as its own variable"]},
  {"option": "endptrbound", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "stack-frame-layout", "strength": "HARD", "rewind": "P6", "issue": "GH-468", "summary": "Recover a stack buffer that a proven pointer loop walks as one array spanning the walk, and express the loop's one-past-the-end bound on that buffer (`&buf[8]`) instead of as the address of whichever local happens to follow it.", "use_when": "On by default. With it OFF, `for (p = buf; p != buf + 8; p++)` over a stack buffer renders its bound as the next local (`while (v3 != v2)`), so the trip count is not readable from the C, and the neighbour's pointer type leaks into the walking pointer and into the buffer's declaration (`unsigned int v1;` for eight bytes). A walk counts only when it is proven: the pointer is the header phi of a natural loop that starts at a stack address and steps by one positive constant on every back edge; a comparison of it against a stack address a whole number of steps later (==, !=, p < end or end <= p) drives the branch that leaves the loop, from a block every back edge passes through; and every dereference through the pointer fits inside one step, with no call, store, other phi or variable offset able to read past it. The frame is left as it was when a hint straddles the range or is type-locked, a non-constant value inside it is typed or not exactly one element wide, typed index evidence inside it uses another element width (a byte walk over an `int` array), or a second walk crosses it; the array never extends further than the frame would otherwise have extended it. Constant initializers that write the buffer in wider pieces keep that form (`v1._0_4_ = ...`). Flip OFF to see the frame layout without the walk extent, for a bisect or an ablation.", "example": "option endptrbound off", "source_decompiler": "kuna", "inspiration": "GH-468 finding 7: a Win64 crackme's 8-byte password loop `lea rsi,[rbp+0x27] ... lea rax,[rbp+0x2f]; cmp rsi,rax` rendered `do { ... } while (v12 != v11);`", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["a pointer loop over a stack buffer ends at the address of an unrelated local (`while (v3 != v2)`)", "a loop's `while (p != vN)` names a local declared at the stack offset just past the buffer the loop reads", "a stack buffer walked bytewise is declared as a scalar of the neighbour's width (`unsigned int v1;` for eight bytes)", "the walking pointer is typed after the neighbouring local instead of the bytes it reads"]},
  {"option": "dynamichashmax", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "alias-facets", "strength": "HARD", "rewind": "P6", "issue": "GH-8467", "summary": "Raise the DynamicHash same-address collision budget 8->16 so dense unrolled code can still resolve a unique dynamic symbol hash.", "use_when": "Fixes a decompilation abort 'Unable to find unique hash for varnode' (e.g. AArch64/Go NEON byte-search loops). On by default (DIV-3); flip off to restore the upstream collision budget (the abort reappears).", "example": "option dynamichashmax on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-8467 (DynamicHash same-address collision budget)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["decompilation aborts with 'Unable to find unique hash for varnode'", "dense unrolled simd/neon loop (aarch64, go) fails to decompile at symbol mapping"]},
  {"option": "stackalias", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P6", "subphase": "alias-facets", "strength": "HARD", "rewind": "P6", "issue": "GH-8500", "summary": "Hold a store-through-a-stack-pointer-alias (int *p=&x; *p=x; return *p) alive across the deadcode race so it is not dropped to an uninitialized stack read.", "use_when": "Set on PER FUNCTION when a take-address-of-local + store-through-pointer returns a spurious uninitialized local (xStack_*); DESTRUCTIVE as a global default (conservatively pins stack stores live, suppressing legitimate dead-store removal).", "example": "option stackalias on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-8500 (stack-pointer-alias store liveness)", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["spurious uninitialized local (xStack_N) returned after storing through a pointer to a local", "store through a take-address-of-local pointer dropped as dead so the later read is garbage"]},
  {"option": "arraystride", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "GH-8724", "summary": "Re-express a strength-reduced array walk: rewrite a loop offset accumulator (acc += sizeof) as counter*stride so the array index is recovered.", "use_when": "A strided loop renders a raw offset accumulator (e.g. iVar += 0x414) instead of an index. On by default (DIV-3); flip off to restore the upstream accumulator rendering.", "example": "option arraystride on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-8724 (strided-array index recovery)", "change_kind": "structure-recovery", "tier": "core", "symptoms": ["loop walks an array with a raw offset accumulator (iVar += 0x414) instead of an index", "strided induction offset never re-expressed as counter*stride array indexing"]},
  {"option": "sparcstructret", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P2", "subphase": "flow-classification", "strength": "HARD", "rewind": "P2", "issue": "GH-6882", "summary": "Let the SPARC struct-return `unimp` after a call fall through instead of becoming a non-returning indirect call.", "use_when": "Set on PER SPARC PROGRAM when a struct-returning callee makes a function render a bogus (*pcVar1)() and drop its tail; DESTRUCTIVE as a global default (an IllegalInstructionTrap-fed BRANCHIND elsewhere is suppressed).", "example": "option sparcstructret on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-6882 (SPARC struct-return unimp fallthrough)", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["bogus (*pcVar1)() indirect call after calling a struct-returning function on sparc", "function tail dropped after a sparc call followed by unimp"]},
  {"option": "condexeplace", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "simplification-quiescence", "strength": "HARD", "rewind": "P3", "issue": "GH-9203", "summary": "Stop ActionConditionalConst from materializing a propagated constant as a COPY inside a loop predecessor block (a spurious `= 0` in the do/while body).", "use_when": "A malformed do/while holds an out-of-place constant assignment in its body. On by default (DIV-3); flip off to restore the upstream COPY placement.", "example": "option condexeplace on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-9203 (ActionConditionalConst loop-block COPY placement)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["malformed do/while whose body holds an out-of-place constant assignment", "spurious = 0 copy materialized inside a loop block"]},
  {"option": "inputvarnodeadjust", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "stack-frame-layout", "strength": "HARD", "rewind": "P6", "issue": "GH-9218", "summary": "Absorb an input Varnode overlapping the high end of a justified parameter container instead of aborting the function.", "use_when": "A frame aborts with 'Cannot properly adjust input varnodes' (overlapping stack params, e.g. mc68k link/unlk). On by default (DIV-3); flip off to preserve the upstream abort.", "example": "option inputvarnodeadjust on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra GH-9218 (overlapping input-varnode absorb)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["function aborts with 'Cannot properly adjust input varnodes'", "overlapping stack parameters (mc68k link/unlk) kill the whole decompilation"]},
  {"option": "retinputhalf", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "output-prototype", "strength": "HARD", "rewind": "P4", "issue": "kuna-returned-input-half", "summary": "Keep a returned register half whose value is an input parameter the function PLACED there. The uncomputed-return repair drops a return-register half that traces back only to things the function never computed, to kill the callee-saved-restore phantom (`undefined16 main(...)` with a `v[8] = <uninitialized slot>` write). An input parameter is unwritten by definition, so a returned pair whose high half is a passthrough argument -- `P wide(unsigned long s, unsigned long x){ p.a=s+1; p.b=x; return p; }`, compiled to `mov %rsi,%rdx` -- lost the half AND the argument, recovering `unsigned long wide(long a0)` for a two-argument function. With this on an unwritten terminal is a real return value when it is input-parameter storage the model would pass an argument in AND it arrived from a DIFFERENT address than the return half it reaches, i.e. the function executed an instruction to put it there. A local frame slot is not parameter storage and an untouched return register is not a placement, so both the restore phantom and the GH-6990 SPARC pass-through are still dropped.", "use_when": "On by default: a value the function was handed and deliberately moved into the return register is a real return, and dropping it also deletes the parameter it came from, so the recovered signature loses an argument the disassembly plainly passes. Byte-identical (0/675) on the datatest corpus; 1 of 7307 functions changes across grep/sort/faillog/libselinux/betaflight and 31 of 1577 on a static-PIE Rust binary, every one recovering a return half or a parameter. Set off to restore the strict `unwritten means leftover` terminal rule.", "example": "option retinputhalf off", "source_decompiler": "kuna", "inspiration": "kuna wide/w2 witness (gcc -O1 x86-64 struct-pair return whose high half is a passthrough argument)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a parameter the disassembly plainly passes is missing from the recovered signature", "a two-register struct return comes back as one register when the second half is just an argument", "two near-identical functions recover different arities, one dropping an argument", "the returned pair survives when the half is arithmetic but not when it is a plain copy of a parameter"]},
  {"option": "retpushedhalf", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "output-prototype", "strength": "HARD", "rewind": "P2", "issue": "kuna-alignment-push-pop", "summary": "Reject a register the function only ever PUSHED as the source of a returned register half. `retinputhalf` keeps a returned half whose value traces back to an input parameter at a DIFFERENT address than the half, on the reading that the function executed an instruction to put it there. An alignment `push %r8` in the prologue paired with a `pop %rdx` in the epilogue satisfies that reading by accident: RDX at the RETURN traces to R8 at entry, so R8 becomes a parameter, and R8 being a parameter is the only evidence that the half is a returned argument. A four-argument decryptor returning `void *` came out `undefined16 sub_10e27(long,int,long,int,unsigned long)` with `v1._8_8_ = a4`. By the time the repair runs, copy propagation has collapsed the store and the load and `RDX = COPY(R8)` is indistinguishable from a deliberate `mov %r8,%rdx`, so the evidence is gathered during the flow build instead: a register some stack-adjusting instruction stores to memory and no instruction in the function ever writes is push-only, and a push-only register is not a placement source.", "use_when": "On by default: it only ever narrows `retinputhalf`, and only for a register the function pushes and never writes -- the ordinary callee-saved save/restore writes the register in the pop, so it does not qualify. Byte-identical (0/675) on the datatest corpus. Set off to restore the address-only placement test, which is what you want if a function really does hand back an argument it preserved on the stack and popped into another register.", "example": "option retpushedhalf off", "source_decompiler": "kuna", "inspiration": "kuna WeeperVM sub_10e27 (gcc x86-64 alignment push %r8 / pop %rdx around a four-argument XOR decryptor)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a function grows one more argument than the disassembly passes, used only as the high half of the return", "an alignment push/pop around the body turns a pointer return into undefined16", "the recovered signature ends in an extra argument whose only appearance is `v._8_8_ = aN;`", "a prologue PUSH of an argument register and an epilogue POP into a different register invent a 128-bit return"]},
  {"option": "rustabi", "values": ["off", "auto", "always"], "default": "off", "destructive_as_default": false, "phase": "P4", "subphase": "output-prototype", "strength": "HARD", "rewind": "P4", "issue": "kuna-rust-scalarpair-return", "summary": "Keep the two-register value a rustc ScalarPair return (Result, Option, a slice, a fat pointer) is carried in, and connect it at the call. kuna already RECOVERS the pair -- the x86-64 cspec join_dual_class output rule matches the RAX/RDX trials and buildReturnOutput builds the join -- and then loses it twice. On the producer, SubvariableFlow::tryReturnPull truncates the RETURN to the logical width of the value it is tracing; rustc materializes the discriminant with `xor %eax,%eax; setb %al`, so RAX is a ONE-BIT logical value and the whole return narrows to it, deleting the other register: `Result<u32,u32>` comes back as `bool prod(uint4 a0)` with the EDX payload gone. On the consumer, FuncCallSpecs::buildOutputFromTrials handles one used output trial and returns early on two, so the call gets NO output at all and every read of the payload register renders as a local the function never assigns -- the phantom `int4 v3; // edx` that a Result guard tests. The two seams get DIFFERENT classifications, because they do not see the same thing. At the producer the concatenation's halves are values the function computed, so classify_return_pair reads their shape (never a size: size does not predict rustc's choice -- `Result<u32,u32>` is 8 bytes and a pair, `Result<Box<u64>,u32>` is 16 bytes and goes through memory). A pair is recognized when the least-significant half is discriminant-shaped -- a value whose known non-zero bits fit in a byte, which covers both the branchy `mov $0/$1` tag and the branchless `setCC` one -- and vetoed when that half traces back to the function's own incoming pointer argument, which is the sret epilogue rather than a tag. At the CALL there are no callee values in the IR at all (both halves are INDIRECT creations), so classify_call_output_pair rests on the model rule that matched, on the caller reading both halves out of the call, and on a bounded decode of the resolved callee: when that decode covers every path to a RETURN and never writes the payload register, the caller's read is a clobber and the pair is REFUSED. That callee check is one-sided -- it can refute a pair, never confirm one -- so `ScalarPair` at the consumer means `no counter-example`, not `the callee returns a pair`. At `auto` the rules act only on an image the loader detected as rustc-produced; at `always` the language test is dropped.", "use_when": "Turn on (`auto`) when decompiling a Rust binary and a `Result`/`Option` payload is missing: the producer recovered as `bool` when the disassembly plainly writes a second return register, or a consumer testing `v & 1` reads a companion local that is declared, read, and never assigned anywhere in the function. Measured on a 4.9 MB static-PIE rustc binary (4194 functions): phantom guards -- conditional guards whose condition reads a local the function never assigns -- fall from 1476 of 9026 to 1116 of 9039 (-24.4%), never-assigned locals from 4805 to 3533 (-26.5%), and the functions containing one from 1225 to 983; 408 functions change (392 with strictly fewer phantoms, 1 with one more), every inspected one gaining a payload variable or a call output (`&str` fat pointers out of `String::borrow`, the `(usize, usize)` out of `physical_core_count`, `Instant::now`). Zero functions change across grep/sort/faillog/libselinux and the betaflight ARM fixture, because `auto` cannot see a non-Rust image, and `/usr/bin/ncat` and `/usr/bin/htop` are likewise byte-identical off vs auto. The call seam's callee veto costs nothing measurable on that corpus -- the whole-binary output is byte-identical with the veto ablated -- because a real ScalarPair producer does write its payload register; what it refuses is the caller that reads a register its callee provably never sets. Speed on that same Rust binary, interleaved off/auto, minimum of 6: 169.04 s -> 169.37 s, **+0.20%** (budget 5%); two separate min-of-3 runs disagreed in sign (+0.38% and -0.78%), so the real cost is below this machine's measurement floor. DEFAULT OFF this round: the classification is deliberately narrow (one discriminant byte, one payload register) and the pair it keeps alive is rendered as a raw `undefined16`/`[u8; 16]` container until a later pass gives it an enum type, so a producer that used to render as a tidy `bool` now renders as a two-field aggregate -- more information, less polish. `auto` cannot touch a non-Rust image at all (the language fact comes from the analyzer tier, which the XML datatest bootstrap never runs), so the risk is confined to Rust output; use `always` only to force the rules on a corpus with no `.comment` record, such as a stage-test bytechunk.", "example": "option rustabi auto", "source_decompiler": "kuna", "inspiration": "rustc -C opt-level=2 ScalarPair return ABI (Result<u32,u32> in EAX:EDX) measured against the SysV cspec, plus the stubbed FuncCallSpecs::buildOutputFromTrials multi-trial branch", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a Rust Result or Option producer recovers as bool and the payload register is missing from the return", "a local commented with a register name is declared and read but never assigned anywhere in the function", "a call to a Rust function that returns Result renders with no output at all", "a discriminant guard tests a variable whose companion payload variable has no definition", "two-register return recovered as one register when the first half holds a setCC flag"]},
  {"option": "loweredswitch", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "angr-LoweredSwitchSimplifier", "summary": "Reconstruct a compiler-lowered comparison cascade (a GCC binary-search if/else tree over one variable) back into a switch with a synthesized BRANCHIND + jump-table (the S2 artifact Ghidra renders switches from).", "use_when": "On by default (DIV-4); the required binary-search-structure guard (a range/jle split) keeps it off hand-written linear if/else-if chains. Set OFF to restore the upstream if/else-if rendering of a lowered switch.", "example": "option loweredswitch off", "source_decompiler": "angr", "inspiration": "angr LoweredSwitchSimplifier (angr/analyses/decompiler/optimization_passes); test_decompiler.py switch-lowering cases", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["switch rendered as nested if/else-if compare tree over one variable", "binary-search cascade of constant compares where source had a switch", "flip off to keep the compiler's lowered compare-tree rendering"]},
  {"option": "loweredswitchlabels", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "re-unsigned-byte-vm-selector", "summary": "Preserve the signedness proved by a recovered lowered switch's required range comparisons and selector-widening paths across the restart and synthesized jump-table install. Signed SLESS/SLESSEQUAL cascades render signed labels; unsigned LESS/LESSEQUAL cascades render unsigned labels; ZEXT proves the underlying selector unsigned and SEXT proves it signed. Equality constants never decide signedness, but safely normalize from the widened comparison width. Mixed range evidence, conflicting extension/opcode evidence, mixed extension paths, inconsistent effective widths, or shrinking SUBPIECE projections that the restart record cannot preserve decline recovery instead of guessing.", "use_when": "On by default (DIV-173): a case label and installed selector must preserve the same signed or unsigned interpretation and projection as the comparison cascade. Set off to reproduce full historical behavior: the original structural recovery runs without the new evidence checks, including its shrinking-SUBPIECE peel, then any selector-width case with its sign bit set makes every label signed.", "example": "option loweredswitchlabels off", "source_decompiler": "ghidra-upstream", "inspiration": "upstream JumpTable::getSwitchType signedness contract; RE-friction unsigned-byte-vm-selector, KataVM_L1 0x12d0", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["an unsigned char switch has a negative case label", "an unsigned selector's high-bit case is printed as a negative number", "a lowered switch's case labels disagree with the signedness of its range comparisons"]},
  {"option": "loweredswitchvalue", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "GH-468", "summary": "Keep a re-rolled lowered switch only when it dispatches on the value its comparison cascade compared. Detection sees the simplified SSA graph and records the storage of the value every compare reads; the install re-lifts raw p-code on the restart and has the new BRANCHIND read storage at the cascade head. That storage need not hold the value there: a parameter copied into another register before its own register is reused as a call argument, or a call result that a later call has overwritten, and the switch then dispatches on the argument or the later result (`v1 = (unsigned int)a1; switch(v1)` for a switch over the first parameter, or `switch(6)` where a guard pinned the overwriting value). With this on, detection gives the compared value a restart-stable name -- the function input at a storage, the effect of the call at an address, or the output of an op of one opcode at an address that is the only such definition -- and the install reads what the head's own compare instruction reads. On the restarted run the BRANCHIND input is named the same way, through copies, casts, extensions, zero-offset truncations and identity arithmetic that keep every byte. A switch reading the recorded value is verified for the read it was made over. A switch reading a provably different value -- another uniquely-defined value or a constant -- costs a restart: it is installed once more over the recorded storage, and if that also differs it is withdrawn and the cascade renders as `option loweredswitch off` renders it. A value SSA construction defines (a phi) or one with no unique definition has no name: its switch reads the recorded storage exactly as `loweredswitch` alone does and is never withdrawn, and an input that is only unnameable is never taken as evidence of a wrong dispatch.", "use_when": "On by default (DIV-181). Turn off only to reproduce the unchecked rendering, where the synthesized switch reads the recorded storage at the cascade head whatever that storage holds by then.", "example": "option loweredswitchvalue off", "source_decompiler": "kuna", "inspiration": "GH-468 finding 4: the switch inlined into MSVC /O2 main compares EBX after argc's own register ECX has been reused; gcc -O2 `mov eax,edi; mov rdi,rsi; cmp eax,3` dispatchers", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a re-rolled switch dispatches on a variable assigned just before it that the cascade never compared", "switch(v) where v was loaded as a call argument or holds an unrelated call result", "a switch over a parameter renders over a local while option loweredswitch off shows the chain comparing the parameter", "a re-rolled switch dispatches on a constant such as switch(6)", "a parameter the compare tree tests disappears from the prototype when the switch re-rolls"]},
  {"option": "loweredswitchexact", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "GH-468", "summary": "Install a re-rolled lowered switch only when it is the compare tree it replaces. Three things made the switch a different program. The install kept one case label per body, so of two values sharing a body only the first reached it and the second fell into the default. The recovered case map can route a value somewhere the tree does not: a range edge the binary search pins to a single value is neither a case nor a default candidate, and a tie between two default candidates is broken by address, so the default can be a case body. And the install deletes every compare block behind the head together with whatever a block computes for the code after it (`v = 0x8a;` before a shared join). With this on, every case value keeps its own label on its body, and detection keeps a recovered cascade only when two checks pass. The routing check replays the compare tree against the switch the install builds: each compare is an equality or a threshold against a constant over the switch variable, widened only by zero or sign extension, so its outcome is constant between the constant's neighbourhood and the variable's signed and unsigned wrap points, and each of those values must reach the block the switch sends it to. The statement check computes the blocks the install makes unreachable and requires every op in them to be a branch or to compute a value read only inside them; a store, a call, a return, an indirect branch or a value read elsewhere keeps the compare tree.", "use_when": "On by default (DIV-183). Turn off only to reproduce the unchecked install, where values sharing a case body lose all labels but the first, and a switch that routes a value differently from the tree or drops a statement is still emitted.", "example": "option loweredswitchexact off", "source_decompiler": "kuna", "inspiration": "GH-468 finding 4 soundness review: re-rolled switches over MSVC and gcc cascades whose shared case bodies, pinned range edges and join assignments did not survive the install", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["two case values that share a body in the compare tree show only one case label and the other value takes the default", "a re-rolled switch's default arm runs code the compare tree only reaches for one specific value", "an assignment on one arm of a compare tree disappears when the tree re-rolls into a switch", "a case body jumps to a join that reads a variable no case arm assigns"]},
  {"option": "loweredswitchheads", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P2", "subphase": "switch-model", "strength": "HARD", "rewind": "P2", "issue": "GH-468", "summary": "Look for a lowered-switch cascade behind every head on the switch variable, not only the first one in block order. `loweredswitch` picks the most-compared variable and recovers a cascade from a head: a compare on that variable that no other compare on it continues into. Any unrelated test of the same variable is a head too, and when it comes first the cascade behind it was never tried. That is the inlined shape: MSVC /O2 inlines `and_chain(argc, argc*7)` and `cascade_switch(argc)` into `main`, so a `TEST EBX,EBX; JLE` on argc comes before the 15-case binary-search tree over the same argc, recovery from that single test finds no case, and the whole cascade renders as a goto ladder into a shared join. With this on, detection tries each head in block order (each past its leading all-ones sentinel guards) and records the first cascade that recovers. The first head keeps exactly the acceptance it had. A cascade found behind any later head is recorded only when the `loweredswitchexact` checks pass for it -- the compare tree replayed against the switch the install builds routes every value to the same block, and no block the install deletes computes anything used outside it -- whether or not `loweredswitchexact` itself is on, when its head block has one predecessor, and when no case body or default is entered from outside the compare tree.", "use_when": "On by default (DIV-184). Turn off to try only the first compare on the switch variable as the cascade head, so a switch inlined after another test of its selector renders as an if/else or goto ladder.", "example": "option loweredswitchheads off", "source_decompiler": "kuna", "inspiration": "GH-468 finding 4: MSVC /O2 ab_o2.exe main, cascade_switch(argc) inlined after and_chain(argc, argc*7)", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["a switch inlined into a larger function renders as an if/else or goto ladder into a shared join while the same switch in its own function re-rolls", "every arm of a compare ladder assigns one variable and jumps to the same label", "option loweredswitch off and on give byte-identical output on a binary-search compare tree preceded by another test of the same variable"]},
  {"option": "evalcurrentproto", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "prototype-source", "strength": "HARD", "rewind": "P4", "issue": "kuna-eval-current-prototype", "summary": "Evaluate a function's OWN unknown prototype with the model its compiler spec nominates in `<eval_current_prototype>` (a `<resolveprototype>` merged model), instead of with `<default_proto>`. The merged model then resolves per function against the trials that are actually active (`FuncProto::resolveModel` -> `ProtoModelMerged::selectModel`), so an x86 Windows binary recovers `__fastcall`/`__thiscall` ECX/EDX arguments while a function that touches neither still comes out `__stdcall`. Six vendored specs nominate a model (x86win, x86borland, x86gcc, CR16, HCS12, HCS12X); every other language is untouched either way.", "use_when": "On by default: the spec's nomination is the compiler's own statement about how its functions are called, and without it a register-passed argument surfaces as a local READ BEFORE IT IS WRITTEN - not merely less informative, but not something a C function can do. Byte-identical on the 675-datatest corpus. Set off to restore the `<default_proto>`-only evaluation (stack-only inputs on x86 Windows). An explicit `option protoeval <model>` outranks both.", "example": "option evalcurrentproto off", "source_decompiler": "ghidra-upstream", "inspiration": "upstream Architecture::parseCompilerConfig ELEM_EVAL_CURRENT_PROTOTYPE (architecture.cc:1321) + FuncProto::resolveModel (fspec.cc:3772); x86win.cspec <eval_current_prototype>", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["an x86 Windows function renders as (void) though it takes arguments", "a local carrying a // ecx or // edx storage comment is read before it is ever written", "__fastcall/__thiscall arguments missing from the signature", "calls pass fewer arguments than the callee reads"]},
  {"option": "callsitestackargs", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "decbench-callsite-stack-args", "summary": "Recover stack-passed call arguments at call sites whose callee prototype is unlocked, by testing the caller-relative argument Varnode address (not the callee-relative trial address) against the caller's local stack range when scoring an active input trial.", "use_when": "On by default: this restores upstream Ghidra behavior (fspec.cc:5618) that a mis-ported argument had disabled, so it is a correctness fix rather than a judgment call and needs no DIV row. With it OFF every stack trial is scored no-use, so calls truncate at the register budget (x86-64 six arguments, i386 none) and any computation whose only consumer was a dropped argument is dead-code eliminated - which deletes real basic blocks, including whole loops. Set OFF only to reproduce that pre-fix output for a bisect or an ablation.", "example": "option callsitestackargs off", "source_decompiler": "ghidra-upstream", "inspiration": "decbench:O0-mydoom-mydoom-msg_b64enc + O2-noinline-openssh-portable-ssh-add-parse_dest_constraint; upstream FuncCallSpecs::checkInputTrialUse (fspec.cc:5585)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["call rendered with exactly six arguments on x86-64", "call rendered with an empty argument list on i386", "a for loop whose exit variable is written through a pointer argument disappears", "ternary guards feeding a logging call are dead-code eliminated", "outgoing-argument stack slots render as locals commented stack - 0xNN"]},
  {"option": "varargstackargs", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "repipe-68149b8a-scanf-varargs", "summary": "Score a variadic call site's stack arguments as their own ParamListStandard::fillinMap resource section. fillinMap (fspec.cc:1560) polices the gaps between a section's trials with two positional rules -- forceNoUse (once a whole exclusion group in the section is definitely-not-used, everything after it is inactive) and forceInactiveChain (a run of more than 2 unused slots ends the section). On AArch64 the general registers x0-x7 and the outgoing stack area are ONE section, and on an ABI that passes variadic arguments on the stack -- Apple arm64 does -- x1-x7 are structurally empty between the last fixed register parameter and the first stack vararg. Seven empty slots is longer than either rule tolerates, so a stack trial that checkInputTrialUse had already scored ACTIVE (ancestor-realism and the local-alias test both passed) is forced inactive again and its argument is dropped from the call. With the option ON the register prefix and the stack tail of a section are scored independently, so the ABI's register hole stops being evidence about the stack argument; forceInactiveChain's trailing gap-filling loop also stays inside the half that produced its max, which is what keeps the fix from fabricating x1-x7 as six invented register arguments. Trial scoring itself is untouched: a stack trial still has to be ACTIVE on its own evidence to survive, so the option can only keep an argument the recovery already believed in. Gated on the callee prototype being variadic (FuncProto::isDotdotdot) -- for a fully known prototype a register hole IS evidence, and only `...` makes the hole a property of the ABI rather than of the recovery.", "use_when": "On by default (DIV-101). Keep it on when a variadic call on AArch64/Apple silicon (or any ABI that passes varargs on the stack) is rendered with its format string and nothing else -- `scanf(\"%d\");` followed by a comparison against an apparently uninitialized local, or `printf(\"Message: %s\\n\");` with no argument for the %s. The dropped destination usually also shows up as a stack local that is read but never written. Flip back OFF if a variadic call gains a trailing argument the disassembly does not pass -- the failure mode of relaxing a positional rule is a spurious argument, and it can only appear at a call whose prototype already carries `...`. OFF is upstream Ghidra's behavior.", "example": "option varargstackargs off", "source_decompiler": "kuna", "inspiration": "repipe:68149b8a8f555589f353117c _main (Mach-O arm64 practice_2_rengr); upstream ParamListStandard::fillinMap/forceNoUse/forceInactiveChain (fspec.cc:1560/1495/1519)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["scanf rendered with only its format string and no destination pointer", "printf with a %s conversion and no corresponding argument", "a variadic call on arm64 keeps only the arguments that fit in registers before the first empty one", "a stack local is compared or printed but never assigned after a scanf call", "Apple arm64 variadic arguments missing from the call"]},
  {"option": "calleearity", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "trial-finalization", "strength": "HARD", "rewind": "P4", "issue": "repipe-69a54bd7-allocator-args", "summary": "Reconcile a call's recovered argument list with a sibling call to the SAME callee in the same function. When a callee prototype is unlocked, kuna recovers the argument list independently at every call site (ActionActiveParam, checkInputTrialUse, ParamListStandard::fillinMap) and nothing reconciles the results, so one allocator wrapper can be called four times in one caller and render with four different arities. The site that loses its argument is typically one where the value is also branched on: Funcdata::onlyOpUse (funcdata_varnode.cc:1851) rejects a trial whose Varnode has a CPUI_BRANCH/CPUI_CBRANCH among its descendants, which an MSVC aligned operator new[] overflow guard (lea rcx,[rbx+0x27]; cmp rcx,rbx; jbe abort; call) always produces. Relaxing that rule directly would fabricate an argument at every `test rcx,rcx; jz; call` in the corpus; what settles the question instead is the other call site. With the option ON, buildInputFromTrials reads the REGISTER argument storage off a sibling call to the same entry address whose list is already final, and promotes this call's matching trials. Register storage only (a finalized call's stack arguments sit at caller-relative addresses that differ per site), real Varnodes only (never a synthetic unreferenced trial), all-or-nothing (parameters are positional), and never subtractive. Direction is limited by finalization order: ActionActiveParam finalizes each spec as soon as it is fully checked, so a call is reconciled against the sites BEFORE it in the function.", "use_when": "On by default (DIV-102). Keep it on when the same internal callee renders with different arities in one function -- `sub_140008160(0x28)` at one site and `sub_140008160()` at another -- which destroys the dataflow into the callee (a buffer size, an object pointer) exactly where you need it. Most common on MSVC x86-64 around allocator wrappers, whose overflow guard branches on the size the call also takes. It cannot by itself help when the FIRST call to that callee in the function is the one that lost the argument -- that direction is `calleearityfwd`, which is on by default and inert with this option off -- and it never removes an argument, so an over-recovered site stays over-recovered. Flip back OFF if a call gains an argument no other site passes; the reconciliation is only as good as the witness site it copies.", "example": "option calleearity off", "source_decompiler": "kuna", "inspiration": "repipe:69a54bd70f5b9757a6a5f72f 0x1400024a0 (PE x86-64 PolyMLP.exe, sub_140008160); upstream Funcdata::onlyOpUse (funcdata_varnode.cc:1851) and FuncCallSpecs::buildInputFromTrials (fspec.cc:5692)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["the same sub_ address is called with an argument at one site and with an empty argument list at another", "an allocator wrapper call loses its size argument", "a call whose argument register is compared and branched on just before the call renders with no arguments", "MSVC operator new[] overflow guard leaves the allocation size unused"]},
  {"option": "calleearityfwd", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "trial-finalization", "strength": "HARD", "rewind": "P4", "issue": "repipe-argument-recovery-knobs-still", "summary": "Complete `calleearity` in the one direction it could not reach: reconcile a call site against a sibling call to the same callee that finalizes LATER. ActionActiveParam finalizes each call spec the moment that spec is fully checked, in qlst order, so the plain rule only ever sees the sites BEFORE the one it is rescuing; when the site that lost its argument finalizes first, there is no witness yet and it declines. That is the MSVC aligned operator new idiom, whose two arms call the same allocator: the large arm writes a fresh rcx (lea rax,[rcx+0x27]; mov rcx,rax; call) and keeps its argument, while the small arm passes rcx live-in (test rcx,rcx; jz; call) and loses it to Funcdata::onlyOpUse, which rejects a trial Varnode that a CBRANCH also reads. Reordering the finalization is the wrong fix: Funcdata::checkCallDoubleUse (funcdata_varnode.cc:2121) reads whether ANOTHER spec is still input-active while scoring a trial, so deferring a spec past its neighbours' checkInputTrialUse changes argument recovery on every binary. With the option ON, a call that finalizes with an EMPTY argument list is set aside -- together with the Varnodes its promotable trials point at, captured before opSetAllInput drops them -- and retried once at the end of the same pass, when every spec is final. The witness search and every refusal are calleearity's own, unchanged: register storage only, real Varnodes only, all-or-nothing, never subtractive, and only a call that recovered nothing at all. It adds no new way to promote a trial.", "use_when": "On by default. Keep it on for the same reason as `calleearity` -- an internal callee rendering with different arities in one function destroys the dataflow you need -- when the site that lost the argument is the FIRST one the recovery finalizes, which `calleearity` alone cannot rescue. Two shapes dominate: the MSVC aligned-allocation wrapper (`if (a0 < 0x1000) return alloc(a0); ... alloc(a0 + 0x27)`) and a first argument the callee-save prologue copies away (`mov bl,cl; call f; mov cl,bl; call f`), where the copy is the extra descendant that sinks the trial. Inert unless `calleearity` is also on, so `calleearity off` still turns all sibling reconciliation off. Flip OFF (or flip `calleearity` off, which covers both) if a call gains an argument no other site passes; the retry is only as good as the witness it copies, and like `calleearity` it never removes one, so an over-recovered site stays over-recovered.", "example": "option calleearityfwd off", "source_decompiler": "kuna", "inspiration": "repipe:argument-recovery-knobs-still, 6547b4d50f4238b24302b588 (PE x86-64 `trappy attack.exe`) 0x1400011c0 / sub_140002c90 at 0x1400016f7 vs 0x1400016d1, and 69a3822f7b3cc38c80464da4 (nikos_crack_me.exe) sub_14002f800 / sub_14002ff34; upstream FuncCallSpecs::buildInputFromTrials (fspec.cc:5692) and Funcdata::checkCallDoubleUse (funcdata_varnode.cc:2121)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["the same sub_ address is called with an argument at one site and with an empty argument list at another and `calleearity` does not fix it", "the FIRST call to a callee in a function is the one that lost its argument", "an MSVC aligned allocation wrapper renders alloc(a0 + 0x27) on one branch and alloc() on the other", "a call loses the argument its callee-save prologue copied into another register"]},
  {"option": "calleearitylive", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "trial-finalization", "strength": "HARD", "rewind": "P4", "issue": "repipe-argument-recovery-options-still", "summary": "Extend a call site's PARTIALLY recovered argument list to a sibling call's, using the CALLEE'S OWN BODY as the discriminator. `calleearity` and `calleearityfwd` reconcile only a site that recovered NOTHING, and that limit was bought by measurement: without it the rule reads \"same callee, same arity\", which the whole-corpus sweep showed is false for a variadic callee and for an over-recovered witness (`Sleep(200)` became `Sleep(200,0)`, and the internal variadic logger `sub_1b11c(5,0,\"Zip: empty archive?\")` gained two arguments its format string has no conversions for). But a short list is not always a shorter call: `graphy` @0x1002c90 calls one helper fifteen times, eleven sites rendering five arguments and four rendering three, from instruction-for-instruction identical code -- `Funcdata::onlyOpUse` (funcdata_varnode.cc:1851) sinks the `ecx` trial because a CBRANCH also reads that value, and `fillinMap`'s positional rule drops `r8d` behind the hole. What settles it is evidence the sibling does not carry. With the option ON, a site whose list is a strict PREFIX of a sibling witness's is extended only when the bounded callee-body decode `calleedeadarg` already takes (`probe_callee_entry_dead`) answers two things: every register the witness claims beyond this site's list is read before written by the callee, AND no other argument register of the prototype model is. The second half is what refuses the sweep's two shapes -- an import has no body to decode, and an AArch64 variadic register-save prologue (`str x3,[sp,#136]; stp x4,x5,[sp,#144]; stp x6,x7,[sp,#160]`) reads argument registers a five-argument witness does not claim. Everything else is `calleearity`'s: register storage only, real Varnodes only, all-or-nothing, never subtractive. Like `calleearityfwd` it is deferred to the end of ActionActiveParam::apply rather than reordering finalization -- on the witness the four short sites are the FIRST four, so an in-order rule sees no witness at any of them.", "use_when": "On by default (DIV-107). Keep it on when one internal callee renders with two different NON-EMPTY arities in the same function -- `f(a,b,c)` at some sites and `f(a,b,c,d,e)` at others from the same instruction shape -- which hides the dataflow into the callee's later parameters exactly where an RE needs it, and which `calleearity`/`calleearityfwd` decline by design because the short site recovered something. The tell is a call whose next argument register is written by a dedicated `mov` immediately before the CALL and whose value is also compared or branched on. Inert unless `calleearity` is also on, so `calleearity off` still turns all sibling reconciliation off, and inert on any callee whose body the decode cannot fully cover -- an import, a thunk, a body past the instruction budget. Flip OFF if a call gains a trailing argument no other site passes; the way that could happen is a callee decoded in the wrong instruction mode, where the bytes read are not the instructions that run.", "example": "option calleearitylive off", "source_decompiler": "kuna", "inspiration": "repipe:argument-recovery-options-still, 69761b7a39e9c4d85c2f9fc1 (ELF x86-64 graphy) sub_1002c90 -> sub_1005250 at 0x10033c7/0x100341d/0x1003470/0x10034c3; upstream FuncCallSpecs::buildInputFromTrials (fspec.cc:5692) and Funcdata::onlyOpUse (funcdata_varnode.cc:1851)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["the same callee renders with five arguments at some call sites and three at others in one function", "a call loses its last two arguments although a mov writes each argument register right before the CALL", "calleearity and calleearityfwd both leave a short argument list alone", "the trailing parameters of a helper are invisible at half its call sites", "an argument register that is also compared and branched on drops that argument and every one after it"]},
  {"option": "calleearitybody", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "trial-finalization", "strength": "HARD", "rewind": "P4", "issue": "repipe-defined-payload-function-loses", "summary": "Recover the argument list of a call that has NO sibling, from the CALLEE'S OWN BODY. `calleearity`, `calleearityfwd` and `calleearitylive` are all sibling reconciliation: each needs another call to the same entry address in the same function whose list is already final and longer. That leaves the callee called exactly once with nothing to compare against, and it is a shape agents hit constantly -- a thread entry point, a one-shot payload, a handler reached from one place. On a Win64 PE, `mov rbx,rcx; cmp dword[rcx+0x238],edi; je payload_call` passes the caller's own first parameter straight through in rcx, but `Funcdata::onlyOpUse` (funcdata_varnode.cc:1851) rejects the trial because the CBRANCH also reads that value, and the only other mention of the callee is an address-taken `lea` for CreateThread, which is not a call site. The call renders `payload();` while decompiling that same callee on its own yields `payload(unsigned int *)`. Relaxing `onlyOpUse` is the wrong fix -- it would fabricate an argument at every `test rcx,rcx; jz; call` there is -- so the witness is instead the bounded callee-body decode `calleedeadarg` already takes (`probe_callee_entry_dead`), standing ALONE rather than only extending a sibling's list. Walk the call's trials in prototype order: the recovered list is the leading run of argument registers the callee `proves_read` (some path reads the register before writing it), and it must END at a register the callee `proves_dead` (it overwrites that register before ever reading it, on every path). The second half is the whole safety of the rule: `proves_read` is an existential and would run to the end of the argument registers on its own, so what bounds the list is a register the callee is PROVEN not to consume. It is also what refuses the shape the family's corpus sweep found -- an AArch64 variadic register-save prologue (`str x3,[sp,#136]; stp x4,x5,[sp,#144]; stp x6,x7,[sp,#160]`) reads every argument register there is, so no dead one follows the run and the rule declines instead of inventing eight arguments. An import, a thunk and a body past the decode budget answer neither and decline the same way. Everything else is `calleearity`'s, unchanged: register storage only, real Varnodes only, all-or-nothing, never subtractive, and only a call that recovered nothing at all -- a site with a partial list is `calleearitylive`'s. Deferred to the end of `ActionActiveParam::apply` and run AFTER both sibling rules, so wherever a sibling can speak the sibling still wins.", "use_when": "On by default (DIV-PENDING). Keep it on when a call to a callee that is called only ONCE renders with an empty argument list although the disassembly passes a register into it -- the tell is `f();` in the body next to `... f(<type> *)` in the header or in a separate decompile of f, and an incoming argument register that the caller also compares or branches on before the call. Thread entry points, one-shot payloads and single-site handlers are the common shapes, since a sibling call is exactly what they do not have. Inert unless `calleearity` is also on, so `calleearity off` still turns all argument-list recovery off, and inert on any callee whose body the bounded decode cannot fully cover -- an import, a thunk, a body past the instruction budget -- or that reads every argument register the model has. Flip OFF if a call gains an argument the disassembly does not pass; the way that could happen is a callee decoded in the wrong instruction mode, where the bytes read are not the instructions that run.", "example": "option calleearitybody off", "source_decompiler": "kuna", "inspiration": "repipe:defined-payload-function-loses, 68bffbe9224c0ec5dcedbe7a (PE x86-64 runtime.exe) sub_14000b62c -> payload 0x14000ca0c at 0x14000b726; upstream FuncCallSpecs::buildInputFromTrials (fspec.cc:5692) and Funcdata::onlyOpUse (funcdata_varnode.cc:1851)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a callee that is called only once renders with an empty argument list", "f() is called where the same output declares f(char *)", "calleearity, calleearityfwd and calleearitylive all leave an empty argument list alone", "a thread entry point or one-shot payload loses the context pointer the caller passes it", "the argument register is compared or branched on just before the CALL and the argument disappears"]},
  {"option": "calleearitycut", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "trial-finalization", "strength": "HARD", "rewind": "P4", "issue": "repipe-bytecode-reader-call-site", "summary": "Accept a `calleearitybody` argument run that is bounded by a CUT decode instead of by a provably dead register. `calleearitybody` recovers a lone call's argument list as the leading run of argument registers the callee `proves_input`, and requires the run to END at an argument register the callee `proves_dead`. That second half bounds an existential, but it is unavailable far more often than the first, because the bounded callee-body walk ends EVERY path at the callee's first nested call: a register the callee clobbers only past that call is invisible to it. A 977 KB ELF crackme shows the shape -- `mov ecx,0x1; call 0x875e0` with rdi/rsi/rdx passed straight through from the caller's own parameters, a callee whose prologue reads all four (`lea rax,[rsi+rdx]`, `mov eax,ecx`, `add word ptr [rdi+0x30],1`) and then calls, and r8 clobbered 0x30 bytes past that call. Four registers prove input, none proves dead, and `sub_875e0();` renders with no arguments at all while decompiling that same callee alone yields `sub_875e0(int8,char *,int8,uint4)`. The caller side cannot rescue it either: `AncestorRealistic::execute` (funcdata_varnode.cc:2159) refuses an input Varnode outright -- upstream policy, `we expect to see active movement into the parameter` -- so the three pass-through registers are inactive, and `ParamListStandard::fillinMap` reads the hole in front of the one written register as the end of the list. This rule supplies the missing bound from the run's own shape: the run must be CONTIGUOUS (no skipped register inside it, so every emitted argument sits where the ABI puts it and the only reachable error is a missing trailing one, never a misplaced one), it must STOP SHORT of the last argument register (a run that consumes all of them has no boundary -- that is the AArch64 variadic register-save prologue `calleearitybody`'s corpus sweep found), and the register it stops at must be QUIET -- the caller must not have written a value into it, since a caller that does is passing a further argument whatever the callee decode can see. All of `calleearitybody`'s own guards still apply first.", "use_when": "On by default (DIV-PENDING). Keep it on when a call to a callee that is called only ONCE renders `f();` although the disassembly passes registers into it AND `calleearitybody` alone does not recover them -- the tell is a callee whose prologue calls another function early, so the argument register after the ones it reads is clobbered only past that call and nothing can prove it dead. Passing the caller's OWN incoming parameters straight through is the shape that needs this most, because upstream ancestor-realism refuses an input Varnode at a call site on principle. Inert unless `calleearitybody` (and so `calleearity`) is also on. Flip OFF if a call gains a trailing argument the disassembly does not pass; the way that could happen is a callee whose real argument list is longer than the run and whose extra registers the caller left untouched by coincidence -- the result is a SHORT list, never a wrong one, because the run is required to be contiguous.", "example": "option calleearitycut off", "source_decompiler": "kuna", "inspiration": "repipe:bytecode-reader-call-site, 673da52e9b533b4c22bd2eeb (ELF x86-64 Layers crackme) sub_9d9c0 -> sub_875e0 at 0x9d9d8; upstream AncestorRealistic::execute (funcdata_varnode.cc:2159) and ParamListStandard::fillinMap (fspec.cc:1560)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call renders with no arguments although the disassembly loads every argument register before it", "calleearitybody leaves an empty argument list alone on a callee whose prologue calls another function", "the caller passes its own incoming parameters straight through and every one of them disappears at the call", "f() is called where a separate decompile of f declares f(int8,char *,int8,uint4)", "only the argument register the caller writes just before the CALL survives trial scoring, and the list still comes out empty"]},
  {"option": "calleearityscratch", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "trial-finalization", "strength": "HARD", "rewind": "P4", "issue": "repipe-hint-printing-call-loses", "summary": "Let a boundary register the caller only used as SCRATCH end a `calleearitycut` argument run. `calleearitycut` bounds a lone call's callee-body argument run by the register the run stops at, and requires that register to be QUIET -- the caller must have placed nothing in it -- because a caller that computes a value into the next argument register right before the CALL is usually passing a further argument, and truncating there would hide it. A register allocator does not respect that: it will spend an argument register on a throwaway. An x86-64 ELF crackme decodes a string with an unrolled XOR loop and hands the buffer to its hint printer -- `lea rdi,[rbp-0x30]` sets up the argument, then `movzx esi,byte ptr [rbp-0x1a]` puts the XOR key in the SECOND argument register and `xor byte ptr [r8+0x6],sil` consumes it, and 0x3e bytes later `call 0x417f80`. The callee reads rdi at its first instruction, so the run is `{rdi}`; its decode is cut three instructions later at a nested call, so nothing proves rsi dead; rsi is the register the run stops at, the caller wrote it, and the whole one-argument list is dropped -- the call renders `sub_417f80();`. This rule separates `the caller wrote it` from `the caller is passing it` using the answer upstream has already computed: `FuncCallSpecs::checkInputTrialUse` runs `Funcdata::ancestorOpUse` (funcdata_varnode.cc:1963) over every trial and marks it ACTIVE only when the value reaches the CALL and nothing else, so the XOR gives rsi's trial an INACTIVE score. An inactive boundary is scratch and bounds the run; an active one is upstream saying the value goes nowhere but the call, and stays refused. A constant is never scratch, however it scored -- materializing a constant into the next argument register right before a CALL is argument setup in the most literal form there is. All of `calleearitycut`'s own guards still apply first: the run must be contiguous and must stop short, so the reachable error stays a MISSING trailing argument, never one printed in the wrong position.", "use_when": "On by default (DIV-PENDING). Keep it on when a call to a callee called only ONCE renders `f();` although the disassembly loads at least the first argument register, `calleearitybody` and `calleearitycut` together still recover nothing, and the register just past the ones the callee reads is written by the caller for the caller's OWN use -- a loop counter, an unrolled XOR key, a scratch byte that is stored or compared before the CALL. Inert unless `calleearitycut` (and so `calleearitybody` and `calleearity`) is also on. Flip OFF if a call gains a SHORT argument list where the register just past it really was a further argument the caller both computed and used for something else; the result is a prefix of the true list, never a misplaced argument, because the run is still required to be contiguous.", "example": "option calleearityscratch off", "source_decompiler": "kuna", "inspiration": "repipe:hint-printing-call-loses, 5e51836e33c5d4439bb2dc24 (ELF x86-64 cr4ckm3_0x) 0x400f30 -> sub_417f80 at 0x401007; upstream Funcdata::onlyOpUse (funcdata_varnode.cc:1851) and FuncCallSpecs::checkInputTrialUse (fspec.cc:5592)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call still renders with no arguments although calleearity, calleearitybody and calleearitycut are all on", "an explicit prototype assert restores the argument that the unlocked call site drops", "the argument register just past the recovered run is written inside an unrolled loop for the caller's own use", "a decoding loop is followed by f() where the buffer pointer is plainly live into the CALL", "the register the callee-body run stops at is compared, stored or XORed by the caller before the CALL"]},
  {"option": "inputparamgap", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "own-input-prototype-recovery", "strength": "HARD", "rewind": "P4", "issue": "repipe-keyboard-callback-uses-undefined", "summary": "Stop a run of unused argument REGISTERS from vetoing a later register that the function's own body reads before it writes. `ActionInputPrototype` (coreaction.cc:4941) registers a trial for every input Varnode and marks it ACTIVE when it has descendants -- i.e. when the body genuinely consumes the incoming register -- and `buildTrialMap` even synthesizes unreferenced filler trials for the argument registers the function ignores. `ParamListStandard::forceInactiveChain` (fspec.cc:1519), called with maxchain = 2, then counts consecutive unused parameter slots and, once the run passes two, marks EVERY remaining trial inactive, active ones included; `fillinMap` only marks the still-active trials used, so the live-in registers past the gap never become parameters and render as locals that are read before any definition. A Wayland `wl_keyboard_listener` key handler is the exact shape: rdi is `data`, rsi/rdx/rcx (`wl_keyboard`, `serial`, `time`) are ignored, and `key`/`state` arrive in r8d/r9d behind a three-register hole -- one past the limit -- so kuna emits `void sub_6500(long a0)` with `int v7; // r8d` and `int v8; // r9d` read out of nowhere. The premise the rule rests on is a call site's, not a function's: at a call `checkInputTrialUse` scores a trial from the CALLER's data flow, where a live argument register is genuinely ambiguous and a long empty run really is evidence the recovery ran past the argument list. For the function's own inputs an active trial is a fact -- the body reads that caller-saved register before defining it, which has one explanation -- while an unused argument register is exactly what an ignored parameter looks like, and a callback whose signature is fixed by the API it is registered with ignores parameters as a matter of course. With the option ON an ACTIVE trial whose own ParamEntry is an exclusion (REGISTER) entry is never demoted by forceInactiveChain during the function's own input recovery, so the trials past the hole survive and the rule's existing tail loop promotes the filler trials buildTrialMap had already built: the witness recovers `void sub_6500(long a0,unsigned long a1,unsigned long a2,unsigned long a3,int a4,int a5)` -- the Wayland prototype, positions and all. Three clauses bound it, and the second was settled by measurement rather than argument. It is carried on the ParamActive that ActionInputPrototype builds and nothing sets it at a call site, so argument recovery at every call in every binary is untouched. Only REGISTER trials are protected -- a stack trial's fate is left exactly to upstream, because the evidence here is a register's: a caller-saved argument register read live-in can only carry what the caller placed there, while a positive-offset stack slot read live-in is much weaker, since a Win64 home slot used as scratch and an over-wide or aliased read look the same. A first design exempted any register GAP SLOT instead; it fixed the witness and left the datatest corpus byte-identical, and it also let one Win64 sub_140010a57 span its four-register hole into the stack resource and promote eleven scratch slots of the caller's argument area into a fifteen-parameter signature. Because trials sort into formal parameter order, protecting only register trials also keeps the tail loop's hole-filling inside the register file, which bounds the recovered list to the ABI -- six parameters on x86-64 SysV, four on Win64. And it never marks a trial active that was not already active, so a register the body does not read before writing is still not a parameter.", "use_when": "On by default. Keep it on when a signature is missing arguments the body then reads as undefined locals -- the tell is a declaration block of `int vN; // <argument register>` entries with no assignment anywhere, and a first statement that branches on one of them. The shape that needs it is a callback reached only through a function-pointer table (a Wayland listener, a qsort comparator, an ISR vector, a GTK signal handler), where the ABI signature is fixed by the API, the middle parameters are ignored, and no call site anywhere in the image can supply a prototype -- so the body is the only evidence there is. Inert unless the ignored run is more than two argument registers wide; a one- or two-register hole already recovered, filler parameters and all. It moves REGISTER parameters only: a stack argument sitting behind a register hole is still dropped, by design. Flip OFF to get upstream's veto back if a function gains trailing parameters it does not take -- the way that could happen is a body that reads an argument register on a path that is not really reachable, or a callee-saved register misclassified as caller-saved by the compiler spec -- or to bisect whether the own-input recovery, rather than call-site argument recovery, moved a signature.", "example": "option inputparamgap off", "source_decompiler": "kuna", "inspiration": "repipe:keyboard-callback-uses-undefined, 6989ca5da15272fa37a80c43 (x86-64 ELF lugosiii) sub_6500, the wl_keyboard_listener `key` callback; upstream ParamListStandard::forceInactiveChain (fspec.cc:1519) and ActionInputPrototype::apply (coreaction.cc:4941)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a function signature takes only the first argument while the body reads later argument registers as undefined locals", "int vN; // r8d and int vN; // r9d are declared and read with nothing ever assigning them", "a Wayland/qsort/GTK callback reached only through a function-pointer table renders with one parameter instead of its ABI signature", "the first condition of a handler tests a variable that has no definition", "arguments the disassembly shows arriving in R8D/R9D are missing from the recovered prototype"]},
  {"option": "stackarggap", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "repipe-sha-finalization-receives-two", "summary": "Let an argument REGISTER the caller never wrote end a CALL SITE's argument list when the next slot is on the stack, instead of being filled in on the way to a stack trial. `ParamListStandard::forceInactiveChain` (fspec.cc:1519) closes holes in a recovered argument list: it records the highest ACTIVE trial in a resource section and marks every inactive trial below it active, so the list it hands `buildInputFromTrials` is contiguous. That is right inside the register file -- a caller can pass an argument in RDX and leave RCX looking empty, and reading the hole as the end of the list is how a call loses arguments it plainly passes, which is `calleedeadarg`'s failure from the other side. It is wrong across the register/stack boundary, and not because the evidence is weak but because the ABI forbids the shape it reconstructs: every ParamListStandard model assigns slots in resource order and reaches the stack only once the register file in front of it is exhausted, so a Win64 call has a fifth argument at [RSP+0x20] only if it has a fourth in R9, and an x86-64 SysV call spills only past R9/XMM7. An argument-register slot with NO Varnode at all -- a filler trial buildTrialMap synthesized because nothing in the caller ever wrote that register -- is therefore not a hole in the middle of an argument list but the end of one, and whatever occupies the outgoing-argument area behind it is the frame's scratch. The witness is an OpenSSL call in a Windows PE: `EVP_DigestFinal_ex(EVP_MD_CTX *, unsigned char *, unsigned int *)` renders as `EVP_DigestFinal_ex(v3,v9,v7,a3,1)`, where the call site loads RCX/RDX/R8, never touches R9, and leaves a `1` in the Win64 fifth-argument slot from the frame's own bookkeeping. The stack slot scores active, the hole-filling loop promotes the unreferenced R9 trial to reach it, and `buildInputFromTrials` materializes the Varnode that trial never had by reading R9 at the call -- which resolves to the caller's untouched incoming R9, so the invented fourth argument is the reader's own parameter and looks load-bearing. Upstream already draws this conclusion one slot to the right: forceInactiveChain ends a call site's list at the first UNREFERENCED STACK trial (isUnref() && isRecoverSubcall() && isSpacebase() sets seenchain), on the reasoning that a call-site stack slot nothing wrote cannot be an argument. This option is that same sentence about the register slot in front of the stack. Four clauses bound it. It reads ParamActive::isRecoverSubcall, so the function's OWN input recovery -- where an untouched argument register is an ignored parameter rather than the end of a list, which is `inputparamgap`'s whole premise -- is untouched. It fires on UNREFERENCED trials only: an inactive register trial still has a Varnode, meaning the caller put something there and trial scoring merely could not prove it was for the callee, and that ambiguous case still fills, so a wrapper forwarding its own fourth parameter is unaffected. It fires only when the next non-eliminated trial in the section is a stack slot, so a hole in the middle of the register file with live registers behind it keeps upstream's fill. And it never marks a trial active, only stops one from being marked -- an argument list can lose an invented tail, never gain a member. A variadic call site reaches none of it, because `varargstackargs` has already split its stack tail into its own fillinMap section and the register prefix is never scored against it.", "use_when": "On by default (DIV-140). Keep it on when a call renders with more arguments than its callee takes and the extra ones are the CALLER's own untouched incoming parameter followed by a constant -- the canonical shape is a Win64 import called with five arguments where the disassembly loads RCX/RDX/R8, never writes R9, and stores something into [RSP+0x20] before the CALL. It is the standard rendering for a body-less IAT import (`kuna functions --json` reports it with size 0), because a callee with no body cannot answer for its own arity and the call site's leftover state is all the recovery has. Supplying the real prototype with `--assert prototype ...` fixes one callee at a time and this fixes the shape. Inert unless a call's argument-register file has a genuinely unwritten slot in front of a scored stack slot, so calls whose registers are all loaded are byte-identical. Flip OFF if a call that really does take a stack argument loses it -- the way that could happen is a callee whose fourth argument the caller passes in a register it never writes because the value is already there, which trial scoring sees as unreferenced.", "example": "option stackarggap off", "source_decompiler": "kuna", "inspiration": "repipe:sha-finalization-receives-two, 6869d705aadb6eeafb399027 (x86-64 Windows PE crackmes.exe) sub_14000b970, the EVP_DigestFinal_ex/EVP_DigestInit_ex call sites; upstream ParamListStandard::forceInactiveChain (fspec.cc:1519)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call to an imported API renders with more arguments than the API takes", "the extra arguments of a call are the enclosing function's own untouched parameter and a constant", "EVP_DigestFinal_ex or another three-argument import is called with five arguments", "a Win64 import call gains a fifth argument from a value stored to [RSP+0x20] that belongs to the frame", "declaring the callee's prototype removes the bogus arguments but every other call then gains some"]},
  {"option": "argclobber", "values": ["on", "off"], "default": "on", "destructive_as_default": true, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "typecampaign-phantom-argument", "summary": "Drop the TRAILING register argument of a call when nothing on the caller's side put a value in that register. Heritage::guardCalls (heritage.cc:1444) plants an INDIRECT *creation* for every killed-by-call register that is also a possible OUTPUT location of the callee's model -- rdx is one on x86-64, because a 16-byte value returns in rax:rdx -- and AncestorRealistic::enterNode (funcdata_varnode.cc:2043) answers pop_success for exactly that op, recording ParamTrial::setIndCreateFormed on the way. With onlyOpUse satisfied the trial is active and ParamListStandard::fillinMap marks it used, so a register the caller never wrote for this call becomes its last argument, and the local it reads is assigned nowhere: coreutils fmt -O2 renders `v12 = sub_3700(stdin,\"-\",v10);` with `unsigned long v9; // rdx` and `unsigned long v11; // rdx` never written, against a DWARF prototype of two parameters, while the other two sites of the same callee render one and two arguments. Upstream reads the same bit back on the RETURN side only -- ParamListStandardOut::fillinMap (fspec.cc:1721) scores a remainder-formed or indirect-creation-formed trial no-use when its ParamEntry is not first in its class -- and the input list has no such test, so the evidence that disqualifies a return value is ignored for an argument (stock Ghidra emits the same three-argument call and names the phantom extraout_RDX). This option applies that sentence to the input list, bounded by eight clauses. The trial must be a REGISTER trial carrying isIndCreateFormed, which is the upstream return-side test applied to the input list. The clobber must reach the call DIRECTLY: the trial's defining op is the indirect creation, or a MULTIEQUAL one of whose immediate inputs is one -- tar's str_days(pc,buffer,n) is called with n live from a dominating block and a clobber merging in behind a second phi, and that argument is real. Every one of those creations must be a creation of THE ARGUMENT REGISTER ITSELF: the first return register of every ABI is also killed-by-call and a possible output location, so a preceding call's result is an indirect creation too until the output seam resolves it, and a value the caller MOVED into an argument register is a value the caller is passing -- u-boot -O2 sub_60827fa4 carries ofnode_read_u32_default(dev->node,\"bus-width\",1) from r0 into r3 through a pair of predicated movs and prints it with %u. EVERY OTHER INPUT OF THAT JOIN must be a division by-product: the value an idiv leaves in the remainder register while the caller uses only the quotient, reached through a SUBPIECE of the low half, a zero or sign extension or a constant mask and nothing else. One clobber among a phi's inputs says nothing about the other paths, so each has to be positively classified as a value nobody placed either, and upstream scores a remainder-formed piece exactly like an indirect-creation-formed one on the return side. Without this clause a caller that forwards its own second parameter on the non-clobber path (`mov %r12,%rdx`) loses both the argument and the parameter, and `void caller(long,unsigned long)` renders as `void caller(long)`. The CALLEE's own RECOVERED PROTOTYPE must exist and must have no parameter overlapping those bytes. This is the clause the rule rests on and the reason it can be a default: nothing on the caller's side can say whether the callee wanted the register, and the parameter list kuna gets from decompiling that function can. protoorder states it for every callee it decompiles before the caller, and states nothing for a callee inside a recursive component, one with no recovered body such as a PLT import, one that recovered no parameters at all, and one whose prototype is already declared -- so \"nothing stated\" and \"cannot tell\" are the same answer and both decline. The recovered list must also ACCOUNT for every argument the drop leaves behind, storage for storage: a recovery short of what the call passes is not a statement that the tail is unwanted but a statement that the recovery did not reach the tail, and a forwarding thunk (mov (%rdi),%rax; jmp *%rax) is exactly that shape -- it recovers one parameter while the function it tail calls consumes three. protoorder does NOT decline a variadic callee: its under-recovery and register-file guards belong to the lock mode, the declared-... decline cannot fire on a stripped image, and a stripped SysV variadic is STATED (long vlog(int,long,...) built with gcc -O2 states three parameters). What declines the drop there is the recovered list itself, because a register-save prologue reads every argument register the convention has and the list therefore carries the one the drop would take. A callee that was never decompiled first states nothing, which makes the rule inert on a single-function kuna decompile, a narrowed decompile-all, a --jobs N run and under --option protoorder off. The callee's BODY is read twice more, and both are required. A bounded entry walk that positively sees those bytes READ before they are written declines the drop (the proves_input half of the per-image entry-liveness summary calleedeadarg builds), and it is what answers for a callee kuna never recovered as variadic -- u-boot's printf is called from 1,924 sites and its va_list prologue speaks for all of them. And the callee must not be able to FORWARD the register to a read no recovery saw. The entry walk ends every path at the callee's first call, so what the register meets past one is asked of that call's TARGET: a declared, non-variadic prototype answers it, and so does the target's own body with the same question put to it recursively; everywhere else -- an indirect call or tail call, a CALLOTHER, an indexed register-file access, an undecodable instruction, a recursive component, an import whose PLT stub jumps through its GOT slot -- the register has to be written already. Naming a target is not accounting for it. That second reading is what a prototype cannot give: `long fwd(struct box *o,long a,long b) { if (!a) return 0; return o->fn(o,a,b); }` compiles under gcc -O2 to `test %rsi,%rsi; je; jmp *(%rdi)`, and `long wrap(void *o,long a,long b) { if (!a) return 0; return ext3(o,a,b); }` with ext3 an unsignatured import has no pointer at all; both recover exactly the TWO parameters a drop would leave behind and forward the third to a function no recovery read. Neither reading can admit a drop, only refuse one. The trial must be TRAILING, so the list keeps its positional shape and no other argument moves. At least one argument must remain, since an empty list is calleearityfwd's failure shape. And no already-final call to the same callee entry in this function may have passed an argument in that storage -- the subtractive twin of calleearity's witness rule, which is also what orders this pass against the additive arity family: it runs immediately after calleearity in buildInputFromTrials, and a slot the sibling rule promoted outranks the clobber evidence. The drop is markNoUse, which calleearitylive/fwd/body all read as definitely-not-used, so none of them puts the argument back. Dropping the argument also NARROWS THE PRECEDING CALL'S RETURN VALUE where that call was the register's only writer: the `SUB168(v,8)` that extracted the rdx half of a 16-byte return loses its only reader and goes with the argument. No drop in the 770-binary corpus does that under this rule -- the one that did, bash expand_prompt's `xrealloc`, is declined now that the import its callee reaches has to answer for rdx -- but docs/features/argclobber/ce-forward-thunk-2param.c shows it happening the moment the rule is wrong, and it is the same mechanism that would delete a real struct half at a callee whose own prototype is under-recovered.", "use_when": "On by default -- but only where a callee was decompiled BEFORE its caller, because the evidence the drop needs is the callee's own recovered prototype and `protoorder` is what parks it. A serial whole-binary `kuna decompile-all` is that surface. A single-function `kuna decompile` (it forks one `decomp_dbg` per function), a run narrowed by `--addr` or `--functions`, `--jobs N`, `decompile-project`, `decompile-graph` and `--option protoorder off` all state nothing, and there the option is inert: a serial run and a `--jobs` run of the same binary can therefore differ by exactly these dropped arguments. The tell it fixes is a call that renders with one more argument than the callee takes, where that last argument is a local nothing ever assigns -- a `// rdx` (or other caller-saved register) declaration with no write anywhere in the function, read only at that one call, with sibling calls to the same callee passing fewer. Measured over the 770 stripped ELFs of decbench `full_run_address_2026-09-11` (O0, O2 and O2-noinline; no run exited non-zero), 19 functions change, one per binary, and each loses exactly one trailing argument at exactly one call site. Every one lands on the callee's TRUE arity, checked against the unstripped twin: `__addvsi3(int,int)` and `__mulvsi3(int,int)` from libgcc at 15 of them, `fmt(FILE *, char const *)` at the two coreutils `fmt` mains, `ext2fs_dblist_sort2(dblist, sortfunc)` at `e2fsck_pass2`, and `efi_create_handle(efi_handle_t *)` at u-boot. The two `fmt` mains also GAIN the second argument at their under-recovered sibling site, so all three calls render `sub_3700(stdin,...)` with two. Twenty register-storage locals disappear and none appears; 34 declarations go net. Hunks outside the argument itself: the dead remainder that fed it and its copies, declarations reordering once one is gone, one folded single-use divisor, and a `while` re-rendered as a `for` (the 11 O0 openssh `permute_args`, whose dead loop-tail copy went with the argument). Over the 444-slice campaign corpus (10,748 scored functions) `type_match` does not move at all: 1,147 perfect in both arms, aggregate identical, 0 improved and 0 worse, and the decompiled-variable count decbench scores is the same for every one of the 10,748 -- the metric cannot see this change, because register locals are never exported and a call's argument is not a variable of the caller. Flip to `off` if a call LOSES an argument the callee really consumes. The one shape that can still do that is a callee whose own parameter list kuna UNDER-recovers by exactly one slot, at exactly the register the clobber wrote, with every other argument still accounted for; the eight programs in `docs/features/argclobber/` are the classes this rule declines, and the forwarding ones among them are declined for different reasons -- `ce-forward-thunk.s` recovers one parameter where two arguments would remain, so the prototype accounting refuses it, while `ce-forward-thunk-2param.c` (through a pointer), `ce-import-forward.c` (through an unsignatured import, no pointer anywhere) and `ce-forward-thunk-2frame.c` (an ordinary direct call in front of the first) each recover exactly the two that would remain and are refused only because `rdx` is unwritten where the callee forwards it and the target cannot answer for it. Note that `protoorder` does NOT decline a variadic callee -- its variadic guards live in the `lock` mode and the declared-`...` decline cannot fire on a stripped image -- so a stripped SysV variadic is stated; what declines the drop there is that its register-save prologue reads every argument register, leaving the recovered list carrying the one the drop would take.", "example": "option argclobber on", "source_decompiler": "ghidra", "inspiration": "coreutils fmt -O2 (decbench full_run_address_2026-09-11) main 0x26a0 -> fmt 0x3700 at 0x2ac1; upstream ParamListStandardOut::fillinMap (fspec.cc:1721) and AncestorRealistic::enterNode (funcdata_varnode.cc:2043)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call passes one more argument than the callee's own recovered prototype takes", "the last argument of a call is a local that is never assigned anywhere in the function", "a declaration carries a // rdx storage comment and no write", "two calls to the same callee in one function render with different arities and the longer one reads an undefined value", "Ghidra names the same value extraout_RDX and kuna renders it as an ordinary local"]},
  {"option": "protoorder", "values": ["off", "types", "lock"], "default": "types", "destructive_as_default": false, "phase": "P4", "subphase": "prototype-source", "strength": "HARD", "rewind": "P4", "issue": "typecampaign-callee-first-prototypes", "summary": "Decompile a whole binary callees-first and state each function's RECOVERED parameter types where its callers read declared ones from, so what one function learned about its own parameters reaches every call to it. `TypeOpCall::getInputLocal` (typeop.cc:689) already types a call argument from the callee's parameter -- but only from a DECLARED prototype, and the only writers of that slot are facts from outside the decompile: a libc/`libctypes` table entry, a DWARF or demangled signature, `--assert prototype`, `parse line extern`. Nothing states what a RECOVERY found, so an internal callee tells its callers nothing and each caller types the call from its own local evidence alone. coreutils fmt -O2 shows the cost: `sub_3700` recovers as `unsigned long sub_3700(FILE *a0, unsigned long a1)` and its caller still renders `sub_3700(stdin,v7)` with `char *v7` typed from nothing. With the option on, `kuna decompile-all` orders the run by the call graph `kuna xrefs` already publishes (`kuna_analysis::listing::xrefs`, the same edges `--reachable-from` walks), decompiles each callee before its callers over Tarjan's SCCs of those edges, and records the parameter types the callee recovered against the storage it recovered them in. At each later call site, argument `i` takes the callee's recovered type for parameter `i` when the two agree about the storage -- position and storage both, so the two recoveries are talking about the same argument. Results are buffered and emitted in address order, so only the decompile ORDER changes and the output stays byte-stable. `types` (the default) states ONLY parameter types: nothing is locked, so `ActionFuncLink` runs the caller's own argument recovery at every call exactly as it does with the option off and no call gains or loses an argument. The recovered type arrives as one more vote in `Varnode::getLocalType`'s `type_order` fold; it is never the type a cast is measured against, so where it loses the argument renders as it does off, and it is refused outright where the caller holds evidence the fold cannot weigh: the argument is the address of a frame object, the value (its COPY/CAST/MULTIEQUAL/INDIRECT family) is declared, global, a frame address, frame memory, an element of a stack struct, array or indexed frame region, or loaded through itself (`p = *p`), the vote is deeper than the inferred pointer cap, a pointer vote lands on a constant inside a function's code (a Thumb address would render as `&sub_8130[1]`, a subscript of a function), another call reads or writes the same value as a different kind (pointer, integer, float), float-ness disagrees with the value or its register, or what the caller loads and stores through a pointer disagrees with a float, composite or non-character primitive pointee (an access or address outside the object, an access across or inside its members, a step of other than one element, integer bits moved through a float member, or a constant stored wider than a non-character primitive pointee, which `SplitDatatype` would print as one store per element; a caller with too many derived addresses to check is refused as well). The callee's recovered return type is not stated: a call's result has no competing evidence, so a wrong one spreads into the caller. `lock` instead parks the recovered prototype in the symbol table, where `ActionDefaultParams` reads a declared one from, which does decide the call's arity: the parked list is a FLOOR rather than a ceiling (the first slot past the recovered parameters starts a variable tail, so `func_link_input` keeps active-input recovery on and a caller that passes more still recovers the rest), and four further rules read the callee's own machine code through the entry-liveness walk `calleedeadarg` owns -- a trailing parameter the body never reads is trimmed rather than stated, a callee that provably READS the register its next argument would arrive in has under-recovered and is declined, a recovered list that saturates the convention's argument registers is declined (the shape a variadic register-save prologue leaves: exactly six parameters on SysV, exactly four on AAPCS), and a list the model would not place in the storage it was recovered in is declined outright. Both modes decline when the function's decompile errored, when it says nothing, when model selection did not settle, when a parameter is a hidden return pointer, when the function is in a call-graph cycle of more than one member (`callees first` has no meaning inside a cycle), and -- always -- when a prototype is already DECLARED for that entry, so an operator's `--assert prototype`, a libc signature, DWARF or a demangled name outranks anything recovery found. The statement is a caller-side fact only: the locked-input branch of `ActionPrototypeTypes` is stubbed, and the callee has already been decompiled when its types are stated.", "use_when": "On by default on a whole-binary `kuna decompile-all`, and a whole-binary surface only -- `kuna decompile` forks one `decomp_dbg` per function, so it cannot see what another function's decompile stated, and the two surfaces may therefore disagree about a call's argument types (angr has the same property). A run narrowed by `--functions`, `--addr` or a triage filter, selecting one function, or on a `--raw-image` (which has no call graph), decompiles in address order and builds no call graph unless the option is named, which orders the selection and notes on stderr that a callee outside it states nothing. `decompile-project` and `decompile-graph` have their own schedules and are not callee-first either; both warn on stderr when the option is asked for explicitly. Leave it on when a type the caller clearly knows -- a `FILE *` from `fopen`, a `char *` from `getenv` -- stops at a call and the callee's parameter stays `long`, or when the same internal callee's parameters are typed at one site and not at another. `types` never moves an arity: no call gains or loses an argument, no call spec is locked, and a vote is refused where the caller's own evidence contradicts it. It does change spellings, so read a changed function rather than assume it is cosmetic: a vote that wins adds a cast at the argument it types (`caller((unsigned char *)0x402000,3)`), a character pointee guessed for a pointer can split one wide constant store through it into character stores of the same bytes (`*(unsigned long *)(a0 + 0xa0) = 0` becomes eight `'\\0'` stores -- the same memory contents, a different access width, which matters on memory-mapped I/O; text prints as `builtin_strncpy`, and a pointee of any other primitive type narrower than a constant stored through the pointer is refused, a buffer filled a word at a time included) or widen a narrow load into a truncation of the wider element (`*(short *)(a0 + 0xc)` becomes `(short)a0[3]` -- the same value, read at a different width), and an unsigned vote can make a caller's parameter unsigned, with casts keeping its signed uses (`(int)a0 >> 2`). A float vote is stricter than the others: it is refused unless every other use of the value is a float or a copy of its bits, so a word the caller also adds, compares, truncates, takes bytes out of or stores, or returns or passes on outside a float register, stays an integer -- where a float travels in a general register (MIPS o32, ARM softfp) a float vote there printed `(short)((unsigned int)v1 >> 0x10)` and `a2[1] = (int)v1`, value conversions of the bits. A pointer vote is held to the same standard one level down: where the caller's own loads and stores through the pointer disagree with the pointee -- an access or address outside it, an access across or inside its members, a step of other than one element, or integer bits moved through a float member -- the vote is refused, so `*a0 = (double)(a1 + 1)` never prints for a union a callee sums as doubles. Measured over 60 stripped binaries (32,406 functions: x86-64 userland, ARM Cortex-M firmware and two PE files) against main with structsynth on: the same call arguments in both arms (every one of more than 258,000), zero functions gaining a parameter of their own, no stack object split, no stack local read without a write, and no new array subscript of a structure or piece assignment; over the 444-slice campaign corpus more than 110 functions reach a perfect type_match and none leave it. Flip to `off` if a caller's parameter or local takes a type from a callee whose own recovery is wrong about it and nothing at the call site contradicts it -- the vote only loses to contrary evidence, not to a better guess. `lock` is the opt-in arity claim: it also states how many parameters the callee has, which collapses a caller that over-recovered (`ext2fs_mmp_start` renders 3 arguments and takes 1) and gains 818 call arguments at 418 sites over the x86-64 half of a 22-binary corpus -- but it FABRICATES parameters where the callee's own recovery over-counted, 155 of 7,553 x86-64 functions and 630 of 6,589 ARM Cortex-M ones gaining an argument row nothing sets, which `type_match` cannot see at all because a decompiled variable with no ground-truth counterpart is never a false positive. Use `lock` on a program where you have checked that, not as a default, and prefer `argclobber` where the complaint is a trailing register argument a previous call left behind -- `argclobber` declines a locked-and-variadic call spec, so `lock` disables it at every stated site, while `types` composes with it. Mutually exclusive with `--jobs N` when asked for explicitly (each worker process loads the binary into its own `Architecture`, so a stated type cannot cross a chunk boundary), exactly like `--assert`; a `--jobs` run under the default states nothing and says so on stderr, and `--option protoorder off` makes it byte-identical to the serial run. Set `KUNA_PROTOORDER_TRACE=1` to have each statement and each decline printed with its reason on stderr.", "example": "option protoorder types", "source_decompiler": "angr", "inspiration": "angr's callee-first pipeline: `Clinic` propagates a recovered `SimTypeFunction` back onto the callee's `Function.prototype` and later callers read it (`analyses/decompiler/clinic.py`), which is why angr commits 43.4% of its arguments to a pointer type on coreutils fmt where kuna commits 30.6%. Ghidra reaches the same place with its decompiler-parameter-ID analyzer, which commits recovered prototypes to the program database before the functions that call them are decompiled.", "change_kind": "analysis-enablement", "tier": "transform", "symptoms": ["a FILE * or char * the caller holds becomes long at the callee's parameter", "an argument is typed at one call site of a callee and not at another", "decompiling one function alone gives it a prototype the whole-binary output does not use at its call sites", "the same sub_ address is called with one argument at one site and three at another in the same function"]},
  {"option": "calleedeadarg", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "repipe-void-callee-spurious-arg", "summary": "Let a bounded decode of the CALLEE's own body veto a register argument the callee provably never reads. ActionActiveParam recovers an unlocked callee's argument list from the caller's data flow alone (FuncCallSpecs::checkInputTrialUse, fspec.cc:5592): a trial is active when the ABI argument register holds a value the caller wrote and does not otherwise use, and AncestorRealistic answers pop_solid for a Varnode defined by a CALL -- so the return value of the preceding call becomes the next call's argument. The result contradicts kuna's own output inside one file: `int _secret_function(void);` in the header and `_secret_function(v3);` in the body, which does not recompile and leaves an agent unable to tell whether the callee consumes the value. The evidence the caller does not have is in the callee: this pass decodes the callee from its entry and asks, per register range, whether ANY path reads those bytes before writing them. Each path carries the register bytes already written on it; a RETURN ends a path with nothing to say; a nested call, an unresolved BRANCHIND, a LOAD/STORE naming the register space, or an undecodable instruction ends it at a CUT past which only already-written bytes stay provable (which is why a body that overwrites x0 and THEN calls printf still proves x0 dead, while a body whose first act is a call proves nothing); and an instruction with internal p-code branching is scored against the set it was entered with and credits none of its writes. A register with no read-before-write on any path and fully written before every cut is dead on entry, cannot be carrying an argument, and its trial is scored no-use like any other definitely-unused trial. The claim is one-sided -- the absence of a counter-example on a walk that covered every path -- so the instruction budget, a runaway written set and any register space kuna cannot name all decline. Only the `register` space is answered; a ram-space (global) trial would need the walk to model memory.", "use_when": "On by default. Keep it on when a call renders with an argument its own recovered declaration does not take -- `int f(void);` declared and `f(v3)` called -- which is the standard shape when the preceding call's return value is still sitting in the first argument register at an unprototyped local callee (`v3 = scanf(...); ... f(v3);`). It is strictly subtractive and only ever fires against a decoded callee body, so it cannot invent an argument; `calleearity`/`calleearityfwd` are the additive direction and are unaffected. Flip OFF if a call LOSES an argument the callee really consumes -- the way that could happen is a callee decoded in the wrong instruction mode (an ARM/Thumb boundary), where the bytes read are not the instructions that run -- or to bisect whether this pass, rather than trial scoring, moved an argument list.", "example": "option calleedeadarg off", "source_decompiler": "kuna", "inspiration": "repipe:void-callee-spurious-arg, 68149b8a8f555589f353117c (Mach-O arm64 practice_2_rengr) _main 0x100003e70 -> _secret_function 0x100003d78; upstream FuncCallSpecs::checkInputTrialUse (fspec.cc:5592) and AncestorRealistic::enterNode (funcdata_varnode.cc:2043)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call passes an argument to a callee the same output declares as taking void", "_secret_function(v3) called where int _secret_function(void) is declared", "the decompiled project does not recompile because a call and its prototype disagree on arity", "the return value of the previous call reappears as the next call's argument", "a scanf/getchar result is passed to an unrelated no-argument function"]},
  {"option": "calleepreserves", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "call-guard", "strength": "HARD", "rewind": "P3", "issue": "repipe-get-pc-helper-loses", "summary": "Let a bounded decode of the CALLEE's own body narrow the killed-register set at a call, so a value the caller computed survives a helper that provably does not touch it. Heritage::guardCalls (heritage.cc:1444) asks each call's prototype what it does to every heritaged range, and a cspec's <killedbycall> block answers for the CONVENTION -- x86gcc.cspec lists EAX/ECX/EDX because cdecl allows a callee to clobber them. At an i386 PIE's get-PC thunk (`mov ebx,[esp]; ret`, two bytes of body that gcc emits once per GOT-base register) that answer is false: the effect comes back killedbycall, an INDIRECT creation is planted at the call, and every read of the register after it reads a local with no definition anywhere in the function -- `sub_8049f55(); return __lxstat(3,path,v1);` with `unsigned int v1; // edx` never assigned, while the argument loaded from the same frame AFTER the call is recovered normally. This pass consults the callee-body write probe kuna already takes for the call-output seam (kuna_rustabi::probe_callee_return_writes): the walk follows fall-through and resolved machine branch targets from the callee's entry, ends a path at a RETURN, and declares itself incomplete -- proving nothing -- at a nested CALL/CALLIND/CALLOTHER, an unresolved BRANCHIND, an undecodable instruction, or its instruction budget. When the summary is complete and proves the callee never writes the range, killedbycall is downgraded to unaffected for that one call and no INDIRECT is planted. The claim is one-sided and the limits are what keep it sound: only a processor-space (register) range is ever answered, because a callee's memory writes are STOREs through a runtime address the walk cannot follow; only killedbycall is downgraded, never promoted; a proto carrying its own effect override is left alone; a PLT stub is a BRANCHIND and so is never narrowed, which keeps every library call on the ABI's answer; and recursion is a CALL and lands in the same place. The positive-evidence gate asks the callee to have written a register the convention promises is PRESERVED, which a helper that clobbers only what it is allowed to never does, so the call's RETURN storage is calleeretpreserves's question on its own evidence.", "use_when": "On by default. Keep it on when a value that provably crosses a call in the disassembly renders as an undefined local -- the standard shape is an i386 PIE where an argument is loaded into EDX/ECX before a call to a two-byte get-PC thunk and reappears as `v1` with no assignment, while a second argument loaded after the same call is recovered. Same shape on any hand-written or compiler-emitted helper that preserves more registers than its ABI promises. It only ever fires against a fully decoded, call-free callee body and only ever KEEPS a value, so it cannot invent one. Flip OFF to get the convention-only answer back: the way this could be wrong is a callee decoded in the wrong instruction mode (an ARM/Thumb boundary), where the bytes read are not the instructions that run, or to bisect whether this pass rather than trial scoring changed a call's surroundings.", "example": "option calleepreserves off", "source_decompiler": "kuna", "inspiration": "repipe:get-pc-helper-loses, 5ab77f5833c5d40ad448c399 (i386 PIE collide) sub_8049f20 0x8049f20 -> get-PC thunk 0x8049f55; upstream Heritage::guardCalls (heritage.cc:1444) and FuncProto::hasEffect (fspec.cc:4239)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call argument is an undefined local the function never assigns", "a value loaded before a call is lost while one loaded after the same call is recovered", "a get-PC thunk or PIE GOT-base helper eats a live register", "an argument register that provably crosses a helper renders as `unsigned int v1; // edx` with no definition", "the recovered return of a call is sourced from a register the callee never writes"]},
  {"option": "calleeretpreserves", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "call-guard", "strength": "HARD", "rewind": "P3", "issue": "repipe-main-returns-invented-cookie", "summary": "Let the CALLEE's decoded body answer for the call's RETURN register, so a value the caller placed there before the call is not replaced by an invented result. x86-64-win.cspec lists RAX in <killedbycall> for the default __fastcall, so Heritage::guardCalls (heritage.cc:1444) plants an INDIRECT creation of RAX at every call and registers a return trial for it. At an MSVC /GS epilogue that answer is false and the compiler says so by setting EAX BEFORE the call: `main` ends `xor eax,eax; mov rcx,[rbp+0x1a0]; xor rcx,rsp; call __security_check_cookie; ret`, and the checker's returning path is `cmp rcx,[cookie]; rol rcx,0x10; test cx,0xffff; ret`, which never touches RAX. kuna emitted `return sub_140007d90(v12);` -- a return value the machine never computes -- and declaring the checker void only traded it for an uninitialised local. calleepreserves is the pass that narrows killedbycall from the callee's own body and it declines here for its own reason: its positive-evidence gate asks the callee to have written a register the convention promises is PRESERVED, the signature of a get-PC thunk, and this helper clobbers only what it is already allowed to. For the RETURN storage the evidence is sharper: a callee that returns a value in RAX must WRITE RAX, so a complete decode that never records a write to the return storage proves the call has no return value at all. The effect is then downgraded to unaffected, no INDIRECT creation is planted, no output trial is registered, and the caller's own definition flows across the call (`sub_140007d90(...); return 0;`). Three gates keep the claim one-sided. The range must characterize as the call's OUTPUT (FuncProto::characterizeAsOutput), so every other killedbycall range stays exactly where calleepreserves left it. The body must write NO part of the return storage: a callee that writes RAX and leaves RDX alone is a scalar-returning function whose second return register is merely dead, and the convention keeps its answer for the whole call. And the body must be a body -- more than one decoded instruction, and a write to a register the convention itself names (an argument register, or one its <unaffected>/<killedbycall> lists mention) -- because `ret` and `endbr64; ret` are what a stub, a placeholder and an entry decoded at the wrong address all decode to, and reading one as a promise about RAX deletes the call results the rest of a function is built on. The same change teaches the callee-body write probe that a Windows `int 0x29` ends the path, under fastfailnoreturn's own gate: SLEIGH lifts INT imm8 to `intloc = swi(imm8); call [intloc]` (ia.sinc:3671), and without that the cookie checker's __fastfail failure path makes the walk incomplete, which proves nothing. The declared-void floating-return extension adds a second, narrower caller-side proof for MSVC `/GS` checks whose failure tail contains an ordinary nested call and therefore remains incomplete: only a locked `void` declaration may fall back to the prototype model's ABI output list, never arbitrary killed scratch storage or a locked non-void output. P7's cycle-aware, 256-link cookie walk collects every exact direct call whose argument is `(cookie ^ SP) ^ SP`, marks the full set before one restart, and leaves every check visible unless `msvcstackguard` independently owns deletion. Incompleteness alone does not veto this exact arm, but recovered facts do: a known write to ANY ABI return storage and every explicit effect override retain the convention's clobber. The probe retains processor writes and STORE spaces recovered before an unresolved edge even when it marks the walk incomplete. A STORE into a processor space used by an ABI output is a possible write to every return location in that space and vetoes the exact arm even when no direct range was recovered; only absence-based proof requires completeness. Eligibility is the exact logical output slice: if Win64's 8-byte XMM0_Qa entry is contained by a 16-byte XMM0 heritage range, P3 preserves that slice, creates killed INDIRECT flanks, and PIECEs the range back together instead of downgrading the upper scratch lane to unaffected.", "use_when": "On by default (DIV-133). Keep it on when a function returns the result of a call the disassembly plainly makes for its side effect -- an MSVC /GS `main` ending `return __security_check_cookie(v12);` where the machine set EAX to zero before the call, or a value assigned from a helper that never writes RAX (`secret = wipe_random(...) & 0xffffff | 0x1000000;`). Declaring the callee void does not fix either shape; it turns the invented call result into an invented local. The generic body arm only ever fires against a fully decoded body that writes no part of the return storage, and it only ever KEEPS a value the caller computed, so it cannot invent one. Keep it on as well when an MSVC score/result is copied from XMM6 into XMM0 before a declared-void cookie check and then disappears: the exact `/GS` arm may tolerate an incomplete nested-call failure tail, but it is limited to ABI return storage and still yields to every recovered ABI-return write, every STORE into an ABI output processor space, and every explicit effect override. All eligible exact sites are marked before one restart, so duplicated epilogues do not exhaust the reflow cap. Flip OFF to get the convention-only answer back: the way either arm could be wrong is a callee decoded in the wrong instruction mode (an ARM/Thumb boundary), where the bytes read are not the instructions that run, or to bisect whether this pass rather than trial scoring changed a call's surroundings. A 16-byte XMM0 definition crossing such a call is handled slice-wise: the low ABI double may survive while a later read of XMM0_Qb remains an undefined killed value.", "example": "option calleeretpreserves off", "source_decompiler": "kuna", "inspiration": "repipe:main-returns-invented-cookie, 69b7492049fa49a2a260218f (MSVC /GS x86-64 PE vm.exe) sub_140005b30 0x140005b30 -> __security_check_cookie 0x140007d90; repipe:declaring-double-score-return, 69a54bd70f5b9757a6a5f72f (PolyMLP.exe SHA-256 01652e8b03e2bec127f5726320db4c9925d495d75583a8bb4534ac640f6ec230) score 0x140003150 -> log 0x14000904f / cookie 0x140008140; upstream Heritage::guardCalls (heritage.cc:1444) and FuncProto::characterizeAsOutput (fspec.cc:5892)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a function returns the result of a call the disassembly makes for its side effect", "main ends `return __security_check_cookie(...)` on an MSVC /GS binary", "a constant the caller moves into EAX just before a call never reaches the RET", "a void helper's call result is assigned to a variable", "declaring the callee void trades the invented call result for an uninitialised local", "a value read after a call the callee provably never writes is an undefined local", "a double accumulator copied from XMM6 to XMM0 disappears across a declared-void MSVC cookie check", "a declared-double helper result becomes a discarded statement after a later cookie call"]},
  {"option": "calleescratchbody", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "call-guard", "strength": "HARD", "rewind": "P3", "issue": "repipe-rax-preserving-stack-probe", "summary": "Accept a decoded callee that clobbers only SCRATCH registers as calleepreserves evidence, so a value the caller sets before a well-behaved helper survives the call. Every MSVC frame larger than a page opens `mov eax,<size>; call __chkstk; sub rsp,rax`, and the out-of-line probe reads RAX and never writes it -- `sub rsp,0x10; mov [rsp],r10; mov [rsp+8],r11; xor r11,r11; lea r10,[rsp+0x18]; sub r10,rax; ...; mov r10,[rsp]; mov r11,[rsp+8]; add rsp,0x10; ret`. kuna lost the constant anyway: the body came out `sub_1408d0000(); v1 = -v2;` with `int8 v2; // rax` never assigned, and every local in the frame was then indexed off that undefined value as `*(unsigned long long *)&(&Stack000000000005dfd0)[v1] = v7;` -- one INDIRECT creation destroying the whole frame rather than one expression. calleepreserves is the pass that narrows a cspec killedbycall set from the callee's own body and its evidence was already sufficient here: the callee-body write probe walks all nineteen instructions, completes, and proves RAX untouched. What declined was its POSITIVE-evidence gate, which asks the callee to have written a register the convention promises is preserved -- the signature of the i386 get-PC thunk the pass was written for, whose `mov ebx,[esp]` writes an <unaffected> register. __chkstk writes RSP, R10 and R11, and x86-64-win.cspec names none of them in either <unaffected> or <killedbycall>: they are scratch, and clobbering scratch is not a departure from the convention, so the gate never fires on a well-behaved helper. This option adds a second, independent way for a summary to count as a body, and requires both marks. The body must have written MEMORY -- a register-preserving helper's own save slot is why it can use a register at all, and `ret`, `endbr64; ret`, a placeholder and an entry decoded at the wrong address store nothing. And it must have written a register other than the stack pointer, which every RET writes. Nothing else about calleepreserves moves: the summary must still be complete, still prove the queried range untouched, the range must still be a register, a proto carrying its own effect override is still left alone, only killedbycall is downgraded and never promoted, so the pass can still only ever KEEP a value the machine keeps.", "use_when": "On by default (DIV-149). Keep it on when a constant or pointer the disassembly plainly carries across a helper call renders as an undefined local -- the standard shape is a Windows x86-64 binary whose large-frame prologue calls __chkstk, where `sub rsp,rax` becomes `v1 = -v2` off an unassigned `// rax` and every frame slot is then indexed by it. Naming the helper __chkstk does not help and neither does declaring it `void`: the killed set, not the prototype, is what loses the value. Flip OFF to require a convention-PRESERVED write again, which is the shipped calleepreserves gate: the way this could be wrong is a callee decoded in the wrong instruction mode (an ARM/Thumb boundary), where the bytes read are not the instructions that run, or to bisect whether this clause rather than trial scoring changed a call's surroundings.", "example": "option calleescratchbody off", "source_decompiler": "kuna", "inspiration": "repipe:rax-preserving-stack-probe, 69d7f15a8afd9d6c48b48871 (x86-64 PE crackme_shroud.exe) sub_140001000 0x140001000 -> __chkstk 0x1408d0000; upstream Heritage::guardCalls (heritage.cc:1444) and FuncProto::hasEffect (fspec.cc:4239)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a large-frame MSVC prologue loses its stack allocation and indexes every local off an undefined local", "`sub rsp,rax` renders as `v1 = -v2` with `// rax` never assigned", "frame slots render as `&(&Stack000000000005dfd0)[v1]` instead of fixed offsets", "a constant the caller loads before a __chkstk / stack-probe call disappears", "naming a helper __chkstk or declaring it void does not recover the value it preserves", "calleepreserves declines a helper that clobbers only volatile registers"]},
  {"option": "indirectanchor", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "call-guard", "strength": "HARD", "rewind": "P3", "issue": "repipe-bcryptgetproperty-status-tested-through", "summary": "Anchor an op inserted after a call's guard INDIRECT to the CALL that INDIRECT speaks for, as upstream opInsertAfter does, so the guard run stays unbroken and the call keeps its return value. Heritage::guardCalls plants one INDIRECT per killed range immediately before a CALL, and the return-value trial's Varnode is the output of the INDIRECT CREATION among them; FuncCallSpecs::collectOutputTrialVarnodes (fspec.cc:5543) finds it by walking backwards from the CALL and stopping at the first op that is not a CPUI_INDIRECT, which is exact only while that run is unbroken. Upstream keeps it unbroken from both sides: opInsertBefore skips back over the run, and opInsertAfter (funcdata_op.cc:373) REDIRECTS -- asked to insert after an INDIRECT it decodes the iop annotation in the marker's second input and inserts after the CALL (or STORE) the INDIRECT speaks for. kuna carried that redirect as a STUB(W3-varnode) because the iop decode belonged to a later wave, so an op inserted after a guard INDIRECT landed BETWEEN the guards and their CALL. At `call qword ptr [0x1400282b8]` (BCryptGetProperty) in a Windows PE, heritage refines the 8-byte frame slot at -0x2d0 into two 4-byte halves and defines the upper half with a SUBPIECE inserted after that slot's own guard INDIRECT; the run became INDIRECT-creation of EAX, sixteen global INDIRECTs, one stack INDIRECT, the SUBPIECE, then seventy-nine more INDIRECTs, and the backward walk stopped at the SUBPIECE eighteen ops short of the EAX creation. No trial Varnode was found, checkOutputTrialUse marked every trial no-use, the CALL got no output, and the INDIRECT creation was left standing -- so the emitted C read `if ((v16 < 0) || ...)` off a local it never assigns, while every other bcrypt import in the same function recovered its result normally. Completing the redirect restores the invariant the rest of the call machinery is written against; a prev that is not an INDIRECT marker, an INDIRECT whose second input is not an iop annotation, and an iop that decodes to a dead op are all left exactly where they were going.", "use_when": "On by default. Keep it on when a call is emitted for its side effect and the very next statement branches on a local nothing assigns -- the standard shape is a Win32 API called through its IAT slot whose NTSTATUS is consumed by `test eax,eax; js`, where sibling calls to the same DLL in the same function DO recover their result. Declaring a prototype for the callee is the manual workaround and is what proves the diagnosis. Flip OFF to get the pre-port placement back when bisecting whether op placement rather than trial scoring moved a call's surroundings.", "example": "option indirectanchor off", "source_decompiler": "ghidra-upstream", "inspiration": "repipe:bcryptgetproperty-status-tested-through, 6a3d73bfa4b247348ae8069e (x86-64 PE selamv2.exe) sub_140003f80 -> BCryptGetProperty 0x1400282b8 at 0x140004766; upstream Funcdata::opInsertAfter (funcdata_op.cc:373) and FuncCallSpecs::collectOutputTrialVarnodes (fspec.cc:5543)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call is emitted as a bare statement and the next line tests a local the function never assigns", "a Win32 API called through its IAT slot loses its NTSTATUS while sibling imports keep theirs", "declaring a prototype for the callee restores the assignment `v15 = f(...)`", "a call whose last stack argument renders as `v70 & 0xffffffff00000000` has no return value", "a call's `NTSTATUS` is tested through `v16` where v16 has no definition anywhere in the function"]},
  {"option": "calloverlap", "values": ["off", "in", "full"], "default": "off", "destructive_as_default": false, "phase": "P3", "subphase": "call-guard", "strength": "HARD", "rewind": "P3", "issue": "GH-275", "summary": "Complete the two upstream call-overlap guards kuna shipped as comment-only stubs: Heritage::guardCallOverlappingInput (heritage.cc:1210) and Heritage::tryOutputOverlapGuard / guardOutputOverlap (heritage.cc:1249/1293). Heritage characterizes each heritaged range against every call site's prototype; when the range is strictly LARGER than the ABI storage inside it the characterization is ContainedBy, and the whole-range guards see nothing. That is routine on x86-64 because SLEIGH models PXOR, MOVDQA, MOVDQU, POR, PAND, MOVQ-to-xmm and ORPD as ONE 128-bit write to the whole XmmReg, so Heritage::refinement's `size > 4 && maxw < size` gate never partitions the range and the 8-byte SysV parameter and return entries inside it stay invisible. At `in` the input guard locates the biggest contained input entry, translates its address from the callee's to the caller's perspective, inserts a SUBPIECE before the CALL that truncates a fresh whole-range Varnode down to that entry, registers the truncated Varnode as an input trial and appends it to the CALL - the guard PROPOSES storage, the P4 trial machinery still judges it. At `full` the output guard additionally turns the biggest contained return entry into an INDIRECT creation at the call and PIECEs the surrounding bytes of the range back around it, so the range still has a definition at the call while the return entry alone carries the output trial, and the range's effect is downgraded to unaffected. The two levels exist because they are separable: `in` alone recovers the dropped argument but leaves the call's return value read from a stale pre-call definition.", "use_when": "Turn ON (`full`, which is upstream Ghidra's behavior) when a call is rendered with missing arguments and/or its result is read from a variable that is never assigned -- the classic rendering is `u(); sink = v1; return v1 + 1.0;` where `v1` has no definition, from the canonical gcc -O2 int-to-double idiom `movapd xmm1,xmm0; pxor xmm0,xmm0; cvtsi2sd xmm0,rdi; call u`. The whole enclosing function can also lose its own signature (`void mix(void)` for a `double mix(int,double)`), and a printf-family call can lose every floating-point vararg its format string asks for. PREFER `full` OVER `in`: measured over 268 decbench O2 binaries / 77,014 functions, `full` changes 283 functions (0.37%) with 94 that stop reading a never-assigned local against 6 that start, while input-only changes 209 with only 10 fixed against 12 newly introduced -- recovering the argument without recovering the return value leaves the call result stale. Use `in` only to bisect which half of the guard moved an output. DEFAULT OFF even though the flip is clean on both gate corpora (0/675 datatests, 0/411 stage assertions with the option forced on) and adds no decompile errors (4,024 error-marked functions before and after): the guards make the whole over-wide range live at the call, which EXPOSES two pre-existing kuna defects on the functions they touch. (1) 54 of the 283 changed functions (19%) render an XMM range that used to be a clean `float4 v9` as a 16-byte `char v9 [16]` with `v9[0] = ...; SUB164(v9,0)` lane traffic -- faithful, but noisier C. (2) At 6 call sites the recovered floating-point argument is emitted in slot 0, ahead of the format string (`log_out((float8)x,(float8)y,a0,\"time: %5.2f/%5.2f\")`), because `build_input_from_trials` does not interleave the integer and float ParamEntry groups back into source order for an unprototyped mixed call. Both are worth fixing before the default flips. Flip back off if a call gains an argument the disassembly does not support, or if a 16-byte lane rendering appears where a scalar read better.", "example": "option calloverlap full", "source_decompiler": "ghidra-upstream", "inspiration": "upstream Heritage::guardCallOverlappingInput (heritage.cc:1210, called at :1508), Heritage::tryOutputOverlapGuard (heritage.cc:1249) and Heritage::guardOutputOverlap (heritage.cc:1293); GH-275 (packed-double atan2 call arguments) and the plain gcc -O2 pxor reproducer behind it", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["call emitted with no arguments or fewer arguments than the disassembly passes", "call result read from a local that is never assigned", "enclosing function loses its own parameter list and return type (void f(void))", "argument register written whole-width by pxor movdqa movdqu por pand orpd or movq before the call", "second call binds a stale pre-call value where the first call should have defined it"]},
  {"option": "spillargtrial", "values": ["off", "reload", "spill"], "default": "off", "destructive_as_default": false, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "GH-275", "summary": "Tolerate the caller's own caller-save spill of an argument when scoring an active input trial. Funcdata::onlyOpUse (funcdata_varnode.cc:1891) answers whether the Varnode reaching a CALL in a candidate parameter register is used ONLY as that call's argument, and it rejects on ANY CPUI_STORE among the Varnode's descendants; checkInputTrialUse then calls markInactive, which also sets CHECKED, so the trial is never re-scored and the argument-producing chain dies to dead-code elimination. That blanket rejection also rejects the opposite idiom: on x86-64 SysV NO xmm register is callee-saved, so a floating-point value that is both an argument and live across the call MUST be spilled by the caller, and the spill is a second descendant of the very Varnode the trial is scoring. At `reload` the STORE stops rejecting only when it writes the walked Varnode's own value (operand 2, never the pointer) into a caller-frame slot AND a later LOAD reads that slot back at the same width, i.e. a genuine caller-save spill/reload pair. Since ActionActiveParam runs before ActionStackPtrFlow, the frame reference is still the raw INT_ADD(stack pointer, #const) and the reload straddles the call that re-defines the stack pointer, so the search walks forward from the store's own base Varnode carrying the running offset delta rather than matching raw constants. At `spill` the reload requirement is dropped and any caller-frame store of the value is tolerated. This is a deliberate DIVERGENCE from upstream, not a port fix: the rule it relaxes exists precisely to stop a value the caller writes to its frame before a call from becoming a bogus extra argument.", "use_when": "Turn ON (`reload`) when a call is rendered with FEWER arguments than the disassembly passes and the missing argument's register is spilled to the frame just before the call -- the canonical rendering is `atan2(SUB168(v1,0));` from clang -O2 packed-double code that calls atan2 more than once, where `movapd XMMWORD PTR [rsp+0x20],xmm1` is the caller-save spill and `movaps xmm1,XMMWORD PTR [rsp+0x20]` after the call is its reload. The dropped argument often leaves a spurious 4-byte stack local in its place (`unsigned int v5; // stack - 0x18`). PREFER `reload` OVER `spill`: `spill` admits any frame store of the value, which fabricates arguments out of ordinary local writes -- measured on a clang -O2 inlined 64-byte memcpy, `spill` turns `sptr2(&v1,a0)` into `sptr2(v1,v5,v9,v13,&v1,a0)`, four arguments invented from the four `movaps` stores that fill the local buffer, while `reload` correctly declines because nothing reads those slots back. Use `spill` only to bisect whether the reload requirement is what is blocking a recovery. DEFAULT OFF: the failure mode of relaxing upstream's rule is a SPURIOUS TRAILING ARGUMENT, and no gate can see one -- the datatest corpus is prototype-declared and GED is blind to arity -- so the option ships as an opt-in with its evidence rather than as a default. Flip back off if a call gains an argument the disassembly does not pass, or if an argument appears whose value is a stale pre-call definition (that second shape is calloverlap's territory: the two options compose, and on the GH-275 copysign variant only `calloverlap full` plus `spillargtrial reload` gives both the right arity and the right values).", "example": "option spillargtrial reload", "source_decompiler": "kuna", "inspiration": "GH-275 (packed-double atan2 call arguments) defect (A), scoped in PR #284; upstream Funcdata::onlyOpUse (funcdata_varnode.cc:1891) and FuncCallSpecs::checkInputTrialUse (fspec.cc:5592)", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["call rendered with fewer arguments than the disassembly passes when the missing argument register is spilled to the frame before the call", "floating-point argument dropped at the FIRST of several calls that reuse the same value", "a spurious 4-byte stack local appears where a call argument should be", "clang -O2 packed-double code calls atan2 with one argument instead of two", "argument register stored to a caller-frame slot and reloaded after the call is not recognized as an argument"]},
  {"option": "loadguardrange", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "load-guard-range", "strength": "HARD", "rewind": "P3", "issue": "GH-182", "summary": "Complete the upstream LoadGuard/StoreGuard range-refinement machinery kuna shipped as stubs: at the end of each heritage pass, run the ValueSet solver (ValueSetSolver::establishValueSets/solve, rangeutil.cc:2416/2524) over the pointer of every newly discovered indexed-stack LOAD/STORE guard and record the real minimum/maximum/step of the addresses the op can touch (LoadGuard::establishRange/finalizeRange, heritage.cc:741/788, driven by Heritage::analyzeNewLoadGuards, heritage.cc:834). The refined guards feed two consumers. (1) P6 variable mapping: MapState::gatherOpen's addGuard loops (varmap.cc:1241-1248) turn a range-locked guard into an open RangeHint with highind = ((max-min)+1)/step - 1 (varmap.cc:1033) -- the ONLY source of a real index bound for a stack array written through a computed index. Without it the only hint is the fallback min_items = 3, so RangeHint::attempt_join rejects any element past index 3 (diffsz > highind) and splits it off as a separate never-assigned scalar while the array prints with a wrong extent and out-of-bounds subscripts. (2) Merge's StackAffectingOps: a refined store guard's isGuarded test now covers only the addresses the STORE can actually hit, so unrelated stack locals stop being treated as clobbered across the indexed STORE. OFF reproduces the pre-port behavior exactly: every guard keeps the whole-space range with step = 0, addGuard bails at its step == 0 gate, and no bound is ever produced.", "use_when": "ON by default (upstream Ghidra's stock behavior; 0/675 datatest ablation). The OFF symptom is self-contradictory C: a stack array declared with a too-small extent (int2 v5 [4]) subscripted by an index the enclosing guard proves reaches past it (if (5 < v2) ... v5[v2] = ...), with the tail elements split off as separate stack scalars that are read (range-checked, stored into structs) but never assigned -- a fake buffer overflow plus fake uninitialized reads, which also break the decompile-project recompile path. Flip OFF only to reproduce kuna's pre-GH-182 output or to bisect whether a stack-frame layout change came from guard refinement.", "example": "option loadguardrange off", "source_decompiler": "ghidra-upstream", "inspiration": "GH-182 (betaflight cliServo int16 arguments[6] split into int2 v5[4] + unassigned v6/v7; diffutils diff3 main rev_mapping); upstream Heritage::analyzeNewLoadGuards (heritage.cc:834), LoadGuard::establishRange/finalizeRange (heritage.cc:741/788), ValueSetSolver (rangeutil.cc:1910-2585), MapState::addGuard (varmap.cc:1003)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["stack array subscripted past its own declared bound under a guard that proves the larger index", "stack scalar at the end of an array read but never assigned and never address-taken", "array written through a computed index capped at 4 elements regardless of the real extent", "decompile-project output recompiles into a stack overflow the binary does not have", "servo/config fields validated and stored from uninitialized stack locals"]},
  {"option": "indexaliasguard", "values": ["off", "load", "full"], "default": "load", "destructive_as_default": false, "phase": "P3", "subphase": "heritage-guard", "strength": "HARD", "rewind": "P3", "issue": "re-quadratic-transform-loses-four", "summary": "Restore the index-alias arm of upstream Heritage::guard (heritage.cc:1194), which kuna shipped stubbed out behind a hard-coded `highPtrPossible == false`. When a stack range is heritaged and a pointer can reach it, upstream adds two kinds of guard op, and this option is a tri-state over them. `load` (the default) runs Heritage::guardLoads (heritage.cc:1570): an addrforce CPUI_COPY read of the range in front of every indexed-stack LOAD whose discovered guard range covers it, so a slot that is only ever READ through a computed pointer still has a reader at the point the pointer is dereferenced. The COPY is artificial -- Heritage::handleNewLoadCopies propagates it away once heritage has finished, keeping only the addrforce marks it earned. `full` adds Heritage::guardStores (heritage.cc:1538), an indirect_store CPUI_INDIRECT in front of every STORE whose space can reach the range, which is what upstream always does; it is not the default because its INDIRECT chain survives into the output as write-backs of values a slot already held and as globals hoisted into temporaries, and over 60 decbench binaries it recovered nothing guardLoads did not. `off` is the pre-port state: neither arm runs, so a frame slot written by a direct `MOV [ESP+k],REG` and read only through a `LEA`-derived pointer has no reader at all and ActionDeadCode deletes the store. kuna::highPtrPossible is the upstream inline (architecture.hh:408) minus the cspec <nohighptr> range list, which only the PIC families set.", "use_when": "`load` by default (DIV-147; 0/675 datatest and 0/714 stages ablation). The `off` symptom is an array that is used before it is set: a stack buffer initialised by a run of direct frame stores, then walked by a pointer loop, prints with its declaration and all of its reads but none of its initialisation -- the analyst sees `unsigned int v11 [4];` followed by `*v6 = *v6 ^ ...` and concludes the buffer starts as garbage, when the binary loaded four constants into it. Flip `off` to reproduce kuna's pre-indexaliasguard output, or to bisect whether a new addrforce mark on a frame slot came from this arm. Flip `full` to reproduce upstream Ghidra exactly, or when a value written through a pointer looks like it is not reaching the frame slot it aliases; expect noisier output (measured +5 lines on the 1.9% of functions the arm touches at all).", "example": "option indexaliasguard full", "source_decompiler": "ghidra-upstream", "inspiration": "RE-friction round 12, need quadratic-transform-loses-four (sddecoder decoder.exe sub_401330, PE i386: four MOV [ESP+k] stores of a 128-bit constant accumulator dropped); upstream Heritage::guardStores (heritage.cc:1538), Heritage::guardLoads (heritage.cc:1570), Architecture::highPtrPossible (architecture.hh:408)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["stack array declared and read but never initialised in the emitted C", "a run of direct frame stores vanishes when the same slots are later read through a pointer", "constants loaded from .rdata into a local buffer do not appear in the decompilation", "accumulator or key schedule buffer looks uninitialised before an XOR loop walks it", "value stored through a pointer does not appear to reach the frame slot it aliases"]},
  {"option": "tiedstorekeep", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "copy-propagation", "strength": "HARD", "rewind": "P3", "issue": "re-getprocaddress-result-discarded", "summary": "Refuse the one RulePropagateCopy marker propagation that deletes a `local = f();` frame store. A `MOV [frame+off],REG` storing a call's return value lowers to an address-tied `stackvn = COPY(REG)`; RulePropagateCopy (ruleaction.cc:3943) rewrites every reader of `stackvn` to read `REG` directly. Ordinary readers are harmless -- the value is the same and the store stays alive for whoever still reads the location. A MARKER reader is not: an INDIRECT across a later call or a MULTIEQUAL at a join never prints, so once the marker has swallowed the last remaining reader the COPY has no descendants at all and dies to dead-code elimination, taking the store with it. Merge normally hides this by merging the register's HighVariable into the tied location's, but when that merge is DECLINED (intersecting covers) nothing repairs it and the local's last PRINTED assignment is whatever came before the store -- typically its NULL initialiser. Upstream already declines this propagation for an `addrforce` output (\"Don't propagate if we are keeping the COPY anyway\"), but `addrforce` is set only on heritage's own guard outputs, never on an ordinary frame store. ON widens that refusal by exactly one case: the marker is about to consume the LAST reader of a non-persistent address-tied COPY whose input is not itself address-tied and whose value comes from a call (CALL/CALLIND/CALLOTHER, or the INDIRECT that carries the return register across the call site before ActionActiveReturn promotes it). Propagating there buys nothing -- the marker is invisible either way -- and costs the store. OFF is upstream Ghidra's behavior exactly.", "use_when": "On by default (DIV-105). The OFF symptom is C that says the program uses a value it never computed: a local initialised to NULL, a call whose result is assigned to a register temporary, and then an indirect call or a dereference through the still-NULL local -- `v108 = NULL; v12 = (void *)GetProcAddress(...); ... v7 = (*v108)(...);`. The dataflow underneath is correct (the CALLIND's target really is the GetProcAddress result); only the printed store is missing, which is worse than a wrong-looking expression because an analyst reads it as a null-pointer call and concludes the import is never used. Flip OFF to reproduce upstream Ghidra's propagation exactly, or to bisect whether a stack-frame variable's appearance came from this brake. Each clause of the predicate was bought by measurement, not by argument: dropping the from-a-call clause turns three upstream datatests' if/else diamonds into `?:` ternaries and degrades union field resolution (7/675 assertions move, 0/675 with it); letting it fire on `persist` globals -- which already have heritage's own RETURN-COPY keeping their last store printed -- made the `copytrim` datatest run for minutes instead of 0.16 s.", "example": "option tiedstorekeep off", "source_decompiler": "kuna", "inspiration": "RE-friction round 2, need getprocaddress-result-discarded (ObfuscationFiesta.exe sub_140002530, PE x86-64, anti-debugging + control-flow obfuscation); upstream RulePropagateCopy (ruleaction.cc:3943) and its own addrforce refusal", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a local is set to NULL and then used as an indirect-call target with nothing assigned in between", "GetProcAddress/dlsym result assigned to a temporary and the function pointer local it is stored into stays NULL", "call result stored to the frame but the store is missing from the emitted C so the later read looks uninitialized", "indirect call through a stack slot that no statement ever writes", "a stack local read after a call shows only its initialiser"]},
  {"option": "loopcounterstore", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "copy-propagation", "strength": "HARD", "rewind": "P3", "issue": "re-live-loop-counter-stores", "summary": "Refuse the one RulePropagateCopy marker propagation that deletes a loop counter's write-back to its frame slot. A counter the compiler keeps on the stack is read, bumped in a register and stored back (`MOV EAX,[RSP+0x84]` / `INC EAX` / `MOV [RSP+0x84],EAX`); after heritage the store is an address-tied `stackvn = COPY(EAX)` whose only reader is the loop header's MULTIEQUAL, and that MULTIEQUAL's output is the same frame slot. RulePropagateCopy (ruleaction.cc:3943) rewrites the marker to read EAX directly, the COPY loses its last descendant and dies to dead-code elimination, and what prints is the register's own def under the register's HighVariable: `for (v55 = 0; v55 <= 4; v7 = v55 + 1)`. That is not a naming nit -- the induction variable is never updated, so the emitted loop does not terminate. Merge normally hides it by merging the register high into the tied high, but where one register carries several counters and a few call returns the covers intersect, the merge is declined, and every such loop in the function comes out this way. ON declines the propagation when the marker is a MULTIEQUAL whose address-tied output has exactly the storage of the COPY's output AND the stored value is computed from that same MULTIEQUAL output -- the self-updating tied phi, i.e. a counter. OFF is upstream Ghidra's behavior exactly. `tiedstorekeep` does not cover this: its predicate requires the stored value to come from a CALL/CALLIND/CALLOTHER/INDIRECT, and a counter bump is an INT_ADD.", "use_when": "On by default (DIV-146). The OFF symptom is a `for` loop whose increment slot assigns to a different variable than the one it initialises and tests -- `for (v55 = 0; v55 <= 4; v7 = v55 + 1)` -- which as written is an infinite loop, and where the same register temporary collects the increments of several unrelated counters plus a few call returns. It shows up on frame-resident counters, so on unoptimized or obfuscated code rather than on -O2 register loops. Flip OFF to reproduce upstream Ghidra's propagation exactly, or to bisect whether a counter's increment came from this brake. The `computed from the phi output` clause is what keeps the brake narrow: without it an ordinary call return stored to a frame local is kept too, which re-prints `v134 = _time64(0);` as a temporary plus a store and un-folds `v43 -= 0x2a` into `v43 = v43 - 0x2a`.", "example": "option loopcounterstore off", "source_decompiler": "kuna", "inspiration": "RE-friction round 5, need live-loop-counter-stores (ObfuscationFiesta.exe sub_140002530, PE x86-64, control-flow obfuscation); upstream RulePropagateCopy (ruleaction.cc:3943) and its own addrforce refusal", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a for loop initialises and tests one variable but assigns its increment to another", "the induction variable is never updated so the emitted loop cannot terminate", "several loop counters lose their increments to one shared register temporary", "a stack-resident counter is incremented into a variable that has no storage comment", "the loop test compares against a value nothing ever assigns"]},
  {"option": "splitstorekeep", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P3", "subphase": "heritage-refinement", "strength": "HARD", "rewind": "P3", "issue": "re-31-byte-buffer-copy", "summary": "Carry the `stack_store` mark from a frame store onto the refinement pieces Heritage::refineWrite cuts it into. A frame store lowers to a CPUI_STORE, and RuleStoreVarnode rewrites it to a direct `stackvn = COPY(value)` whose output it marks `stack_store` -- \"originally came from a CPUI_STORE\". That mark is the whole reason the store survives: ActionDirectWrite (coreaction.cc:1366) calls a COPY into the frame a DIRECT WRITE only when its output isStackStore(), and ActionDeadCode (coreaction.cc:4146) opens by dropping the addrforce mark of anything that is not a direct write, after which nothing consumes the store and the COPY is swept. Heritage::refinement (heritage.cc:1891) breaks that chain: when two accesses to one stack range overlap, refineWrite (heritage.cc:1807) re-points the store's defining op at a fresh unique and rebuilds its storage from SUBPIECEs, one per partition cell, and splitByRefinement builds those cells with plain Funcdata::newVarnode -- so none of them carries the mark. Every refined piece is a non-direct-write, loses its addrforce and is deleted, while the unrefined stores beside it survive. ON copies the mark onto the cells, which claims nothing new: a piece of a store is a store. OFF is upstream Ghidra's behavior exactly.", "use_when": "On by default (DIV-153). The OFF symptom is a buffer copy that silently loses its middle: the compiler copies a 31-byte array with four 8-byte moves at offsets 0, 8, 15 and 23, so the middle two overlap on byte 15, refinement splits exactly those two, and the emitted C copies bytes 0..7 and 23..30 with the fifteen in between never written -- `v7[7] = v6[7];` followed straight by `v7[0x17] = v6[0x17];`. It is the compiler's standard idiom for any word-copy of a size that is not a multiple of eight, so it shows up on struct assignments and inlined memcpy rather than on loops, and an analyst reads the gap as an uninitialised tail. Inert on any function whose stack accesses all agree on their boundaries, because refinement does not split there at all: 0/675 datatest assertions move and a 40-binary decompile-all sweep changed 6 functions, every one of them by ADDING the copies of an overlapping-store run. Flip OFF to reproduce upstream Ghidra's unmarked pieces, or to bisect whether a newly-appearing frame store came from this mark.", "example": "option splitstorekeep off", "source_decompiler": "kuna", "inspiration": "RE-friction round 12, need 31-byte-buffer-copy (0xJam3z-Medium sub_15dc, ELF x86-64 crackme: a 31-byte stack-to-stack copy loses bytes 8..22); upstream Heritage::refineWrite (heritage.cc:1807), Heritage::splitByRefinement (heritage.cc:1734), RuleStoreVarnode (ruleaction.cc:4396), ActionDirectWrite (coreaction.cc:1366)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a stack-to-stack buffer copy is missing the bytes in its middle", "a struct or array assignment emits only its first and last word", "two of the four MOVs of an inlined memcpy have no statement in the emitted C", "a copied buffer looks half-uninitialised although the disassembly writes every byte", "the stores that vanish are exactly the ones whose 8-byte ranges overlap"]},
  {"option": "regionstructure", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-region-structurer", "summary": "Structure the CFG by walking the KunaRegionIdentifier region tree and matching angr Phoenix/SAILR schemas (acyclic sequence + ITE + cyclic loops [Inc 3] + acyclic switch-case [Inc 4], with the SAILR H1/H3-ordered virtualize-to-goto fallback) instead of running Ghidra's CollapseStructure. DIV-12 default-on: the primary structuring path. Byte-identical to Ghidra's CollapseStructure on reducible code; falls back to CollapseStructure on irreducible (multi-entry) loops.", "use_when": "On by default (DIV-12): the region-driven structurer is the primary S8 path. It is byte-identical to Ghidra on reducible code and falls back to CollapseStructure when a function cannot be collapsed to a single structured root (irreducible/multi-entry loops). Set OFF to force the upstream Ghidra CollapseStructure path unconditionally.", "example": "option regionstructure on", "source_decompiler": "angr", "inspiration": "angr Phoenix/SAILR structurer (angr/analyses/decompiler/structuring/{recursive_structurer,phoenix,sailr}.py); _match_acyclic_sequence, _virtualize_edge, _order_virtualizable_edges", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["goto placement follows the sailr region walk instead of ghidra's collapse order (flip off to force CollapseStructure)", "uncompilable c where a loop body folded into a while condition", "structuring differs from upstream ghidra on the same function"]},
  {"option": "guardarm", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "decbench-ifnoexit-guardarm", "summary": "Break the ARM TIE in CollapseStructure::ruleBlockIfNoExit by code layout instead of by out-index. The rule folds the condition block and ONE terminal out-arm into a BlockIf; the other arm survives as the fall-through, so the pick decides which half of the function is nested inside the `if`. Upstream walks the out-edges in index order and takes the first eligible one (in-degree 1, out-degree 0, a decision edge). Usually only one arm qualifies and there is no choice -- but when the condition guards a fatal no-return call and the other side is the function's own `return`, BOTH arms qualify and index order decides a coin flip. Index order carries no source information at that point: `negateCondition`'s `swapEdges` re-orients the block between the two `collapse_all` runs ActionBlockStructure performs (the pending-flip realization runs between them), so the arm sitting at out(0) in the deciding run is not the arm the branch was assembled with. When the `return` wins the tie kuna emits `if (ok) return v; fatal();` -- condition negated, body re-parented one level deeper, fatal call demoted to the function's trailing fall-through -- where the source, IDA and Ghidra all write `if (!ok) fatal(); return v;`. With this on, a tie is resolved to the arm whose front leaf lies EARLIER in the address space: an unoptimized compiler emits the taken clause of `if (c) A; B;` in front of `B`, and the `then` side of an if/else in front of the `else` side, so the earlier arm is the source's `if` body in both shapes. TIES ONLY -- a block with one eligible arm is byte-identical to upstream, which is why the great majority of guards are untouched. The predicate deliberately does NOT read the artificial-halt (op_mark_halt / pcodeop_flags::noreturn) bit: preferring the no-return arm fixes openssh scp xcalloc and dpkg-query control_list but INVERTS coreutils make-prime-list xalloc, whose source really is `if (p) return p; fprintf(...); exit(1);`. Layout order gets all three right because it reads what the compiler recorded about the source instead of guessing from the callee.", "use_when": "Turn ON when a guard clause that ends in a fatal/no-return call comes out INVERTED -- the condition negated, the whole rest of the function nested one level deeper inside the `if`, and the fatal call emitted as the function's last statement -- where IDA, Ghidra and the source all write the flat `if (bad) fatal(); <body>; return K;`. The tell is a function whose only `return` sits inside an if-arm while the body ends on a `// no-return` call. Verified on the decbench O0 corpus: dpkg-query control_list (both of its two guards come out flat; its second guard was already correct and is unchanged), openssh scp xcalloc, coreutils `[` posixtest. Equally verified NOT to move the two functions a no-return-arm preference would break: coreutils make-prime-list xalloc @0x1906 and coreutils tail xlseek @0x4804 are byte-identical with the option on. MEASURED BIDIRECTIONALLY over the whole decbench O0 slice (32,339 functions scored in both arms, 265 slices, `scripts.decbench.optsweep`): 6,821 bodies change, GED 230,291 -> 227,646 (-2,645), GED-perfect 14,785 -> 15,082 (+297), 316 functions moved TO perfect against 19 moved OFF (16.6:1, McNemar z = +16.2), improved 726 / worsened 243. Per architecture the gain is x86-64 (+305 perfect, -2,672 GED); ARM Cortex-M is -7 and i386 PE -1, on 7 and 1 functions respectively. Every one of the 6,821 changed bodies was audited mechanically: identical callee, string and statement multisets, gotos 1,021 -> 1,019, labels 620 -> 619, `while( true )` and `// no-return` counts unchanged -- the only content difference is the comparison constant a negated condition renders (`x <= 0xb` becoming `10 <= x`). TWO KNOWN FAILURE MODES, both in that 19. (1) The metric, not the option: on betaflight cliDumpPrintLinef and zlib test_inflate the ON pane matches the preprocessed source LITERALLY (`if (!((dumpMask & DO_DIFF) && equalsDefault)) {...} return false;` and `if (strcmp(...)) { fprintf; exit(1); } else { printf; }`) and GED still charges 8 and 6. (2) A real limit: when an arm is a return-split CLONE its start address is the SHARED EPILOGUE's, not the source position of the `return`, so the layout signal is destroyed -- e2fsprogs preenhalt's source `if (!(ctx->options & E2F_OPT_PREEN)) return;` inverts. Default OFF: it is a divergence from upstream Ghidra's out-index order, so the flip needs its own DIV row plus the 0/675 ablation and a speed number.", "example": "option guardarm on", "source_decompiler": "kuna", "inspiration": "decbench O0 triage round 4, cluster `ifnoexit` symptom A (docs/decbench/features-round4.md Rank 2); the layout predicate replaces the refuted `prefer the no-return arm` predicate recorded in the same file as Killed K13", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["guard clause with a fatal no-return call comes out inverted and the whole body is nested inside the if", "the function's only return sits inside an if-arm and the body ends on a no-return call", "kuna negates a guard condition that ida and ghidra leave alone", "a leading validation guard is emitted at the tail of the function"]},
  {"option": "loopcondhoist", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "decbench-ifnoexit-loopcondhoist", "summary": "Give the non-loop-heads a first pass in CollapseStructure's DEFERRED ruleBlockIfNoExit scan, so ruleBlockWhileDo keeps the loop's head test. The deferred scan walks the live components in order and folds the FIRST block whose terminal arm qualifies, then restarts the whole cascade. A head-tested loop whose exit arm is a `return` (after ActionReturnSplit/returndup has given that return in-degree 1) is such a block, and it sits ahead of its own body in that order -- so the head folds to `if (!C) return X;` and drops to ONE out-edge, at which point ruleBlockWhileDo can never match it again and the loop is emitted as `while( true ) { if (!C) return X; BODY; }`. The body block that carries the loop's `break` is ALSO a candidate in the same scan and folding it first is strictly better: the break arm absorbs the loop's follower (which the rule's own `size_in() == 1` test already requires be reached from nowhere else), the body collapses to a single back-edge clause, and the head test hoists into the `while`/`for` header on the next cascade pass with no new machinery. With this on the scan makes one pass skipping blocks that are live loop heads (resolved through the collapsed graph exactly as LoopBody::update resolves them) and only falls back to the unrestricted upstream pass when that finds nothing, so a function with no loop-head candidate is byte-identical to upstream and the scan still terminates on the same fixpoint.", "use_when": "Turn ON when a loop comes back as `while( true ) {` with the real loop condition immediately inside it as a guarded exit -- `while( true ) { if (!C) return X; BODY; }` -- where the source and IDA write `while (C) { BODY }`. Witnesses: coreutils od read_char becomes `while (dat) { ... }`, and libacl getfacl walk_tree_visited reaches the source's exact `for (v1 = ...; v1 != a0; v1 = ...) { ... } return 0;`. The named hazard is handled by the existing rule rather than by a new predicate: libacl getfacl get_list, whose loop follower FREES the list and returns NULL, keeps its semantics exactly (the free+return moves into the `break` arm and the normal exit returns the list) because ruleBlockIfNoExit only folds a clause of in-degree 1; bash mksyntax main is byte-identical. MEASURED BIDIRECTIONALLY over the whole decbench O0 slice (32,339 functions scored in both arms, 265 slices, `scripts.decbench.optsweep`): 2,281 bodies change, GED 230,291 -> 224,670 (-5,621), GED-perfect 14,785 -> 15,454 (+669), 680 functions moved TO perfect against 11 moved OFF (61.8:1, McNemar z = +25.5), improved 1,347 / worsened 256. EVERY architecture gains: x86-64 +542, ARM Cortex-M +123, i386 PE +4. Over the 2,281 changed bodies the shape audit reads `while( true )` 2,087 -> 223, gotos 621 -> 409, labels 399 -> 251, lines 123,744 -> 118,891, and `// no-return` unchanged. Sixty-six of those functions lose duplicated callees (openssh addr_match_cidr_list emits `free(list)` once instead of five times) -- that is the returndup-cloned loop follower being re-shared as the single loop exit, and the resulting pane is the openssh source verbatim. Default OFF: it changes WHICH of two legal folds happens first, a divergence from upstream's component order, so the flip needs its own DIV row plus the 0/675 ablation and a speed number.", "example": "option loopcondhoist on", "source_decompiler": "kuna", "inspiration": "decbench O0 triage round 4, cluster `ifnoexit` symptom B (docs/decbench/features-round4.md Rank 2); the scan-order predicate replaces the refuted `region_structurer.rs cb.size_out() != 1` diagnosis and the `form A first` plan recorded in the same file as Killed K7 and K8", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["loop emitted as while( true ) with the head test as a guarded exit on the first statement", "a for/while header condition is left inside the loop body", "kuna writes while(true) where ida writes while (cond)", "a loop's early-exit return is the loop's first statement instead of its condition"]},
  {"option": "regionlooprefine", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-tr-o2-parse-str", "summary": "Extend the region structurer (regionstructure) with cyclic loop-successor refinement: a multi-exit / multi-latch / mid-entry (irreducible) loop that the base cyclic schemas cannot fold is refined by virtualizing its SECONDARY exits and latches to gotos (lowered to break;/continue; by the existing scopeBreak / loop-construction passes) so it folds into a structured while/do-while/for/inf-loop instead of falling back to CollapseStructure. A strict superset of regionstructure's cyclic schemas: a loop the base schemas already fold is untouched, so reducible code stays byte-identical (675/675). Port of angr RegionIdentifier._refine_loop_successors_to_guarded_successors / _ensure_jump_at_loop_exit_ends (the force_loop_single_exit path).", "use_when": "A function with a multi-exit or irreducible loop renders goto-heavy where angr recovers a clean while/for with break/continue (e.g. tr_O2.o::parse_str: 37 -> fewer gotos). On by default (DIV-14; requires regionstructure on, its prerequisite); flip OFF to restore regionstructure-without-refinement. Only loops that would otherwise fall back to CollapseStructure are refined; if refinement cannot make a loop foldable it still falls back (never worse than OFF).", "example": "option regionlooprefine on", "source_decompiler": "angr", "inspiration": "angr RegionIdentifier._refine_loop_successors_to_guarded_successors + _ensure_jump_at_loop_exit_ends (force_loop_single_exit); test_decompiling_tr_O2_parse_str", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["goto-heavy multi-exit loop where angr recovers a clean while/for with break/continue", "irreducible or multi-latch loop falls back to raw gotos instead of folding", "secondary loop exits rendered as gotos rather than break statements"]},
  {"option": "regionedgeorder", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-sailr-edge-ordering", "summary": "Improve the region structurer's (regionstructure) last-resort edge-virtualization ORDERING: when no schema applies and the structurer MUST virtualize an edge to a goto, pick the order that minimizes the resulting goto count. Replaces the flat H1 (sibling-count) / H3 (return-edge) + address tiebreak with angr's _last_resort_refinement dominance-tiered bucketing (classify candidate edges into crossing / secondary / other via forward immediate-dominators over the acyclic graph, and virtualize the highest-priority bucket first) and the SAILR _order_virtualizable_edges H2 post-dominator heuristic (prefer the edge whose removal yields the most post-dominators, i.e. the most-linear structure), with the postdom_max_edges (10) / postdom_max_graph_size (50) caps so post-dominator computation stays bounded. Only changes WHICH goto is chosen when the structurer is already forced to virtualize, so on reducible code (where the structurer never virtualizes) output is byte-identical (675/675). Port of angr SAILR phoenix._last_resort_refinement + sailr._order_virtualizable_edges.", "use_when": "Set ON (with regionstructure on, its prerequisite) when a goto-heavy function would render with fewer gotos under a smarter virtualization order — i.e. the structurer is forced to virtualize multiple edges and the bucketing/post-dominator order exposes more structure than the address tiebreak. Default OFF: byte-identical to regionstructure's existing H1/H3 + address ordering. The caps bound the post-dominator cost; above them only the H1/H3 + node-order tiebreak applies (no regression).", "example": "option regionedgeorder on", "source_decompiler": "angr", "inspiration": "angr SAILR phoenix._last_resort_refinement (crossing/secondary/other bucketing via immediate_dominators + to_acyclic_graph + post-order node_seq) + sailr._order_virtualizable_edges (H1/H2/H3, postdom_max_edges/postdom_max_graph_size caps)", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["more gotos than angr on a function forced to virtualize several edges", "goto cut at an awkward edge where a dominance/post-dominator ordering exposes more structure"]},
  {"option": "condfold", "values": ["off", "on", "wide"], "default": "off", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-tr-build-spec-list", "summary": "Relax the ONE gate that makes Ghidra decline a short-circuit fold: the is_complex(orblock) check inside ruleBlockOr (and its verbatim twin try_block_or in the region structurer). BlockBasic::isComplex is a >2-statement readability budget, so a single spill, address computation or extra call parked in front of the second test costs a crossing goto back into the first arm's clause (coreutils tr build_spec_list: `result_idx = &v9; if (es_match(es_00,v11,'=')) goto label_40242e;`) or a goto+label into a guard cascade's shared body (lighttpd server.c::main in the angr corpus binary newbury: 1 goto / 1 label where angr emits 0 / 0). With this on, a complex sibling is accepted when it fits the level's PRINTED-WIDTH BUDGET -- the operand renders at most 5 comma elements at `on` / 9 at `wide`, counted with the printer's own skip rules (not_printed, markers, implied outputs, the bare BRANCH), the terminal CBRANCH included -- AND either of two admission rules takes it. Rule A (bounded prefix): a BlockCopy of ONE basic block, branch-free, comment-free, with at most 1 statement-root call. Rule B (statement shape): every op is a marker, an op with an output, a void CALL/CALLIND, a STORE, or the single terminal CBRANCH (RETURN, a second branch, BRANCHIND, CALLOTHER and no-return calls all decline), with <=2 conservatively-scored statements and <=2 calls per block, no comment, and -- because Rule B also admits a NESTED BlockCondition so a guard cascade can fold -- <=4 condition leaves and <=4 total scored statements at the fold site. The absorbed statements render inside the &&/|| operand as a C comma expression, which the printer already supports (comma_separate). The fold moves no p-code: it re-parents two existing structuring nodes, and C short-circuit plus comma sequencing preserves the original execution paths and order, so predicates that call functions need no purity analysis. Applied at BOTH structuring paths. Port of the angr Phoenix MultiStatementExpression relaxation (_should_use_multistmtexprs / _build_multistatementexpr_statements) under its default MAX_ONE_CALL policy; `on` uses angr's statement threshold (5) and `wide` raises the budget to 9 for BOTH rules, because kuna's printed-statement granularity is finer than angr's AIL for the same block (argument recovery and address-tied copy shadows split one AIL statement into several printed ones). THREE ACCURACY NOTES. (1) Rule A's call cap counts only calls printed as their own comma-chain element: the eligibility walk mirrors the printer, whose implied-output skip necessarily runs before the call test, so a call inlined into the sibling's own condition is not charged and a folded operand can render more than one call (measured: mv -O2 copy_internal at `wide` renders cached_umask(...) counted and fchmod(...) not counted). This is deliberate and is a readability bound, not a soundness bound; Rule B counts every call. (2) Rule B's <=2 SCORED statements is a calc_explicit approximation, NOT a width: a block can score 2 and render 7 comma elements, which is exactly why the printed-width budget above is enforced on top of it. The scored count survives only because the <=4 leaf / <=4 statement cross-leaf caps are expressed in it, so those caps bound the relaxation at each site where it is exercised but do NOT bound the summed printed width of a folded cascade, nor the final condition's global width -- a purely upstream-legal fold (both operands non-complex) can still extend a cascade on the left without consulting them, exactly as it can with the option off. (3) The width is measured on the op list as it stands at structuring time: later passes can add or drop an op, and where a call's stack-effect ops are still live (unprototyped callee) it over-counts, which only ever declines a fold.", "use_when": "kuna emits a crossing `if (cond) goto label_X;` from the else arm back into the then arm's clause, or a goto+label into the shared body of a permission/validation guard cascade, or a nested if where angr short-circuits into `A || B` / `A && B` -- because the second condition block also assigns something (a spill, a pointer setup, a call whose result is the test). Default off: byte-identical to upstream (the eligibility precompute is skipped and every gate disjunct is dead). Try `on` first: it is the angr-parity width and it closes the guard-cascade case (newbury main reaches 0 gotos / 0 labels there). Go to `wide` when the sibling is a kuna-noisy block (copy shadows, unrecovered call arguments) that `on` still declines -- that is what tr build_spec_list needs to reach angr goto parity, and what true_ubuntu1804 usage needs for its ifs 9 -> 7 fold. THIS IS NOT A MONOTONE GOTO REDUCER: folding changes which edges the structurer can subsequently match, so individual functions can GAIN a goto even though the aggregate is a clear net win (measured on 2827 functions over 7 real binaries: gotos 2294 -> 2219 at `on` and -> 2191 at `wide` overall; 2 functions gain one -- clientloop.o process_escapes 26 -> 28 at both levels, true_ubuntu1804 usage 1 -> 2 at `wide` only). Flip back off when you prefer a nested if to a comma expression in the condition, when the folded operand reads as too dense (an operand can render two calls when one is inlined into the test), or when you need a kuna advisory comment that the fold can drop: the comment guard declines on any comment buffered at structuring time, but comma_separate suppresses emitCommentGroup, so an advisory produced by a LATER pass can vanish (newbury main emits 4 WARNING lines at off and 3 at on, losing its `WARNING: branchflip:` line). The emitted C is unaffected by that loss.", "example": "option condfold wide", "source_decompiler": "angr", "inspiration": "angr PhoenixStructurer._match_acyclic_short_circuit_conditions types a-d (phoenix.py:2578) + MultiStatementExpression wrap at phoenix.py:2688 gated by _should_use_multistmtexprs (MultiStmtExprMode.MAX_ONE_CALL, _multistmtexpr_stmt_threshold=5) and _build_multistatementexpr_statements; coreutils tr build_spec_list (proposal #39) and lighttpd server.c::main in the angr corpus binary newbury (proposal #56)", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["crossing goto from the else arm back into the then arm's shared clause", "goto plus label into the shared body of a guard cascade whose arms reconverge", "nested if where angr emits one compound short-circuit condition", "second condition block assigns one value or calls something before its test and the fold is declined"]},
  {"option": "gotoreduce", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ReturnDuplicator", "summary": "After structuring, rewrite `if (cond) goto T` into `if (cond) { <tail> }` when T is a small single-successor basic-block chain ending in `return`, by duplicating that return tail inline (a print-tree copy of the same ops — no p-code cloned). Eliminates the residual goto/label that Ghidra's CollapseStructure must emit for a shared multi-predecessor return tail.", "use_when": "kuna emits 1+ gotos to a shared `... return v;` tail that angr renders goto-free (e.g. `if (x) goto label; ... label: return 0;`). On by default (DIV-14); flip OFF to restore the upstream goto/label rendering. On, the bounded (<=3 blocks / <=8 ops, no call/store) return-constant tail is duplicated so the cross-edge becomes a structured early return.", "example": "option gotoreduce on", "source_decompiler": "angr", "inspiration": "test_decompiling_dd_argmatch_to_argument_noeagerreturns; SAILR/Phoenix goto-reduction (ReturnDuplicator); argmatch_to_argument", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["goto to a label that only returns", "label_N: return; shared tail after an if", "more gotos than angr for the same function"]},
  {"option": "ifelseflatten", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-IfElseFlattener", "summary": "After structuring, flatten a 3-component `if/else` whose true-clause is statement-terminating (every endpoint returns / aborts / gotos away) by dropping the `else` and re-parenting the former else body as the `if`'s immediate follower (a print-tree edit of the structured `list`/`parent` — no p-code cloned). Mirrors angr's `IfElseFlattener`: `if (c) { ...return } else { body }` becomes `if (c) { ...return } body`, removing the syntactic `else` (and any residual goto/label it carried).", "use_when": "kuna keeps an `else { ... }` whose matching `if`-branch always returns/aborts (or ends in a goto), so the `else` is redundant guard-clause noise — the early-return form angr emits. On by default (DIV-14); flip OFF to restore the upstream explicit-`else` rendering. The region structurer already flattens most clean diamonds, so this catches the residual terminating-if/non-terminating-else shapes the structurer (or the CollapseStructure fallback) left with an explicit `else`.", "example": "option ifelseflatten on", "source_decompiler": "angr", "inspiration": "test_ifelseflatten_iprule / test_ifelseflatten_clientloop; angr IfElseFlattener (region_simplifiers/ifelse.py); SAILR guard-clause flattening", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["else block kept although the matching if branch always returns or aborts", "guard-clause body indented one level deeper than the source", "redundant else after an early-return arm"]},
  {"option": "crossjumprevert", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-CrossJumpReverter", "summary": "After structuring, revert compiler cross-jumping (Irreducible Statement Condensing): rewrite `if (cond) goto T` into `if (cond) { <tail> }` when T is a small single-successor *non-return* basic block that falls through to a successor S, by duplicating that cross-jump tail inline (a print-tree copy of the same ops — no p-code cloned). Only fires when the if's own structured fall-through also converges on S (a next_flow_after convergence precondition), so duplication is semantics-preserving. Sibling of gotoreduce (return tails) for the non-return fall-through case.", "use_when": "Set on when kuna emits 1+ gotos to a shared, condensed *non-return* tail that angr's CrossJumpReverter renders goto-free (e.g. `else if (c) { f(); goto label; } ... else { label: v = *p; } if (v == 2) ...`). On by default (DIV-14); flip OFF to restore the upstream goto rendering. On, the bounded (one tail block, <=8 printed ops, no STORE, <=1 call) cross-jump tail is duplicated back into the goto source so both paths fall straight through and the goto/label vanish. Each duplication is logged (`crossjumprevert:`). Runs after gotoreduce (last deoptimization).", "example": "option crossjumprevert on", "source_decompiler": "angr", "inspiration": "test_who_condensing_opt_reversion; SAILR CrossJumpReverter/ReturnDuplicatorLow (angr/analyses/decompiler/optimization_passes/cross_jump_reverter.py); who.o scan_entries", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["goto into the middle of a sibling branch's arm (compiler cross-jumping)", "shared non-return tail reached by goto from an else-if chain", "label planted inside an else block targeted from another branch"]},
  {"option": "retsplitglobal", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "repipe-simd-constant-string-initializer", "summary": "Stop a shared RETURN block that stores to MORE THAN EIGHT globals from being cloned into every predecessor. `ActionReturnSplit::isSplittable` (blockaction.cc:2242) decides whether a multi-predecessor RETURN block is a bare epilogue -- cheap enough that giving every predecessor its own private copy is free. It admits only MULTIEQUAL, COPY and RETURN and, for COPY, checks only that no INPUT is free. It never looks at the COPY's OUTPUT. Upstream that is harmless because the epilogue COPYs it was written for move registers, which die at the return; a COPY into a PERSISTENT Varnode is a store to a global the program can observe, and it is exactly what the C prints as `dat_<addr> = ...`. Both splitters share this one predicate -- upstream ActionReturnSplit and kuna's ActionReturnDup (`returndup`) -- which is why `option returndup off` does not stop the cloning: the goto-driven upstream action picks the block up instead. A macOS crackme initializer ends in one 72-store block reached three ways and kuna emits it three times, 216 statements, 46% of the function. Nothing about that is incorrect (node_split gives each clone its own in-edge, so every copy runs on exactly one path); it is a readability defect, and the fix is a BOUND rather than a veto, which is the house style of this phase -- `crossjumprevert` and `dedupitetail` cap a duplicated leaf run at 8 printed ops, `taildup` at 12. A two-store epilogue is cheap to clone and reads BETTER cloned, because the merged form needs a phi per global and can print a global's own value back into it on a path where the binary stores nothing; seventy-two is not. Only COPY is counted: a MULTIEQUAL writing a persistent Varnode is a phi, not an observable write, and RETURN writes nothing. `Heritage::guardReturns`' synthetic `glob = glob` self-copies are excluded two ways, either sufficient -- the `return_copy` flag and input and output naming the same storage -- because those are liveness markers, one per persistent global, present before every RETURN in every function that has a global, and counting them would price a bare epilogue by how many globals the whole PROGRAM has. Declining is one-directional: it can only ever make a split NOT happen, so it cannot create a duplication or change what the surviving merged block does.", "use_when": "On by default. Keep it on when the same long run of `dat_<addr> = ...` statements is emitted two or three times in one function, once per path that reaches the exit -- a compiler-shared epilogue that fills message or table globals, where the reader has to notice the blocks are identical before any of them can be read. Flip OFF to restore upstream's predicate and get the per-predecessor private epilogues back (byte-identical to upstream), which is the right call when you are following one path at a time and want each exit's stores inline rather than merged behind a phi. Inert unless a shared RETURN block stores to more than eight distinct globals, so a small epilogue still splits exactly as upstream, and inert on any function whose epilogue only moves registers however long it is.", "example": "option retsplitglobal off", "source_decompiler": "kuna", "inspiration": "repipe:simd-constant-string-initializer, 653d88600f4238b24302b0ec (macOS x86-64 Crackme0x04) sub_100003790; upstream ActionReturnSplit::isSplittable (blockaction.cc:2242) shared with kuna ActionReturnDup", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["the same run of dat_<addr> = ... statements is emitted two or three times in one function", "a long global-filling epilogue is duplicated once per path that reaches the return", "option returndup off does not stop a shared epilogue from being cloned", "46% of a function's text is three copies of one exit block"]},
  {"option": "taildup", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ReturnDuplicatorLow-call-tail", "summary": "After structuring, rewrite `if (cond) goto T` into `if (cond) { <tail> }` when T is a small single-successor basic-block chain ending in `return` that *contains a call* (e.g. a `free(p); return;` epilogue), by duplicating that return tail inline (a print-tree copy of the same ops — no p-code cloned). The third SAILR goto-reduction pass: it fills the gap between gotoreduce (return tail, rejects ALL calls) and crossjumprevert (non-return fall-through tail) — neither handles a return tail with a call. Mirrors angr ReturnDuplicatorBase whose budget is calls (max_calls_in_regions=2), not call-rejection.", "use_when": "kuna emits 1+ gotos to a shared `... free(p); return;` style epilogue (a return tail WITH a call) that angr renders goto-free by duplicating it into each arm (e.g. morton my_message_callback: 1 goto -> 0). On by default (DIV-14); flip OFF to restore the upstream merged rendering — right when many early-exit error checks share ONE cleanup epilogue (the C `goto out;` idiom, e.g. openssh kex_choose_conf), where duplication forks the cleanup into source-absent copies. On, the bounded (<=3 blocks / <=12 printed ops / <=2 calls, no STORE) return-call tail is duplicated so the cross-edge becomes a structured early return. Safe because the tail ends in `return`: the duplicated call is reached on exactly one structured path (the early-return arm), never both. Each duplication is logged (`taildup:`). Runs right after gotoreduce.", "example": "option taildup on", "source_decompiler": "angr", "inspiration": "test_decompiling_morton_my_message_callback; SAILR ReturnDuplicatorLow / ReturnDuplicatorBase max_calls_in_regions (angr/analyses/decompiler/optimization_passes/return_duplicator_{base,low}.py); my_message_callback free();return; epilogue", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["goto to a shared free(p); return; style epilogue", "cleanup-call return tail reached via label instead of duplicated inline", "flip off when many error checks share one cleanup block (the goto out; idiom)"]},
  {"option": "dedupitetail", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ITE-region-dedup", "summary": "After structuring, deduplicate a cloned `if/else` tail: when both arms of a 3-component `if` begin (prefix) or end (suffix) with one or more *leaf* blocks that emit identical C — same printed-op sequence, matching constants by value, named storage by location, and call targets by resolved callee — hoist that shared run out of the `if` (prefix before it, suffix after) so it is emitted ONCE instead of twice (a print-tree edit — no p-code cloned). The inverse of the SAILR duplication passes (gotoreduce/crossjumprevert/taildup, which DUPLICATE a shared tail to drop a goto); here kuna's collapse-based structurer over-duplicated a shared continuation and the readability win is to merge the copies. Mirrors angr's structurer ITE region deduplication (converging two predecessors onto one copy instead of cloning). Bounded (<=8 leaves, <=64 ops/leaf), exact-or-decline, and suffix-sound only when each arm's divergent middle falls through to the suffix.", "use_when": "kuna emits the SAME leaf statement(s) at the head or tail of BOTH arms of an if/else (a compiler-duplicated shared prefix/suffix the structurer cloned into each arm), e.g. a shared cleanup epilogue or a `--help`-style shared call repeated in both branches. On by default (DIV-14); flip OFF to restore the upstream duplicated-arm rendering. On, the shared leaf run is hoisted out of the `if` (one copy), each merge logged (`dedupitetail:`). Conservative: only flat C-equivalent leaf blocks are merged (never a nested if/loop or a labelled/goto block), and a suffix is hoisted only when both arms provably reach it. Deeply entangled duplication where the shared code shares a basic block with divergent code (e.g. true_1804 usage's online-help printf folded into the setlocale condition block) is out of scope — that needs the full label-convergence transform.", "example": "option dedupitetail on", "source_decompiler": "angr", "inspiration": "test_decompiling_true_1804_x86_64::usage; angr structurer condition-processing / ITE region deduplication (RegionSimplifier; structuring/structurer_base.py _merge_nodes); inverse of SAILR ReturnDuplicator/CrossJumpReverter", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["both arms of an if/else begin or end with the same duplicated statements", "shared cleanup or help call repeated verbatim in the two branches", "compiler-duplicated tail emitted twice instead of hoisted out of the if"]},
  {"option": "iteregion", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ITE-region-converter", "summary": "After structuring, rewrite a narrow two-arm assignment *diamond* — a 3-component `if` whose condition block is a single `CBRANCH`, whose true- and else-clauses are each a single `COPY` to the SAME variable (`if (c) v = A; else v = B;`), converging on one tail — into a single `?:` ternary statement `v = ( c ) ? A : B;`. A **print-only** mark on the condition `CBRANCH` (no p-code cloned/mutated); the two arm COPYs and the condition stay live so the merged variable's later uses render unchanged, only the `if`'s layout differs. The S9 emit hook is the one genuinely new piece (Ghidra's printc has no ternary operator). Mirrors angr's `ITERegionConverter` / `_find_ite_assignment_regions` (a conditional-jump region with two single-assignment children on one tail). Halves the branch count of format/print/flag code full of `flags ? \"%s,\" : \"%s\"` diamonds (iproute2 print_link_flags: 41 ifs/0 ternaries -> 21 ifs/20 ternaries, GED 140 -> small).", "use_when": "Default ON (DIV-17). The diamond->ternary form matches the common format/print/flag source where the ternary dominates (compilers turn `cond ? \"%s,\" : \"%s\"` into exactly this diamond; iproute2 print_link_flags, coreutils output helpers), so kuna emits `v = c ? A : B;` matching source/angr. Still a RUNTIME CHOICE an agent can flip OFF (`option iteregion off`, byte-identical to upstream) per function when explicit `if/else` is the likely source, to avoid inventing a ternary the author never wrote — the SAME object code is emitted for both, so the binary cannot distinguish them. A print-only mark; on, each rewrite is logged (`iteregion:`).", "example": "option iteregion on", "source_decompiler": "angr", "inspiration": "decbench:O0-iproute2-ip-print_link_flags; angr ITERegionConverter (angr/analyses/decompiler/optimization_passes/ite_region_converter.py); _find_ite_assignment_regions", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["if (c) v = A; else v = B; assignment diamond where a ternary v = c ? A : B is expected", "format/flag builder full of two-arm constant assignments to the same variable", "flip off when the source likely wrote an explicit if/else"]},
  {"option": "iteexpr", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ITE-region-converter-expr", "summary": "Extend `iteregion` from single-`COPY` arms to any single-statement COMPUTED pure-value arm (`if (c) v = *p; else v = q;` -> `v = c ? *p : q;`, or `v = c ? b + 5 : b - 3;`). The arm's assignment op may be any pure value op (`LOAD`, `INT_*`, `PTR*`, `CAST`, `SUBPIECE`, …) whose second-level operands are single-use implied varnodes; only side-effecting/control ops (a `STORE`, a call, a branch, an `INDIRECT`/`MULTIEQUAL`) are rejected. The C ternary evaluates only the taken branch, so the rewrite is semantics-preserving. Print-only (same mechanism as iteregion). Matches angr's aggressive `?:` recovery: on decbench O0 coreutils angr emits ~1389 ternaries to kuna's ~177; this roughly doubles kuna's recovery (ls 27->46, sort 2->21, du 19->34).", "use_when": "Turn ON to recover `?:` over computed arms like angr/IDA do, when the source used ternaries (common in format/print/flag/size code). A RUNTIME CHOICE, default-off: it diverges when the source used an explicit if/else (the same object code is emitted either way). Requires `option iteregion on` (default) — it broadens iteregion's arm match. Print-only; on, each rewrite is logged (`iteregion:`).", "example": "option iteexpr on", "source_decompiler": "angr", "inspiration": "decbench:O0-coreutils (ternary under-recovery vs angr/ida); angr ITERegionConverter computed-arm regions", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["if (c) v = <expr1>; else v = <expr2>; where a ternary v = c ? expr1 : expr2 is expected", "kuna emits far fewer ?: ternaries than angr/ida on format/flag/size code", "computed-arm assignment diamond (v = *p / v = b + k) not folded to a ternary"]},
  {"option": "iteboolean", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "decbench-shortcircuit-boolean-materialization", "summary": "After structuring, re-roll a SHORT-CIRCUIT 0/1 select diamond back into a single boolean assignment: a 3-component `if` whose condition is the folded `&&`/`||` chain and whose two arms are each a single `COPY` of the constants `1` and `0` to the SAME integral variable becomes `v = ( a && b );` (arms `0`/`1` -> `v = !( a && b );`) instead of `if (a && b) v = 1; else v = 0;`. This is the case P3 `RuleConditionalMove` cannot fold: it requires each `MULTIEQUAL` input block to be the CBRANCH root or a SINGLE-predecessor pass-through (upstream `ruleaction.cc:9427` `inblock->sizeIn() != 1` -> bail), and a short-circuit chain gives the constant arm 2+ predecessors. Upstream's bail is also correct at the IR level -- there is no single dominating CBRANCH standing for the whole chain, and hoisting a guarded operand out of its branch would evaluate it unconditionally -- so the re-roll is only safe AFTER structuring, where the chain is already one `BlockCondition` that C's own `&&`/`||` renders with short-circuit semantics. A **print-only** mark on the condition's terminal `CBRANCH` (no p-code cloned/mutated); the condition is emitted by the SAME renderer that produced the `if (...)` header, so evaluation order, short-circuiting and comma-expression side effects are preserved verbatim. Declines a pointer/float destination, a labelled arm, a computed arm and any second statement in an arm.", "use_when": "Turn ON to recover the source shape of `-O0` boolean assignments (`x = a && b;`, `x = p && (p->flags & F);`), which gcc materializes as an explicit constant diamond that kuna otherwise prints as `if (...) v = 1; else v = 0;`. `-O0` C materializes booleans everywhere, so this is broad: it is the whole residual structural gap vs Hex-Rays on bash `time_command` (two diamonds = 6 CFG blocks / 8 edges of a 28-node function; GED 26 -> 6). On by default (DIV-51): the flip changes 0/675 datatest assertions and is inside the speed budget. Still a RUNTIME CHOICE an agent can flip OFF (`option iteboolean off`, byte-identical to the pre-DIV-51 render) for the same reason as `iteregion` (DIV-17): an explicit `if (c) x = 1; else x = 0;` in the source compiles to the SAME object code, so the re-roll diverges from a source that really did write the if/else. Print-only; on, each re-roll is logged (`iteboolean:`).", "example": "option iteboolean on", "source_decompiler": "ida", "inspiration": "decbench:O0-bash-bash-time_command", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["if (a && b) v = 1; else v = 0; where the source has a single boolean assignment v = a && b", "-O0 boolean materialization printed as an explicit 0/1 constant diamond", "extra CFG blocks/edges vs Hex-Rays around a short-circuit boolean assignment"]},
  {"option": "itecondlist", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "decbench-ite-every-other-diamond", "summary": "Let the `iteregion` and `iteboolean` diamond matchers see through a multi-component `BlockList` in the diamond's CONDITION position, by descending it to its LAST component. Their shared `leaf_bblock` helper descends a one-component list but bails on a list of two or more, and the collapse structurer concatenates a just-collapsed predecessor onto the next diamond's condition block — so in a RUN of structurally identical diamonds every other one is declined, and kuna re-rolls exactly ceil(N/2) of N (measured N=1..8: 1 1 2 2 3 3 4 4). Sound because the rewrite is print-only and the printer already renders this shape: `emit_block_if` emits the condition component once under NO_BRANCH (its statements) and once under ONLY_BRANCH (its branch), and `emit_block_ls` emits every component under NO_BRANCH but only the LAST under ONLY_BRANCH — so the leading components print as ordinary statements before the `if` header either way, and the ternary/boolean emitters do the same two emissions on the same block. The arms keep the strict single-statement match and the labelled-goto-target guard still applies to the descended leaf. Restricted to `BlockList`: a `BlockGraph` is excluded because `emit_block_graph` ignores ONLY_BRANCH and would print the whole graph twice.", "use_when": "kuna re-rolls only SOME of a run of structurally identical assignment/boolean diamonds -- one `v = c ? A : B;` (or `v = a || b;`) followed by an explicit `if (c) v = A; else { v = B; }` with the same condition and arms, alternating. The tell is that which diamonds are missed depends on the NEIGHBOURING structure, not on the diamond itself: an immediately preceding structured `if` suppresses the very next one. On by default (DIV-56): the flip changes 0/675 datatest assertions and is inside the speed budget. Flip OFF (`option itecondlist off`) to restore the pre-DIV-56 every-other rendering; it is a strictly narrower match, so OFF can only ever fold FEWER diamonds. Requires `option iteregion on` and/or `option iteboolean on` (both default) -- it widens their condition match only, and never fires on its own.", "example": "option itecondlist on", "source_decompiler": "ida", "inspiration": "decbench:O0-bash-bash-shell_initialize; widens angr ITERegionConverter (_find_ite_assignment_regions) + iteboolean condition matching over a concatenated BlockList", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["only every other ternary in a chain of identical ?: assignments is recovered", "a re-rolled v = c ? A : B; is immediately followed by an identical un-re-rolled if (c) v = A; else v = B;", "iteboolean re-rolls 1 of 3 identical short-circuit boolean assignments", "a diamond folds in isolation but declines when a structured if precedes it"]},
  {"option": "returndup", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ReturnDuplicatorHigh", "summary": "Before the final block structuring, duplicate a SHARED bare-epilogue RETURN block into each of its predecessors but one, so the classic guard shape `if (cond) { body; return X; } return Y;` -- which gcc -O0 compiles to a single multi-predecessor RETURN block -- structures as per-predecessor early returns instead of ONE merged, comma-folded exit. kuna (like upstream Ghidra) otherwise keeps the merged form: CollapseStructure::rule_block_or fuses the guards (which share the epilogue out-target) into one short-circuit condition with comma-expression side effects (e.g. `if ((A||B) && (v=f(..), C||D)) { rest }`), and the existing ActionReturnSplit (the goto-driven ReturnDuplicatorLow analog) only splits when structuring left a goto into the return -- which the clean guard collapse never does. This is the gotoless complement: it reuses the SAME `return_split_is_splittable` filter (only MULTIEQUAL/COPY/RETURN over constant/annotation/non-free inputs -- a side-effect-free epilogue, angr's `_is_simple_return_graph`) and the SAME `node_split` machinery ActionReturnSplit drives, splitting every in-edge but one. Mirrors angr SAILR `ReturnDuplicatorHigh` (the gotoless return duplicator). SELECTIVE like angr: it duplicates ONLY when the shared return returns a CONSTANT (or a phi/COPY chain resolving to constants) -- angr's `_should_duplicate_dst = dst_is_const_ret`. A `return <variable>` shared block is left merged (the unselective version diverged from the source's short-circuit form often enough to regress the decbench GED-perfect count ~976 across 21768 firings, which reverted DIV-18; the const-return gate drops ~40-60% of firings -- the over-eager variable-return splits -- while keeping the guard-clause wins like coreutils factor, and it is what turned the aggregate measurement positive again for DIV-54).", "use_when": "On by default (DIV-54), which SUPERSEDES the DIV-18 revert. It recovers the source early-return shape: where kuna/upstream Ghidra collapse two-or-more source early-return guards that share a compiled epilogue into ONE giant short-circuit `if` with comma side effects and a single trailing return, returndup duplicates the shared bare-epilogue RETURN block (<= 16 predecessors, <= 64 splits/function) into each predecessor so the structurer emits separate `if (c) return X;` clauses matching the source/angr/ida (coreutils factor: kuna 3 ifs / 0 early returns -> 4 ifs / early returns, GED 12 -> 0; also closes the ghidra-beats-kuna bash compspec_dispose shape). Bounded and side-effect-safe (the splittable filter admits only a return-only block, so no call/store is ever cloned), each duplication logged (`returndup:`). It shipped default-ON once before and was reverted 2026-07-05 at -976 GED-perfect; that measurement predates the const-return gate, and the SELECTIVE pass re-ablated on the same corpus (536 slices / 52,862 functions) is +417 GED-perfect and -7,756 aggregate GED, net-positive in all nine partitions tested (split count, void vs value return, distinct return values, return count, switch, all-returns-identical, body size, project, opt level). Still a RUNTIME CHOICE: flip OFF (`option returndup off`, byte-identical to upstream) per function where the source plainly used the merged short-circuit form -- the same object code is emitted either way, so the binary cannot distinguish them. The one measured net-negative subpopulation is a split that de-structures a loop (321 firings, -257 GED); a gate for it was built and scored at +2 perfect / -209 GED and NOT kept.", "example": "option returndup on", "source_decompiler": "angr", "inspiration": "decbench:O0-coreutils-factor-factor; O0-e2fsprogs-e2fsck-ext4_fc_handle_add_extent; ReturnDuplicatorHigh; return_duplicator_base (angr/analyses/decompiler/optimization_passes/return_duplicator_{high,base}.py)", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["giant short-circuit if with comma-expression side effects merging several source early-return guards", "one trailing return shared by many guard paths where the source used per-guard early returns", "merged guard condition containing v = f(...) assignments inline"]},
  {"option": "orchain", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "decbench-returndup-o0-shortcircuit", "summary": "Decline a `returndup` split whose shared bare-epilogue RETURN block is the out-target that two conditionals must keep in COMMON for `CollapseStructure::rule_block_or` to fuse them -- i.e. the block is an operand chain of a short-circuit expression, not a set of independent source early-return guards. `returndup` gives every predecessor of a shared return its own private return; for guard clauses that is the source shape, but for the operand blocks of `a || (b && c)` it removes the shared out-target `rule_block_or` matches on, and because `ActionReturnDup` runs in the `returnsplit` group while `rule_block_or` runs later in `collapse_conditions`, the fold can never be recovered -- one source boolean expression comes back as a cascade of constant-return guards. The gate is a read-only replay of `collapse_conditions` on the bblocks CFG (`kuna_orchain::shortcircuit_shared_targets`), mirroring `rule_block_or`'s own admission test rather than approximating it: two condition nodes fuse when the sibling has exactly one in-edge, has two out-edges, is not a switch block, is not `BlockBasic::isComplex`, and shares one out-target with the head; each fuse records that shared target and rewrites the head's out-targets, so a chain folds inside-out exactly as the structurer's fixpoint does. The three upstream tests it omits cannot hold at this point in the schedule (no goto has been elected yet, and a back edge into a single-in-edge block would make that block unreachable). When any of the function's own split candidates is a recorded shared target the whole function's splits are declined, which is exactly the counterfactual the corpus measurement priced. The predicate additionally protects a block joining two or more single-exit arms that are themselves recorded fold targets: before the constants are propagated into the epilogue phi a chain's arms are one bare `v = K` block each and the RETURN sits one edge further on, which is the graph the FIRST returndup invocation of the schedule's fullloop sees.", "use_when": "On by default (DIV-69). Flip OFF (`option orchain off`) to restore the pre-DIV-69 rendering, where one source `return a || (b && c);` (or a source `if (A && B && C)` guard) comes back as a cascade of `if (a) return 1;` / `if (b) return 0;` constant-return guards -- the shape `option returndup off` also removes, at the cost of every OTHER early-return recovery in the function. It is returndup-local and one-directional: it can only ever make `returndup` fire LESS, never more, and it is inert with `option returndup off`. Measured over the full decbench corpus (85,195 functions / 795 slices, three optimisation levels; `docs/decbench/returndup-o0-measurement.md` for the motivating ablation): +611 GED-perfect at O0 for -13 at O2 and -15 at O2-noinline, +583 net and -967 aggregate GED, 781 functions moved to a perfect structural match against 198 moved off (3.94 : 1), and every architecture net-positive. It is 92% of the +630 ceiling the post-hoc 'a short-circuit fold was lost' signal prices, at 94% byte-identity with the `returndup off` body on the 3,059 -O0 functions it fires for. The residual cost is real and is not observable from the binary: where the SOURCE wrote the guard cascade and the compiler merged it, declining the split is wrong (coreutils factor, GED 0 -> 12).", "example": "option orchain on", "source_decompiler": "kuna", "inspiration": "decbench:O0-iproute2-ip-sci_complete; O0-cleanflight-cleanflight_DALRCF405-USBD_DataInStage; O0-crazyflie-cf2-DCD_HandleUSBSuspend_ISR; protects the precondition of Ghidra CollapseStructure::ruleBlockOr against the angr SAILR ReturnDuplicatorHigh port", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["a source boolean return renders as a cascade of if (c) return 0; / if (c) return 1; guards", "option returndup off collapses the whole function to one short-circuit expression", "a source guard testing several conditions with and/or becomes that many identical constant-return guards", "the return-dupe slug on a function whose source wrote one short-circuit condition"]},
  {"option": "earlyreturn", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ReturnDuplicatorHigh-peredge", "summary": "Hoist a leading const-guard into an early return (`if (c) return K;`) by peeling only the CONSTANT arm of a MIXED return phi, so a function that opens with an argument-validity check recovers the source's early-return + de-indented body instead of kuna's inverted diamond (`if (guard) { entire body } else { v = default; } return v;`). This is the PER-EDGE narrowing of angr SAILR `ReturnDuplicatorHigh` that the whole-block `returndup` gate structurally cannot reach: the shared RETURN block is `v = MULTIEQUAL(#K /*guard-false arm, constant*/, <var> /*body arm, variable*/); return v`, and `returndup`'s `returndup_is_const_ret` requires the WHOLE return value const, so it rejects this mixed diamond outright. earlyreturn instead computes the per-in-edge const set (the phi input slots that resolve to constants) and splits ONLY those edges via the SAME `node_split` machinery, leaving the variable body return merged. Each peeled const arm becomes its own `return K` predecessor; `branchflip` then orients it as the `if`-true and `ifelseflatten` drops the else and de-indents the body -- yielding angr's exact `if (c) return K; ...body...; return <var>;` (coreutils realpath::relpath: kuna 73 loc diamond -> angr's 52 loc early-return). Because it only ever peels a CONSTANT arm (never a `return <variable>` share), it CANNOT re-introduce the variable-return over-firing that made broad returndup regress the aggregate GED-perfect count ~976; it targets the mixed const/variable diamond that whole-block returndup skips. Bounded: return-only epilogue (the splittable filter rejects calls/stores), never splits the last edge (body keeps its return), <= 16 predecessors, <= 32 splits/function; each hoist logged (`earlyreturn:`).", "use_when": "A function whose first action is an argument-validity check (`if (!p) return 0;`) renders as the inverted diamond `if (p != 0) { whole body } else { v = 0 } return v;`. On by default (DIV-23: the decbench ablation measured it NET-POSITIVE, +47 perfect / -576 summed GED / 158:54 improved:regressed); flip OFF to restore the upstream merged-diamond rendering. It is MORE permissive than the whole-block `returndup` gate (it fires on mixed const/variable diamonds returndup skips), but by construction it cannot cause the variable-return over-firing that reverted returndup (it splits only constant arms).", "example": "option earlyreturn on", "source_decompiler": "angr", "inspiration": "decbench:O0-coreutils-realpath-relpath; O0-tar-tar-from_header; ReturnDuplicatorHigh._is_simple_return_graph (angr/analyses/decompiler/optimization_passes/return_duplicator_{high,base}.py)", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["inverted diamond: if (guard) { entire body } else { v = 0; } return v;", "leading argument-validity check wraps the whole body instead of an early return", "function opens with if (p != 0) { everything } where source wrote if (!p) return 0;"]},
  {"option": "switchreturn", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "angr-ReturnDuplicatorHigh-switchphi", "summary": "The direct continuation of `earlyreturn` (the per-edge const narrowing of angr SAILR `ReturnDuplicatorHigh`), extended from the narrow if/else-if diamond to the WIDE multi-way SWITCH const-phi return. An enum/switch dispatch whose every case assigns a constant to a shared temp and breaks to one exit compiles to `v = MULTIEQUAL(#K0, #K1, …); return v` with one `break` per case; kuna keeps the merged `switch (x) { case A: v = K0; break; … } return v;` (~2 CFG nodes/case more than the source's per-case `case A: return K0;`). `earlyreturn` ALREADY recovers per-case returns for a switch whose merge block has <= 16 predecessors (findutils get_fts_info_name, a 14-case switch, hoists to `case 1: return \"FTS_D\"; …`), but its MAX_EARLYRETURN_INEDGES=16 cap leaves the WIDE dispatch table merged: a 17-case switch merges 17 in-edges (> 16) so earlyreturn skips it (libedit tty__getcharindex). switchreturn is the SAME peel with the in-edge cap lifted (256): it splits each constant case in-edge (all but the last, which keeps the block alive) via the SAME `earlyreturn_const_edges` + `node_split` machinery, so every case ends in its own `return K`. Because it still only ever splits a CONSTANT case arm (never a `return <variable>` share), it inherits earlyreturn's safety and CANNOT re-introduce the variable-return over-firing that made broad returndup regress the aggregate GED-perfect count ~976; any non-const body/default/fall-through arm is left merged. Orthogonal to earlyreturn (a separate option): when both are on, the narrower earlyreturn consumes the <= 16-edge cases first, so switchreturn's incremental effect is precisely the wide dispatch tables earlyreturn's cap skips. Bounded: return-only epilogue (the splittable filter rejects calls/stores), never splits the last edge, <= 256 predecessors, <= 256 splits/function; each hoist logged (`switchreturn:`).", "use_when": "A WIDE multi-way const dispatch renders merged — `switch (x) { case A: v = K0; break; … } return v;` — instead of the source's per-case `switch (x) { case A: return K0; … }` (more cases than earlyreturn's 16-in-edge cap; e.g. libedit tty__getcharindex, iproute2 accept_msg, coreutils stat fmt_to_mask). On by default (DIV-25: the incremental wide-switch ablation on top of earlyreturn-on measured NET-POSITIVE, +2 perfect / -107 summed GED / zero regressions); flip OFF to restore the merged rendering. By construction it cannot cause the variable-return over-firing that reverted returndup (it splits only constant case arms).", "example": "option switchreturn on", "source_decompiler": "angr", "inspiration": "decbench:O0-libedit-tty__getcharindex; O0-iproute2-ip-accept_msg; O0-coreutils-stat-fmt_to_mask; ReturnDuplicatorHigh._is_simple_return_graph (angr/analyses/decompiler/optimization_passes/return_duplicator_{high,base}.py)", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["wide switch where every case assigns a constant and breaks to one shared return", "switch (x) { case A: v = K; break; ... } return v; instead of per-case return K", "17-plus-case const dispatch keeps a merged return variable"]},
  {"option": "foldcallret", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "explicit-marking", "strength": "HINT", "rewind": "P6", "issue": "angr-call-return-variable-folding", "summary": "Fold an order-safe single-use call return value into its use site, inlining the call expression instead of spilling it to a named temporary (e.g. `if (timespec_cmp(...) <= -1)` instead of `v5 = timespec_cmp(); if (v5 <= -1)`).", "use_when": "kuna spills a call result to a `vN = call(); use(vN)` pair that is used exactly once where angr folds the call expression into its use site. On by default (DIV-14); flip OFF to restore the upstream explicit-temporary form (Ghidra forces every call output explicit). Only folds when the single use is in the same block and nothing the call would be moved past is a barrier, over the whole distance from the call to the statement the folded expression is really printed in -- which is further than the use whenever the use op's own result is implied. A barrier is a call, a LOAD/STORE/CALLOTHER, or a write to persistent or address-tied storage. That last one is not an opcode: heritage promotes a write to a fixed address, or to a frame slot whose address escaped into the call, into a plain COPY, and without it `int t = f(7); k = 42; return t + k;` folded to `k = 0x2a; return f(7) + 0x2a;` -- the binary returns 50, that C reads 91 (GH-657, fixed). Two things are deliberately not barriers: a COPY back into the storage its input already occupies (what RuleIndirectCollapse leaves behind), and the ops the folded expression travels through, each of which consumes the previous one's value.", "example": "option foldcallret on", "source_decompiler": "angr", "inspiration": "test_call_return_variable_folding; angr call-return variable folding (expression folding in angr/analyses/decompiler); print_long_format", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["call result spilled to a temp used exactly once: v5 = f(); if (v5 < 0)", "single-use call return not inlined into its use site", "flip off to force every call output into a named temporary (ghidra style)"]},
  {"option": "foldcallretphi", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "explicit-marking", "strength": "HINT", "rewind": "P6", "issue": "kuna-foldcallretphi", "summary": "Let a call result that `foldcallret` already proved order-safe survive the second gate as well. `foldcallret` only relaxes ActionMarkExplicit's forced-explicit call arm; ActionMarkImplied then re-tests the candidate in checkImpliedCover, whose Merge::inflateTest arm forces it explicit when one of the call's operands has another live SSA version of the same HighVariable over the call output's cover. Upstream never reaches that arm with a call output (Ghidra marks every call output explicit one pass earlier), and the version it collides with is usually the CPUI_INDIRECT the call itself attaches to that operand's storage -- a call may write any global, so reading the global `stdin` as an argument is enough to make the fold look unsafe. This option discounts exactly that self-inflicted collision: the rejection is ignored only when every colliding version is an INDIRECT effect of the call being folded, the operand's high is not part of a VariableGroup, and the use op does not itself read one of those effects. It also declines when the call's output is narrower than what the callee returns: an unlocked output that wider return storage contains -- `eax` out of `rax`, or `eax` as the low half of the `edx:eax` pair an i386 `long long` comes back in (`r0` of `r0:r1` on ARM; the model is asked where an integer of each size up to `long long` would be returned) -- because the callee may write all of it, its own decompilation prints that width, and the spilled local's declaration is the only place the narrowing is written: `int v15 = sub_c5b0(stdin); v15 == -1` folded into `sub_c5b0(stdin) == -1` against an `unsigned long sub_c5b0()` that returns `0xffffffff` is always false. A locked output (a declared or library prototype) folds; so on a 32-bit target only declared callees fold under this option. And it declines when the call's value would reach a BOOL_AND or BOOL_OR as its right-hand operand: p-code evaluates both operands, but C prints them as `&&`/`||`, which skip the right-hand one, so `r = fflush(stdout); gflag = (r == 0) && (a > 5)` folded into `gflag = 5 < a0 && !fflush(stdout)` would make a call the binary always makes conditional. A call reaching the left-hand operand is always evaluated and still folds. foldcallret's own order-safety predicate does the barrier half for both options: since GH-657 it runs over the whole distance from the call to the statement the folded expression is really printed in (following the implied chain, not just to the single use), that print point must be in the call's own block so a use whose own result is implied cannot carry the call across a branch into a later block, and a write to persistent or address-tied storage is a barrier there alongside call/load/store/callother. What this option adds over that same distance is its own: no op up to and including the landing statement may read a value the call writes indirectly. foldcallret asks that question only as far as the single use, because past the use it declines folds that are faithful -- `dat = *__errno_location()` is one call, one load and one store in that order in the binary. The folds that travel further are the ones the merge machinery was holding, which is exactly what this option releases, so here the question is asked over the whole span; without it ssh O2 sub_4fd30 prints its call after a copy of an escaped stack slot the binary reloads afterwards.", "use_when": "On by default: a single-use call result that takes a global as an argument folds into its use like any other order-safe call result when the callee's return type is declared or the output fills the return register, so `v15 = strcmp(v98,optarg); v31 &= 0xffffffff; if (v15)` prints as `v31 &= 0xffffffff; if (strcmp(v98,optarg))`. A call whose output is an undeclared `eax` read out of `rax`, or out of the `edx:eax` pair on i386 (`r0:r1` on ARM), keeps its spill, since the spill's declaration is the only thing narrowing what the callee returns: `v15 = sub_c5b0(stdin), v15 == -1` stays as it is. So does a call whose result is the right-hand side of a `&&` or `||`, which C would skip. Turn it off to get the spilled form back byte-for-byte, for a listing where each call is its own statement, or when a `--assert type vN` / `--assert name vN` directive was written against output from before the flip: folding removes a declaration and renumbers the remaining `vN` locals, so such a directive can name a different variable. The flip changes no scored variable: over 32 stripped binaries (11,279 functions) the `--json` `variables[]` multiset is identical in every function and no arity moves; over fmt/ls/sort/du at O0 and O2 the emitted C loses 14 declared locals and 7 single-def/single-use temporaries. A folded call moves past nothing but an assignment to a local whose address is never taken -- a register, or a stack slot such as the phantom return-address slot -- and never past a call, a load, a store through a pointer, a global write or a branch, nor into an operand of `&&` or `||`; 0 of 675 datatest assertions change. What a fold can still drop is a same-width signedness conversion, where the callee prints an unsigned return the caller reads signed; over the 32 binaries no folded call of an undeclared callee feeds a relational comparison, shift or division.", "example": "option foldcallretphi off", "source_decompiler": "angr", "inspiration": "angr call-return variable folding (the fold angr performs unconditionally where kuna's second gate declines); foldcallret", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["single-use call result still gets its own local although the call and the use are adjacent", "call takes a global (stdin/stdout/errno) as an argument and its result will not fold", "foldcallret on but v3 = f(glob); use(v3) is still two statements", "flip off to put every call result that reads a global back in its own local", "a --assert type vN / name vN directive written before the flip now names a different local", "v = sub_x(stdin), v == -1 stays two statements: the undeclared callee's eax output is narrower than the rax it returns, and the spill's int declaration is that narrowing", "v = f(g) stays two statements on i386/ARM32: an undeclared callee may return a long long in edx:eax / r0:r1", "v = f(g); x = a && !v stays two statements: folded into the right of &&, the call would be skipped when a decides"]},
  {"option": "impliedrefs", "values": ["2", "3", "4", "<n>"], "default": "2", "destructive_as_default": false, "phase": "P6", "subphase": "explicit-marking", "strength": "HARD", "rewind": "P6", "issue": "kuna-impliedrefs", "summary": "How many times a computed value may be read and still be inlined at each use instead of being declared as its own local. ActionMarkExplicit::baseExplicit counts a Varnode's descendants and forces the value explicit once the count exceeds this bound, so the bound is exactly the line between `v3 = a + b;` used three times and `a + b` written out three times. It shipped as a hard-coded 2 whose own upstream comment reads `2 is best, in specific cases a higher number might be good`, with no way to reach it from a command. Raising it trades declarations for expression size: measured on coreutils fmt -O2 (decompile-all, 151 functions), the emitted C declares 369 locals at the shipped 2, 366 at 3 and 363 at 4, and in the other direction 384 at 1 and 630 at 0, where nothing stays implied and every computation gets its own statement. The bound is a program-wide tunable, not private to the explicit-marking pass: BlockBasic::isComplex (the `ruleBlockOr` OR-clause absorb test) and `condfold`'s printed-width test read the same field, so raising it also lets a slightly larger block fold into a condition.", "use_when": "An ablation lever, and a readability knob for a listing that is either too fragmented or too dense. Raise to 3 or 4 when kuna declares a swarm of single-expression temporaries that each carry no meaning (`v7 = v3 + 8;` read three times); lower to 1 to force every multiply-read value into a named local when you are hand-tracing data flow, or to 0 to see one statement per computation. Leave at the shipped 2 for output that matches every other kuna run and the datatest baselines -- this is a per-run judgement, not a defect fix, so nothing here is more correct than the default.", "example": "option impliedrefs 4", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra's own `Architecture::max_implied_ref` comment: `2 is best, in specific cases a higher number might be good` -- a tunable upstream expects to be edited, reachable only by recompiling until now", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["too many single-use temporaries: v7 = v3 + 8 declared and read once", "a value read three times is written out three times instead of being declared", "want to ablate the implied-expression threshold without rebuilding", "expressions are too deeply nested to read and every sub-term should get a name"]},
  {"option": "termdup", "values": ["2", "3", "4", "<n>"], "default": "2", "destructive_as_default": false, "phase": "P6", "subphase": "explicit-marking", "strength": "HARD", "rewind": "P6", "issue": "kuna-impliedrefs", "summary": "How many terminal terms one implied expression may duplicate at each use before the value is declared instead. A value that survived the `impliedrefs` descendant test with more than one reader is walked by ActionMarkExplicit::processMultiplier, which counts the leaf terms (the explicit variables and constants, spacebase excluded) the expression would re-print at every use and forces the value explicit once that count exceeds this bound. It shipped as a hard-coded 2 whose own upstream comment reads `2 and 3 (4) are reasonable`, with no way to reach it from a command. It is the second, independent half of the same decision: `impliedrefs` bounds how MANY places an expression is copied to, `termdup` bounds how BIG the copied expression may be. Measured on coreutils fmt -O2 (decompile-all, 151 functions), the emitted C declares 369 locals at the shipped 2, 366 at 3, 363 at 4 and 362 at 6, and in the other direction 380 at 1 and 385 at 0.", "use_when": "Raise to 3 or 4 together with (or instead of) `impliedrefs` when the listing is full of two-term temporaries whose expression is small enough to read at each use; leave at the shipped 2 for output that matches every other kuna run. It has no effect on a value with a single reader, so it is the finer of the two levers -- reach for `impliedrefs` first and use this one to let the slightly larger expressions through.", "example": "option termdup 3", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra's own `Architecture::max_term_duplication` comment: `2 and 3 (4) are reasonable` -- a tunable upstream expects to be edited, reachable only by recompiling until now", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["a two-term expression is declared as a temporary instead of being inlined at its two uses", "want to ablate the term-duplication threshold without rebuilding", "raising impliedrefs did not inline an expression because the expression itself is too wide"]},
  {"option": "stackguard", "values": ["on", "off"], "default": "on", "destructive_as_default": true, "phase": "P7", "subphase": "edge-virtualization", "strength": "HARD", "rewind": "P7", "issue": "angr-StackCanarySimplifier", "summary": "REMOVES CODE: strips the -fstack-protector canary epilogue (the `if (canary != *(fs:0x28)) __stack_chk_fail()` check) from the output. Strips the glibc -fstack-protector canary epilogue (the check + its no-return call block) AND the entry-side canary init (`slot = *(fs:0x28)` and its TLS-base residue, released to dead-code once the last check is stripped), collapsing the shared single-return point so each path returns directly (no goto/label, no dead canary store).", "use_when": "On by default (DIV-14): removes compiler-inserted stack-protector boilerplate and the shared-return goto it forces. Flip OFF to keep the real canary-check instructions in the output (e.g. to audit the protector itself, or if an unusual non-glibc `ptr+0x28` compare guarding a call is being matched). It is marked destructive because it deletes those real instructions, but it is byte-identical over the 675 datatests (the `Partial splitting` cases opt out via `option stackguard off`).", "example": "option stackguard on", "source_decompiler": "angr", "inspiration": "test_ite_region_converter_missing_break_statement; angr StackCanarySimplifier (angr/analyses/decompiler/optimization_passes/stack_canary_simplifier.py); authenticate", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["stack-protector canary compare against fs:0x28 and a __stack_chk_fail branch cluttering the epilogue", "shared-return goto forced by the canary check block", "flip off to keep the real canary instructions for auditing the protector"]},
  {"option": "msvcstackguard", "values": ["on", "off"], "default": "off", "destructive_as_default": true, "phase": "P7", "subphase": "edge-virtualization", "strength": "HARD", "rewind": "P7", "issue": "GH-468", "summary": "REMOVES CODE: strips the MSVC `/GS` frame-cookie boilerplate -- the epilogue `__security_check_cookie((cookie ^ SP) ^ SP)` call and the entry-side `cookie = __security_cookie ^ SP` scramble with its frame slot. The sibling `stackguard` pins the glibc canary by its compare (`slot != *(fs:0x28)` guarding a `__stack_chk_fail` branch); MSVC keeps the compare inside the checker, so a `/GS` function contains no CBRANCH, no `fs:0x28` load and no failure edge, and `stackguard` is a no-op on it. The trigger here is the arithmetic instead: a CALL argument computed as `(K ^ SP) ^ SP` with BOTH stack-pointer references resolving to the same frame offset, which is what makes the two XORs cancel. The call is dropped with the stock `delete_call_specs`/`opDestroy` pair and the cookie slot released to dead-code, so the two `/GS` lines and the slot declaration leave the function.", "use_when": "Flip ON when reading a Windows PE built with `/GS` (the default for MSVC): every protected function otherwise opens with `v8 = dat_140074040 ^ (unsigned long long)v1;` and ends with `sub_1400015b0(v8 ^ (unsigned long long)v1);`, two lines of compiler boilerplate plus a declaration in each one. Default OFF because it is destructive -- it deletes a real call and a real computation -- so it is opt-in the same way you would leave `stackguard` off to audit the protector itself. Recognition is shape-only and does not depend on the callee being named `__security_check_cookie`, so a stripped PE is covered; it also does not depend on the compiler spec, so a raw MSVC `.text` blob loaded under any x86 cspec is covered. GCC/Clang read the canary from `%fs:0x28` and never mix the stack pointer into it, so the pattern cannot occur in `-fstack-protector` output and the option is byte-identical (0/675) on the datatest corpus. Composes with `cookiescramble`, which fixes a different defect (argument loss) on the same instruction.", "example": "option msvcstackguard on", "source_decompiler": "kuna", "inspiration": "GH-468 finding 3: MSVC 18 /Od /GS testbed and a Win64 crackme, where `--option stackguard on` and `off` are byte-identical", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["a Windows PE function opens with `v8 = dat_140074040 ^ (unsigned long long)v1;` and ends with a one-argument call taking `v8 ^ (unsigned long long)v1`", "an unnamed one-argument callee (`__security_check_cookie`) called just before every return", "`--option stackguard on` and `off` give byte-identical output on a Windows binary", "an otherwise unused 8-byte stack slot declared in every function of an MSVC binary"]},
  {"option": "securitycheck", "values": ["on", "off"], "default": "on", "destructive_as_default": true, "phase": "P7", "subphase": "edge-virtualization", "strength": "HARD", "rewind": "P7", "issue": "oxidizer-SecurityCheckRemover", "summary": "REMOVES CODE: strips rustc's bounds / slice / divide-by-zero panic branches from Rust output. Every checked slice index, string slice and non-constant `/`/`%` compiles to a conditional branch to a tiny diverging block that calls one of seven `core::panicking` / `core::slice::index` / `core::str` helpers; the branch doubles the CFG of ordinary Rust code and carries nothing a reader wants. The edge is severed and the orphaned handler collected, so the guarded access becomes a plain statement.", "use_when": "On by default (DIV-82): a Rust binary reads with half the branches and none of the `panic_bounds_check()` noise. Flip OFF to keep the real check instructions (auditing whether a particular access is actually bounds-checked, or reviewing a panic path). The trigger is the callee NAME, and all seven names are Rust-only, so the pass is structurally inert on a C binary -- it is byte-identical over the 675 datatests and over the C regression fixtures. It is marked destructive because it deletes real instructions, and there is one visible second-order effect: a length argument whose only reader was the removed compare becomes genuinely unused, so parameter recovery may trim it from the signature (the same trade `stackguard` makes with the canary slot). It cannot fire on a call whose callee name kuna did not recover: in a default (PIE, dynamically linked) Rust binary the helper is reached through an unrelocated GOT slot and has no name, so the pass is currently a no-op there.", "example": "option securitycheck off", "source_decompiler": "angr", "inspiration": "SEFCOM Oxidizer SecurityCheckRemover (angr/rust/optimization_passes/security_check_remover.py, with angr/rust/mixins/cfa_mixin.py match_call and cfg_transformation_mixin.py remove_block); FakeCrypt; rustc bounds-check lowering", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["Rust output littered with `panic_bounds_check()` / `slice_error_fail()` / `panic_const_div_by_zero()` calls", "every slice index guarded by an `if` whose arm only panics", "Rust function CFG roughly twice the size of the source control flow", "flip off to keep the real bounds-check instructions for auditing"]},
  {"option": "branchflip", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "readability-rewrites", "strength": "HINT", "rewind": "P8", "issue": "angr-SAILR-condition-polarity", "summary": "Flip a negated-guard if/else for linearity: when an `if (x == 0) {A} else {B}` (equality-to-zero / negated guard) can be flipped in place, rewrite it to the positive complement `if (x != 0) {B} else {A}` so the common path reads top-to-bottom (angr-style positive guard vs `if (x == 0)`).", "use_when": "An `if (x == 0)` negated guard reads inverted versus the angr-style positive form. On by default (DIV-14): the non-negated comparison becomes the `if` condition and the if/else arms swap to match; flip OFF to keep Ghidra's polarity. A flip is logged as a `branchflip:` warning comment at the if. Only fires on `if/else` (3-component) blocks whose condition flips cleanly in place.", "example": "option branchflip on", "source_decompiler": "angr", "inspiration": "tee_O2 x2nrealloc (if (a0 == 0) vs angr if (a0)); angr SAILR structurer condition processing", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["if (x == 0) guard with the common path in the else arm", "negated condition where angr renders the positive complement first", "if/else polarity inverted versus the source's reading order"]},
  {"option": "litpoolconst", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "repipe-default-arm-output-hides", "summary": "Fold a read from EXECUTABLE read-only memory to the constant it holds, without the program-wide option readonly. An ARM immediate too wide for the instruction encoding is parked in a literal pool in .text and loaded PC-relatively, so the value a function returns can be a word a few bytes past its own last instruction: main of a statically linked ARM crackme ends `ldr r3,[0x8458]` with `0x8458 39050000 .word 0x00000539` in the same listing kuna disassembles, and the default C says `v3 = dat_8458;` -- the number the program returns is not in the output at all. kuna already paints that word Varnode::readonly (.text is SHF_ALLOC without SHF_WRITE), but folding a read-only global is gated by readonly, which is off by default because it folds every .rodata read in the program. This is the narrow half of that switch: a read that lies ENTIRELY inside an allocated, executable, non-writable, file-backed region is folded, because those bytes are the instruction stream -- mapped r-x, so a store to them faults and the image's copy IS the run-time value. A non-writable DATA section (.rodata, .data.rel.ro) keeps today's behaviour unless the image maps it executable too, which a single-RX-region firmware image does: the warrant is the permission, not the section name. That is the direction that matters on a corpus of packers and protectors, where a data section's flags are least trustworthy. Ranges come from the loader's section table, or from the PF_X load segments when the image carries no sections; an image whose loader reports neither (the XML datatest corpus, the raw-bytes loader) contributes none and the option is structurally inert.", "use_when": "On (default, DIV-136): an ARM/MIPS/PPC literal-pool constant renders as its value instead of dat_<addr>. Flip off to restore the pre-feature rendering exactly -- every in-code pool read back to an opaque global. Use `option readonly on` instead when you also want .rodata folded program-wide.", "example": "option litpoolconst off", "source_decompiler": "kuna", "inspiration": "A statically linked ARM 1337 crackme: main assigns dat_8458 where kuna disassemble already prints .word 0x00000539 four bytes past main's last instruction, so the success value could only be read by inspecting the literal pool separately.", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["an arm function returns or assigns dat_<addr> where the disassembly shows a .word literal", "a pc-relative literal-pool constant renders as an opaque global instead of its value", "option readonly on exposes a constant the default output hides", "a value loaded from inside the function's own extent has no readable definition"]},
  {"option": "loopbreak_recovery", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P8", "subphase": "goto-quality-acceptance", "strength": "HARD", "rewind": "P8", "issue": "angr-1after909-doit", "summary": "Lower loop-exit `goto <successor>` edges to structured `break;` (and switch-case exits to empty `break;`), a port of Ghidra BlockGraph::scopeBreak run in ActionFinalStructure between finalizePrinting and markUnstructured; the now-dead successor label is suppressed.", "use_when": "A loop's error/exit paths render as `goto <successor-label>;` plus a synthesized `label_NNNN:` (angr emits `break;`). On by default (DIV-10, clean ablation + converges to upstream Ghidra, which always runs scopeBreak); set OFF to restore kuna's prior byte-identical raw-goto rendering.", "example": "option loopbreak_recovery off", "source_decompiler": "angr", "inspiration": "test_decompiling_1after909_doit; Phoenix/SAILR loop-successor break/continue recovery (Ghidra BlockGraph::scopeBreak); doit", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["loop exit rendered as goto label_N; plus a synthesized label instead of break;", "switch-case exit gotos where break; is expected", "error paths leave a loop by goto to its successor label"]},
  {"option": "namestyle", "values": ["angr", "ghidra"], "default": "angr", "destructive_as_default": false, "phase": "P9", "subphase": "naming-policy", "strength": "HARD", "rewind": "P9", "issue": "angr-naming", "summary": "Master default-name scheme: angr-style (locals vN, args aN, global data dat_<addr>, unnamed functions sub_<addr>, labels label_<addr>, plus a // rax / // stack - 0x10 source-location comment per local) vs upstream Ghidra (iVar/uVar/pcVar, param_N, <type>Ram<addr>, func_, code_).", "use_when": "Set to ghidra to reproduce upstream Ghidra naming byte-for-byte; angr (default) makes default output read like the angr decompiler.", "example": "option namestyle ghidra", "source_decompiler": "angr", "inspiration": "angr decompiler default naming scheme (vN / aN / sub_<addr> / dat_<addr> / label_<addr>)", "change_kind": "presentation-default", "tier": "core", "symptoms": ["iVar1/uVar2/param_1 ghidra-style names wanted instead of v1/a1 (set ghidra)", "v-numbered locals and sub_/dat_/label_ names in the default output", "byte-for-byte comparison against upstream ghidra naming"]},
  {"option": "signedness", "values": ["upstream", "auto", "prefer-signed", "prefer-unsigned"], "default": "auto", "destructive_as_default": false, "phase": "P9", "subphase": "naming-policy", "strength": "HARD", "rewind": "P9", "issue": "kuna-signedness", "summary": "Decide a declared integer local's signedness from the operations the body applies to it, instead of from whichever type-inference vote was most specific. Datatype::type_order ranks SUB_UINT_PLAIN (16) ahead of SUB_INT_PLAIN (17), so in get_local_type's most-specific fold a single uint vote outranks every int vote and nothing downstream re-decides; on -O2 x86-64 those uint votes are structural (INT_AND/INT_RIGHT/INT_XOR from a strength-reduced loop, INT_ZEXT from a 32-bit instruction widening into its 64-bit register), which is why a counter compiled from `int` is declared `unsigned int` and then cast back at every comparison. This pass accumulates, per declared HighVariable, the size-tagged operation set reachable from its printed members -- SLESS/SLESSEQUAL/SDIV/SREM/SRIGHT/SEXT/SCARRY/SBORROW demand signed, LESS/LESSEQUAL/DIV/REM/RIGHT/ZEXT/CARRY demand unsigned, LEFT pulls unsigned as a stated preference rather than a soundness rule (a bit buffer is spelled unsigned in C source, and a signed `<<` is what -fsanitize=undefined reports), `+ - * & | ^ == !=` and the value-moving ops are signedness-independent at a fixed width, and anything else (a pointer index, a dereference, a float, an indirect branch, a cast to anything but a same-width plain integer) vetoes the variable -- and picks TYPE_INT or TYPE_UINT for the declaration only. `At a fixed width` is a precondition: C promotes anything narrower than `int` to `int` before any operator runs and the declaration decides which extension that is, so a narrow local is never re-declared. The width that decides this is the `int` of the compiler reading kuna's output, 4 bytes, and not the target cspec's `integer_size`, which is 2 on avr8gcc/avr8egcc/TI_MSP430/TI_MSP430X/CR16/PIC24/x86-16. The default `auto` re-signs a declaration only when every signedness-sensitive reader of the value agrees and nothing vetoes it, so the only text that can move is the declaration itself and the casts that declaration makes no-ops: 0/675 datatest assertions, PARITY OK on the stage corpus, and over twelve binaries from nine projects at -O0 and -O2 (15,124 functions, x86-64 and ARM32) 377 declaration flips, 443 cast tokens dropped, none added, no flip on a declaration narrower than `int`, and no other hunk of any kind. `prefer-signed` keeps that walk and additionally declares a value no operation settles signed, which is what C source does -- DWARF agreement over 238 unstripped twins goes 93.4% to 98.4% overall and 71.9% to 94.8% at -O2 -- but it stays opt-in rather than default because it moves 7,081 declarations image-wide and the datatest corpus pins the upstream spellings. `prefer-unsigned` is the opposite tie-break, kept as its control and not recommended: 4 of its 40 DWARF-judged flips are right. `upstream` restores the declaration type inference produced, byte for byte. A flip is written only for a high that owns its own declaration line: the printer's candidate list is filtered to sole-named entries before any declaration collapse runs, because every collapse pairs two candidates rendering the same name, so which declarations exist is exactly what upstream produces. No Varnode type changes and no inference re-runs, so the type lattice, the JSON variables surface and the recovered prototypes are untouched -- the decbench type_match metric normalizes signedness away and scores the default flip at exactly zero.", "use_when": "Nothing to set for the common case: the default `auto` already declares an -O2 loop counter `int`/`long` when every comparison on it is signed, instead of declaring it `unsigned int`/`unsigned long` and then writing `(int)v`/`(long)v` at each comparison. Set `prefer-signed` when you want the C convention for values no operation settles either -- the most DWARF-faithful arm, and the one to reach for when reading -O2 code -- and `prefer-unsigned` for the opposite tie-break (its own measurement says it is worse than doing nothing; it exists as that control). Set `upstream` to get the declaration type inference produced, byte-identical to not having the option, which is what to do when diffing against Ghidra or against kuna before this option existed. This is a readability/fidelity option: the decbench type_match metric normalizes signedness away, so it scores exactly zero either way.", "example": "option signedness prefer-signed", "source_decompiler": "kuna", "inspiration": "TRex (Bosamiya, Woo, Parno, USENIX Security 2025) sections 3.3.5 and 5.1: signedness is decided at type-rounding time from the accumulated operation set rather than during propagation, with a flag that prefers signed for the ambiguous case because C programmers write `int` when it does not matter", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["an -O2 counter is declared unsigned int but every comparison on it is written (int)v", "unsigned long locals whose only uses are signed compares", "the declared signedness contradicts the casts in the body", "want C-conventional signed declarations for values no operation settles", "want the declaration type inference produced, without any rounding"]},
  {"option": "realtypes", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "literal-format", "strength": "HARD", "rewind": "P9", "issue": "kuna-realtypes", "summary": "Render residual TYPE_UNKNOWN (xunknownN) values as real C types by size — 1->char, 2/4/8->unsigned short/int/long (conservative on sign), pointer-to-unknown->void * — instead of the xunknownN/undefined<N> placeholder.", "use_when": "On (default) emits real C types for un-inferred values; off restores the upstream xunknownN/undefined<N> rendering for byte-for-byte comparison.", "example": "option realtypes off", "source_decompiler": "kuna", "inspiration": "user request: drop fake unknownN types in favour of size-correct C types", "change_kind": "presentation-default", "tier": "core", "symptoms": ["undefined8/xunknownN placeholder types wanted for upstream comparison (flip off)", "size-guessed unsigned long/int/char types shown for values the inference never typed"]},
  {"option": "voidtailreturn", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P9", "subphase": "brace-form", "strength": "HARD", "rewind": "P9", "issue": "kuna-voidtailreturn", "summary": "Elide the trailing bare `return;` of a void function -- the statement the C source it was compiled from does not have, because the source falls off the end of the body. kuna (faithfully to Ghidra) always prints the function's final RETURN, so a void function ends `... }` newline `return;` newline `}`. Four conditions must all hold, checked once per function in `elidable_void_tail_return`: the prototype returns void (a `return <value>;` is never redundant); the op is the tail of the LAST structured leaf, found by descending only the containers that print no construct of their own (Graph/Ls) so a return nested inside an if/loop/switch arm is never touched; that leaf is not an unstructured goto target (bash `rl_echo_signal_char` prints `label_115e79:` directly above its trailing `return;`, and eliding it would leave a label with no statement -- invalid C); and exactly one structured leaf carries that RETURN op, because `returndup`/`taildup` clone a shared epilogue by ALIASING one op across several leaves, so suppressing by identity would also delete genuine mid-body early returns. A BlockCopy mirror is resolved through to the bblocks block it stands for, which is the normal shape of a structured function's trailing block.", "use_when": "The emitted C ends a void function with a redundant `return;` that the source does not have. The shipped catalog default is OFF (the XML datatest corpus pins the upstream rendering and applies no mode); it is ON in the `aggressive` preset, which `auto` selects for any binary under 500 KiB -- so the elision is the default rendering of `kuna decompile`, `decompile-all`, `decompile-project` and the web front-end (DIV-98). Flip OFF to restore upstream Ghidra's trailing `return;`. Measured bidirectionally over the whole O0 decbench slice (32,329 functions / 265 slices, `scripts.decbench.optsweep`): GED-perfect 15,308 -> 15,933 (**+625** for the option in isolation), 687 moved TO perfect against 62 moved OFF (11.08 : 1, McNemar z = +22.84) -- of which **preset membership delivers +439**; the residual +186 is on binaries >= 500 KiB, where `auto` selects `reliable` and its override list is empty, so only a catalog-default flip reaches them, aggregate GED 259,329 -> 253,985, improved 1,561 / worsened 186, and EVERY architecture net-positive (x86-64 +282, ARM +338, PE-x86 +5) with no net-negative project. Controls: 21,478 unchanged bodies score identically, 0 slug-only diffs, baseline arm agrees with the tree's published verdict on 31,584/32,329. Speed +0.17% (coreutils/ls O0, interleaved min-of-5). The 62 regressions are the shape where the SOURCE itself ends with an explicit `return;` (concentrated in betaflight 24 / cleanflight 21 register-poke helpers), which the binary cannot distinguish. The measured motivation is structural: pyjoern merges FUNCTION_END into its predecessor only when that predecessor's sole successor is FUNCTION_END, so when a function's tail is an `if` or a loop the source CFG has NO node there -- and kuna's printed `return;` re-materialises one (+1 node, +1..2 edges, and an is_exitpoint role flip that alone breaks GED's isomorphism test). 68 of the 250 decbench sample-set outputs carry exactly one bare trailing `return;`.", "example": "option voidtailreturn on", "source_decompiler": "kuna", "inspiration": "decbench sample set: two independent lenses measured the same lever -- deleting the trailing bare `return;` by text surgery and re-scoring through decbench's own GED moves 8-9 functions to a perfect structural match with 18 improved, 1 worsened and 0 perfects lost", "change_kind": "presentation-default", "tier": "core", "symptoms": ["a void function ends with a redundant return; the source does not have", "GED counts one extra exit node against an otherwise perfect function", "the last statement of a void function is a bare return"]},
  {"option": "ptrdepthcap", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P5", "subphase": "type-propagation", "strength": "HARD", "rewind": "P5", "issue": "kuna-ptrdepthcap", "summary": "Refuse to deepen an already unsatisfiable pointer equation while `ActionInferTypes` propagates types. A small-string-optimized C++ object (MSVC `std::basic_string`, an `ostringstream` buffer) writes `T == ptr(T)` into the type lattice: one MULTIEQUAL edge carries `p = &obj` (a `PTRSUB(spacebase,-0xN)`, typed pointer-to-the-mapped-local) and the other carries `p = obj.ptr` (a LOAD from that same address, typed as the local itself), because the SSO union stores either the characters or a pointer to them in the same 8 bytes. No finite type satisfies that, so `propagateOneType` adds exactly ONE pointer level per pass and only stops at the empirical seven-pass settle ceiling -- the object, and every temporary that touches it, is then declared `unsigned long long *****` / `char *****`. Upstream Ghidra already refuses to build such a chain at the one seam it noticed, `TypeFactory::getTypePointerNoDepth` (`type.cc:1509`, used by `TypeOpLoad`/`TypeOpStore::propagateType`), but the spacebase-PTRSUB arm that actually drives this escalation never routes through it. When on, `kuna_ptrdepth::cap_pointer_depth` applies that same rule at the single propagation funnel: a candidate whose target is itself a pointer-to-pointer collapses to `ptr(undefined<N>)`, and `ptr(ptr(undefined<N>))` collapses one more level when `N` is the pointer width. `ptr(undefined<N>)` is a fixed point, so the lattice settles instead of running to the ceiling. Depth 1 and depth 2 over a concrete base are untouched, so `char **argv`, `char **envp` and a real pointer-to-pointer parameter keep their spelling.", "use_when": "The emitted C declares locals with three or more levels of indirection that the binary plainly does not have -- most visibly `unsigned long long *****` / `char *****` around C++ std::string / ostringstream objects in MSVC binaries, where the false types dominate the listing and hide which buffer holds which string. Default OFF in the catalog (it changes inferred types, and the datatest corpus pins the upstream spellings) but ON in the `aggressive` preset, which `auto` selects for anything under 500 KiB -- so it is the default rendering of `kuna decompile`, `decompile-all`, `decompile-project` and the web front-end. Flip OFF to restore upstream Ghidra's unbounded escalation. Measured over 90 binaries / 22,307 functions (RE dataset, ELF + PE, `decompile-all` both arms): 103 functions change (0.46%), three-or-more-star occurrences fall 1,617 -> 28 and the functions carrying one 80 -> 17, and ZERO call sites are lost or gained. Every residual difference is an addressing SPELLING -- `*(char *)((long long)v + N)` becomes `v[N]`, `v = (T ****)((long long)v + N)` becomes `v = &v[N]` -- except one named cost: where the object really is an array of 8-byte pointers, an 8-byte zero store re-renders as eight one-byte stores (12 such lines across the whole sweep, in 2 functions). Speed -0.12% on a 1,027-function binary where the option is inert, +5.3% whole-binary on the witness where 1 of 144 functions changes (~40 ms, all of it in that one function).", "example": "option ptrdepthcap on", "source_decompiler": "kuna", "inspiration": "RE-friction round 2 (`c-string-objects-become`): a tester reversing a Windows crackme reported the checker was usable only after hand-tracking fields through `unsigned long long *****` and `char *****` declarations that dominated the 9.9 KB output", "change_kind": "type-inference", "tier": "transform", "symptoms": ["a local is declared with three or more levels of pointer indirection", "unsigned long long ***** or char ***** appears in the output", "C++ std::string / ostringstream locals get absurd pointer types", "the type of a stack object grows one pointer level per decompilation pass"]},
  {"option": "codescalar", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "type-propagation", "strength": "HARD", "rewind": "P5", "issue": "repipe-decompiler-emits-void-scalar", "summary": "Refuse a `code` pointee as the data-type of a dereferenced VALUE. `TypeFactory::getTypeCode` builds the generic `code` type with size 1 so that `code *` arithmetic steps one byte at a time; `TypeOp::propagateFromPointer` (typeop.cc:207) decides what a dereference yields by testing only `ptrto->getSize() == sz`, so it reads that convenience as a claim that a byte-wide value can hold a function body. A 1-byte LOAD or STORE through a function pointer -- the INT3-patch check `v1 = *(code **)(v3 + 0xd4); if (*v1 != 0xcc) v5 = (*v1)(); *v1 = f();` that packers and anti-debug stubs are built from -- therefore types the loaded and stored bytes `code`, `INT_EQUAL` spreads that to the compared constant, and the merged high is declared `code v4; // al`, printed `void v4;` by the C back-end. A scalar with no width cannot be declared, assigned or added in C, and the byte the code manipulates loses its type entirely; the arithmetic that follows is then re-cast back (`v4 = (char)((char)v4 + '$')`). When on, `kuna_codescalar::blocks_value_type` declines the pointee on the value side of a LOAD/STORE propagation, and the same test suppresses the STORE's cast to it (which prints `(void)`), so the value falls back to the size-correct default and the store needs no cast. The POINTER keeps its own `code *` type, so the indirect call still renders `(*v1)()` and a `code **` load is untouched -- only a pointee whose metatype is `TYPE_CODE` is refused.", "use_when": "A local is declared `void` with no `*` and then used as a number -- `void v4; // al` followed by `v4 = (void)f();` and `v4 = (char)((char)v4 + '$')` -- or a store through a recovered function pointer carries a `(void)` cast. On by default: a `code` value type is never right, so blocking it can only replace a widthless scalar with the width the access already has. Flip OFF to restore upstream Ghidra's reading, in which the size-1 `code` type satisfies `propagateFromPointer` for any 1-byte dereference.", "example": "option codescalar off", "source_decompiler": "kuna", "inspiration": "RE-friction round 9 (`decompiler-emits-void-scalar`): two testers on two crackmes reported a scalar local declared `void` and then used in byte arithmetic, on `crkme.exe` 0x44ac6b an INT3-detection stub that patches its own callee", "change_kind": "type-inference", "tier": "transform", "symptoms": ["a local is declared void with no pointer star and then assigned or added", "a call result is cast (void) and stored", "void v4; // al followed by (char)v4 arithmetic", "a byte loaded or stored through a function pointer has no scalar type"]},
  {"option": "boolbyte", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "type-propagation", "strength": "HARD", "rewind": "P5", "issue": "kuna-boolbyte", "summary": "Declare a byte whose every read is a truth test as `bool` instead of `char`. `TYPE_BOOL` is only ever minted as the OUTPUT of a comparison -- `TypeOpEqual` and the other `booloutput` ops return `getBase(size, TYPE_BOOL)` -- and nothing in `ActionInferTypes` mints it for the value being compared: `TypeOpEqual::getInputLocal` votes `getBase(1, TYPE_INT)` for that, and the cached one-byte `TYPE_INT` is the ASCII `char` (\"Char is preferred over other int types\", `TypeFactory::cacheCoreTypes`). So a flag byte comes out `char`, as coreutils basename -O0 shows: `void sub_2b45(unsigned long a0, long a1, char a2)` whose only use of `a2` is `v1 = (a2) ? 0 : 10`, where the program declared `_Bool use_nuls`. When on, `kuna_boolbyte::truth_value_type` walks the byte's transitive readers through value-preserving identity -- COPY, MULTIEQUAL and INT_ZEXT, breadth-first with a visited set and a ten-hop cap, so the cap is a property of the graph rather than of the pop order, and running out of hops refuses -- and offers a `bool` candidate when every terminal read is a truth test and at least one of them is a real one: a CBRANCH condition, a BOOL_NEGATE/AND/OR/XOR operand, or an INT_EQUAL/INT_NOTEQUAL against zero. CPUI_INDIRECT is NOT value-preserving and refuses on both sides of the walk: an INDIRECT output is the value after the op it annotates, so a call whose callee may have written the storage breaks the chain in both directions -- crossing it would declare `bool` for a byte a callee fills with 200. A RETURN of the value is neutral, carrying neither acceptance nor refusal, because a `_Bool`-returning function is indistinguishable from a `char`-returning one from inside the callee. Everything else refuses, the call argument most deliberately: `putchar(c)` must not make `c` a bool just because something else also tests it. A Varnode the function itself writes must additionally carry a non-zero mask of at most 1, which is `ActionNonzeroMask`'s transitive \"every def writes 0 or 1, or copies something that does\" fact, computed in the pass that runs immediately before this one -- and it is what lets `x == 1` and `x & 1` count as truth tests for a proven flag while refusing them on an unproven byte, where they distinguish 1 from 2 and test parity respectively. A function INPUT has no def and always carries the full mask, so for a parameter the use shape is the whole of the evidence: a byte the function only ever branches on is what a `_Bool` parameter looks like from the inside. The candidate is FOLDED into the same `getLocalType` fold by `Datatype::type_order` rather than installed as a replacement seed, so a more specific vote still wins -- `SUB_BOOL` is 10, which beats `SUB_INT_CHAR` 19 and `SUB_UINT_PLAIN` 16 but loses to a callee's locked parameter type. Nothing else in the type system moves: the `TYPE_BOOL` propagation gate that refuses to carry bool into a Varnode whose mask exceeds 1 is left exactly as upstream wrote it, so the seed cannot travel along a copy chain into a value that can hold more than 0 or 1. One printer arm comes with the declaration: `CastStrategyC::isSubpieceCast` (cast.cc:411-432) lists the destination metatypes a SUBPIECE may print as a cast and TYPE_BOOL is not among them, because upstream never puts one there, so without the arm a truncation into the new `bool` falls to the functional form and prints the raw `SUB41(x,0)` p-code intrinsic -- an undeclared identifier, the class `subright` exists to keep out of the emitted C. With the option on such a truncation prints as a cast, and whole-binary counts of the intrinsic go DOWN rather than up (tar -O2 4 -> 1, ls -O2 1 -> 0, du -O0 1 -> 0). The cast is `(bool)x` only when every bit of `x` above the byte is proven zero, and `(bool)(unsigned char)x` otherwise: a C conversion to bool tests the whole operand, and a bool can reach a truncated byte through a sibling copy (`unsigned char c = x & 0x201, d = c; ... d = 0;`), where `(bool)(x & 0x201)` would be 1 for x = 0x200.", "use_when": "On by default: a one-byte parameter or stack local whose every use in the body is `if (v)`, `!v`, `v == 0`, `v ^ 1` or `v ? a : b` -- the shape a `_Bool` takes once the compiler has erased the type -- is declared `bool`, so a flag argument reads as a switch (`hash_find_entry(..., bool delete)`, gnulib's `bool ok` chains, option parsing) instead of as a character. Turn it off when you need kuna's declarations to stay in the upstream Ghidra vocabulary, when you are looking at code that really does pack flags into character data, or when a script addresses locals by the generated `vN` names an older run printed: §6 merges by type, so a `bool` and a `char` in one storage stop being merge candidates and two `bool`s start -- over eight whole binaries 10 of the 105 functions the default changes gain or lose a declaration, or have an expression come to name the other half of a split, and the remaining `vN` locals renumber. The flag constants re-render with the type (`v = '\\x01'` becomes `v = 1`). Off is the upstream fold exactly: the candidate is never offered and the printer's truncation arm is not consulted.", "example": "option boolbyte off", "source_decompiler": "ida", "inspiration": "IDA's Hex-Rays declares a byte whose only uses are conditions as `bool`, and its microcode carries a `mop_b` boolean kind for the same values; this rule reaches the same declaration from Ghidra's own lattice, by supplying the `TYPE_BOOL` candidate the `getLocalType` fold never has. Measured over the 444-slice decbench type sweep: 866 one-byte variables kuna commits to `char`/`unsigned char` have DWARF `_Bool` ground truth, and 538 of them are read only as truth values.", "change_kind": "type-inference", "tier": "core", "symptoms": ["a parameter is declared char and every use of it is if (a0) or !a0", "a one-byte local is char but only ever assigned 0 and 1", "a flag argument reads as a character, so a call site looks like it passes text", "char v1; followed by v1 = 0; v1 = 1; if (v1)", "DWARF says _Bool and kuna says char or unsigned char"]},
  {"option": "charbyte", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P5", "subphase": "type-propagation", "strength": "HARD", "rewind": "P5", "issue": "kuna-charbyte", "summary": "Keep `char` for a byte loaded through a `char *` when the only unsigned vote on it is the zero-extension x86 uses to load every byte. `movzx` loads a byte whether the program meant it signed or not, so `*p` is a LOAD followed by INT_ZEXT, and `TypeOpIntZext::getInputLocal` votes `getBase(1, TYPE_UINT)` for the byte; in `getLocalType`'s fold `SUB_UINT_PLAIN` (16) outranks `SUB_INT_CHAR` (19), so the byte starts propagation as `uint1`. The `char *` that later reaches the LOAD's address cannot displace it -- `propagateTypeEdge` only moves a Varnode to a MORE specific type -- and the byte then pushes `unsigned char *` back over the pointer, which it does win. A string walk comes out in the wrong vocabulary, as coreutils fmt -O2 get_line shows: `v1 = (unsigned char *)*dat_c100; v7 = strchr(\"([\\'`\\\"\",(int)(char)*v1);`, and every mbrtowc wrapper takes `unsigned char *a1` and passes `(char *)a1`. When on, `kuna_charbyte::note_edge` watches the LOAD edges while `ActionInferTypes` propagates and records each byte whose pointer carries `char *` while the byte holds `uint1` only because of the zero-extension -- seen from whichever end the propagation order reaches first. If anything was recorded, the pass starts over: the local types are rebuilt, `kuna_charbyte::seed_char` seeds those bytes `char` (the fold they would have had without the zero-extension's vote), and propagation runs again by the unchanged rules. No edge is overridden: overriding the pointer edge instead would leave everything the byte had already typed `uint1` behind, and `char v1; ... v1 == 0xe9` is never true in C, whereas re-seeding lets the constant take `char` with the byte (`v1 == '\\xe9'`). \"Only because of the zero-extension\" is checked on the readers: an INT_ZEXT must read the byte and no other reader may vote TYPE_UINT, so a byte that is masked, compared unsigned, shifted right, divided unsigned or passed as an `unsigned char` argument keeps `uint1`. The widened value is never claimed -- the INT_ZEXT output keeps its type and the printer shows the zero-extension of a `char` as the `(unsigned char)` cast it is -- and a byte read through an `unsigned char *` never meets a `char *`, so a byte from an `unsigned char` table stays `unsigned char`. The second propagation is kept only if every Varnode ends at the first propagation's type or the same type with `uint1` read as `char`, through pointers and arrays (`kuna_charbyte::keep_or_restore`); otherwise the first propagation's types are put back, because a fresh propagation can tip a contest the rule has no stake in (bash -O2 param_expand: a string pointer joined with an `int *` parameter came out `int *`). Two kinds of byte are never retyped, recorded or not (`kuna_charbyte::read_without_cast`, `kuna_charbyte::is_counter`): a byte that reaches a CALL, CALLIND or CALLOTHER argument, a RETURN or a BRANCHIND switch, directly or through copies, joins and one-byte arithmetic -- kuna shrinks a call argument to the byte when the `movzx` that widened it folds away, and C would sign-extend a `char` argument with no cast to show it (`logit(\"%d\",c)` passes -128 for 0x80 where the binary passes 128), and a `char` switch variable never reaches `case 0x80:` -- and a counter, whose own one-byte sum or difference is stored back through the address it was loaded from (`*p = c + 1`), because the only `char` it meets is that sum's default signed vote. Either one in the second propagation, including a sibling byte read through the retyped pointer (`logit(\"%d\",a0[1])`), puts the first propagation's types back, and so does a byte that took `char` while the pointer it is loaded through did not, which would print as a cast of its own load (`(uint4)(uint1)(char)v2[1]`), or a pointer that took `char *` while one it copies into or out of stayed `unsigned char *`, which would print as two variables joined by a cast (`v5 = (unsigned char *)&a0[1]`). The second propagation runs only in a function where something was recorded.", "use_when": "On by default: a byte read through a `char *` whose only unsigned vote is the `movzx` that loaded it is declared `char`, so a string walk reads in C's own vocabulary (`char *a1`, `if (v3 != '\\t')`, `*v4 == '-'`) instead of `unsigned char *a1` with `(char)` casts and numeric constants (`if (v3 != 9)`, `*v4 == 0x2d`, `strchr(\"...\",(int)(char)*v1)`, `mbrtowc(a0,(char *)a1,...)`). It retypes a byte only where C shows how each read of it is widened: the zero-extension prints as the `(unsigned char)` cast it is (`__ctype_b_loc()[(unsigned char)c]`) and constants compared with it become character literals; on 34 binaries every distinct value-bearing form that reads a retyped byte, on changed and unchanged lines alike, evaluates the same as the off arm's for all 256 byte values. A byte that reaches a call argument, a return or a switch, where C would sign-extend a `char` silently, and a counter whose own sum is stored back keep `unsigned char`, and so does every byte read through the same pointer. Turn it off when you need kuna's byte types in upstream Ghidra's vocabulary, or when diffing against output from before it was on: because the speculative merge only joins variables of one type, a `char` byte and an unrelated `unsigned char` value that used to share a declaration can split into two (or two `char *` join), renumbering the `vN` locals after them, and a constant added to such a byte prints the way kuna prints arithmetic on any `char` (`c + '\\xd0'` for `c - 0x30`). With `structsynth` on, a synthesized struct whose pointer field moves from `unsigned char *` to `char *` can become identical to one already synthesized and share its name, which shifts the `struct_N` numbers of the functions decompiled after it.", "example": "option charbyte on", "source_decompiler": "ida", "inspiration": "Hex-Rays declares a byte read through a `char *` as `char` and prints the `movzx` that widens it as `(unsigned __int8)c`; this rule reaches the same declaration from Ghidra's own lattice without overriding any edge: it re-seeds the byte `char` -- the fold it would have had without the zero-extension's vote -- and lets the unchanged propagation rules run again from there.", "change_kind": "type-inference", "tier": "core", "symptoms": ["a string pointer is declared unsigned char * and its bytes print as (char)*p", "a comparison against a character constant prints as a number: v3 != 9, *p == 0x2d", "a wrapper takes unsigned char *a1 and passes (char *)a1 to mbrtowc or strlen", "strchr(\"...\",(int)(char)*v1)", "a loop variable that walks text is unsigned char"]},
  {"option": "ptrfromuse", "values": ["off", "byte", "void"], "default": "void", "destructive_as_default": false, "phase": "P5", "subphase": "type-propagation", "strength": "HARD", "rewind": "P5", "issue": "kuna-ptrfromuse", "summary": "Type a function parameter that is only ever used as a memory base as a pointer. `ActionInferTypes` seeds every Varnode from local op semantics and then pushes the seeds along the def-use graph, and neither half can reach a parameter the function dereferences at a constant offset. `TypeOpIntAdd::getInputLocal` votes `int<N>` for each `p + k` reader, so the seed fold has no pointer candidate at all; the pointer that DOES exist -- `TypeOp::propagateToPointer` types the `p + 8` result `undefined8 *` -- never travels back, because `TypeOpIntAdd::propagateType` (typeop.cc:1217) refuses to carry a pointer from an output to an input. coreutils fmt -O2 with `libctypes off` shows both halves at once: `put_word`, which dereferences its argument at offset 0, is `void sub_3000(unsigned long *a0)`, while `get_line`, which dereferences the same kind of argument at +8 and +0x10, is `void sub_3420(long a0, unsigned int a1)`. When on, `kuna_ptrfromuse::pointer_from_use` walks each function input's transitive descendants through COPY/MULTIEQUAL/INDIRECT identity, the base slot of PTRADD/PTRSUB and constant-offset INT_ADD (breadth-first bounded worklist, visited set, ten hops, so the cap is a property of the graph rather than of the pop order) and offers a pointer candidate when at least one terminal use is the address operand of a LOAD or STORE. The candidate is FOLDED into the same `getLocalType` fold by `Datatype::type_order`, not installed as a replacement seed, so a more specific vote still wins: a `FILE *` arriving from a callee's locked prototype is `SUB_PTR_STRUCT` and outranks this rule's `SUB_PTR`. The walk refuses on any use no pointer survives -- INT_MULT, INT_DIV/SDIV/REM/SREM, INT_2COMP/NEGATE, a shift, a PIECE that assembles the value from halves, any float op, a comparison against a non-zero literal, and a CALL whose callee prototype has already committed that argument to a non-pointer -- and a variable-offset addition is neutral, carrying neither acceptance nor refusal, because either operand of `x + y` could be the base. A *literal* addend is the one ambiguous shape: `p->field` and `table[i]` lower to the same INT_ADD of a value and a constant, and only the constant says which operand is the base, so the walk asks `ActionConstantPtr::isPointer`'s own question of it -- the default data space's pointer bounds, `resolveConstant` into that space, then the global scope -- and grades the answer. A constant that resolves to a global object kuna knows about IS the base, so the walked value is a subscript and the candidate is refused. A constant that is address-like but names nothing settles nothing and is neutral, exactly like a variable addend: that is what a stripped image's .bss table looks like, and also what a genuinely large struct looks like (bzip2's 0x13f0-byte bzFile is read at +0x1394). Anything below the bound is an ordinary field offset and the walk carries on. Only inputs whose width is the default data space's address size are considered (eight bytes on the 64-bit images the option is named for, four on a 32-bit image), and only storage the prototype model says could carry a parameter, which keeps a segment-base register such as x86-64's FS_OFFSET out of it. `byte` points at one unknown byte, which `realtypes` renders `char *`; `void` points at nothing and renders `void *`. Only function inputs are considered: a parameter is one declaration per function rather than one per local, and it is the argument-position match that a metric or a reader sees first.", "use_when": "On by default (`void`): a pointer-width parameter whose every memory use is `*(T *)(a0 + k)` -- a struct or buffer pointer once the compiler has folded the field offsets in -- is declared `void *a0`, and its field accesses keep their byte arithmetic with a cast on the base (`*(long *)((long)a0 + 0x28)`). With `protoorder` at its default the callee's `void *` also types the matching argument at each call site, which is how a wrapper that only forwards its arguments (the `qsort` comparators and hash-table callbacks whose declared parameters are `void const *`) is declared with `void *` parameters: over the 444-slice decbench sweep the default moves 204 functions onto a perfect type_match and 85 more up, none down. `byte` declares `char *a0` instead and rewrites the arithmetic into indexing (`*(long *)&a0[0x28]`, `RulePtrArith`); it moves 7 functions onto perfect. Turn it off to get upstream's integer declaration and uncast arithmetic back, when a script matches body text an older run printed (the `(long)a0 + k` casts, and the local renumbering where a retyped value merges differently), or when a parameter really is an integer the function also uses as an address -- committing it to a pointer is a claim the width-only integer spelling did not make. Prefer `libctypes` where the argument reaches a libc call: a named `FILE *` outranks this rule's candidate and is kept. KNOWN AMBIGUITY: an index into a global array is the same shape as a base at a constant offset -- both are `INT_ADD(value, constant)` -- so the rule grades the constant. A named global object refuses the candidate (`char table[256]; int lookup(long i){return table[i];}` keeps `long a0` and its named global; typing it would also replace `table` in the body with its raw address). An address-like constant that names nothing is neutral, so a parameter used ONLY that way is still declined, but one that is also dereferenced at an ordinary field offset is typed on the strength of that other use -- which is right for a large struct and wrong for a subscript into an unnamed table that the function also uses as a base. A constant below the space's pointer lower bound is always read as a field offset.", "example": "option ptrfromuse off", "source_decompiler": "angr", "inspiration": "angr's pointer-arithmetic type closure (`analyses/typehoon/simple_solver.py`): anything loaded from or stored to is a pointer, and `ptr + const` is the same pointer. angr commits 43.4% of its arguments to a pointer type on coreutils fmt where kuna commits 30.6% and leaves 43.0% at the 8-byte integer default.", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["a parameter is declared long or unsigned long and is only ever dereferenced", "every use of an argument is *(T *)(a0 + k) but the argument is not a pointer", "one function takes T *a0 and its sibling takes long a0 for the same kind of argument", "an argument dereferenced at offset 0 gets a pointer type but one dereferenced at +8 does not"]},
  {"option": "charptr", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P5", "subphase": "type-propagation", "strength": "HARD", "rewind": "P5", "issue": "kuna-charptr", "summary": "Commit a pointer-width parameter or stack local the program only ever uses on characters to `char *`. `ptrfromuse` decides that a value IS a pointer; nothing says what is on the other end, and its shipped `void` says so honestly. The element type is usually already on the table -- `strlen`'s declared `char *`, a `%s` conversion `formatstring` resolved, a walk that reads the object one byte at a time -- and it is lost for the same structural reason the pointer was: `TypeOpCall::getInputLocal` (typeop.cc:689) states the callee's declared parameter type about the Varnode that IS the call's operand, and `propagateTypeEdge` will not carry a pointer back over an INT_ADD, a MULTIEQUAL, or the store/load pair an -O0 spill puts in between. coreutils `realpath` -O0 shows it: `bool sub_2bbc(void *a0, void *a1)` for `path_prefix(char const *, char const *)`, whose body walks both arguments a byte at a time. When on, `kuna_charptr::char_pointer_from_evidence` collects that evidence with the bounded breadth-first walk `ptrfromuse` uses -- COPY/MULTIEQUAL/INDIRECT/CAST identity, the base slot of PTRADD/PTRSUB, a literal-addend INT_ADD graded by `ActionConstantPtr::isPointer`'s own three-way question, ten hops, visited set -- and asks of every use whether it is about CHARACTERS rather than whether it is a dereference at all. Three uses say yes: the value reaches argument i of a call whose callee DECLARES `char *` there (declared meaning stated from outside the decompile -- a `libproto`/`libcsigs` signature, a `libctypes` shell, DWARF, a demangled name, `--assert prototype`, or the per-call-site override `formatstring` installs for a resolved `%s` -- read through `declared_input_type_local`, so `protoorder`'s recovered callee vote is deliberately NOT evidence: counting it was measured on the 444-slice sweep and scored lower); the value is dereferenced AT THE BASE and every such dereference is one byte wide, or it is stepped one byte at a time (PTRADD of element size one); and the value is defined by or compared against a constant that resolves to a character array in the image. A byte read at a FIXED non-zero offset is NOT evidence -- that is a one-byte struct field, and reading it as a character is what turns coreutils `ginstall`'s `announce_mkdir(char *dir, void *options)`, which reads the `bool` at `options + 0x3c`, into a wrong `char *`. The candidate is one more vote in the same `getLocalType` fold, folded by `Datatype::type_order`, and it may additionally REFINE a pointer that points at nothing (`void *`, `undefined1 *`) while a pointer at a NAMED, aggregate or WIDER pointee -- `FILE *`, `stat *`, `long *`, a synthesized `struct_3 *` -- is left exactly as it was. `unsigned char *` is deliberately not protected: the guard reads the type IN FLIGHT at the moment the vote is taken, so where the `uint1 *` spelling would only have arrived by later propagation the `char *` vote is already there and wins -- the element width agrees and only its signedness moves, which is the same call `charbyte` makes one level down. One refusal anywhere withdraws the candidate: a dereference or element step wider than a byte, a call argument the callee declared as some other pointer or as a scalar, integer multiplication/division/shift/PIECE, a float op, a comparison against a non-zero literal that is not a string, and an INT_ADD whose constant addend resolves to a global object. Only storage a reader sees declared is considered: a function input the prototype model could place a parameter in, or a Varnode in the stack space; a type-locked Varnode and one seeded from a type-locked symbol are never touched.", "use_when": "Off by default. Turn it on when a string parameter or stack local reads as `unsigned long` or `void *` and you want the `char *` the body already proves -- most visibly at -O0, where every value is spilled to the frame and reloaded, which is exactly the hop the ordinary propagation cannot cross. Measured on the 444-slice decbench type sweep (10,748 functions): 4 functions move onto a perfect `type_match` and 14 more up, 3 move down and none off perfect; aggregate +4.93. The reason that is small rather than large is measured and published in `docs/features/charptr/analysis.md`: ground-truth `char *` is the largest class in the corpus (14,645 of 65,715 scored variables) but half of it lives in registers, which kuna does not export at all, kuna's ARGUMENT recovery is already within 6.6 points of Binary Ninja's, and 932 of the 1,648 stack misses are frame slots reported as `undefined8` because the value that lived there was copy-propagated into a register -- a chapter-06 gap, not a chapter-05 one. The declared-callee half alone is worth +0.14 aggregate over the whole corpus, which is why it is not a separate strength. KNOWN COST: committing a pointer forfeits the width-only free pass an 8-byte scalar gets, and `char *` changes how the body reads -- `*(long *)((long)a0 + 8)` becomes `*(long *)&a0[8]` (`RulePtrArith`), and a byte compared through it prints as a character constant. The three measured regressions are of exactly those two shapes: a correct `void *` parameter refined to `char *` (coreutils `od::print_long_double`), and an `__off_t` the body also uses as an address (coreutils `head::elide_tail_bytes_pipe`).", "example": "option charptr on", "source_decompiler": "binja", "inspiration": "Binary Ninja recovers `char *` for 71.4% of the ground-truth character pointers held in stack locals and 71.5% of those held in arguments, against kuna's 44.8% and 64.9% on the same 444 slices; its advantage comes from reading what the program DOES with a value rather than from what propagation can carry. This rule takes the readable half of that -- the call-site declaration, the byte-wide dereference at the base, the one-byte element step -- without changing how types propagate.", "change_kind": "type-inference", "tier": "transform", "symptoms": ["a string parameter is declared unsigned long and every call site passes it to strlen or printf", "a buffer walked one byte at a time is declared void *", "two functions take the same string and one says char * and the other unsigned long", "a -O0 parameter is an integer although the callee's prototype declares char *", "bool sub_2bbc(void *a0,void *a1) for a function that compares two strings byte by byte"]},
  {"option": "pebnames", "values": ["off", "auto", "on"], "default": "auto", "destructive_as_default": false, "phase": "P5", "subphase": "type-propagation", "strength": "HARD", "rewind": "P5", "issue": "GH-468", "summary": "Type the Windows thread-environment segment base as `TEB *teb`, so reads through it render as the TEB and PEB fields a reverser recognises. A Windows user-mode thread keeps its Thread Environment Block at the base of `GS` (x86-64) or `FS` (x86), and x86 SLEIGH lowers a segment-prefixed operand to `GS_OFFSET + disp` / `FS_OFFSET + disp` over a register input nothing in the function writes. With no type on that register the anti-debug probe `MOV RAX,GS:[0x60]; MOVZX ECX,byte [RAX+2]; MOV EAX,[RAX+0xBC]` renders as `long long v1; // gs_offset` and `*(char *)(*(long long *)(v1 + 0x60) + 2)`: magic offsets into nothing, with `gs_offset` being only the lowercased register name. Before heritage, when the raw p-code reads the segment-base register and never stores through it, a type-locked and name-locked local Symbol `TEB *teb` is mapped over its input storage, and ordinary pointer-arithmetic and field rendering does the rest: `teb->ProcessEnvironmentBlock->BeingDebugged`, `teb->ProcessEnvironmentBlock->NtGlobalFlag`, `teb->Self->ProcessEnvironmentBlock`, `teb->ThreadLocalStoragePointer`, `teb->StackLimit`. A field is named only when its offset, name and size agree in every PDB-derived layout the Vergilius Project publishes (x64 XP SP2 to Windows 11 25H2; x86 XP SP3 to Windows 10 22H2 plus every WOW64 TEB32/PEB32), so a release-dependent name such as `ApiSetMap` or `CrossProcessFlags` stays `field_0x<off>`. Both structures are variable-length at the largest published size, so an offset past the end stays an integer addition instead of naming a field of a phantom neighbouring TEB. A function that writes through the base (on x86, every function linking an SEH registration record through `fs:[0]`) is left untyped, because that record's stack layout is inferred from how the untyped base is used. The base is only a TEB in user mode -- kernel code keeps its KPCR there and firmware nothing -- so `auto` acts only on an image the loader proved is a Windows GUI or console PE (optional-header subsystem 2 or 3); `on` trusts a Windows compiler spec alone. The register is GS on x86-64 and FS on x86, so the x86-64 ELF canary at `fs:0x28` and the x86 one at `gs:0x14` never reach the pass; both modes also require a Windows compiler spec, which keeps a non-Windows image that does read that register untyped.", "use_when": "On (`auto`) by default: a Windows PE whose C reads a `// gs_offset` or `// fs_offset` local through constant offsets -- anti-debug `BeingDebugged`/`NtGlobalFlag` probes, MSVC `thread_local` access through `gs:[0x58]`, `__chkstk` stack-limit checks -- gets the field names. Besides the names, a reader's types can move: a stub that returns the TEB or PEB pointer is declared `_TEB *` or `PEB *`, a `BeingDebugged` read is `uint1` rather than `char`, a local that keeps the PEB pointer is declared `PEB *`, and an opaque field compared with a pointer gains a cast. A variable that holds `teb->Self` or `teb->ProcessEnvironmentBlock` on one path and an unrelated value on another takes the pointer type, so the unrelated value gains a cast (seen once, in an obfuscated binary); a parameter merged with such a value can take it too. Set `on` for code with no PE container that still runs as a Windows user-mode thread: shellcode or a raw `.text` blob loaded under a `windows` compiler spec, a stage bytechunk, or the Ghidra front-end, which never supplies the loader fact `auto` needs. Set `off` to restore the untyped register. It never fires on a kernel driver, an EFI image, a COFF object or anything loaded under a non-Windows compiler spec; a Symbol the user already mapped over the register, a program type already named `TEB`/`PEB` with a different layout, or a function whose segment-override instructions store through the base (an x86 SEH frame) makes it decline. A store through a pointer loaded from the TEB (`mov eax,fs:[0x18]; mov [eax],esp`) is not seen by that check and is typed. Measured in DIV-175: every function that changes is a segment-base reader, no call is added or removed, and 0/675 datatests move even with `on` forced as the default.", "example": "option pebnames on", "source_decompiler": "kuna", "inspiration": "GH-468 finding 6: an MSVC anti-debug crackme reads the PEB through GS:[0x60] and probes BeingDebugged ([PEB+2]) and NtGlobalFlag ([PEB+0xBC]); kuna rendered `long long v15; // gs_offset` followed by `*(long long *)(v15 + 0x60) + 0xbc` compares", "change_kind": "type-inference", "tier": "transform", "symptoms": ["a local commented // gs_offset or // fs_offset is read through constant offsets", "an anti-debug check reads *(char *)(*(long long *)(v1 + 0x60) + 2)", "PEB fields such as BeingDebugged or NtGlobalFlag render as magic offsets", "a thread-local or stack-limit read renders as *(long long *)(v1 + 0x58) or (v1 + 0x10)"]},
  {"option": "structsynth", "values": ["off", "param"], "default": "param", "destructive_as_default": false, "phase": "P5", "subphase": "aggregate-union", "strength": "HARD", "rewind": "P5", "issue": "kuna-structsynth", "summary": "Synthesize a structure type from the constant-offset dereferences of a pointer parameter, so the reads render as named fields instead of raw offset arithmetic. A stripped binary keeps no record of the aggregate a pointer points at, so `put_word` in a `-O2` coreutils `fmt` renders as `void sub_3000(unsigned long *a0)` with `v2 = *(unsigned int *)&a0[1]` and `v3 = (unsigned char *)*a0` -- the reader carries `offset 0 is a character pointer, offset 8 is a 4-byte count` in their head across every function that touches the same object. With `param`, once type propagation has reached its fixpoint, every live LOAD/STORE whose address peels to `(parameter, constant)` is bucketed by offset, a `struct_N` is completed with one field per observed offset, and the parameter varnode is type-locked to `struct_N *`; the existing pointer-arithmetic and field rendering does the rest, giving `void sub_3000(struct_0 *a0)` with `v2 = a0->field_0x8` and `v3 = a0->field_0x0` over `struct struct_0 { unsigned char *field_0x0; int field_0x8; }` -- offsets 0 and 8 of the real `WORD { char const *text; int length; ... }`. The pass invents a layout, never pointer-ness: the base must ALREADY carry a pointer type, so `fmt`'s `get_line`, whose first parameter is the real `FILE *` but is still typed `long`, is left alone, and a pointee that is a named composite (DWARF, a parsed declaration, a libc shell, an earlier synthesized structure) always wins. The evidence is deliberately narrow. Only `COPY`/`CAST` and constant pointer arithmetic are peeled, never a phi, because chasing a loop-carried MULTIEQUAL to its root turns `while (*p) p = &p[1]` into offsets `{0,1}` and would synthesize a two-field structure over a string; a base that reaches a phi at all declines, as does one used as an integer, one with a non-constant offset term (that is an array, not a structure) whether or not this function dereferences the result, one with fewer than two distinct offsets or no access at offset 0, a negative or >= 0x8000 offset, a run of equal-width offsets on a regular grid, which reads as an array rather than a layout (Howard NDSS 2011 section 4.5: three or more outright, two when the element is narrower than a pointer, so `int pipefds[2]` and `arg[0]`/`arg[1]` over a `char *` decline while a pair of pointer-sized fields does not; a gap does not break the run below pointer width, because a function that writes ten of a buffer's twelve bytes is ordinary, but it does break it at pointer width, where a record with an untouched member between two observed ones is the common case; an optimized `int fd[2]` that is also saved and restored as one 8-byte word has mixed widths and reaches the same decline through the layout rule instead, because both elements sit inside the wide access and nothing but a single field is left), a stack- or spacebase-rooted base, a type-locked base, and a function whose lattice never settled. At a conflicting offset the WIDEST access wins: a field wider than an access renders as a cast of the field, while a narrower one loses the field name entirely. A field commits to a signedness only when every access of its width does; a signed read and an unsigned or undefined one leave it `undefined<N>`, because a signed field makes C sign-extend a read the binary zero-extends into a call argument. The layout is then pruned so that a C compiler reading the exported header back puts `field_0xK` at offset K: an access whose width the header cannot spell at that width (`undefined3` is a 4-byte `unsigned int` there), one at an offset its own alignment does not divide, and one inside the bytes a wider access already claimed each become a hole instead of a field. What survives is naturally aligned and 1, 2, 4 or 8 bytes wide, and two such ranges either nest or are disjoint, so the declaration cannot overlap at all. The layout is then made dense and C-sized, which is about the body rather than the declaration: every byte from 0 to the end belongs to a member, a gap becoming an `undefined1 field_0x<hex>[N]` of alignment 1 at its own offset, because the printer names an in-range offset no field covers `field_0x<hex>` anyway while the exported header would render that gap as `undefined1 _pad<hex>[N]`, and address arithmetic reaches those offsets without a dereference; and the size is the end of the last surviving field rounded up to the widest surviving field's width, with that rounding covered by `undefined1` filler like any other gap, because a structure the decompiler sizes 0x15 and a compiler sizes 0x18 puts `a0[1]` at two different addresses. Past that end there is no member and no invented name: the printer spells the address as an element of the structure array plus a byte offset, which is still the address the access meant. Names are program-wide `struct_N`, handed out by a layout ledger that reads back every `struct_N` the program's type factory already holds. A measured layout is answered with a minted structure that CONTAINS it -- every claimed field at the same offset, the same width and the same type, and the structure at least as large -- and not only with one whose signature matches exactly, so two functions that read overlapping but unequal subsets of one record share a definition instead of getting a name each (`ls`'s two `fileinfo` comparators, and a stage test's `{0, 8}` and `{0, 8, 0xc}` readers; over twelve binaries it is 471 names where exact-signature dedup minted 515). Agreement on a shared field is exact: two types at one offset are a conflict rather than a refinement, and a field a reader could not type is its own answer, not a wildcard the other may fill in -- a wildcard rule was measured and it unifies records that merely share a shape. Containment stops being evidence once the container dwarfs the measurement or the measurement is too generic to name a record, so a structure that says strictly MORE than the reader measured must also claim at most twice as many fields and be at most four times its size, and the reader must claim two fields of which one is a typed pointer, or three without one: unbounded, an `ls` reader that measured the two words of a `hash_entry` is declared to hold a 200-byte record, and two integer words are how `struct stat`, gnulib's `cycle_check_state` and a list node all begin, so without the pointer rule `cycle_check`'s `struct stat *` is declared as its `cycle_check_state`. A pointer to code, `void` or an undefined type is not a typed pointer. A reader whose every claim past offset 0 is such a pointer is a table (an interface, an ops vector, a vtable) and is answered only by its own shape, since distinct tables differ only in how many slots they have: without that, `rsyslogd`'s seven `*_if_s` interface records all took one 144-byte layout. A reader with no typed pointer is answered only by a structure of its own size that claims nothing past the reader's last field, which may fill in the holes between its fields but not the bytes after them, because records that begin alike part ways after the last field a reader touched (every `ip` netlink reader measures the same `nlmsghdr` words ahead of a payload of its own, and dpkg's `pkg_queue` ends in the padding where `pkg_spec` keeps two flag bytes). No structure answers with a member inside the alignment padding a reader's claims leave between two of them, because records part ways in their first words too: every `bash` command record begins with an `int flags`, and `for_com` puts an `int line` at 4 where `if_com` pads before its first pointer. And a structure answers for a reader only if it lays exactly the reader's own members over every byte range the reader accessed without claiming a field (an unaligned word, a 16-byte copy): the engine's load/store ordering test compares two addresses without their widths, so a container that started a member inside such an access printed a four-byte load at offset 7 after the byte store at 8 that overwrites part of it. When containment runs the other way the minted structure cannot be widened -- `find_add` refuses a second definition of a held name and the completed structure is already shared by every function that took a pointer to it -- so the larger layout mints beside it and the smaller is SUPERSEDED: still defined, and handed out again only to a layout no live structure answers for. Because `decompile-all` decompiles in address order, the function that minted the smaller layout still names it, so after the batch exactly the results that spell a superseded name are decompiled again and each moves to the survivor where the survivor is in its reach; a redo that fails where the first pass succeeded keeps the first body. The bounds are not transitive, so a record can keep two names, but the sweep never mints a third, and the exported header declares one definition per name still in use. None of that happens under `kuna decompile`, which spawns one engine per function, and the streaming project export (`--stream`) has written a body before a name can be superseded, so it converges only as far as its write order allows.", "use_when": "On (`param`) by default: any stripped function that reads the same object at fixed offsets -- a linked-list or record walker, a parser state block, a callback context, anything whose listing would be a wall of `*(int *)(p + 0x18)` -- gets a `struct_N *` parameter whose reads render as `p->field_0x18`. It acts only on a parameter the engine already believes is a pointer, never on a stack slot, a global, or anything a declared, DWARF or libc type already names. The default was flipped on measured evidence: 0 of 675 datatest assertions move; over a 14-binary, 5,610-function `decompile-all` sweep (x86-64 coreutils/findutils/grep/gzip/bzip2/diffutils/tar at O0/O2 plus two ARM32 firmwares) 483 functions change: 350 are verified mechanically as the same statements with each parameter access respelled from its byte offset to a field, and the other 133 were read by hand as the same memory accesses spelled through the new pointee (an index rescaled to the structure's size, a filler array decaying to its address, constant byte stores merged into one store of the field, a load folded into its use, one more duplicated return block); no function in either arm has a `goto` whose label is missing, and every exported `offsetof(struct_N, field_0xK)` equals K. The known cost is decbench `type_match`, which compares pointee spellings by name and so cannot credit an anonymous `struct_0 *` against a ground-truth `WORD *`: over 444 slices the flip is worth -1 perfect function (959 -> 958, aggregate -0.05%). Its measured false positives are buffers of primitive elements that the array rule does not recognise -- a GMP limb array read at offsets 0 and 8 (coreutils `factor` `powm2`/`millerrabin2`), and character buffers whose writes are not all one width (`ls` `strmode`, `shred` `passname`). Functions that read overlapping parts of one record share one `struct_N`: the smallest minted structure that contains every field a function measured, under growth bounds and evidence rules. Against DWARF that is a trade rather than a free win: pooled claimed-field precision over 169 held-out builds is 0.8908 against 0.8902 for naming every reader's subset separately (0.8852 against 0.8855 on the last 34, chosen before any result under the padding rule was seen), recall is higher on every set, 6 of 177 builds lose precision (`kmod` and `grep` O2 under a point each), and 4 of the 258 parameters answered by a larger structure are given one measured from a different record (all `e2fsck` readers without a typed pointer, filled in between their own fields; two take `ext2_inode_large` for `ext2_inode`, the same record for its first 128 bytes). Set `off` for anything that depends on the exact spelling of a parameter type (a type-name matcher, a diff against another decompiler) or to restore the raw offset arithmetic. `struct_N` names are program-wide only where one process decompiles the whole program (`decompile-all`, a serial `decompile-project`); a sharded `decompile-project --jobs N` runs its workers with `structsynth off` because per-process numbering cannot give one header one layout per name. The layouts themselves are printed above each function by `structdefs` and live in the `decompile-project` header.", "example": "option structsynth param", "source_decompiler": "angr", "inspiration": "angr `simple_solver.py` `_determine`'s struct branch (bucket every `*(p + k)` access by `k` with `map_offsets_to_bases`, emit `struct_N { field_0; field_8; }` and declare `p` as `struct_N *`), with TRex USENIX Security 2025 section 3.3.3's dynamic-offset-is-an-array rule, SecondWrite PLDI 2013 section 5.1's running-max structure size, and Howard NDSS 2011 section 4.5's memset defense", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["a parameter is declared `unsigned long *` or `void *` and read at two or more fixed offsets", "the body is a wall of `*(int *)(p + 0x18)` casts over one pointer", "`*(unsigned int *)&a0[1]` appears where a record field is meant", "two functions read the same object and neither names its fields"]},
  {"option": "framelayout", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "naming-policy", "strength": "HARD", "rewind": "P6", "issue": "kuna-framelayout", "summary": "Report the union of every stack-frame slot any `restructure_varnode` pass recovered on the `decompile-all --json` `variables` surface, not only the slots that survived to the final pass. `restructure_varnode` re-derives the frame from the LIVE stack Varnodes and clears the previous pass's unlocked symbols first, so a slot whose spill store/load pair the dataflow folded into a COPY and then away is present in an early layout and gone from the last one. Dropping it from the emitted C is right -- there is no expression left to declare -- but the frame still has the slot. Each pass's NO_CATEGORY stack symbols fold into a per-Funcdata union (first writer wins, because the earliest pass saw the most dataflow standing), and any offset no parameter or surviving local already covers is appended. A slot the type system never committed to is reported as the width-only `undefined<N>` instead of the `char[N]` its array-of-unknown carrier renders as, and Ghidra's internal `$$undefNNNNNNNN` placeholder is renamed to its stack-view name `local_<hex>`. JSON surface only: no p-code, no emitted C, so GED and byte_match cannot move.", "use_when": "On by default (DIV-97). A consumer wants the recovered stack FRAME rather than the printed declarations -- the `decompile-all --json` `variables` array, which is what decbench's type_match metric reads and what IDA's stack view and Binary Ninja's variable list are the analogues of. Most visible at -O0, where every local is a spill slot and a constant-folded function ends up with an empty `variables` array although its frame has named slots. Flip OFF to restore the pre-DIV-97 surface: parameters plus only the stack symbols still live at the final restructure pass.", "example": "option framelayout off", "source_decompiler": "kuna", "inspiration": "decbench type_match: over 82,049 scored functions kuna reported 154,716 of the 184,522 ground-truth stack variables (84%) against angr's 203,329 and IDA's 178,337, and that recall gap was the whole of kuna's 4th place on the one metric it ranked worst on", "change_kind": "presentation-default", "tier": "core", "symptoms": ["the JSON variables array is empty although the function plainly has a stack frame", "fewer locals reported than the source declares", "a stack slot the disassembly writes is missing from variables", "a recovered slot is typed char[N] rather than N bytes of unknown type", "a variable is named $$undef00000000"]},
  {"option": "bytehonest", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "naming-policy", "strength": "HARD", "rewind": "P6", "issue": "kuna-bytehonest", "summary": "Spell a one-byte value the type system never committed to as the width-only `undefined1` on the `decompile-all --json` `variables` surface, instead of the `char` that `realtypes` renders its residual TYPE_UNKNOWN carrier as. `char` is the right rendering for the C TEXT -- it is the one-byte C type that reads as a value -- but on a machine-readable surface it asserts an element type the recovery never established: the program's own type there may as well be `_Bool`, `unsigned char`, a one-byte enum or one byte of a struct nobody split. The predicate is the datatype's own metatype and size (TYPE_UNKNOWN, size 1 exactly, never an array, so a `char[264]` buffer keeps its array-ness), not its spelling, so it holds with `realtypes` off too -- where the same row otherwise exports kuna's internal `xunknown1`. Parameters and stack locals only; the exported `size` stays 1 and the emitted C is byte-identical, so the same variable can read `char` in the `.c` text and `undefined1` in `variables[]`.", "use_when": "On by default. A consumer reads `variables[]` as a report of what kuna RECOVERED rather than as C source -- the `decompile-all --json`/`decompile-project` surface, which is what decbench's type_match metric scores and what IDA's stack view and Binary Ninja's variable list are the analogues of. Flip OFF to get the pre-feature surface, where an uncommitted byte is spelled exactly as the printer spells it (`char` under the default `realtypes`, `xunknown1` with `realtypes off`). The emitted C never changes either way.", "example": "option bytehonest off", "source_decompiler": "kuna", "inspiration": "decbench type_match: `undefined1` is scored as an uncommitted WIDTH and matches a ground-truth `char` and `_Bool` alike, where the committed `char` matches only `char`; over 5,298 scored functions of the frozen O0+O2 corpus (coreutils, grep, gzip, diffutils, bzip2, findutils) the flip moved 121 functions up and none down, 10 of them onto a perfect score. Ghidra reports the same fact as `undefined1` and IDA exports `_BYTE`", "change_kind": "presentation-default", "tier": "core", "symptoms": ["a JSON variable is typed char although the decompiler never established an element type", "a one-byte parameter of a boolean-valued function is reported as char", "the variables array claims a committed type the C text only picked for legibility", "xunknown1 appears as a type name with realtypes off"]},
  {"option": "ctypes", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P9", "subphase": "literal-format", "strength": "HARD", "rewind": "P9", "issue": "kuna-ctypes", "summary": "Spell the NAMED core types as the target's own C type names -- int4 -> int, uint1 -> unsigned char, float8 -> double, float10 -> long double, code * -> void * -- resolved against the compiler spec's decoded <data_organization>, so an 8-byte integer reads `long` on LP64 and `long long` on ILP32/LLP64. Extends `realtypes`, which relabels only residual TYPE_UNKNOWN; that split is why one function can declare `unsigned int v3;` and `int4 v1;` in the same block. Sizes no C type has (3/5/6/7, 16-byte integers) keep their undefined<N> form rather than being widened, because widening a truncating cast changes its meaning.", "use_when": "The emitted C uses Ghidra's internal type vocabulary (int4/uint1/float8/code) instead of C type names, or mixes the two within one function. ON in the `aggressive` preset, which `auto` selects for any binary under 500 KiB -- so this is the default rendering of `kuna decompile`, `decompile-all`, `decompile-project` and the web front-end. The shipped catalog default is OFF, which is what the XML datatest corpus runs at: 42 of its assertions pin the Ghidra spellings.", "example": "option ctypes on", "source_decompiler": "kuna", "inspiration": "user report: the same function declares `unsigned int v3;` and `int4 v1;`; Ghidra and IDA both keep a private vocabulary and ship a header, so neither emits valid C from the decompiler core", "change_kind": "presentation-default", "tier": "core", "symptoms": ["int4/uint1/uint4/float8/float10 appear in the emitted C instead of C type names", "the same function mixes `unsigned int` with `int4`", "`code *` appears as a function-pointer type", "the emitted C does not compile because its type names are not C"]},
  {"option": "dedupvardecls", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "naming-policy", "strength": "HARD", "rewind": "P9", "issue": "angr-duplicate-decls", "summary": "Collapse local-variable declarations onto one line per mapped ScopeLocal symbol (same containing symbol + same identifier, declaring the symbol's own type when the highs disagree about it) and then onto one line per fully-rendered signature (type + name + array adornment + storage comment), so a stack slot mapped onto many scalar HighVariables is declared once instead of one line per high (the scalar analogue of the existing composite-symbol declaration collapse).", "use_when": "On by default (DIV-7): a stack slot is declared once even when many same-named scalar HighVariables share it (e.g. x86_64/cvs main, where the per-high rendering repeats `int4 option_index; // stack - 0x3c` ~200x), including when two live ranges of the slot recovered different types and so rendered two differently-typed declarations of one name -- invalid C (DIV-52). Set OFF to restore the one-declaration-per-high rendering.", "example": "option dedupvardecls off", "source_decompiler": "angr", "inspiration": "test_decompiling_x8664_cvs; angr variable recovery (one variable per storage location, declared once); main", "change_kind": "presentation-default", "tier": "core", "symptoms": ["the same local declared once although many HighVariables share the stack slot", "flip off to see one declaration line per high (e.g. int4 option_index repeated hundreds of times)"]},
  {"option": "declhightype", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "naming-policy", "strength": "HARD", "rewind": "P9", "issue": "repipe-byte-comparisons-incorrectly-dereference", "summary": "Declare a merged local at the data-type its own body was cast against. Upstream writes a local's declaration from the Symbol it is mapped to (`PrintC::emitVarDecl`, printc.cc:1719 `sym->getType()`), and that type is the merged HighVariable's -- the type of the member Varnode `HighVariable::getTypeRepresentative` (variable.cc:377-396) picks as the most specialized. The same type is what `ActionSetCasts` reads through `Varnode::getHighTypeReadFacing` when it decides whether a use needs a cast, so upstream's declaration and body agree by construction. kuna's printer walks HighVariables instead of Symbols and takes the declared type off its DECLARATION representative -- the first address-tied member Varnode, else member zero -- which is a different Varnode. For a high whose members all carry one type the two agree and nothing moves. For a merged register high whose members carry different types, the declaration states a type that no use in the body was ever checked against, and a pointer is the case where that is not cosmetic: an obfuscated x86-64 state machine merges one RAX across the whole function, so `MOVSX EAX,byte ptr [RAX]` after a `char *`-returning call prints as `unsigned long long *v9;` plus an uncast `v8 = (uint4)*v9;`. The cast machinery saw a one-byte pointee and correctly left the dereference alone; read against the declaration the same token is an eight-byte load, seven bytes past the object and with the sign extension dropped. This option declares the type representative's type -- the `sym->getType()` upstream would have written -- so the declaration agrees with the casts already in the body. It is presentation only: no cast, no statement and no p-code moves, and the declaration representative still supplies the storage comment, the array adornment and the mapped-Symbol composite override, all of which outrank it.", "use_when": "On by default. Keep it on when a declaration contradicts its own uses: the tell is one identifier that is declared a pointer and dereferenced at a different width than the declared pointee (`unsigned long long *v9` with `(uint4)*v9`), or declared one pointer type and assigned another without a cast. It is the standard rendering wherever one register is merged across a whole function -- obfuscated dispatch loops, and any `-O2` body that reuses a scratch register for both a pointer and a count -- because the merged high then holds members of several types and the address-tied one decides the declaration by accident. Flip OFF to restore the declaration-representative type, which is worth doing only to see which member Varnode the old declaration came from. Inert on any local whose member Varnodes agree on a type, so a function with no width-conflicting merge is byte-identical.", "example": "option declhightype off", "source_decompiler": "kuna", "inspiration": "repipe:byte-comparisons-incorrectly-dereference, 68a915938fac2855fe6fb805 (x86-64 Windows PE crackme.obf.exe) sub_1400542a1, the MOVSX at 0x140054b87; upstream PrintC::emitVarDecl (printc.cc:1719) and HighVariable::getTypeRepresentative (variable.cc:377-396)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a variable is declared a pointer to one width and dereferenced at another", "a byte comparison reads through an unsigned long long pointer", "the declared type disagrees with the cast on the line that assigns the variable", "an integer is assigned to a variable declared as a pointer with no cast", "a one-byte load prints without the sign extension the instruction performs"]},
  {"option": "paramrefdecl", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P9", "subphase": "naming-policy", "strength": "HARD", "rewind": "P9", "issue": "repipe-address-taken-key-parameter", "summary": "Do not declare an `&parameter` reference as a body local. Upstream declares a function's locals by walking the ScopeLocal Symbol table for `no_category` Symbols only (`PrintC::emitScopeVarDecls`, printc.cc:2667), so a `function_parameter` Symbol is rendered in the signature and nowhere else. kuna's printer instead walks HighVariables and decides that a high is a parameter by asking whether a `function_parameter` Symbol CONTAINS THE STORAGE of one of the high's member Varnodes. That test cannot see an address-taken parameter. `ActionNameVars::linkSpacebaseSymbol` (coreaction.cc:3005) encodes `&symbol` as the offset CONSTANT of a `PTRSUB(spacebase, off)` and binds that constant's high to the Symbol owning the referenced frame slot; when the slot is an incoming stack parameter and the body never touches the slot any other way, every Varnode of that high lives in the constant space, so no member storage is contained by anything and the storage test answers `no`. The high is then declared in the body -- under the name it took from the parameter Symbol it is bound to, and with the constant high's own data type. The witness is a 32-bit Windows PE whose key-derivation helper reads its four-byte key parameter only by address: `void sub_401571(unsigned int a0,int4 a1,int4 a2)` opens with `int4 a0;` and the sole use is `CryptDeriveKey(v4,&a0,4,0)`, whose `&a0` maps to `LEA ECX,[EBP + 0x8]` -- the parameter's own slot on an i386 cdecl frame. Two objects of one name and two types in one scope is not valid C, so a `decompile-project` export of such a function cannot be recompiled, and a reader cannot tell that the address handed to the callee is the caller's argument. This option supplies the Symbol-IDENTITY test the storage test cannot make: a high bound to a `function_parameter` Symbol is that parameter wherever its Varnodes live, so the printer skips its declaration and the `&a0` in the body resolves to the signature's `a0`. It is bounded three ways. It reads the Symbol the reference is already bound to (`HighVariable::kuna_ref_symbol`, kuna's port of `Varnode::setSymbolReference`), so it invents no association and cannot merge two distinct frame slots. It keeps the storage arm's guard that the high's name is one of the prototype's parameter names, so a divergence between the Symbol's name and the signature's leaves the declaration in place rather than emitting an undeclared variable. And it only ever REMOVES a declaration that duplicates a signature parameter -- a reference to a `no_category` local (`&v7`) has a different category and is untouched. The same Symbol identity is carried to the `variables` JSON surface, which collects a parameter's uses by matching Varnodes against the parameter's STORAGE and so reported an address-taken parameter as unused while the C showed the reference; the reference is attributed to the parameter only when no storage-backed or name-backed evidence was found.", "use_when": "On by default (DIV-143). Keep it on when a function's body re-declares one of its own parameters -- the shape is a signature `f(unsigned int a0, ...)` immediately followed by `int4 a0;`, with `&a0` the only use, and the disassembly showing that `&a0` is a `LEA` of the parameter's own frame slot. It is the standard rendering for a parameter passed by address to an API (`CryptDeriveKey`, `GetTokenInformation`, any out-parameter), because the body then never reads the slot as a value and the parameter's storage carries no Varnode. Inert unless an `&symbol` reference resolves to a `function_parameter` Symbol, so any function that does not take the address of a parameter is byte-identical. Flip OFF to restore the duplicate declaration -- worth doing only to confirm that a variable you expected to be a local really is the parameter.", "example": "option paramrefdecl off", "source_decompiler": "kuna", "inspiration": "repipe:address-taken-key-parameter, 64f1f7fad931496abf909535 (i386 Windows PE illusion.exe) sub_401571, the CryptDeriveKey call site at 0x401641; upstream PrintC::emitScopeVarDecls (printc.cc:2667)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a parameter from the signature is declared again as a local at the top of the body", "the duplicate declaration has a different type from the signature (unsigned int a0 then int4 a0)", "the only use of the redeclared name is &name passed to a call", "a decompile-project export does not compile because a local shadows a parameter of the same name", "--json reports a parameter with no line_numbers although the C shows it used"]},
  {"option": "noreturn_known", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-noreturn", "summary": "Run the known-no-return analysis pass: mark exit/abort/_Unwind_Resume/std::terminate-style functions no-return (the kuna analog of Ghidra's NoReturnFunctionAnalyzer, plus the Rust panic/handle_alloc_error list).", "use_when": "On (default) suppresses dead fall-through after a no-return call; off leaves the call rendering as if it returns (dead code reappears).", "example": "option noreturn_known off", "source_decompiler": "kuna", "inspiration": "Ghidra NoReturnFunctionAnalyzer (Known) + the analyzer default-on/off model", "change_kind": "analysis-enablement", "tier": "transform", "symptoms": ["dead code kept after calls to exit/abort/panic when off", "call to a known no-return libc function still shows a fall-through path", "unreachable epilogue after std::terminate or a rust panic call"]},
  {"option": "peimportcall", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "decbench-O0-mydoom-mmsender_th", "summary": "Bind a call through a PE IAT slot or a typed Mach-O lazy/non-lazy symbol-pointer entry to the import the loader already resolved at that slot. Such a call lifts to a CALLIND through a global and the only pass that resolves it (ActionDeindirect) requires `Varnode::externref`, which upstream sets from an ExternRefSymbol kuna never creates. On: paint `externref` over the exact pointer-width typed import slots so the call deindirects to the existing FunctionSymbol and carry its no-return flag onto the merged prototype. Mach-O stubs, exports, LOCAL/ABS indirect entries and arbitrary data such as `__objc_msgrefs` are excluded. The upstream Win32 no-return API list (ExitProcess/ExitThread/FreeLibraryAndExitThread/KeBugCheck/longjmp/...) remains strictly PE/COFF-only.", "use_when": "On by default (DIV-57; Mach-O scope extension DIV-171). On, a Windows PE/DLL renders Win32 calls by name (`ExitThread(0)`, `HeapFree(...)`) instead of `(*dat_411324)()`, and a Mach-O direct call through `__got` renders `objc_msgSend(...)` instead of `(*dat_100004038)(...)`. Flip OFF to restore the anonymous import-slot rendering byte for byte on either format; ELF and other formats are byte-identical either way.", "example": "option peimportcall on", "source_decompiler": "kuna", "inspiration": "decbench O0-mydoom-mydoom-mmsender_th (IDA/Ghidra/angr GED 0, kuna 37); RE need mach-o-objc-msgsend; Ghidra Scope::addExternalRef + ExternRefSymbol, PEFunctionsThatDoNotReturn", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["windows api calls render as (*dat_411324)() with no name", "a Mach-O objc_msgSend import renders as (*dat_100004038)() through __got", "a PE function runs past ExitProcess or ExitThread and absorbs the next function's body", "no Win32 API names anywhere in a decompiled PE"]},
  {"option": "libproto", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-libproto", "summary": "Run the library-prototype analysis pass: seed common libc signatures (puts(char*), printf(char*,...), ...) onto matching imports so call arguments get typed (the kuna analog of Ghidra's ApplyDataArchiveAnalyzer).", "use_when": "On (default) types call arguments so string literals render (puts(\"...\")); off leaves the bare constant/untyped argument.", "example": "option libproto off", "source_decompiler": "kuna", "inspiration": "Ghidra ApplyDataArchiveAnalyzer (.gdt library archives)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["puts/printf arguments untyped so string literals render as bare constants or dat_ addresses", "imports carry no signatures and call arguments stay untyped"]},
  {"option": "libcsigs", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-libcsigs", "summary": "Seed the MEASURED extension of the built-in libc prototype table: the ~200 further signatures the 27-entry libproto table does not carry (exit, gettext, dcgettext, error, strerror, strdup, strcasecmp, close, open, read, write, stat, getenv, syslog, the FORTIFY __printf_chk/__fprintf_chk/__memcpy_chk family, __errno_location, __stack_chk_fail, ...), ranked by call-site frequency over the frozen decbench C corpus and reduced from the platform headers. Applied ONLY to names the image imports and does not itself define, so a program's own function that happens to share a libc spelling is never retyped.", "use_when": "On (default) types the arguments of every common libc call, so a caller whose parameter only flows into one gets a concrete type (char *path) instead of the inferred unsigned long, and the callee's return type is known. Flip OFF when a binary links a private library that reuses libc spellings with different signatures and is NOT statically linked (the pass already skips any name the image defines), or to ablate this table's contribution to a type-recovery difference; off is byte-identical to the 27-entry base table alone.", "example": "option libcsigs off", "source_decompiler": "kuna", "inspiration": "Ghidra ApplyDataArchiveAnalyzer (.gdt library archives); IDA Pro's til type libraries; measured on decbench C corpus (1587 ida-perfect/kuna-imperfect type_match cases)", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a caller's parameter is unsigned long where it is only ever passed to a libc function that takes a char */int", "a libc call renders its string argument as a bare 0x... constant instead of a literal", "a wrapper around a libc function loses its return value or return type", "__printf_chk / __fprintf_chk arguments are shifted by the fortify flag", "an exit or __stack_chk_fail call shows phantom arguments"]},
  {"option": "libctypes", "values": ["off", "opaque", "glibc"], "default": "opaque", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-libctypes", "summary": "Spell the libc/POSIX aggregate pointers in the built-in prototype tables by NAME instead of void *: FILE *, DIR *, stat *, dirent *, passwd *, group *, tm *, option *, timespec *, timeval *, sigaction *, sigset_t *, mbstate_t *, termios *, sockaddr *, pthread_mutex_t *. The Ty vocabulary of those tables is width-stable, so every aggregate pointer in them is a void * today - honest about the width and silent about the pointee, which is the one thing a declaration actually knows. The retarget is enumerated slot by slot (a blanket void *->FILE * would type the va_list of vasprintf/vsnprintf/__vfprintf_chk/verr as a stream), and it brings the stdio names neither shipped table carries: __uflow, fgetc, rewind, freopen, popen, pclose, getdelim, flockfile, funlockfile. Each named type is interned as a shell carrying its real glibc x86-64 width (FILE 216, stat 144, dirent 280, ...), never width 0 - a zero-width pointee keeps a PTRSUB alive through RulePtrsubUndo and the printer then emits the functional PTRSUB(p,0x28) form - and is left INCOMPLETE so the exported .h declares it opaque rather than as a struct with a width and no members. On an image that carries debug info the platform's own definition is used instead, fields and all: the pass runs after the DWARF importer and adopts an aggregate of the declared width already held under the name (or under glibc's _IO_FILE spelling of FILE) - complete or still being populated - and declines the signature rather than touch a name held by anything else, a different width included. The third value, `glibc`, fills in the nine aggregates whose layout the platform PUBLISHES and an application is meant to read - FILE, stat, timespec, timeval, tm, passwd, group, option, dirent - with their real x86-64 members, so an access renders under its own name (`f->_IO_read_ptr`, `st->st_size`, `opt[i].name`) instead of `f->field_0x8`, and the loaded value takes the FIELD's type rather than whatever the cast said - `st_size` is a signed long, so a wrapper that returns -1 on failure stops printing its sentinel as 0xffffffffffffffff. Every offset and width is measured with offsetof/sizeof against the installed headers, not restated from memory (docs/features/libctypes/glibc.md). Two claims the tables refuse to make: no field is spelled as a pointer to the struct being filled (_IO_FILE::_chain is void * - completing a struct re-keys it into a new Rc, so a self-pointer taken mid-completion would strand the program on two stream types), and no layout is installed unless it is TRUE of the image - x86-64 ELF whose .dynstr names glibc (the libc.so.6 soname or a GLIBC_2.x symbol version). musl, another libc, another architecture and a statically linked image all keep the opaque shells, which for a static image costs nothing because these tables are matched against imported names only. DIR, sigaction, sigset_t, mbstate_t, termios, sockaddr and pthread_mutex_t stay opaque under `glibc` too: glibc publishes no layout for DIR, and the rest are reserved words no caller reads by name. The same tables also type the stdio STREAMS themselves, which no prototype slot can reach: `stdin`/`stdout`/`stderr` are storage, not an argument, so a function that never calls a typed stdio function sees only an `undefined8` word, and on a shared object the slot has no name at all. Under any enabled value the pass emits a typed DATA symbol for each stream slot, read off the DYNAMIC RELOCATION that binds it - which is also the evidence that the name is the C library's and not a global the program spells `stdout` itself. A COPY relocation names a .bss word the run-time loader fills with libc's own `FILE *stdout`, so that slot is `FILE *` under the stream's own name; a GLOB_DAT on an UNDEFINED symbol names a GOT word holding the stream's ADDRESS, so it is `FILE **` under the minted name `stdout_ptr` - calling that one `stdout` would make the emitted `*stdout_ptr` read as an indirection the program does not perform. Because `stdout_ptr` is kuna's coinage and not the image's, the GLOB_DAT arm declines when either symbol table (.symtab as much as .dynsym) already spells it, rather than printing two addresses under one identifier. Linked ELF only, four architectures, and a spelling that occurs twice in .dynsym is declined outright. The slots point at the same `FILE` the rest of the table uses, so `opaque` gives `stdout->field_0x28` and `glibc` `stdout->_IO_write_ptr`, and an image whose own debug info holds a different `FILE` contributes no stream symbol rather than a contradictory one.", "use_when": "On by default (opaque): a pointer the callee's own libc declaration names carries that name - get_prefix(FILE *f) instead of sub_2f30(long a0) on a stripped -O2 coreutils reader, fopen/fdopen/popen results as FILE *, stat/lstat/fstat filling a stat *, getopt_long reading an option * table, readdir returning a dirent *, localtime a tm *. Flip OFF to ablate the table: to check whether a pointee name came from this declaration rather than from inference, when a target statically links a private function under a libc spelling that the imports-only restriction did not catch (it would then be typed against libc's aggregate, not its own), or when a decompile-project export has to stay as close to compilable as it gets - the shells are deliberately INCOMPLETE, so the exported .c reads fields out of a type its own .h declares opaque (measured +58 cc errors on an ls export - 51 of them from reading fields out of the incomplete shells, the rest from a type name that collides with a function name of its own in the exported body; the .h itself is 0 errors either way). Off is byte-identical to the shipped void * tables, because with the gate off the pass does not run and not one named shell is interned. No datatest loads a file, so the 675 assertions cannot see this row either way; the stage corpus can, and tests/stages/kuna-libctypes.xml pins it in both arms. The flip's evidence is the corpus sweep in docs/features/libctypes/record.json. Flip to `glibc` for the field NAMES: on an x86-64 ELF against glibc it installs the published layouts of FILE/stat/timespec/timeval/tm/passwd/group/option/dirent, turning `*(unsigned char **)&f->field_0x8` into `f->_IO_read_ptr` and `*(unsigned long *)&st->field_0x30` into `st->st_size`, and giving the loaded value the field's type. Prefer it when reading stdio-heavy or stat-heavy code; stay on `opaque` when the target is not glibc/x86-64 (the layouts are then simply not installed, so `glibc` is safe but pointless), or when a decompile-project export must not declare a type the toolchain's own headers also declare. The default also NAMES and TYPES the stream slots: `*(long *)(stdout + 0x28)` becomes `*(long *)&stdout->field_0x28` (`stdout->_IO_write_ptr` under `glibc`), and a shared object's `*(long *)(*dat_3fe0 + 0x28)` becomes `*(long *)&(*stdout_ptr)->field_0x28`. That reach is the function that makes no typed stdio call and the shared object whose stream never had a name - over twelve whole binaries 45 lines move, 38 of them a GOT slot gaining its name, and eight of the twelve are byte-identical because inference had already typed those streams from a typed stdio call in the same function. Its cost is local merging: a slot that takes `FILE *` stops merging with the unrelated values a scalar local had absorbed, which adds a declaration and renumbers the locals after it. Naming a data object stays `datasyms`' call - with that option off the stream slot goes back to `dat_<addr>`, since the type rides on the symbol - so ablate the stream half with `datasyms off` and the whole table with `libctypes off`.", "example": "option libctypes off", "source_decompiler": "ghidra", "inspiration": "Ghidra ApplyDataArchiveAnalyzer applying generic_clib_64.gdt, whose FILE */struct stat * slots carry the header's own pointee; signatures reduced from gcc -aux-info over the platform headers (_GNU_SOURCE + _FORTIFY_SOURCE=2), widths from sizeof/_Alignof", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a function that only ever passes its argument to a libc stdio call still declares it long or unsigned long", "a fopen or fdopen result is a void * that nothing can name", "a stat or lstat call fills an untyped void * buffer", "getopt_long takes a bare (void *)0x... long-option table", "readdir returns a void * whose fields are casts and offsets", "a stream parameter is void * where the ground truth is FILE *", "a FILE * or stat * renders its members as field_0x8 / field_0x30 instead of _IO_read_ptr / st_size", "a value loaded out of a named libc struct keeps the cast's type instead of the field's", "stdin/stdout/stderr render as an untyped word and their fields as raw offsets ((long)stdout + 0x28)", "a shared object's stream slot has no name at all (*(long *)(*dat_3fe0 + 0x28))", "two functions in one binary disagree about what stdout is, one calling it FILE * and the other a long"]},
  {"option": "win32sigs", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "repipe-resource-loader-c-retains", "summary": "Seed built-in Win32 API prototypes onto a PE's imported API names, keyed by ENTRY ADDRESS. kuna carried libc signatures (libproto, libcsigs) and not one Windows API signature, so on a PE every LoadLibraryExW/CreateFileW/WriteFile call reached ActionDefaultParams with an empty prototype and its arguments had to be recovered from the call site alone; where that recovery loses -- the ordinary case for an image that writes its outgoing slots well before the call -- the call renders `LoadLibraryExW()` and the argument stores survive as mapped stack locals, along with the return-address push the call itself makes. The table is ~130 WINAPI exports ranked by an import histogram over the 54 PE images of the RE arena corpus (admitted at >= 5 images), plus the resource/loader family (FindResourceW, LoadResource, SizeofResource, EnumResourceTypesW/NamesW) below that bar. Each signature is reduced to the width-stable Ty vocabulary and a declaration with a slot that has no honest spelling (a by-value LARGE_INTEGER, a DWORD64) is rejected rather than approximated. Address is the key because a PE import is TWO FunctionSymbols -- the size-0 IAT slot the engine constant-folds through and the `FF 25` thunk veneer a direct `call` targets -- and the global by-name query used by libproto answers with the slot, which is not the address ActionDefaultParams asks about. Imported names only, so a program's own function sharing a Win32 spelling is never retyped; no CRT (__cdecl) spelling is admitted, because on x86 the PE default model is __stdcall and a locked arity is also the callee's stack pop.", "use_when": "On by default (DIV-141). On, a Windows PE renders `LoadLibraryExW(v1,0,8)` / `WriteFile(h,buf,n,&written,0)` instead of `LoadLibraryExW()` with the pushed values left behind as `v41 = 0x401baf;`-style stack stores. Flip OFF to ablate the table's contribution -- to check whether a Win32 call's arguments came from the parked signature rather than from recovery, or if a target statically links its own function under a Win32 spelling that the imports-only restriction did not catch. Every non-PE target is byte-identical either way.", "example": "option win32sigs off", "source_decompiler": "kuna", "inspiration": "Ghidra ApplyDataArchiveAnalyzer (.gdt library archives); IDA Pro's til type libraries; import histogram over the 54 PE images of the RE arena corpus", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a Windows API call renders with an empty argument list", "LoadLibraryExW() or CreateFileW() has no arguments but the pushes are visible above it", "a PE function declares dozens of stack locals that only ever hold outgoing call arguments", "CALL fall-through addresses are assigned to stack variables", "a Win32 call's wide string argument renders as a bare 0x... constant", "calleearity and varargstackargs both on and a Windows API call is still argumentless"]},
  {"option": "declaredlibcproto", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-declaredlibcproto", "summary": "Answer a DECLARED function name out of the built-in libc signature tables, so naming a callee also gives it a prototype. `libproto` and `libcsigs` match a name the IMAGE carries -- its FUNC symbols, its imports -- which is exactly the evidence a stripped, statically linked target does not have. There the name exists only because the operator supplied it (`--define-function 0x8048968=ptrace`, `--assert 'function 0x8048968 ptrace'`), and until now that named the entry and nothing more: the call sites still rendered `ptrace()` with no arguments, because the callee's arity has to come from somewhere and a glibc varargs wrapper (`long ptrace(enum __ptrace_request, ...)`, the rest fetched with `va_arg`) cannot yield it to any amount of body analysis. With this on, a declared name is looked up in both tables and the matching signature is parked on the DECLARED ENTRY ADDRESS -- the key `ActionDefaultParams` reads back per call site, and the only key that survives two symbols sharing a name. The imports-only restriction the `libcsigs` table carries is deliberately lifted here: it exists so a coincidental spelling cannot retype a function the image defines itself, and that is a judgement about evidence, which is different once a human or an agent has identified the entry outright.", "use_when": "On by default: naming a callee is the one thing an RE agent always does, and a name that buys no prototype buys almost nothing. The shape that needs it is a stripped or packed image where argument recovery has nothing to work from -- the tell is a call rendered `f()` with the pushed argument slots visible as raw stores on the lines above it (`*(int *)(esp - 4) = ...` four times, then `v += ptrace();`). Structurally inert unless a run declares a function name (`--define-function START=NAME`, `--assert function`, console `function bounds ... as NAME`), so it is byte-identical over both parity corpora, and inert again unless that name is one of the ~230 the built-in tables carry. Flip OFF when a target defines its own function that merely shares a libc spelling and the declaration is about that function, not the library one -- or to check whether the parked signature, rather than recovery, is what moved a call's arguments. An explicit `--assert prototype` on the same function still wins: it is applied after the declaration.", "example": "kuna decompile ./crackme sub_80483b0 --define-function 0x8048968=ptrace", "source_decompiler": "kuna", "inspiration": "Ghidra ApplyDataArchiveAnalyzer (.gdt archives applied on rename); IDA Pro's til type libraries, which retype a function the moment it is given a library name", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a call to a function I named with --define-function still renders with no arguments", "the argument stores are emitted as raw stack writes on the lines above an argumentless call", "naming a callee changes the spelling of the call and nothing else", "calleearity and varargstackargs both on and a stripped statically linked libc call is still argumentless", "a declared ptrace/read/write/open call loses every argument"]},
  {"option": "strings", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-strings", "summary": "Run the string-literal analysis pass: detect NUL-terminated ASCII strings (min length 5) and plant a typelocked char[N] data symbol at each (the kuna analog of Ghidra's StringsAnalyzer).", "use_when": "On (default) lays char[N] data at detected strings; off leaves those addresses undefined.", "example": "option strings off", "source_decompiler": "kuna", "inspiration": "Ghidra StringsAnalyzer", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["string constants render as raw addresses or unnamed data instead of quoted char[N] literals", "no data symbols at ascii runs in rodata"]},
  {"option": "widestrings", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "re-wide-api-string-args", "summary": "Scan the loaded image at 2-byte (UTF-16LE) width as well as 1-byte, and plant a typelocked wchar2[N] data symbol at each wide literal -- the allCharWidths arm of Ghidra's StringsAnalyzer that kuna's 1-byte port left as a documented seam. Read at 1-byte width a UTF-16LE literal is a ONE-CHARACTER string (`L\"ntdll.dll\"` is 6e 00 74 00 ..., whose first NUL closes the run after `n`), so nothing is marked up there, the constant keeps whatever char * the type lattice gave it, and the printer reads to the same NUL: every wide Windows-API argument rendered as its own first character -- LoadLibraryW(\"n\"), FindWindowW(\"O\",0). The widened matcher is the 1-byte MinLengthCharSequenceMatcher over 2-byte little-endian code units and nothing else: the same printable-ASCII recognizer over each unit's low byte, the same require-NUL-end rule, the same minimum length of 5, over exactly the address set the 1-byte pass scans, on even addresses only. The two widths cannot both claim a run -- a wide unit demands a zero high byte, so five consecutive 1-byte-charset bytes never occur inside a wide run -- and the 1-byte facts are committed first regardless, so the established width owns any address both could reach. With the symbol planted the existing printer path does the rest: the char type's size 2 is what emits the L prefix and reads the bytes two at a time.", "use_when": "On by default. A wide (UTF-16) string argument renders as a one-character literal -- LoadLibraryW(\"n\") where the binary says L\"ntdll.dll\", FindWindowW(\"O\",0) where it says L\"OllyDbg - [CPU]\" -- which is most of what a Windows anti-debugging or anti-VM check contains. `kuna strings --encoding utf16` already reported those literals in full while the decompiled C did not, and the difference between the two surfaces is the tell. Flip OFF to restore markup that is exactly the 1-byte pass's -- e.g. on an image whose 16-bit data tables read as plausible wide text, or to ablate this width's contribution.", "example": "option widestrings off", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra StringsAnalyzer's allCharWidths setting (the UTF-16 arm of MinLengthCharSequenceMatcher); measured on crackmes.one 65acadf3eef082e477ff5ede (ObfuscationFiesta.exe, PE32+), where sub_140002530's LoadLibraryW / FindWindowW anti-debug checks rendered as one-character strings", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a wide Windows API argument renders as a one-character string literal", "LoadLibraryW(\"n\") or FindWindowW(\"O\",0) in the emitted C", "kuna strings --encoding utf16 reports a literal the decompiled C truncates to its first character", "a UTF-16 string address carries no data symbol while the ascii ones do", "an anti-debugging check names no DLL or window class"]},
  {"option": "entry_disc", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-entry", "summary": "Run the entry-discovery analysis pass: find function entry points for stripped targets (e_entry, DT_INIT/FINI + INIT_ARRAY tables, .eh_frame FDE starts, the _start->main libc idiom, prologue patterns).", "use_when": "On (default) discovers + names extra entry points; off limits functions to the symbol stream.", "example": "option entry_disc off", "source_decompiler": "kuna", "inspiration": "Ghidra EntryPointAnalyzer/FunctionStartAnalyzer/GccExceptionAnalyzer (.eh_frame)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a stripped binary yields almost no functions (symbol stream only)", "functions discovered via e_entry/init_array/.eh_frame/prologues missing from the list"]},
  {"option": "eh_frame_full", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-ehframe-lsda", "summary": "Extend the always-on entry-discovery pass with full .eh_frame + .gcc_except_table (LSDA) markup (GccExceptionAnalyzer): for each FDE, follow the CIE 'L' augmentation to its LSDA pointer in .gcc_except_table, decode the call-site table, and emit each exception landing-pad PC (lpStart + non-zero cs_landing_pad) as a discovered function entry. A landing pad is a real code target reached only by the unwinder, so a stripped C++ binary's entry-disc otherwise misses it. The DW_CFA_* call-frame instructions are NOT recovered at the decompiler tier (kuna's own S5/S7 frame analysis already recovers the stack frame from the code) — CFI is inherited, not rebuilt. Output-changing (adds entries), so default-OFF: a default run is byte-identical to before.", "use_when": "Off (default) limits .eh_frame use to FDE pcBegin function starts. Flip on to also discover exception-handler landing pads (catch/cleanup blocks) from the .gcc_except_table LSDA in a C++ try/catch binary.", "example": "--option eh_frame_full on", "source_decompiler": "kuna", "inspiration": "Ghidra GccExceptionAnalyzer + ghidra.app.plugin.exceptionhandlers.gcc.* (Cie/Fde 'L' augmentation -> LSDAHeader/LSDACallSiteTable landing pads)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["c++ catch/cleanup landing pads missing from a stripped binary's function list", "exception-handler code never discovered as entries", "gcc_except_table call-site targets left unexplored"]},
  {"option": "fdeinterior", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-fdeinterior", "summary": "Reject a discovered function entry that falls strictly inside another function's .eh_frame FDE body. A kuna FunctionSymbol is an entry address with no extent, so the commit boundary cannot answer 'is this candidate already inside a known function?' and every discovery oracle plants a sub_<addr> in the middle of a body it cannot see: eh_frame_full promotes each .gcc_except_table landing pad to a top-level function, aif starts one at the first undecoded byte of an unwinder-only region (which is routinely mid-instruction), and the prologue patterns match a 16-byte-aligned push rbp; mov rbp,rsp inside a larger body. Such a function inherits its parent's live frame pointer, so it decompiles with an uninitialised rbp and every local is a garbage dereference. Each .eh_frame FDE records one function's [pcBegin, pcBegin+pcRange) by construction (one .cfi_startproc/.cfi_endproc pair), which is exactly the extent the symbol table never carried, so an entry strictly inside one is not a function on the unwinder's own authority - the model IDA Pro uses, where get_func of a landing pad returns the enclosing function taken from the FDE. Only ranges that describe a single function are used: no other named function start inside, no other FDE start inside, and no overlap with a linker stub section (.plt/.plt.sec/.plt.got/.iplt/.MIPS.stubs), so the linker's single whole-PLT FDE never suppresses an import stub. An entry AT an FDE pcBegin is always kept, so the .eh_frame FDE oracle's own product is preserved. ELF-only and inert on any image without .eh_frame FDEs, which includes 95 of the 98 decbench bare-metal ARM images (they unwind through .ARM.exidx).", "use_when": "On (default) keeps mid-function artifacts out of the function list: the tell-tale is a sub_<addr> whose body dereferences an undefined frame pointer, or a run of sub_<addr> entries between two real functions in a C++ binary built with exceptions. Flip off to restore the previous discovery set exactly - e.g. to inspect a landing pad as its own function, or on a hand-written-assembly image whose .eh_frame deliberately covers several entry points with one FDE.", "example": "--option fdeinterior off", "source_decompiler": "kuna", "inspiration": "IDA Pro 9.2 (get_func(landing_pad).start_ea is the enclosing function, taken from the FDE range); measured on a g++ -O0 probe where kuna mints sub_2b0b/sub_2b1a/sub_2b8a inside probe_try_catch [0x2acf,0x2ba7)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["spurious sub_<addr> functions inside a c++ function that uses try/catch", "a decompiled function body dereferences an uninitialised frame pointer so every local is garbage", "function count inflated by unwinder-only landing pads", "a function entry lands in the middle of an instruction", "extra entries between two real functions in a binary built with exceptions"]},
  {"option": "pdatainterior", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-pdatainterior", "summary": "Reject a discovered function entry that falls strictly inside another function's .pdata RUNTIME_FUNCTION body - the PE half of fdeinterior, which does the same job from .eh_frame on ELF. A kuna FunctionSymbol is an entry address with no extent, so the commit boundary cannot answer 'is this candidate already inside a known function?' and every discovery oracle plants a sub_<addr> in the middle of a body it cannot see. An x64 PE's exception directory answers it exactly: each RUNTIME_FUNCTION records one function's [BeginAddress, EndAddress), which is the extent the symbol table never carried, so an entry strictly inside one is a point inside a function rather than a function of its own. The oracle that trips this on PE is aif, whose gap walk decodes whatever recursive descent could not reach and starts a function at the first undecoded byte - on an obfuscated dispatcher that is routinely mid-body and sometimes mid-instruction. funcboundflow then truncates the enclosing function's flow when it falls through into one, so the function the image itself describes decompiles as a fragment. Only ranges that describe a single function are used: no other named function start inside, no other record's BeginAddress inside, and no overlap with the range kept before it. An entry AT a BeginAddress is always kept, so the .pdata oracle's own product is preserved. x86/x64 PE only - the 8-byte ARM/ARM64 RUNTIME_FUNCTION carries no EndAddress and a PE without an exception directory vouches for no body, so the pass abstains on both.", "use_when": "On (default) keeps mid-function artifacts out of a PE's function list: the tell-tale is a sub_<addr> inside the address range of a function you already have, an unaligned sub_<addr> that starts mid-instruction, or a decompiled function that stops early with the funcboundflow truncation warning at an address the exception table says is inside it. Flip off to restore the previous discovery set exactly - e.g. on a packed or hand-written image whose .pdata deliberately describes several entry points with one record.", "example": "--option pdatainterior off", "source_decompiler": "kuna", "inspiration": "The .pdata analogue of kuna's own fdeinterior (DIV-61); measured on nikos_crack_me.exe, whose exception table declares one 43,152-byte body at [0x140023350,0x14002dbe0) and where aif mints sub_140023e34, sub_14002b324 and sub_14002b6a8 inside it", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a pe function entry inside another function's known address range", "an unaligned sub_<addr> that starts in the middle of an instruction", "decompiled output stops early with a funcboundflow truncation warning", "function count inflated by gap-walk starts on an obfuscated pe", "a large .pdata-described function decompiles as a fragment"]},
  {"option": "pdbinterior", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-pdbinterior", "summary": "Reject a discovered function entry that falls strictly inside a procedure the image's PDB describes - the PDB half of pdatainterior, for the functions the exception table has no record of. A kuna FunctionSymbol is an entry address with no extent, so the commit boundary cannot answer 'is this candidate already inside a known function?'. .pdata answers it for every function that needs unwind data, but a frameless leaf gets no RUNTIME_FUNCTION, and when the compiler also inlined that leaf into its only caller the out-of-line copy has no caller either: recursive descent never decodes it and the PDB is the only record of where it starts and ends. aif's gap walk then probes those undecoded bytes as a hole between functions and accepts any 4-byte-aligned interior address whose two-mnemonic prologue matches what the image's other functions start with - on an MSVC /O2 switch cascade that is the fall-through mov eax,imm; mov edx,imm of a CMOV-lowered case - and funcboundflow truncates the leaf at that entry, so a case of the switch vanishes from the emitted C. Every S_GPROC32/S_LPROC32 record in the fingerprint-matched .pdb (the pdb pass's own search and GUID/age gate) carries the procedure's code length, which is exactly the missing extent. A record is used only when its length is non-zero and does not overflow, maps to one contiguous range, agrees with every other record at the same start and lies inside one executable section; and only for procedures that describe a single function: no named function start (image symbol, PDB public or other procedure start) strictly inside and no overlap with the procedure kept before it. A .pdata BeginAddress inside a procedure does not disqualify it, because MSVC splits one function's unwind data across chained records. At the commit a procedure rejects entries only when a function is committed at its start (a surviving entry, a pass's function symbol, or one already in the symbol table): a static function has an S_LPROC32 but no public, and when nothing else admits its start the entries inside it are the only functions its code has, so they stay. Inside a used procedure only the entries its own code reaches are rejected: the commit decodes from the start, following fall-through and jumps without leaving the procedure, entering a callee, or falling through a call it cannot prove returns (one the decompiler treats as no-return, or one with no resolvable target such as an indirect no-return import call or int 0x29), and drops an entry that lies inside a decoded instruction and is called by no used procedure. An entry in code only a jump table, an exception handler or a call from any procedure reaches stays, since nothing else renders that code. An entry AT a procedure start is always kept. Applied only while pdb is also on, so switching the PDB off withdraws its extents too. x86, x64 and ARM64 PE only; 32-bit ARM abstains because a Thumb entry can carry the interworking bit.", "use_when": "On (default) keeps gap-walk artifacts out of a PE whose .pdb sits beside it: the tell-tale is a sub_<addr> between a PDB-named function's start and its end, or a PDB-named leaf that decompiles with the funcboundflow truncation warning and fewer cases than its source. A sub_<addr> inside a static function nothing else names, or in a switch case, exception handler or called routine the procedure's own flow does not reach, is left in place on purpose, since removing it would leave that code with no function. Flip off to restore the previous discovery set exactly - e.g. on a hand-patched image whose .pdb predates code inserted inside an existing procedure.", "example": "--option pdbinterior off", "source_decompiler": "kuna", "inspiration": "The PDB analogue of kuna's own pdatainterior (DIV-155); measured on the GH-468 MSVC /O2 testbed ab_o2.exe, where aif mints sub_140001178 and sub_1400011bc inside cascade_switch [0x1400010e0,0x1400011d6) and the render loses the 0xf043 case", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a sub_<addr> inside a function the pdb names", "a pdb-named leaf function decompiles with a funcboundflow truncation warning", "a switch case missing from a function whose .pdb is present", "function count inflated by gap-walk starts inside crt leaves of an msvc binary", "an entry in the middle of an instruction inside a pdb procedure"]},
  {"option": "unmappedentry", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-unmappedentry", "summary": "Refuse to create a function entry at a direct CALL target the Listing's recursive-descent walk is not allowed to decode. The walk runs two worklists that disagreed about what counts as code: the INSTRUCTION worklist gates every address on the executable-range universe (the flow.rs out-of-bounds analog) and drops anything outside it, while the FUNCTION worklist took a direct CALL target unconditionally. A call into unmapped memory therefore still became a DiscoveredFunction, which fast_funcdisc and funcdisc_recursive commit as a real entry - a sub_<addr> with no bytes, no extent and no body, reported with size 0 and decompiling to nothing. The targets are not decode failures: the CALL itself decoded fine, it is the bytes that are junk. An always-taken branch followed by anti-disassembly filler (xor eax,eax; je +1; e8 ...), a switch default arm read at the wrong alignment, and padding decoded as code all produce one. Only the FUNCTION claim is withheld - the Call cross-reference is filed in both directions either way, so kuna xrefs still reports that the instruction calls that address. A target inside an executable section is admitted exactly as before even when the decode there fails, because that is a real gap in the walk rather than a fabricated entry.", "use_when": "On (default) keeps the function inventory free of entries at addresses the image does not contain: the tell-tale is a sub_<addr> in kuna functions whose address is outside every section and whose size is 0, typically on an obfuscated or anti-disassembly binary. Flip off to restore the previous discovery set exactly - e.g. to see every address the walk followed as a call target, including the unmapped ones.", "example": "--option unmappedentry off", "source_decompiler": "kuna", "inspiration": "crackmes.one 66fd63059b533b4c22bd0b91 (Follow the asm call!, 32-bit ELF, anti-disassembly): kuna functions reported sub_fe6dca9f at 0xfe6dca9f, size 0, in a 25 KB image whose highest mapped byte is 0x7000 - the target of the call encoded one byte early behind the always-taken je at 0x450b", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a function entry at an address outside every section of the image", "kuna functions lists a size-0 sub_<addr> far above or below the image", "decompiling a discovered function returns no body at all", "the function inventory of an anti-disassembly binary contains an address that is not mapped", "a call target from junk bytes behind an always-taken branch becomes a function"]},
  {"option": "ppclocalentry", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-ppclocalentry", "summary": "Refuse to create a function entry at a PPC64 ELFv2 LOCAL entry point, which is a point inside the function that declares it rather than a function of its own. The OpenPOWER ELFv2 ABI gives a function two entries: the global entry is the symbol's st_value and materialises the TOC pointer r2 from r12 (addis r2,r12,hi; addi r2,r2,lo), and the local entry is where a caller that already holds the right r2 - anything in the same module - branches instead. The distance between them is recorded in the symbol's st_other field, which readelf -sW prints as [<localentry>: 8]. Nothing read that field, so the Listing walk saw an intra-module bl land eight bytes past a function symbol and minted a function there like any other CALL target, which fast_funcdisc and funcdisc_recursive then commit. On ordinary gcc ppc64le output that splits EVERY locally called function in two: the named symbol truncated to its 8-byte TOC prologue plus the whole real body filed under an anonymous sub_<hex> - and because p2's funcboundflow truncates a fall-through that reaches a known function entry, decompiling the named symbol emits an empty husk with a funcboundflow warning while the body is only reachable under the generated name. On, an address a defined STT_FUNC symbol declares to be its own local entry is never claimed as a function. Four guards keep the fold honest: the st_other field must decode to a real offset (bits 5-7 as (1 << n) >> 2 << 2, so only n in 2..6 folds; 0 and 1 mean the entries coincide and 7 is reserved), a sized symbol must contain its own local entry, the local entry must not be the address of some other defined text symbol, and the global entry must itself be a walk seed with no other seed in between. That last guard is what makes the walk's instruction closure invariant - the bytes at the local entry are reached as the global entry's fall-through either way - so this can only ever remove the duplicate second entry, never a body. The Call cross-reference is filed in both directions either way, as with unmappedentry. PPC64-only and inert on an image whose symbols carry no local-entry annotation.", "use_when": "On (default) on any PPC64 ELFv2 image. The tell-tale is kuna functions reporting a size-8 named function immediately followed by a size-N anonymous sub_<hex> 8 bytes later, for every function in the image, and kuna decompile <that name> returning an empty body with the funcboundflow fall-through-reached-the-next-function-entry warning. Flip off to restore the previous discovery set exactly - e.g. to see every address the walk followed as a call target, local entry points included.", "example": "--option ppclocalentry off", "source_decompiler": "kuna", "inspiration": "The OpenPOWER ELFv2 ABI's two-entry-point model (st_other localentry field), read the way binutils' PPC64_LOCAL_ENTRY_OFFSET macro reads it; measured on this repo's plt_ppc64le fixture, where __do_global_ctors_aux at 0x940 decompiled to an empty husk while its constructor loop sat under sub_948, and the same 8-byte/anonymous pairing split deregister_tm_clones, __do_global_dtors_aux and frame_dummy", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["every function in a ppc64 binary is listed twice, as an 8-byte named husk plus an anonymous body", "kuna decompile of a named ppc64 function returns an empty body with a funcboundflow truncation warning", "kuna functions reports size 8 for a ppc64 function that is plainly longer", "a ppc64le function body is only reachable under a generated sub_<hex> name", "a function entry sits 8 bytes into another function on PowerPC"]},
  {"option": "picbase", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-picbase", "summary": "Fold a 32-bit PIC binary's base register into the cross-reference index, so a GOT-relative address formation is a reference to what it forms. The on-demand xref query reads a reference out of one instruction's p-code: a decode-time constant that lands in a mapped section. That is the whole answer on x86-64, where `lea rax,[rip+0x36a]` lifts to a COPY of the absolute address. It is no answer in position-independent i386 code, where the address of a string is not in the instruction at all - the program materialises the GOT pointer at run time (`call <next>; pop ebx; add ebx,imm`, an idiom that exists for no other purpose) and every literal, global and function pointer is reached as base-plus-displacement. The literal address occurs nowhere in the image, so no scan over constants can find it and every string reports xrefs_count 0. On, a small abstract machine over the same whole p-code the query already keeps interprets the idiom - constants and stack offsets only, memory modelled only at stack offsets, a constant tainted as PC-derived when it equals its own instruction's fall-through, so a plain `mov ebx,imm` cannot establish a base - and reports three shapes: the address a LOAD reads, the address a STORE writes, and a constant that lands in a register (the address-taken case). Two claims are licensed differently. A function that runs the idiom itself computes the value and assumes nothing. A function that only USES an inherited base is claimed for only when the recovered value equals the image's own `_GLOBAL_OFFSET_TABLE_` and every idiom in the program agrees on one register, which is what says the register really is the ABI's module-wide GOT pointer. That module-wide half is load-bearing rather than decorative: the filing crackme's prompt routine is split into four inventory entries at its int3 traps, and the lea that forms the prompt lands in a different entry from the idiom that set the register up. Ownership is protected by refusing rather than guessing: the base is offered to a function only when its body never writes the register, or writes it only in the establishment itself, so a body that reuses the register for its own purposes contributes no references rather than wrong ones. Query surface only - kuna xrefs, kuna strings and the decompile-all xref section read this index, and no p-code, prototype or emitted C depends on it - and structurally inert on any image with no GOT section and on every target whose code does not run the idiom.", "use_when": "On by default (DIV-104). Makes a 32-bit position-independent binary's strings, globals and function pointers report the functions that use them: the tell-tale is `kuna strings --json` returning xrefs_count 0 and an empty functions list for EVERY string in the image while `kuna xrefs --to <function>` answers normally, or `kuna xrefs --to <string address>` finding nothing on an address the disassembly plainly forms. Flip off to restore the previous answer exactly - e.g. to see only references that are literally encoded in an instruction, or on a hand-written-assembly image that uses the ABI's GOT register for something else and whose base-relative displacements are therefore not addresses.", "example": "--option picbase off", "source_decompiler": "kuna", "inspiration": "Ghidra's ConstantPropagationAnalyzer/SymbolicPropogator, which propagates a computed register value into the reference index rather than scanning for literal addresses; measured on crackmes.one 5ab77f6333c5d40ad448ca52 (pancrackme v1.0, 32-bit i386 PIC ELF, stripped) where all 23 strings - including the `Password: ` prompt at 0x8049127 that `lea eax,[ebx-0x1119]` forms two instructions before the write(2) that prints it - reported xrefs_count 0", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["kuna strings reports xrefs_count 0 and an empty functions list for every string in a 32-bit binary", "a string the disassembly plainly forms the address of has no cross-references", "kuna xrefs --to a .rodata address finds nothing in a position-independent i386 executable", "no function is reported as the owner of a prompt the program prints", "globals reached as [ebx+disp] have no readers or writers in the xref index"]},
  {"option": "entrymainproto", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "prototype-source", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-entrymainproto", "summary": "Give the function the PE C-runtime startup calls with argc/argv/envp the prototype that call site establishes. kuna recovers a callee's parameters from the callee's OWN body - an ABI argument register read before it is written is a parameter - which is why a stripped PE's helpers come out correctly typed. It is also why main comes out void(void): a main that ignores argc and argv never reads rcx/rdx/r8, so body-driven recovery finds nothing, and the agent reading the output sees a callee declared to take nothing being called with three arguments a few lines up in its caller. The entry point is the one place where the arguments are visible WITHOUT the body, because on a PE the C runtime startup is inside the image and kuna already decompiles it: MSVC's __scrt_common_main_seh fetches each argument through a named CRT accessor (__p___argc, __p___argv/__p___wargv, _get_initial_narrow_environment/_get_initial_wide_environment) in the instructions immediately before the call, and those names are imported by the startup and by nothing else. The window between the accessor cluster and the next direct call to a non-accessor therefore names the entry function. Parameters are typed at the WIDTH the call site establishes (the 4-byte argc slot, two pointer-width slots) and named after the accessor that produced each; they are deliberately not typed int / char ** , which would assert the C library's declaration of main - the same shape carries wmain's wchar_t ** and a hand-rolled entry point need not be main at all. PE-only (on ELF the CRT lives in libc, so there is no in-image call site to read), skipped whenever the callee already carries a function symbol from .symtab / an export / a PDB / DWARF, and skipped unless at least the argc and argv accessors appear - envp adds its slot only when its own accessor does.", "use_when": "On (default) makes a stripped PE's entry function declare the arguments its caller passes: the tell-tale is `void sub_<addr>(void)` decompiled on its own while the CRT startup calls `sub_<addr>(v7,v1,v6)`, so every argument the body would use is unnamed and untyped. Flip off to restore the void(void) form exactly - e.g. when the image is not really a C main (a repurposed startup, a packer stub) and the three slots would be misleading, or to remove the ONE measured side effect: making argc a live parameter puts a value in the first ABI argument register at main entry, so a call to a Win32 import kuna has no prototype for picks it up and renders IsDebuggerPresent(CONCAT44(dat_c,argc)) where it used to render IsDebuggerPresent(). That is pre-existing behaviour at any unprototyped callee reached with a live argument register - the real fix is a Win32 prototype table, not this option - and it was measured at 4 of the 30 images the pass fires on across 139 PE crackmes. Note also that the prototype is parked by NAME and the name is minted at load, so combining this with a non-default `namestyle` makes it a silent no-op rather than a wrong prototype.", "example": "--option entrymainproto off", "source_decompiler": "kuna", "inspiration": "crackmes.one 65acadf3eef082e477ff5ede (ObfuscationFiesta.exe, PE32+ x86-64, stripped): kuna renders `void sub_140002530(void)` while its own decompilation of sub_14000518c renders `sub_140002530(v7,v1,v6)` three lines after __p___argc / __p___argv / _get_initial_narrow_environment; IDA Pro and Ghidra both label and type that callee from the same CRT idiom", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["main or the entry function is declared void(void) but is called with arguments", "a stripped PE's entry function takes no parameters while its caller passes three", "argc and argv are invisible in the decompiled main of a stripped windows binary", "the CRT startup calls sub_<addr>(a,b,c) and sub_<addr> declares (void)", "command-line handling in main cannot be followed because the arguments have no names"]},
  {"option": "machomain", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "prototype-source", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-machomain", "summary": "Name the Mach-O `LC_MAIN` entry routine `main` and declare it `int main(int argc, char **argv)`. A stripped Mach-O executable answers `kuna functions` with an inventory of `sub_<addr>` and nothing else, so the one function an agent needs first - where the program starts - is indistinguishable from the other twenty-three, and finding it means reading bodies until one looks like a prompt loop. The image already says which it is, and says it in a place `strip` does not touch: `LC_MAIN` is a load command whose `entryoff` field is documented as the file offset of `main()`, `ld64` emits it for every normally-linked executable, and `dyld` calls that address as `main(argc, argv, envp, apple)`. The name is therefore a restatement of the container, not an inference, and it is spelled without the Mach-O assembler's `_` prefix because the C name is what was asked for. The prototype rides the same fact. kuna reads a callee's parameters out of the callee's OWN body - an ABI argument register read before it is written is a parameter - so a `main` that ignores its arguments reads neither and is declared `void(void)`; on Mach-O there is no in-image call site to read them off, because the C runtime that calls `main` lives in `libdyld.dylib`. This is the Mach-O counterpart of `entrymainproto`, and it is typed differently on purpose: that pass reports the widths a recovered PE call site establishes and refuses to assert the C library's declaration, since the same shape carries `wmain`'s `wchar_t **`, whereas `LC_MAIN` IS the POSIX `main` by definition, so the real `int`/`char **` spelling is the honest one and a string literal renders through `argv[i]`. `envp` is deliberately not declared - `dyld` passes it and a fourth `apple` pointer, but the extra unused slots cost more noise than they buy. Refuses on anything that is not an `MH_EXECUTE` Mach-O, on an `LC_UNIXTHREAD`-only image (that entry is the crt `start`, not `main`), on an entry outside every executable section, on an entry that already carries a function symbol (a non-stripped image names it `_main` itself and that name wins), and on an image that already defines a symbol spelled `main`, so the by-name prototype park can never land on a different function. Structurally inert on every ELF, PE and COFF target.", "use_when": "On by default (DIV-111). Makes a stripped Mach-O report its program entry as `main` instead of one more `sub_<addr>`, with argc and argv named: the tell-tale is `kuna functions` on a macOS binary listing no `main` at all, and `kuna decompile --addr <the LC_MAIN entry>` rendering `unsigned long sub_100003a80(void)`. Flip off to restore the `sub_<addr>` / `void(void)` form exactly - e.g. on an image whose `LC_MAIN` entry is not really a C `main` (a hand-rolled entry point, a repurposed startup, a packer stub), where the name and the two slots would both be misleading, or to see the parameters body-driven recovery finds on its own.", "example": "--option machomain off", "source_decompiler": "kuna", "inspiration": "crackmes.one 653d88600f4238b24302b0ec (crackme0x04, Mach-O 64-bit x86_64 PIE, stripped): `kuna functions` listed 24 functions, all `sub_<addr>`, and the program routine only as `sub_100003a80`, which `kuna decompile` rendered `unsigned long sub_100003a80(void)` -- while `LC_MAIN` in the same file states `entryoff = 0x3a80`. IDA Pro and Ghidra both label that address from the same load command", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["kuna functions on a macOS binary lists no main at all", "the entry of a stripped Mach-O is reported only as sub_<addr>", "a Mach-O program routine decompiles as unsigned long sub_<addr>(void)", "argc and argv are invisible in the decompiled main of a stripped mach-o binary", "there is no way to tell which of a stripped Mach-O's functions the program starts in"]},
  {"option": "armlibcmain", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-armlibcmain", "summary": "Recover `main` from the crt1 `_start` of a NON-PIE ARM32 ELF, where the address is carried by the image rather than by a relocation. Entry-discovery oracle 4 already reads the `_start` -> `__libc_start_main(main, ...)` idiom on x86-64, AArch64, RISC-V and position-independent ARM, but the ARM path is built around the `R_ARM_RELATIVE` that relocates the GOT slot crt1 loads `main` from; a non-PIE executable carries no such relocation, because the linker knows the final address and simply stores it, so that cross-check finds nothing and the oracle returns None. The cost is not a shorter inventory. kuna gives a discovered entry the extent that runs to the next one, so on a stripped image the whole gap between the last discovered entry and the end of .text is nominally owned by whichever entry precedes it - typically an INIT_ARRAY routine that ends in an unconditional branch a few instructions in. The recursive-descent walk follows that routine's real control flow, stops at its real terminator, and never decodes the rest of the gap, so every literal the missing code loads has no reader: `kuna strings` reports the program's own prompt with `xrefs_count 0` and an empty `functions` list, and `kuna xrefs --to` that address finds nothing. This pass decodes the two shapes glibc's ARM crt1.o has shipped, both anchored on the `bl` to the PLT stub the import table names `__libc_start_main` - the classic literal-pool form (`ldr r0,[pc,#imm]`, the pool word IS main's address) and the GOT-indexed form a modern -no-pie link supplies (`ldr r0,[pc,#imm]` then `ldr r0,[rN,r0]`, where the pool word is a .got-relative offset and the slot the linker filled holds main). The call site is what makes the recovered word main rather than a plausible code address: the ARM procedure call standard puts the first argument in r0, so the last write to r0 before that call is by definition what the C runtime is being handed. The address is emitted as a function entry and named `main`. Refuses on anything that is not a 32-bit ARM ELF, on a Thumb `_start` (glibc's ARM crt1 is A32), on an image with no PLT stub named `__libc_start_main` (a static link, or an entry point that is not a C runtime), on a `bl` with no `ldr r0,[pc,#imm]` before it in the `_start` window, on a recovered address outside every executable section or equal to `_start` itself, and on one that already carries a function symbol - whatever named it has the better name. Structurally inert on every non-ARM target and on every PIE ARM image, where the existing oracle already resolves the GOT slot through its relocation.", "use_when": "On by default (DIV-132). Makes a stripped non-PIE ARM32 binary report `main` and, through it, the body that reaches most of the program: the tell-tale is `kuna strings` giving every literal `xrefs_count 0` and `functions []` on an ARM image whose disassembly plainly shows `ldr r0,[<pool>]` a few instructions before a `printf` call, together with a `kuna functions` inventory whose largest entry is an `_INIT_<i>` or `_start` covering most of .text. Flip off to restore that inventory exactly - e.g. on an image whose entry point is a hand-rolled loader that calls `__libc_start_main` with something other than a C `main`, where the name would be misleading.", "example": "--option armlibcmain off", "source_decompiler": "kuna", "inspiration": "crackmes.one 5ab77f5d33c5d40ad448c69c (arm_kgme1, ELF 32-bit LSB executable, ARM EABI4, dynamically linked, stripped): the prompt at 0x9124 reported xrefs_count 0 and functions [] although 0x8d20 `ldr r0,[0x9024]` loads it immediately before the printf call, because main at 0x8cf8 was never discovered - _start hands it over in the pool word at 0x8398 and the image carries no relocation for it", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["kuna strings reports xrefs_count 0 and functions [] for every literal in a stripped ARM binary", "kuna functions on an ARM ELF lists no main and one entry covering most of .text", "a prompt an ARM binary plainly prints has no owning function", "kuna xrefs --to a .rodata address finds nothing in a non-PIE ARM executable", "most of a stripped ARM binary's .text is never decoded although it is inside a discovered function's extent"]},
  {"option": "elfmain", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "prototype-source", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-elfmain", "summary": "Name the ELF routine crt1 hands to `__libc_start_main` `main`, and declare it `int main(int argc, char **argv, char **envp)`. kuna already finds the address: entry-discovery oracle 4 decodes the `_start` -> `__libc_start_main(main, ...)` idiom on x86-64, AArch64, ARM and RISC-V and seeds the recovered VMA as a function entry - but address-only, so on a stripped ELF the program's own starting point arrives in the inventory as one more `sub_<addr>`, carrying whatever prototype reading its body alone produces. On coreutils `fmt` built -O2 and stripped that is `unsigned long sub_26a0(int a0,char **a1)`: the two slots exist because this particular `main` happens to read them, their widths are right, and everything else is absent - which function this is, that `a0` counts the strings `a1` points at, and that the value handed back is the process exit status rather than an `unsigned long`. The return type in particular is unrecoverable from the body, because `main`'s result is consumed by `__libc_start_main`, outside the image; kuna types it from the widest register write it can see. The C runtime's contract states all of it: the first argument to `__libc_start_main` IS `main`, and what that runtime then calls is `main(argc, argv, envp)`. This is the ELF counterpart of `machomain` (which reads the same two facts off a Mach-O `LC_MAIN`) and it is typed the same way and for the same reason, in contrast to `entrymainproto`, which reports only the widths a recovered PE call site establishes because the same shape carries `wmain`'s `wchar_t **`. All three arguments are declared, including the `envp` most programs ignore, because the prototype is applied LOCKED: declaring two parameters asserts there is no third one, and on a `main` that reads `envp` that deletes a parameter recovery had already found - the entry value stops being an input and the read becomes an uninitialised local the body still passes on. Nothing readable at load time separates that `main` from one that truly ignores its third argument (the in-tree ARM fixture armlibcmain_le32 @0x103dc never touches `r2` at all; it forwards it to `__printf_chk`), and of the two mistakes only one is wrong output. The lock costs something in the other direction and the corpus says how much: a `main` whose body reads an argument register PAST the third loses the slot that read was living in, so the value is dropped from the call site it was handed to, or - when the body stores it - declared as a local nothing assigns. 28 of the 645 stripped decbench ELFs whose `main` this pass names recover more than three parameters from the body, and 13 of those render such a local. None of the extra slots is a real parameter (the unstripped twins of the two loudest, openssh `sftp` -O2 @0x5250 and shadow `login` -O2 @0x3d20, both declare `int main(int argc, char **argv)`), so what changes is how an undefined register read is rendered rather than whether a parameter survives - and on coreutils `true`/`false` the same flip replaces the `undefined16 ... return v2._0_16_ << 0x40` that slot produced with `return 0`. The in-tree fixture elfmainextra_x86_64 @0x1026 pins both shapes. Refuses on a non-ELF image, on an ELF that names no `__libc_start_main` at all (a fully stripped static link states nothing that distinguishes a glibc crt1 from a hand-rolled entry point), on a recovered address equal to `_start` or outside every executable section, on an address that already carries a function symbol (a non-stripped ELF names it `main` itself and that name wins), and on an image that already spells a symbol `main`, so the by-name prototype park can never land on a different function. The non-PIE ARM32 shape oracle 4 cannot see is `armlibcmain`'s, which already installs the name; only the prototype is added there, so flipping that option off still restores its inventory exactly.", "use_when": "On by default. Makes a stripped ELF report its program entry as `main`, with `argc`, `argv` and `envp` named and an `int` result, instead of one more `sub_<addr>` returning a guessed width: the tell-tale is `kuna functions` on a stripped Linux binary listing no `main` at all, and `kuna decompile --addr <the crt1 argument>` rendering `unsigned long sub_<addr>(int a0,char **a1)` or, when that `main` ignores its arguments, `void sub_<addr>(void)`. Flip off to restore the `sub_<addr>` / width-only form exactly - e.g. on an image whose `_start` is a hand-rolled loader that calls `__libc_start_main` with something other than a C `main`, where the name and the two slots would both be misleading, or to see the parameters and the return width body-driven recovery finds on its own.", "example": "--option elfmain off", "source_decompiler": "kuna", "inspiration": "decbench coreutils `fmt` built -O2 and stripped: `kuna decompile-all fmt --addr 0x26a0` renders `unsigned long sub_26a0(int a0,char **a1)` while the DWARF of the unstripped twin declares `int main(int argc, char **argv)` at that address, and kuna's own entry oracle 4 recovered 0x26a0 from `_start` in the first place. IDA Pro and Ghidra both label and type that address from the same call site", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["kuna functions on a stripped Linux binary lists no main at all", "the entry of a stripped ELF is reported only as sub_<addr>", "a stripped ELF program routine decompiles as unsigned long sub_<addr>(int a0,char **a1)", "argc and argv are invisible in the decompiled main of a stripped elf binary", "main returns unsigned long instead of int in a stripped ELF", "there is no way to tell which of a stripped ELF's functions the program starts in"]},
  {"option": "funcstart_patterns", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-funcstart-patterns", "summary": "Run the FULL byte-pattern function-start pass: a faithful port of Ghidra's FunctionStartAnalyzer over the entire vendored pattern corpus (Ghidra/Processors/<P>/data/patterns/*.xml), parsing every <patternpairs> (the DittedBitSequence prepattern/postpattern pairs) and bare <funcstart/> pattern for x86/x86-64 (headline) plus AArch64/ARM/RISC-V/MIPS/PPC. A candidate is a function start iff a postpattern (the prologue shape) matches at it AND a prepattern (the preceding context: a RET/JMP/NOP/...) matches the bytes immediately before it, at the instruction alignment. This is the full superset of entry_disc's always-on oracle 5, which ports only a minimal three-prologue x86-64 subset. Default-OFF because it is output-changing (it discovers MORE functions): when off, the pass's facts are dropped at commit and every decompilation is byte-identical to the entry_disc-only baseline. The after=defined / validcode=N pattern post-rules and the <possiblefuncstart/> / thunk / label actions are dropped (they need a PseudoDisassembler the analyzer tier does not have) — a documented LOSS, the same wall entry_disc's oracle-5 docs record.", "use_when": "On discovers additional function starts in a stripped binary via the full Ghidra prologue pattern set (e.g. a `push rbx; mov rbx,rdi` function after NOP padding that the minimal oracle misses); off (default) keeps only the entry_disc + symbol-stream functions so the output is byte-identical to the baseline.", "example": "--option funcstart_patterns on", "source_decompiler": "kuna", "inspiration": "Ghidra FunctionStartAnalyzer + PatternPairSet/DittedBitSequence (the byte-pattern function-start matcher)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["functions after nop padding missing in a stripped binary", "a push rbx; mov rbx,rdi prologue never discovered as a function start", "code-bearing gaps between discovered functions left undefined"]},
  {"option": "cortexmvectors", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-cortexmvectors", "summary": "Widen the ARM Cortex-M hardware vector-table signature (entry discovery oracle 6). The shipped signature confirms a table only when (a) it starts a section the loader maps executable, (b) its word 0 is in the architectural SRAM block 0x20000000-0x3FFFFFFF, and (c) its word 1 equals e_entry. All three over-constrain real firmware: the table is DATA the CPU reads, so a bare-metal link script normally emits .isr_vector as an A-only section in a read-only PT_LOAD; STM32F4 and -M7 parts put the initial stack in CCM/TCM at 0x10000000; and e_entry is the ELF start symbol, which a link script is free to point somewhere other than the reset vector. With this on, a candidate is any allocated section whose word 0 lands in 0x10000000-0x3FFFFFFF and whose slots from word 1 yield at least three Thumb handler pointers (odd, inside an executable section) — the run of handlers replaces the e_entry equality, and three consecutive conforming slots is a shape a .data structure does not reach by accident. Confirming a table arms the reset/exception handler seeds AND the whole-image Thumb (TMode=1) region paint, so a stripped Cortex-M image goes from a handful of functions to the whole firmware. The widened scan runs ONLY where the shipped signature found nothing, so an image that resolved a table before resolves the same section with the same harvest: the option can add discovered entries, never remove one. ARM-only and real-object-path only, so every XML datatest is structurally untouched. Output-changing (it discovers more functions) => default-OFF.", "use_when": "Flip on for a stripped bare-metal ARM Cortex-M firmware image (STM32/nRF/SAM/...) that decompiles to only a few functions, or whose code decodes as A32 garbage, because kuna never confirmed its vector table. The tell-tales: the .isr_vector section is flagged A (not AX) and sits in a read-only PT_LOAD, the first word is a 0x1000xxxx CCM/TCM stack pointer rather than 0x2000xxxx, or the second word does not match the ELF e_entry. Stands alone (it needs no other option), and composes with the listing + funcstart_patterns + aif set the decompile-all/functions drivers already inject on non-x86-64. The `aggressive` preset carries it (DIV-93) and `auto` selects `aggressive` under 500 KiB, so on the whole-binary surfaces the widened signature is what a stripped image of that size gets by default; the shipped catalog default stays off, which is what the datatest corpus and an explicit `--mode reliable` see. Off keeps the strict e_entry-matching signature, so any image that already worked is byte-identical either way.", "example": "--option cortexmvectors on", "source_decompiler": "kuna", "inspiration": "decbench:O2-cleanflight-cleanflight_DALRCF405, O2-betaflight-betaflight_STM32F405, O2-crazyflie-cf2, O2-nuttx-nuttx; kuna's own ARM entry-recall measurement (docs/features/cortexmvectors/analysis.md; step 1 of the arm-entry-granularity proposal)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["stripped cortex-m firmware yields only a handful of functions", "arm firmware body decodes as a32 garbage instead of thumb", "nmi/hardfault/systick exception handlers never discovered as entries", "isr_vector table present but no vector-derived function starts"]},
  {"option": "ptrentry", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-ptrentry", "summary": "Discover ARM function entries that are reachable ONLY through a code-pointer word (a vtable / fops struct / ISR table / literal-pool constant). The shipped Stage-3 code-pointer scan already collects every 4-byte-aligned Thumb pointer in every allocated section, then accepts the target only if it opens with a stack-frame prologue AND disassembles into more than two instructions. Those two shape predicates reject the bulk of the pointer-referenced population on bare-metal firmware: 93 percent of the missed entries establish no frame at all, and 41 percent are leaves of 8 bytes or less (movs r0,#0; bx lr, or a bare bx lr, which is a perfectly valid Cortex-M exception handler). With this on, a target is admitted on CONTAINMENT evidence instead of shape: no word that references it may be the bytes of a decoded instruction (that word is an operand, not a table slot), and none may lie in the same discovered function as the target itself (that is the ldr pc,[pc,r] switch-table shape, whose slots point into their own body); the length floor is replaced by a terminating-routine check, so a one-instruction routine that reaches a clean return is accepted while a data word that merely decodes is not. Measured on the 48-binary decbench Cortex-M corpus: +1613 recovered ground-truth entries, 95.2 percent of the new entries are DWARF function addresses, ZERO split a real function body and ZERO previously-recovered entries are lost. The accepted entries are emitted as an additive fact stream and never re-seed the recursive-descent walk, which is what makes never-removes-an-entry a property of the wiring. ARM-only and Listing-tier, so it is a strict no-op on every other architecture and without --option listing on. Output-changing (it discovers more functions) => default-OFF.", "use_when": "Flip on for a stripped ARM firmware image whose callback / vtable / ISR-handler functions produce no output at all: the tell-tale is a hole in the decompilation, where kuna emits a function at one address and the next at a higher one with real code in between (a 4-byte movs r0,#0; bx lr driver stub, an exception handler that is a bare bx lr). Composes with the listing + funcstart_patterns + aif set the decompile-all/functions drivers already inject on non-x86-64, and with cortexmvectors; needs --option listing on to do anything. The `aggressive` preset carries both it and `listing` (DIV-93) and `auto` selects `aggressive` under 500 KiB, so on the whole-binary surfaces it is live by default for a stripped image of that size; the shipped catalog default stays off, which is what the datatest corpus and an explicit `--mode reliable` see. Off keeps the prologue-plus-three-instruction shape test, so any binary that already worked is byte-identical either way.", "example": "--option ptrentry on", "source_decompiler": "kuna", "inspiration": "decbench:O2-nuttx-nuttx, O2-betaflight-betaflight_STM32F405, O2-cleanflight-cleanflight_DALRCF405, O2-crazyflie-cf2, O2-libopencm3-cdcacm; kuna's own ARM entry-recall measurement (docs/features/ptrentry/analysis.md; step 2 of the arm-entry-granularity proposal, PR #239). Precision model after Ghidra's OperandReferenceAnalyzer / DataOperandReferenceAnalyzer split (create functions from instruction operands, never from a raw data pointer).", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a small arm callback or isr handler produces no decompiled output at all", "kuna emits one function then the next with un-decompiled code in between", "bare bx lr exception handlers never discovered as entries", "vtable or fops-struct callbacks missing from a stripped arm binary", "driver op-struct function pointers left undiscovered"]},
  {"option": "poolentry", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-poolentry", "summary": "Reconstruct the ARM PC-relative literal pools that kuna's Listing never defines as data, and use them to fix the two defects that follow. `run_aif` slides its gap cursor ONE BYTE at a time with no instruction-alignment filter, because `first_undefined_after` is byte-granular by construction. A literal pool is data, so it is an undefined gap; the cursor probes every byte of it and accepts the first one whose 2-mnemonic fingerprint clears the histogram threshold. On STM32 Thumb the pool words are SRAM addresses 0x2000xxxx whose HIGH halfword decodes as movs r0,#0, so the accepted entry lands at pool_word+2, one halfword before the real function, and on accept the cursor jumps past the body so the true entry is never probed. In A32 the whole word decodes, so the phantom lands on the word itself. Upstream Ghidra does not have this: its reference analyzer defines pc-relative literal targets as DATA before AIF runs. This pass recovers the missing definition from the references that are there - every resolved [0xADDR] operand and every [pc,#imm] literal load, including the second word of a 64-bit vldr/ldrd - and takes maximal stride-4 runs of referenced words as the pools, which is strictly more conservative than an ELF $d mapping-symbol oracle. Two consumers. RECALL: emit an entry fact at the first address after a pool that abuts a return-class terminal, when it is undefined and passes AIF's own fingerprint and valid-subroutine tests; purely additive, so never-removes-an-entry is a property of the wiring as in ptrentry and tailcallentry. PRECISION: drop an AIF accept that lies inside an inferred pool, but ONLY when the pool end carries a replacement entry, which makes the removal a MOVE rather than a delete - unpaired it destroys 531 real bodies, paired it destroys zero. Measured on 98 stripped ARM images (48,553 DWARF addresses): 191 new entries, 188 ground truth (98.4 percent), ZERO ground-truth entries lost, ONE real body split, and 4,220 phantom entries retired of which zero are ground truth and 4,218 were strictly inside a real function body, with ZERO real bodies made unreachable. Cortex-M recall 97.25 to 97.43 percent, u-boot A32 58.35 to 60.65 percent, and the share of reported functions that are real DWARF entries 76.4 to 82.3 percent. ARM-only in effect: on 20 x86-64 ELFs and 8 i386 PE images it adds and removes exactly zero entries, because x86-64 RIP-relative loads target .rodata rather than .text interstices and i386 has no PC-relative addressing at all. Inert without --option listing on and without aif.", "use_when": "Flip on for a stripped ARM firmware image whose function list is full of sub_<addr> entries two bytes below a real function, or whose functions each appear twice at addresses two bytes apart. The tell-tale in the C is a leading dead movs r0,#imm that kuna then constant-folds through the body, so a function loses a parameter or returns a constant. Also the only thing in the ARM entry sequence that moves an A32 image such as u-boot. The `aggressive` preset carries it (DIV-93) and `auto` selects `aggressive` under 500 KiB, so on the whole-binary surfaces the pool inference is live by default for a stripped image of that size; the shipped catalog default stays off, which is what the datatest corpus and an explicit `--mode reliable` see. Off keeps the current AIF accept list exactly, so any binary that already worked is byte-identical either way.", "example": "--option poolentry on", "source_decompiler": "kuna", "inspiration": "Ghidra's reference analyzer defines pc-relative literal targets as DATA before AIF runs, so the pool is never an undefined gap there; decbench O0/O2 betaflight, cleanflight, crazyflie, u-boot; step 5 of the arm-entry-granularity sequence, PR #239", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a stripped arm function list carries sub_<addr> entries two bytes below the real function", "the same arm function appears twice at addresses two bytes apart", "a decompiled arm function returns a constant or drops its first parameter because of a dead leading movs", "kuna reports far more functions than the binary has on bare-metal arm", "an arm function entry lands one halfword before the real prologue", "a32 u-boot recall is far below the cortex-m images"]},
  {"option": "arm_markers", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-armmarkers", "summary": "Run the ARM/Thumb decode-mode marker pass: paint the SLEIGH TMode context from ARM mapping symbols ($t/$a) + the STT_FUNC odd-address (LSB=1 => Thumb) convention so Thumb code decodes as Thumb. ARM-only; a no-op on every other language.", "use_when": "On (default) decodes Thumb regions as Thumb on ARM; off leaves the default (ARM) decode mode.", "example": "option arm_markers off", "source_decompiler": "kuna", "inspiration": "Ghidra ARM_ElfExtension.evaluateElfSymbol + ArmSymbolAnalyzer", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["thumb code misdecoded as arm garbage instructions", "$t/$a mapping symbols ignored so the wrong decode mode applies"]},
  {"option": "mips_gp", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-mipsgp", "summary": "Run the MIPS $gp-recovery pass: seed t9 = func_entry as a tracked register value at each MIPS function entry (the PIC jalr t9 ABI convention) so a PIC prologue's addu gp,gp,t9 folds to the real $gp and $gp-relative GOT/.sdata loads resolve. MIPS-only; a no-op on every other language.", "use_when": "On (default) resolves $gp-relative loads on MIPS; off leaves the raw $gp (unresolved *(gp + offset)).", "example": "option mips_gp off", "source_decompiler": "kuna", "inspiration": "Ghidra MipsAddressAnalyzer (assumeT9EntryAddress + checkForGlobalGP)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["unresolved *(gp + offset) loads on mips", "got/.sdata references never fold to real addresses in a pic mips binary"]},
  {"option": "i386_pie_plt", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-i386pieplt", "summary": "Decode i386 PIE GOT-relative (jmp *disp(%ebx), FF A3 <disp32>) PLT stubs so dynamic imports (exit/dcgettext/__printf_chk/...) are named and known-no-return functions (exit) are flagged. The i386-PIE analog of the x86-64/aarch64 PLT decoders; i386-only, a no-op on every other language. Loader-tier: read via the kuna_i386_pie_plt env var (the PLT->name map is baked at load file).", "use_when": "On (default) names i386-PIE libc calls and lets exit be marked no-return (collapsing the spurious do{}while(true)/goto and restoring stack recovery); off leaves the GOT-relative stubs as sub_<addr> and the bogus fall-through loop (the pre-fix rendering).", "example": "option i386_pie_plt off", "source_decompiler": "angr", "inspiration": "test_decompiling_nl_i386_pie; angr CLE PLT/GOT import resolution + libc no-return database; usage", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["i386 pie libc calls render as sub_<addr> instead of exit/dcgettext", "spurious do{}while(true) or goto loop after an unnamed exit stub in a 32-bit pie binary"]},
  {"option": "relocrebase", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "GH-289", "summary": "Rebase the load-time analysis facts of a relocatable object (ELF ET_REL .o, COFF .obj) into the loaded image's address space. The loader lays such an object out synthetically above RELOC_BASE (relocobjects), but the analysis passes re-parse the same file and compute PRE-LINK, section-relative addresses, so two address spaces mix in one inventory. This re-presents the object to the analyzer tier before any pass reads it: each laid-out section carries the loader's relocated bytes and its load VMA, each unlaid .debug_* section has its relocations applied, and every ELF symbol is shifted by its own section's delta (sections are laid out non-contiguously, so there is no single global offset). A fact that still lands in no laid-out section is dropped rather than passed through unrebased. Loader-tier: read via the kuna_relocrebase env var (the analyzer tier runs inside load file).", "use_when": "On (default) makes kuna functions and kuna decompile-all agree on a .o/.obj, and attaches that object's DWARF names, string literals and named globals to the loaded image. Flip off to restore the pre-fix behavior -- phantom sub_<section-offset> entries beside every real function, one DWARF function at address 0, and no string/data attachment.", "example": "option relocrebase off", "source_decompiler": "kuna", "inspiration": "GH-289: kuna functions ptx.o reported 26 real functions plus 27 phantoms at 0x0/0x20/0x34 (the unrelocated .eh_frame FDE fields) while decompile-all reported 26; the DIV-68 inventory-agreement invariant", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["kuna functions on a .o or .obj lists phantom sub_<addr> entries below 0x400000 beside the real ones", "functions and decompile-all disagree on how many functions a relocatable object has", "a -g .o names one function at address 0 and leaves the rest sub_<addr>", "string literals and dwarf-named globals in a .o never attach to the loaded image"]},
  {"option": "dynrelocs", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-dynrelocs", "summary": "Apply a LINKED image's dynamic relocations (.rela.dyn / .rel.dyn / .rela.plt) when the loader snapshots it, and mark the slots PT_GNU_RELRO freezes as constant. kuna maps the PT_LOAD bytes the linker wrote, but a PIE (ET_DYN) or dynamically linked ET_EXEC leaves every R_*_RELATIVE / GLOB_DAT / JUMP_SLOT slot at 0 for the run-time loader, so the whole .got reads back null and a call through a function pointer the linker itself resolved renders (*dat_<addr>)(...). This fills the slots in (RELATIVE = load bias + addend; GLOB_DAT/JUMP_SLOT only when the symbol is DEFINED in this same image, so imports and the PLT naming path are untouched) and reports the written slots that PT_GNU_RELRO covers as constant, which is what lets the engine fold the load and resolve the call to its name. x86-64, AArch64, i386 and ARM; both RELA and REL (in-place addend) tables. Distinct from relocobjects/relocrebase, which own the pre-link ET_REL .o path. Loader-tier: read via the kuna_dynrelocs env var (the image bytes are snapshotted at load file).", "use_when": "On (default) names calls made through a relocated GOT slot in any PIE, and gives the relocated value to anything that reads that memory; an unrelocated slot holds 0, which the run-time image never holds, so this is a correctness fix rather than a judgement call. Flip off to restore the pre-fix bytes exactly - every GOT slot back to 0 and every such call back to (*dat_<addr>)(...).", "example": "option dynrelocs off", "source_decompiler": "kuna", "inspiration": "A static-PIE Rust binary rendered 27 of its own calls as (*dat_<addr>)(...): 640 of its 1539 R_X86_64_RELATIVE relocations point into .text, and every one of those GOT slots read back as 0. Ghidra's ElfRelocationHandler applies the same relocations at import; angr CLE applies them in the loader.", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a call in a pie binary renders as (*dat_<addr>)(...) with no name although the callee is a named function in the same image", "a got slot reads back as 0 in the decompiled output", "reading a relocated function-pointer table in a pie binary yields all zeroes"]},
  {"option": "symbolnamerepair", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "environment-binding", "strength": "HARD", "rewind": "P1", "issue": "kuna-symbolnamerepair", "summary": "Keep one degenerate qualified symbol name from aborting the load of an entire binary. Database::find_create_scope_from_symbol_name splits a loader symbol name on every :: and nests one Scope per component; an EMPTY component (a::::b, ::b) is rejected by attach_scope with 'Non-global scope has empty name'. The symbol table is installed inside load file, so that error escapes bootstrap_from_object rather than costing one symbol -- every command (decompile, decompile-all, functions, decompile-project) then answers 'could not build an architecture' and emits nothing. A symbol name is attacker-controlled data that no header check validates, so a hostile binary can buy a total denial of analysis for a few .strtab bytes. On, the empty component is skipped and the symbol keeps the rest of its scope path. Loader-tier: read via the kuna_symbolnamerepair env var (the symbol install runs inside load file, upstream of every option command).", "use_when": "Leave on. Flip off when you are investigating a binary's symbol table itself and want the degenerate name to fail loudly with the scope error rather than being repaired silently.", "example": "option symbolnamerepair off", "source_decompiler": "kuna", "inspiration": "A MinGW-built malware DLL (private/native_payload.dll) failed to load at all: 1184 libstdc++ symbols sat in a C++ anonymous namespace, whose name-only demangling reduced to an empty :: component. That root cause is fixed in kuna-analysis analyzers/demangle; this gate is the backstop for every other way a name can arrive degenerate, including a deliberately crafted one.", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["could not build an architecture for <binary>: Non-global scope has empty name", "an unstripped C++ binary fails to load entirely while a stripped copy of it loads fine", "kuna functions, decompile-all and decompile-project all fail on the same binary with the same scope error", "a binary loads in ghidra or ida but kuna refuses it at load with a symbol-scope error"]},
  {"option": "symbolnamechars", "values": ["off", "safe", "ident"], "default": "safe", "destructive_as_default": false, "phase": "P1", "subphase": "environment-binding", "strength": "HARD", "rewind": "P1", "issue": "kuna-symbolnamechars", "summary": "Sanitize a symbol name's raw bytes where the name is MINTED (the loader's .symtab/.dynsym/PLT/ET_REL walks and the analysis passes' recovered names), so the bytes a binary claims cannot restructure the C document they are printed into. Nothing between .strtab and emitted C validates a name today: a */ closes the // Function: header comment early, a raw 0x0a splits the header, the .h prototype, the definition, the call site and the .asm label across two lines each, // comments out the rest of the line, and an invalid UTF-8 byte becomes U+FFFD so two distinct symbols (byte 0x80 vs byte 0x81 in the middle of the same name, at two addresses) collapse onto ONE String and the export carries a C redefinition. safe rewrites only that structural set - ASCII control bytes 0x00-0x1F and 0x7F, the quote and escape characters (double quote, single quote, backslash), a * or / that forms a comment delimiter, and any byte of an invalid UTF-8 sequence - each to its _x<hh> hex escape, which is injective so distinct names stay distinct. Everything else (period, dollar, at, colon, dash, plus, angle brackets, parentheses, semicolon, braces and valid multi-byte UTF-8) is left alone, so safe is a NO-OP on every name a real toolchain emits, gcc's .constprop.0/.part.1/.isra.0/.cold clone suffixes included. ident additionally folds every character outside A-Za-z0-9_ per :: component (the same reduction the Itanium RTTI class-name recovery applies), which DOES rewrite those clone suffixes and is why it is reachable rather than default. Loader-tier: read via the kuna_symbolnamechars env var, because names are minted inside load file, upstream of every option command.", "use_when": "Leave on safe. Flip to off when you are auditing what a binary's symbol table literally claims and want the bytes verbatim, accepting that a hostile name corrupts the export. Raise to ident when you intend to COMPILE the decompile-project export: it makes every name a valid C identifier, at the cost of rewriting err_fatal.constprop.0 to err_fatal_constprop_0 on most -O2 binaries, so the name you pass back to kuna decompile <name> changes with it.", "example": "option symbolnamechars ident", "source_decompiler": "kuna", "inspiration": "GH-340: a fixture whose .symtab names a function a*/b exports a .c whose header comment closes mid-line; one whose name holds a real newline splits every rendering of it in two; and two functions whose names differ only in one invalid UTF-8 byte export two identical definitions and two identical prototypes.", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a function name in the emitted c contains a newline and splits the declaration across two lines", "the // Function: header comment ends early and the address becomes code", "decompile-project emits two functions with the same name and the .h has a duplicate prototype", "a symbol name renders with a U+FFFD replacement character", "kuna decompile <name> cannot address a function that kuna functions printed", "the exported .c does not compile because a function name contains */ or //"]},
  {"option": "symbolnamebound", "values": ["<n>", "off"], "default": "256", "destructive_as_default": false, "phase": "P1", "subphase": "environment-binding", "strength": "HARD", "rewind": "P1", "issue": "kuna-symbolnamebound", "summary": "Cap how many Scopes one qualified symbol name may nest, so an attacker-controlled name cannot exhaust the process. Database::find_create_scope_from_symbol_name splits a loader symbol name on every :: and nests one Scope per component with no bound on the count, the per-component length or the total; a Scope costs ~1.5 KB resident (RangeList, three BTreeMaps, two Strings and a maptable sized to the address-space count), and the interning key includes the parent, so a repeated component name still allocates a fresh Scope at every level. That makes a name a ~498x input-to-RSS amplifier: measured on kuna functions, a .symtab carrying one function named ('a::' * N) + 'z' costs 52,932 KB at N=0, 79,288 KB at N=20,000 and 345,132 KB at N=200,000, i.e. 291,724 KB for 600,001 bytes of .strtab. decompile-all is worse -- quadratic in depth -- so 60 KB of .strtab already buys a 20-second stall. The value is the scope-component ceiling; a name over it keeps the first n-1 components and folds the rest into one synthetic component carrying a deterministic FNV-1a 64 hash of the exact dropped bytes, so distinct names stay distinct and the base name is never rewritten. Per-component (1024 B) and total scope-path (4096 B) length ceilings ride along; the base name is exempt from both, so a long FLAT name such as a Go generic instantiation is untouched. off restores the historical unbounded behavior. SCOPE OF THE FIX: this caps what ONE name costs, not what a symbol TABLE costs -- the amplifier is per-Scope, and 3,000 distinct 64-component names (1.9 MB of ELF) still cost 343 MB with the gate on, because none exceeds the ceiling. It closes the reported primitive and the quadratic decompile-all blowup that rode on it; a cap on the total Scope population is a larger, separate change. Loader-tier: read via the kuna_symbolnamebound env var (the symbol install runs inside load file, upstream of every option command).", "use_when": "Leave at 256. The deepest :: nesting measured over 1,683,515 demangled names (repo fixtures, 14 large C++ objects, 9 rustc binaries including a 153 MB librustc_driver, and the 60 largest ELF objects under /usr/lib/x86_64-linux-gnu and /usr/bin) is 21, and the deepest over a rustc binary DWARF names is 79 -- the DWARF path does not strip template arguments, so every :: inside <> counts -- so nothing real reaches it. Raise it for a genuinely deeper generated binary, and note that folding changes the name you must type for load function / kuna decompile <name> (the folded spelling is what the listing renders, and both the create and the resolve path apply the same fold, so either spelling resolves). Set off to reproduce a report against the historical unbounded behavior.", "example": "option symbolnamebound 512", "source_decompiler": "kuna", "inspiration": "GH-338: 600 KB of .strtab in one deeply nested symbol name turned into 292 MB of RSS, a denial-of-analysis primitive that costs an attacker a few bytes and survives every header check the loader applies, because a name is not parsed until it is split. The default is set from measurement rather than from the DWARF walks' own MAX_SCOPE_DEPTH = 32: the deepest real name found anywhere is 79 (a templated addr2line DWARF name in a rustc binary), and 32 or 64 would have folded two real names on that binary.", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["kuna functions or decompile-all uses hundreds of megabytes of rss on a small binary", "kuna stalls for tens of seconds or is oom-killed loading a binary whose symbol table is a few hundred kilobytes", "a stripped copy of a binary loads instantly while the unstripped copy hangs", "memory grows roughly 1.5 kb per :: in a symbol name"]},
  {"option": "msvcfpconst", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-msvcfpconst", "summary": "Recover an MSVC __real@ floating-point constant COMDAT from its mangled symbol name, and let the engine fold it. MSVC never encodes a floating-point literal into the instruction stream (x87 and SSE both load one from memory), so it emits each literal as a COMDAT whose NAME spells the value: __real@8@3ffec90fdaa22168c000 is pi/4. COMDAT folding keeps the definition in exactly one translation unit, so in every other object that symbol is UNDEFINED -- no section, no bytes -- and kuna binds it to a synthetic extern slot with nothing behind it, leaving v6 = (... * dat_402020 + dat_402040) * dat_400ae0 with every operand an opaque address. The defined half is no better at the shipped defaults: its bytes are mapped and read-only, but folding a read-only global is gated by the program-wide option readonly, which is default-off. On, three spellings are decoded -- __real@<size>@<20 hex> (an x87 80-bit extended datum, with 4 = float and 8 = double giving the storage width), __real@<16 hex> (IEEE double bits) and __real@<8 hex> (IEEE float bits, what MSVC has emitted for a float literal since VS2005) -- the bytes are materialised at the extern slot, and BOTH halves are added to the loader's constant-by-construction range list so the whole expression renders as literals rather than half of one. A defined COMDAT's bytes are cross-checked against its own name before its range is admitted, and every encoding with no faithful f64 image (an x87 Inf/NaN, a denormal, an unnormal) plus every other mangling (__xmm@, __ymm@) is refused: a wrong constant is worse than an honest dat_<addr>. Loader-tier: read via the kuna_msvcfpconst env var (the bytes are materialised at load file).", "use_when": "On (default) turns the floating-point expressions of any MSVC .obj back into readable arithmetic; the mangled name IS the datum, so an empty extern slot is missing information rather than a judgement about it. Flip off to restore the pre-feature bytes exactly - every __real@ operand back to dat_<addr>, and the undefined slots back to having no backing at all.", "example": "option msvcfpconst off", "source_decompiler": "kuna", "inspiration": "A Command & Conquer bounce.obj rendered its whole physics pipeline in opaque addresses: 12 of its floating-point constants are __real@ COMDATs, 5 of them undefined in that object. Every one is a recognisable literal from the original source (pi/4, 2pi, 3.0, 150.0, the 2^-31 scale that turns a 31-bit random integer into a fraction), which is what makes the object a sound oracle for the decoder. IDA and Ghidra both read the value out of the name.", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a floating-point expression in an msvc .obj is written entirely in dat_<addr> operands", "an msvc object's fp constant reads as dat_<addr> although its symbol name spells the value", "one operand of an fp expression is a literal and the neighbouring one is still dat_<addr>", "a __real@ symbol in a .obj has no bytes behind it and any read of it fails"]},
  {"option": "ifuncfpret", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "interp-bee-ifunc-fpret", "summary": "Name the x86-64 IFUNC (R_X86_64_IRELATIVE) .plt.sec/.iplt PLT stubs the symbol-keyed PLT pass leaves undiscovered (an IRELATIVE slot carries no symbol, only its resolver's address), so a tail `jmp` to one is recovered as a tail call (tailcalljump) to a discovered `ifunc_<resolver>` function instead of flowing into the stub and rendering `(*dat_...)(...)`. Loader-tier: read via the kuna_ifuncfpret env var (the PLT->name map is baked at load file). Stage A of the ifuncfpret chain; the FP-return-type recovery (a Ghidra-divergent indirect-tail-return change) is the documented follow-up.", "use_when": "A glibc math/mem/str wrapper (e.g. sub_15620 / log) tail-jumps to an IFUNC stub and renders `(*dat_...)(...)` with the callee dropped; on names the stub `ifunc_<resolver>` and recovers the tail call. x86-64 only; default off (opt-in, synthetic stub names).", "example": "option ifuncfpret on", "source_decompiler": "ida", "inspiration": "interp-bee/jit-bee IDA study: f64_log_base @ 0xf090 reads xmm0 uninitialized because log (sub_15620) tail-jumps to an undiscovered IFUNC .plt.sec stub", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a glibc math/mem/str wrapper tail-jumps to `(*dat_...)(...)` with the callee dropped", "an x86-64 IFUNC .plt.sec stub is not a discovered function", "xmm0 read uninitialized after calling a void-typed ifunc-dispatching wrapper"]},
  {"option": "mips_isa", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-mipsisa", "summary": "Run the MIPS16 ISA_MODE decode-mode marker pass: paint the SLEIGH ISA_MODE context at each MIPS16e/microMIPS function entry (marked by the STT_FUNC LSB-set address OR st_other & 0xf0 = STO_MIPS_MIPS16/MICROMIPS) so the body decodes in the alternate ISA instead of being misdecoded as MIPS32 (the MIPS analog of arm_markers' TMode painting; the kuna analog of Ghidra's MIPS_ElfExtension.applyIsaMode). MIPS-only; a no-op on every other language.", "use_when": "On (default) decodes MIPS16 regions as MIPS16 on MIPS; off leaves the default (MIPS32) decode mode (a MIPS16 function misdecodes).", "example": "option mips_isa off", "source_decompiler": "kuna", "inspiration": "Ghidra MIPS_ElfExtension.applyIsaMode (ISA_MODE context, STO_MIPS_MIPS16/MICROMIPS + LSB)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["mips16/micromips function body misdecoded as mips32 garbage", "odd-address mips functions decode in the wrong isa mode"]},
  {"option": "dwarf", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-dwarf", "summary": "Run the DWARF recovery analysis pass: recover function/global names and typed signatures from .debug_* sections (the kuna analog of Ghidra's DWARFAnalyzer). Skips cleanly on a non-DWARF binary.", "use_when": "On (default) applies DWARF names/types; off ignores debug info (names/types come from symbols + inference only).", "example": "option dwarf off", "source_decompiler": "kuna", "inspiration": "Ghidra DWARFAnalyzer", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a -g binary still shows default names and inferred types", "dwarf function/global names and typed signatures ignored"]},
  {"option": "datasyms", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "GH-184", "summary": "Name data globals from the ELF symbol table: the loader reads the data half of the same .symtab/.dynsym walks that have always named functions -- every defined, named STT_OBJECT entry with a non-zero st_size, @VERSION stripped, deduplicated by address (.symtab before .dynsym) -- and the engine installs each as a named undefined<size> global with namelock only. The arm commits LAST, after the DWARF globals and the detected string literals, and skips any address a richer source already claimed, so what it fills is exactly the set neither reaches: a copy-relocated libc extern (stderr, stdout, stdin, optind, optarg, __progname) has a real .bss address and a .dynsym entry but no DIE in the program's own .debug_info, and a stripped or DWARF-less binary's own statics have only .symtab. Zero-size entries are dropped (the linker's section-boundary markers __bss_start/_edata/_end are exactly the sizeless ones). The st_size extent is what makes the covering-symbol query match at the real access width, so an 8-byte load of stderr finds the symbol instead of falling back to dat_<addr>.", "use_when": "On (default) renders symbol-table-named data globals by name, matching IDA Pro and Ghidra -- fprintf(stderr, ...) reads as an error path instead of fprintf(dat_61a0, ...). Flip off to restore raw dat_<addr> rendering for every global the DWARF pass does not name -- e.g. to see exactly which names came from the symbol table rather than from debug info, or if a hostile/corrupt symbol table plants misleading names.", "example": "--option datasyms off", "source_decompiler": "kuna", "inspiration": "Ghidra ElfSymbolTable data-symbol import + IDA Pro (both name STT_OBJECT entries independently of debug info); GH-184 witness: shadow faillog @0x3320 __fprintf_chk(dat_61a0,...) -> __fprintf_chk(stderr,...)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["libc globals render as dat_<addr> where every other decompiler prints stderr/stdout/optind", "fprintf stream argument shows a raw dat_ address so error paths are indistinguishable from output paths", "a stripped binary's copy-relocated externs unnamed", "an unstripped dwarf-less binary's own statics render dat_<addr>", "decompile-project exports reference undeclared dat_<addr> identifiers where a stdio global belongs"]},
  {"option": "dwarf_lines", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-dwarf-lines", "summary": "Run the DWARF .debug_line source-line comment pass: parse the .debug_line program, map each instruction PC to its source file:line, and attach it as an instruction comment so the decompiled output carries the source location (the kuna analog of Ghidra's DWARFLineInfoCommentScript). DISABLED by default (it changes the output by adding /* file:line */ comment lines). Skips cleanly on a binary without .debug_line.", "use_when": "Off (default; byte-identical output). Flip on to annotate each decompiled instruction with its DWARF source file:line.", "example": "option dwarf_lines on", "source_decompiler": "kuna", "inspiration": "Ghidra DWARFLineInfoCommentScript (.debug_line)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["no /* file:line */ source-location comments in the output", "want each instruction annotated with its dwarf source line"]},
  {"option": "cppproto", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-cppproto", "summary": "Recover C++ function prototypes from DWARF: resolve a subprogram DEFINITION through its one-hop DW_AT_specification/DW_AT_abstract_origin link (an out-of-line member definition carries no DW_AT_name of its own, so the name-only walk drops it whole), qualify the name by its namespace/class ancestry, map DW_TAG_class_type like a structure and a C++ reference like a pointer, degrade a single unmappable parameter type instead of discarding the whole signature, and bind the recovered prototype by entry ADDRESS rather than by name.", "use_when": "On (default) gives a -g C++ binary its real signatures: Account::deposit(Account *this,int amount) instead of Account::deposit(int4 *a0,int4 a1). Flip off to restore the name-only DWARF walk (every out-of-line member function loses its parameter names, types and stack locals).", "example": "option cppproto off", "source_decompiler": "kuna", "inspiration": "Ghidra DWARFFunctionImporter/DIEAggregate (specification + abstract-origin fusion, DWARFName namespace path)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["c++ member functions decompile with a0/a1 parameters despite -g debug info", "Foo *this renders as void *", "a namespaced or templated function loses its dwarf prototype", "one unmappable parameter type discards the whole dwarf signature"]},
  {"option": "typedepth", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-typedepth", "summary": "Resolve a DWARF type DIE chain to its full depth, guarded by upstream's per-DIE re-entry counter (DWARFDataTypeImporter.trackRecursion) instead of a flat three-hop budget that counted the transparent typedef/const/volatile/restrict links as well. The budget was an unfaithful reduction of the cycle guard: it fired on ordinary declarations (const char **, const size_t *, char *const [], char ***, a const member function's this), which then fell back to void, so a -g binary lost the concrete type of stack locals, globals and deep-pointer parameters. Also collapses the transparent qualifier hops on the C path so an anonymous aggregate is interned under its typedef name (mbstate_t) rather than the shared anon_struct.", "use_when": "On (default) types a -g binary's locals/globals/parameters from the debug info: char **authors, const size_t *pn, mbstate_t *ps instead of void *. Flip off to restore the pre-fix three-hop budget (deep-enough DWARF types truncate to void).", "example": "option typedepth off", "source_decompiler": "kuna", "inspiration": "Ghidra DWARFDataTypeImporter.trackRecursion (per-DIE-offset re-entry counter)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a dwarf-typed local or parameter renders void * despite -g debug info", "const char ** or char *const [] loses its element type", "a typedef'd struct pointer degrades to void *", "a global array of string pointers is marked one byte wide", "deep pointer chains (char ***) truncate to void **"]},
  {"option": "dwarfstructs", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-dwarfstructs", "summary": "Import a DWARF aggregate's LAYOUT: the DW_AT_byte_size of a DW_TAG_structure_type/union_type/class_type plus its DW_TAG_member children, each placed at its DW_AT_data_member_location verbatim (bitfields via DW_AT_bit_size with either the DWARF 4/5 DW_AT_data_bit_offset or the DWARF 2/3 byte_size+bit_offset spelling), with every member type recursed through the same DIE mapper. The mapper previously interned every aggregate as a named, EMPTY, zero-size shell, which is enough for a pointer-to-struct to render and nothing else: a zero width is not a conservative answer, it is one the ABI classifier acts on, so a by-value aggregate parameter degraded to a raw integer and an 8-byte struct return was misclassified as a hidden-return-buffer call that grew a phantom rethidden parameter the body then did arithmetic on. Aggregates are interned under their parent-qualified name, and under a size-suffixed name if that is still held by a different-sized aggregate, because populating fields makes the factory's name collision live (rustc names every enum payload struct bare: Some, Ok, Err). DW_TAG_variant_part/DW_TAG_variant/DW_AT_discr are deliberately NOT read, so a Rust enum recovers its width and no fields.", "use_when": "On (default) gives a -g binary its real aggregates: take_struct(P8 p,int k) returning p.a + p.b instead of take_struct(unsigned long,int) shifting a register, P8 ret_struct(uint x) instead of a phantom-sret P8 ret_struct(P8 rethidden,uint x), n->inner.a and b->lo instead of casts and shifts. Flip off to restore the name-only mapping (every aggregate becomes a zero-size named opaque again).", "example": "option dwarfstructs off", "source_decompiler": "kuna", "inspiration": "Ghidra DWARFDataTypeImporter.makeDataTypeForStruct/makeDataTypeForUnion (byte-size + DW_TAG_member walk at DW_AT_data_member_location)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a struct passed by value degrades to an integer parameter despite -g debug info", "a small struct return grows a phantom rethidden parameter that the body does arithmetic on", "struct fields render as casts and offsets instead of p->field", "a bitfield member renders as a shift-and-mask of the whole word", "a union parameter loses its member names", "a dwarf-recovered struct or union has size 0"]},
  {"option": "dwarfvariants", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-dwarfvariants", "summary": "Import a DWARF DW_TAG_variant_part: the DW_AT_discr discriminant member (its offset, width and type), every DW_TAG_variant's DW_AT_discr_value, and each variant's payload member and the name DWARF gives it (Ok, Err, Some, None, Cons). A Rust tagged enum carries NO DW_TAG_member of its own -- its whole layout lives under the variant part -- so dwarfstructs recovered its DW_AT_byte_size and zero fields, and a field-less aggregate is still misclassified by the ABI (an 8-byte Result return came out as a phantom-sret Result *ret_result(Result *rethidden,uint4 x), with the real parameter shifted a slot right). The recovered type is a struct of the tag plus a union of one payload struct per variant, forced to a single overlay offset, and the variant names and discriminant values come from the COMPILER, not from codegen shape -- shape does not distinguish a Result from a repr(C) struct, a (u64,u64) tuple or a fat pointer. A DW_TAG_variant with no DW_AT_discr_value is the DEFAULT (niche) variant and is kept as such; when the discriminant bytes overlap the payload there is no byte range that is only the tag, so the recovered type is the overlay union alone, with no enclosing struct and no tag field. A fieldless variant (None, Nil) overlays nothing and gets no union member -- an empty struct of the overlay width ties with the real facet in ScoreUnionFields and wins on declaration order, so ablating the exclusion makes a std-linked rustc -g witness write an Option<i64> payload as v13.payload.None and read drop glue as (*a0).dropfn.drop.None (2 functions of 612) -- but it keeps its name and discriminant value on the side table. The geometry is also recorded in a side table on the TypeFactory that nothing in the analysis reads, for a later match/if-let/Ok(v) renderer. Needs FULL debug info (-C debuginfo=2 / cargo debug = true): a Rust binary whose DWARF carries no type DIEs gains nothing. A union member selects itself by OFFSET and the discriminant is never consulted, so a variant NAME is only installed where it is forced: a facet keeps its DW_TAG_variant name only when no other variant claims a byte it claims, and a field inside a facet keeps its DWARF name only when every other variant either claims none of its bytes or names exactly that range the same way. Option<T> has one payload-carrying variant, so Some survives; Result<T,E> has two over one range, so both the union member and the facet type are spelled field_0x<offset> -- the same offset rendering dwarfvariants off produces. LOSS: for every enum with two or more payload-carrying variants -- which is most tagged enums, Result included -- no variant name appears in the emitted C at all, and the rule is conservative at facet granularity, so labels an access WIDTH could have singled out (an 8-byte store at offset 8 of enum Multi { P{a,b}, Q(u64) } can only be Q) go too. What is lost is the label, never the layout: offsets, widths, member types and the enum size are what DWARF states either way, and every variant name and DW_AT_discr_value stays on the side table for a later match renderer. Before this rule a Result<u64,u64> witness printed Ok on both arms of an if/else and on the consumer Err arm, and Err nowhere.", "use_when": "On (default) gives a -g Rust binary its enums: Result<u32, u32> ret_result(uint4 x) instead of a phantom-sret pointer to a field-less shell, and rethidden->tag = 0 / (rethidden->payload).A.__0 = x instead of *(uint4 *)rethidden = 0 / *(uint4 *)&rethidden->field_0x4 = x. A variant name only appears where exactly one variant claims the bytes (Some on an Option, A/B on an enum whose payloads do not overlap); on a Result and on any enum with two payload variants over one range the payload is spelled (dst->payload).field_0x8.__0, which is the same offset rendering this option off produces -- the discriminant field, the recovered size and the lost phantom sret are the gain there, not the label. Flip off to restore the dwarfstructs mapping (a tagged enum keeps its width and gets no fields). Extends dwarfstructs and is gated on it too, so dwarfstructs off suppresses this arm as well. Inert on any binary without full debug info, and on C (a C aggregate has no variant part).", "example": "option dwarfvariants off", "source_decompiler": "kuna", "inspiration": "DWARF 5 5.7.10 (variant entries); rustc -C debuginfo=2 enum layout; Ghidra DWARFDataTypeImporter.makeDataTypeForStruct (the member walk this extends)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a rust enum return grows a phantom rethidden parameter despite -g debug info", "a dwarf-recovered rust enum has a byte size but no fields", "Result or Option renders as an opaque container with byte offsets", "a tagged-union discriminant renders as an unnamed integer at offset 0", "a variant payload renders as a cast-and-offset instead of a named field", "a rust enum's discriminant and payload have no names even though the binary was built with full debug info"]},
  {"option": "cppsig", "values": ["off", "proven", "inferred"], "default": "proven", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-cppsig", "summary": "Apply the DEMANGLED C++ signature - the class type for `this` plus the declared parameter types - to every function whose mangled symbol survives, which on a STRIPPED C++ shared library is the whole exported API in .dynsym and the only signature source left. Parses the full c++filt-style form (the same string the Ghidra GNU demangler parses), maps a class-shaped parameter to a named opaque structure reachable only as a pointee, refuses any by-value aggregate or overloaded operator, and parks the prototype by entry ADDRESS. The return type is deliberately NOT applied: Itanium encodes one only for a template function, so the function keeps whatever kuna's own recovery finds. Three-valued because Itanium mangling cannot tell a static member function from a non-static one and adding a `this` that is not there shifts every following parameter: `proven` applies only the shapes the mangling entails (constructors, destructors, cv-/ref-qualified members, unqualified globals, explicit MSVC access/static/convention), `inferred` also decides the ambiguous nested names from class evidence mined out of the binary's own symbols.", "use_when": "proven (default) is precision 1.0000 / recall 0.7093 on google/leveldb: `void leveldb::Cache::~Cache(Cache *this)` instead of `void leveldb::Cache::~Cache(unsigned long a0)`. Raise to inferred for precision 0.9278 / recall 0.9978 - it additionally recovers plain (non-const, non-ctor) member functions like `leveldb::TableBuilder::WriteBlock(TableBuilder *this,BlockBuilder *a1,BlockHandle *a2)` and namespaced free functions like `leveldb::NewMemEnv(Env *a0)`, at the cost of a spurious `this` on a static member. Flip off to restore name-only demangling.", "example": "option cppsig inferred", "source_decompiler": "kuna", "inspiration": "Ghidra DemangledFunction.applyTo / GnuDemanglerAnalyzer \"Apply Function Signatures\"", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["c++ member functions on a stripped binary decompile with a0/a1 parameters", "this renders as int8 * or unsigned long instead of the class type", "a mangled symbol names the function but not its parameter types", "no signature recovery on a stripped c++ shared library"]},
  {"option": "callfixup", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-callfixup", "summary": "Run the call-fixup analysis pass: tag each function whose name matches a cspec call-fixup <target> (e.g. the -pg mcount/__fentry__ profiling stubs) so the engine replaces the CALL with the fixup body (the kuna analog of Ghidra's CallFixupAnalyzer).", "use_when": "On (default) dissolves matched fixup calls; off leaves the raw CALL (e.g. the mcount call stays).", "example": "option callfixup off", "source_decompiler": "kuna", "inspiration": "Ghidra CallFixupAnalyzer", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["mcount/__fentry__ profiling calls clutter every -pg function prologue", "cspec call-fixup targets rendered as plain calls instead of dissolved"]},
  {"option": "addrtable", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-addrtable", "summary": "Run the address-table analysis pass: scan .rodata/.data for a run of pointer-width values landing inside an executable section (an absolute address/jump table) and emit Data symbols + a readonly range. DISABLED by default (matches Ghidra AddressTableAnalyzer.setDefaultEnablement(false); the pointer-run scanner over-accepts).", "use_when": "Off (default, parity + over-acceptance risk); flip on to recover absolute pointer tables in a stripped target.", "example": "option addrtable on", "source_decompiler": "kuna", "inspiration": "Ghidra AddressTableAnalyzer (default-off)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["absolute function-pointer table in rodata never recognized", "indirect calls through an unrecovered address table in a stripped target"]},
  {"option": "operand_refs", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-operand-refs", "summary": "Run the scalar/operand reference-markup pass (ScalarOperandAnalyzer/ElfScalarOperandAnalyzer): linear-decode the executable sections and, for each scalar immediate operand that points into allocated read-only data (passing the <4096/byte-mask value filter and the .got/.plt exclusion), plant a typed char[N]+readonly fact so the printer renders the reference as a string literal. DISABLED by default (Ghidra ScalarOperandAnalyzer.getDefaultEnablement = !isElf, i.e. off for every ELF; the ELF subclass only removes bad .got/.plt refs kuna never creates; the useful product is already delivered by s1_strings + libproto typing; a per-instruction immediate scan over-accepts).", "use_when": "Off (default; net-negative for an ELF decompiler per docs/history/analysis-port-buildplan.md §1.2 — ELF-default-off upstream, covered-elsewhere, over-acceptance-prone). Flip on to type residual short / s1_strings-missed read-only string operands pointed at by a no-prototype call.", "example": "option operand_refs on", "source_decompiler": "kuna", "inspiration": "Ghidra ScalarOperandAnalyzer / ElfScalarOperandAnalyzer (!isElf default-off)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["an immediate operand pointing at a rodata string renders as a bare number", "string argument of a no-prototype call not typed as char[N]"]},
  {"option": "formatstring", "values": ["off", "static", "full", "on"], "default": "static", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-formatstring", "summary": "Type printf/scanf-family variadic arguments per call site. `static` (default) resolves each call site's format constant AT LOAD — the Listing supplies the call edges, the cspec supplies the format parameter's register, and a bounded constant-fold over the call's own basic block supplies the value — then parks a per-call-site prototype override (fixed types ++ format-derived varargs) the first decompile consumes, so the typing costs nothing per function. The override is installed with an open tail: the call is scored like the untyped call and drops whatever it took past its declared arguments once every call in the function has its arguments, so the calls around it keep what `off` gives them. `static` does this on x86 only; `full` resolves at load on every target the vararg rule below admits, and adds Ghidra's FormatStringAnalyzer half-B loop on top for the sites the static resolver declined: decompile, read the constant off the lifted CALL, install the override, decompile again. `off` types nothing.", "use_when": "Static (default; the load-time resolver runs wherever the Listing is built, which is every decompiling CLI surface under `--mode auto`, `aggressive` or `reliable`; only `--mode fast` turns it off). `static` types x86 targets only: a declared argument, now certain, can veto the same value at a neighbouring call. On AArch64 and ARM32 firmware, measured with the prototype closed from the start, the neighbouring calls it moved mostly went wrong (an `x8` of `0` ahead of the real arguments, iproute2 ip's parse_rtattr losing its len); over 350 x86-64 binaries (decbench and Ubuntu /usr/bin) the 8 it moved all lost a phantom. Ask for `full` to type AArch64/ARM32/RISC-V varargs anyway. Both `static` and `full` type a site only where the target passes a vararg as a named argument: every conversion on x86 and standard AArch64, integer and pointer conversions no wider than a pointer on ARM32, RISC-V, MIPS, PowerPC and Windows AArch64, nothing on Apple AArch64 or an unchecked processor; and only from a format string in a read-only section. Use `full` when a site the static resolver declined matters (a statically linked image, a format computed at run time) and the second decompile of every changed caller is affordable; use `off` to restore untyped varargs. `on` is accepted as an alias of `full`, the spelling this option had while it was a toggle.", "example": "option formatstring full", "source_decompiler": "kuna", "inspiration": "Ghidra FormatStringAnalyzer (Variadic Function Signature Override, default-off); angr _get_format_string_arg_types (engine_ail.py, the load-time literal read)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["printf/scanf variadic arguments render untyped at the call site", "%d and %s arguments carry generic types instead of int/char *", "error/warn/syslog format arguments carry generic types"]},
  {"option": "listing", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-listing", "summary": "Build the Listing/xref disassembly tier: a program-wide recursive-descent disassembly over the loadimage bytes (reusing the SLEIGH decoder + the lifted S2 flow classifier) producing three read-only sub-models (instruction / cross-reference / discovered-function) shared with the consumer analysis passes. DISABLED by default (the Listing is never built; real-ELF path only, so every parity gate is byte-identical).", "use_when": "Off (default; the Listing is not built and there is zero decode cost). Flip on to make the program-wide instruction/xref/function model available to consumer passes (e.g. discovered-no-return) on a real-ELF target.", "example": "option listing on", "source_decompiler": "kuna", "inspiration": "Ghidra Listing / ReferenceManager / FunctionManager (recursive-descent disassembly)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["noreturn discovery inert on a stripped binary", "analysis passes that need whole-image xrefs do nothing", "no program-wide instruction/xref/function model for consumer passes"]},
  {"option": "fast_funcdisc", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-fast-function-discovery", "summary": "Discover real function bodies for latency-sensitive whole-project exports without the exhaustive prologue and AIF gap scans. Starting from loader-backed entries, build one recursive Listing walk that promotes every direct CALL target. Then recover indirect-only callbacks from absolute code pointers in non-executable initialized data. On non-ARM objects, admit a target only when its first two decoded mnemonics match a fingerprint shared by at least four already-reached functions and a bounded control-flow probe validates a >2-instruction subroutine with a terminating path or informative call/edge into known code; ignore tables above 256 slots, require two distinct eligible tables when the image has more than 512 unique candidates, and validate at most 4096 candidates. ARM instead reuses the established Thumb-pointer prologue oracle. Pointer-derived roots are emitted without recursively walking their disconnected callees. Default-off as an output-changing analysis; the fast preset enables it while keeping listing, full prologue patterns, and exhaustive AIF off.", "use_when": "Enabled by fast mode for unfiltered or name-selected decompile-all/decompile-project/functions so a stripped binary does not collapse to imports plus its entry point. Flip off to restore the old metadata-only fast inventory, or when even the rooted whole-image recursive decode is too costly. Explicit --addr selections skip the preset's discovery work unless this option is explicitly turned on.", "example": "--mode fast --option fast_funcdisc on", "source_decompiler": "kuna", "inspiration": "Ghidra recursive disassembly/CreateFunctionCmd + AddressTableAnalyzer; AIF fingerprint histogram and PseudoDisassembler.checkValidSubroutine", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["fast decompile-project emits only import veneers and the binary entry", "internal direct callees have no project bodies", "function reachable through a callback or vtable pointer is absent while explicit --addr decompiles it"]},
  {"option": "rawdiscover", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-raw-image-function-discovery", "summary": "Recover a function inventory for a headerless --raw-image load by recursive descent from its --entry seeds. The object-backed discovery tier opens by parsing an object::File and every seed oracle under it reads a symbol table, an exception table or a section list, so a raw image declines the whole tier and its inventory is exactly the entry addresses the caller typed: one function spanning the file. The recursive-descent walk needs none of that metadata, so it is run directly over the raw loader's synthetic CODE section with those seeds as roots, promoting every direct call target to a function entry. The decode-mode painter and the PPC64 local-entry fold are empty, which is what a headerless image honestly records; --isa arm|thumb has already painted any whole-image mode claim. Seeds are never dropped, so the inventory can only grow.", "use_when": "On by default for --raw-image. Flip off to restore the seeds-only inventory when the walk follows a decoded call into data and mints entries that are not functions, which a bytes-are-code image with no section table can do. A function no direct call reaches and no --entry names is still undiscovered either way; assert it with --define-function.", "example": "kuna functions fw.bin --raw-image --target Cortus:LE:32:APS3:default --base 0x80000000 --entry 0x8000010c --option rawdiscover off", "source_decompiler": "kuna", "inspiration": "Ghidra recursive disassembly/CreateFunctionCmd driven from an explicit entry rather than from loader metadata", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["kuna functions on a --raw-image reports one function whose size is the whole file", "decompile-all on a raw image emits a single enormous body", "a firmware image decompiles only at addresses passed by hand"]},
  {"option": "noreturn_disc", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-noreturn-disc", "summary": "Run the discovered-no-return consumer (the first Listing/xref consumer): the evidence-tally fixpoint over the Listing — a call target is no-return when at least 3 of its call sites show no valid fall-through (the instruction after the call is not a valid instruction start / fell into data / is another function's entry), iterated to a fixpoint (a function whose last act is a call to an already-discovered-no-return function is itself no-return). The kuna analog of Ghidra's FindNoReturnFunctionsAnalyzer. Requires the Listing (option listing on) — a no-op when the Listing is absent. On by default (DIV-22, matching Ghidra's default-on analyzer), but gated on the Listing (default-off), so every parity gate is byte-identical (real-ELF Listing path only).", "use_when": "On by default (DIV-22), but a no-op unless the Listing is built (option listing on) — with the Listing off there is zero behavior change. With listing on it marks heavily-called custom/tail-calling no-return wrappers — that the static name lists do not know — no-return by the >=3-call-site evidence tally, eliminating the post-call dead code on a real-ELF target. Flip OFF (with listing on) to keep the post-call fall-through code.", "example": "option listing on --option noreturn_disc on", "source_decompiler": "kuna", "inspiration": "Ghidra FindNoReturnFunctionsAnalyzer (discovered no-return, evidence threshold)", "change_kind": "analysis-enablement", "tier": "transform", "symptoms": ["heavily-called custom die()/fatal() wrapper still treated as returning", "dead code after a stripped sub_ wrapper that never falls through at 3+ call sites", "caller swallows the next function after a wrapper call"]},
  {"option": "noreturn_discstrict", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-noreturn-discstrict", "summary": "RESTORES CODE: narrow noreturn_disc's evidence tally to the arms that observe the program, so a decoder gap can no longer forge a no-return verdict. The legacy predicate votes a call site as no-return evidence when the byte after the call is not a decoded instruction start — but the Listing walk pushes every call's fall-through onto its worklist unconditionally, so that arm fires exactly when decode_one returned an error (or the successor is out of every executable range). It is a decode-failure detector, not a fact about the callee: three undecodable bytes are enough to conclude a plainly-returning function never returns, and the engine then DELETES the live tail of every caller (GH-312). When on, only the terminal arm (a call with no fall-through at all — a tail jump lowered to a call) and the two POSITIVE arms survive: the successor is data (outside every executable range, so the compiler emitted nothing to fall into) or the successor is another function's entry (the compiler left the caller no tail). Dropping the bare arm also makes the data arm live for the first time — is_data implies !is_instruction_start, so the legacy order made it unreachable. Requires the Listing (option listing on) — a no-op when the Listing is absent. On by default (DIV-92), so every parity gate is byte-identical (real-ELF Listing path only).", "use_when": "On by default (DIV-92), but a no-op unless the Listing is built (option listing on) — with the Listing off there is zero behavior change. Flip OFF to restore the legacy three-arm tally, which also counts a call whose successor failed to decode. Turning it off is worth trying only on a target where a KNOWN no-return wrapper is being treated as returning AND its call sites are followed by bytes kuna cannot decode; the cost is that any decoder spec gap after three calls to the same callee deletes those callers' tails. On a target whose ISA kuna decodes imperfectly (Thumb, MIPS16, PPC VLE) leave it on.", "example": "option listing on --option noreturn_discstrict off", "source_decompiler": "kuna", "inspiration": "GH-312; Ghidra FindNoReturnFunctionsAnalyzer checkForNoReturn (the arms that survive are the ones that observe the program)", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["live code after a call disappears and the callee is annotated // no-return", "a plainly-returning function is reported no-return", "caller collapses to void f(); // no-return where it should return a value", "dead code removed after a call on an architecture with decoder gaps (Thumb/MIPS16/PPC VLE)"]},
  {"option": "fid", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-fid", "summary": "Run the FID fingerprint matcher (a Listing/xref consumer): the kuna analog of Ghidra's FID (Function-ID) identification analyzer. Over the built Listing, fingerprint each function with the byte-exact operand-masked FNV-1a64 hash (the MessageDigestFidHasher port, driving Sleigh::instruction_mask) and look the full hash up in a kuna `.fid` fingerprint database (named by the `kuna_fid_db` environment variable). When the full-hash bucket collapses to exactly one name (conservative — never guess on a tie), RENAME the matched function — but only when it still carries an engine FUN_*/sub_* placeholder (the label gate; a real .symtab/DWARF name is never overwritten). This re-identifies a function in a STRIPPED binary purely by its instruction-stream fingerprint (e.g. sub_4017c0 -> kuna_crc32). Requires the Listing (option listing on) and a configured `.fid` DB — a no-op when either is absent. DISABLED by default (real-ELF path only, so every parity gate is byte-identical).", "use_when": "Off (default; no Listing consumer runs and there is zero behavior change). Flip on (with option listing on, and `kuna_fid_db` pointing at a `.fid` database built by `kuna fid build`) to re-identify library functions in a STRIPPED real-ELF target by full-hash fingerprint — renaming a FUN_*/sub_* placeholder back to its known library name.", "example": "option listing on --option fid on", "source_decompiler": "kuna", "inspiration": "Ghidra FID (Function-ID) identification analyzer (FidServiceImpl full-hash re-identification)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["stripped static-linked library function stays sub_<addr> although its fingerprint is known", "no .fid database renames applied in a stripped binary"]},
  {"option": "rtti", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-rtti", "summary": "Run the MSVC RTTI / vftable class-name recovery pass: the kuna analog of Ghidra's RttiAnalyzer (a Microsoft-PE analyzer). On a Windows PE, parse the run-time-type-information graph entirely in the loaded image (.rdata/.data): find the common type_info vftable (the pointer every RTTI0 TypeDescriptor shares), locate the TypeDescriptors carrying the `.?A...@@`-mangled class names, byte-search back to each CompleteObjectLocator (COL = ref - 12), validate the COL -> RTTI3 ClassHierarchyDescriptor -> RTTI2 BaseClassArray -> RTTI1 BaseClassDescriptor -> RTTI0 TypeDescriptor reachability chain, and demangle each class name via the existing MSVC demangler (the Ghidra RttiUtil `??_R0...@8` wrap; zero new demangler code). Handles BOTH x86 (raw-VA inter-structure refs, RTTI0 name at offset 8) AND x64 (image-base-relative IBO32 disp32 refs, RTTI0 name at offset 16) via a refkind dispatch on is64Bit. Emits Data symbols labelling each class's vftable (`<Class>::vftable`), CompleteObjectLocator (`<Class>::RTTI_Complete_Object_Locator`), and TypeDescriptor (`<Class>::RTTI_Type_Descriptor`), so the C++ class names (Box/Shape) surface as recovered symbols and the virtual-dispatch metadata graph is named. PE-only (registered in passes_for only for a PE image, and the pass self-gates on PE in run). DISABLED by default (real-PE path only, output-changing: it adds named data symbols, so every ELF/XML parity gate is byte-identical).", "use_when": "Off (default; no RTTI pass runs and there is zero behavior change). Flip on for a Windows PE built by MSVC (or clang -target ...-windows-msvc) carrying polymorphic C++ classes, to recover the C++ class names and label the RTTI/vftable metadata graph (the raw .rdata bytes / unnamed DAT_* vtable become `Box`/`Shape` class names + `<Class>::vftable` / `<Class>::RTTI_*` labels).", "example": "option rtti on", "source_decompiler": "kuna", "inspiration": "Ghidra RttiAnalyzer / RttiUtil (Microsoft RTTI / CompleteObjectLocator vftable recovery)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["msvc c++ class names missing and vftables left as unnamed data", "no Class::vftable or RTTI_ labels in a windows pe with polymorphic classes"]},
  {"option": "itaniumrtti", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-itaniumrtti", "summary": "Run the Itanium (GCC/Clang) RTTI + vtable recovery pass -- a capability Ghidra does NOT have (its RttiAnalyzer is a Microsoft-PE analyzer, and its GCC class recovery is script-tier: RecoverClassesFromRTTIScript / RTTIGccClassRecoverer, never auto-analysis), so on a stripped g++ binary Ghidra leaves the Shape vtable as DAT_00107a80. IDA Pro does reconstruct it, and this is kuna's analog, built against the Itanium C++ ABI. On an ELF, discovery is EXACT rather than a byte-pattern scan: the three __cxxabiv1 typeinfo vtables live in libstdc++, so every _ZTI... typeinfo object's leading vptr word is an UND dynamic relocation naming __class_type_info / __si_class_type_info / __vmi_class_type_info with addend 2*ptr -- the relocation's r_offset IS the typeinfo address and its symbol IS the flavour, and `.rela.dyn` is a loader input that `strip --strip-all` cannot remove from a shared object. A defined _ZTI... OBJECT symbol is a second discovery source (unstripped / static link), its flavour sniffed from the object's shape. Each typeinfo's _ZTS... type-name string is demangled to the class name by wrapping it back into the _ZTS symbol form and reusing the existing cpp_demangle Itanium arm (the exact analog of the MSVC ??_R0...@8 wrap; ZERO new demangler code), and its base list gives the inheritance DISPLACEMENTS. Vtables are then reached from the typeinfo, not guessed: one scan finds every pointer slot holding a discovered typeinfo address (a sub-vtable's second header word), and two exact ABI constraints reject the coincidental hits -- offset-to-top is always <= 0, and a real sub-vtable has >= 1 slot pointing into an executable section. A slot whose file word is 0 but which carries a dynamic relocation is an IMPORTED virtual method (__cxa_pure_virtual, a base method in another image); the walk steps over it instead of terminating, so an abstract interface keeps its true extent. Emits Data symbols `<C>::typeinfo`, `<C>::typeinfo_name`, `<C>::vtable`, and for a secondary sub-vtable the base subobject its displacement identifies (`Widget::vtable_for_Drawable`); a Function symbol `<C>::vtable_<i>` per virtual slot, with an inherited slot attributed to the class that DEFINES it rather than to whichever vtable the scan reached first (so Shape::perimeter, repeated in Circle's and Square's tables, is named once for Shape); and the slot arrays as read-only ranges. ELF-only (registered in passes_for only for BinaryFormat::Elf, and the pass self-gates on ELF in run). DISABLED by default (real-ELF path only, output-changing: it adds named data and function symbols, so every XML parity gate is byte-identical).", "use_when": "Off (default; no Itanium RTTI pass runs and there is zero behavior change). Flip on for a GCC/Clang-built C++ ELF carrying polymorphic classes -- especially a STRIPPED one, where the relocation anchor is the only surviving type information -- to recover the class names, the inheritance graph and the vtables (an unnamed DAT_* vtable slot becomes `Shape::vtable_2`, the raw .data.rel.ro bytes become `Shape::typeinfo` / `Shape::vtable`). It recovers NOTHING from a binary built -fno-rtti (google/leveldb is such a build: 18 _ZTV symbols, zero _ZTI/_ZTS, zero __cxxabiv1 relocations), and nothing from plain C.", "example": "option itaniumrtti on", "source_decompiler": "kuna", "inspiration": "Itanium C++ ABI 2.9.5 / 2.5.2 (type_info + vtable layout); IDA Pro's GCC RTTI reconstruction. Ghidra has no equivalent analyzer.", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["gcc/clang c++ class names missing and vtables left as unnamed DAT_ data", "no Class::vtable or typeinfo labels in a stripped elf with polymorphic classes", "virtual dispatch slot resolves to sub_<addr> with no class"]},
  {"option": "noreturn_propagate", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-noreturn-propagate", "summary": "REMOVES CODE: propagates no-return through the call graph, dropping unreachable code after no-return calls. Runs the structural no-return propagation consumer (the second Listing/xref consumer): the kuna analog of angr's CFGFast call-graph no-return propagation. Seed the terminal set from the Known no-return list and conclude a function no-return when its last real instruction (skipping trailing NOP alignment padding) is a CALL (or tail JMP) to an already-no-return callee, with no RETURN path, no computed jump, and no branch escaping the reachable body — iterated to a fixpoint, with NO evidence threshold (unlike noreturn_disc's >=3 call sites). Catches a custom no-return wrapper (e.g. coreutils xalloc_die, which ends in `call abort` followed by padding) that the static name list does not know and the >=3-evidence rule does not reach, so a caller (e.g. tee_O2 x2nrealloc) no longer grows a spurious while(true)/goto from the dead fall-through. Emits the existing NoReturnFact -> the existing set_function_no_return commit seam (no new commit arm, no S7 work). Requires the Listing (option listing on) — a no-op when the Listing is absent. On by default (DIV-14), but gated on the Listing (default-off), so every parity gate is byte-identical (real-ELF Listing path only).", "use_when": "On by default (DIV-14), but a no-op unless the Listing is built (option listing on) — with the Listing off there is zero behavior change. With listing on it concludes a custom no-return wrapper (a die()/fatal()/xalloc_die() that unconditionally ends in abort/exit) is no-return where it would otherwise be treated as returning — angr renders the caller cleanly but kuna would emit a spurious while(true) loop + goto + dead stack spills from the post-call fall-through (e.g. tee_O2 x2nrealloc). Broader than noreturn_disc: no >=3 call-site threshold and it seeds from the Known list. Flip OFF (with listing on) to keep the post-call fall-through code in the output.", "example": "option listing on --option noreturn_propagate on", "source_decompiler": "angr", "inspiration": "test_decompiling_tee_O2_x2nrealloc; angr CFGFast no-return propagation (returning / _iteratively_analyze_function_features); x2nrealloc", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["unreachable code after a call to an exit/fatal wrapper", "function truncated after calling a cold wrapper", "spurious while(true) around a call that never returns", "stripped binary's unnamed exit wrapper swallows the functions after it"]},
  {"option": "noreturn_error", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-noreturn-error", "summary": "REMOVES CODE: concludes an error(nonzero,...) wrapper is no-return, dropping the dead fall-through at every caller. The value-conditional slice of Ghidra's discovered-no-return analyzer, folded into the noreturn_propagate consumer: glibc error(int status, int errnum, const char *fmt, ...) and error_at_line(int status, ...) call exit(status) and NEVER return when status != 0 — but DO return for status == 0 — so `error` cannot be a Known no-return. Yet an internal wrapper whose tail is `call error(2,...)` (GNU pfatal_with_name, and every die()-via-error helper) is unconditionally no-return. When on, the propagation treats such a tail call as terminal by checking the call site's first int-arg register (x86-64 SysV EDI/RDI = the `int status`) is a nonzero literal (MOV EDI,0x2), concludes the wrapper no-return, and the existing fixpoint propagates that to every caller — which then truncates at the wrapper call instead of decoding the cold fall-through / next-function bytes as live code (e.g. diffutils diff `sip` collapses from 99 lines to ghidra's ~43, GED 347 -> 0). A zero status (XOR EDI,EDI / MOV EDI,0x0 — error() returns) or any non-constant/unprovable status is rejected (conservative: a false positive would drop live caller code). Emits the existing NoReturnFact -> the existing set_function_no_return commit seam (no new commit arm, no S7 work). Requires the Listing (option listing on) AND noreturn_propagate on — a no-op otherwise. On by default (DIV-16), but gated on the Listing (default-off), so every parity gate is byte-identical (real-ELF Listing path only).", "use_when": "On by default (DIV-16), but a no-op unless the Listing is built (option listing on) and noreturn_propagate is on — with the Listing off there is zero behavior change. With listing on it concludes an internal wrapper that tail-calls error(nonzero,...) (e.g. GNU pfatal_with_name) is no-return where kuna would otherwise treat `error` as returning (since error(0,...) returns) and keep decoding the cold error path / adjacent function's bytes as live code past every wrapper call, ballooning the caller (diffutils diff `sip`: 99 lines / GED 347 vs ghidra 43). Ghidra's discovered-no-return analyzer catches exactly this; angr's callgraph returning-fixpoint (CFGBase._determine_function_returning) is the analog. Flip OFF (with listing on) to keep the post-call fall-through code (the wrapper is treated as returning again).", "example": "option listing on --option noreturn_error on", "source_decompiler": "ghidra", "inspiration": "decbench:O2-noinline-diffutils-diff-sip; CFGBase._determine_function_returning / Ghidra discovered-no-return; error(nonzero,...)", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["caller keeps decoding the cold path after error(2, ...) into the next function", "function balloons past a wrapper that tail-calls error with nonzero status", "dead fall-through after a gnu error(nonzero, ...) call"]},
  {"option": "noreturn_reach", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-noreturn-reach", "summary": "REMOVES CODE: the CFG-reachability no-return rule — the port of Ghidra's FindNoReturnFunctionsAnalyzer.targetOnlyCallsNoReturn ('Non-Returning Functions - Discovered' analyzer). kuna's noreturn_propagate concludes a wrapper no-return only when its LAST real instruction is a terminal call to an already-no-return callee (a tail-call subset). That misses three shapes Ghidra catches by walking the CFG: (1) a no-return call MID-body with a dead tail after it (e.g. openssh sshpkt_fatal, whose FIRST statement is `call sshpkt_vfatal` and whose write()/return tail is unreachable); (2) a RETURN that is present but UNREACHABLE (the path to it dies at a no-return call); (3) a SWITCH (indirect jump) whose every arm ends in a no-return call (openssh sshpkt_vfatal). When on, for each candidate the rule walks the instruction-level reachable CFG from entry, treats a call/jump to an already-no-return callee as terminal (its fall-through is dead), and concludes no-return iff NO RETURN is reachable and at least one path ends at a no-return transfer — iterated to a call-graph fixpoint by the same propagate loop, emitting the same NoReturnFact -> set_function_no_return seam. Conservative: a reachable RETURN, an unresolved indirect jump, a branch escaping the body to a possibly-returning neighbour, or a call with no modelled fall-through that is not itself terminal all reject (a false positive would drop live caller code). Requires the Listing (option listing on) AND noreturn_propagate on — a no-op otherwise. On by default (DIV-19), gated on the Listing (default-off), so every parity gate is byte-identical (real-ELF Listing path only).", "use_when": "On by default (DIV-19), a no-op unless the Listing is built (option listing on) and noreturn_propagate is on. With listing on it concludes internal fatal wrappers that kuna's tail-call rule (noreturn_propagate/noreturn_error) misses because the no-return call is mid-body, the return is dead, or the function routes through a switch — where kuna would otherwise decode the cold error path / adjacent function's bytes as live code past the wrapper call, ballooning the caller and inflating GED vs ghidra (openssh sshpkt_fatal/ssh_tun_confirm, shadow oom). This is the general 'Non-Returning Functions - Discovered' reachability that ghidra runs by default. Flip OFF (with listing on) to fall back to the strict tail-call rule (keeps the post-call fall-through code).", "example": "option listing on --option noreturn_reach on", "source_decompiler": "ghidra", "inspiration": "decbench:O2-openssh-portable-sshpkt_fatal, O2-openssh-portable-ssh_tun_confirm, O2-shadow-oom; Ghidra FindNoReturnFunctionsAnalyzer.targetOnlyCallsNoReturn (Non-Returning Functions - Discovered)", "change_kind": "correctness-fix", "tier": "transform", "symptoms": ["wrapper with a mid-body fatal call still treated as returning", "unreachable write()/return tail keeps a fatal wrapper looking like it returns", "switch whose every arm ends in a no-return call not concluded no-return", "caller decodes garbage past an sshpkt_fatal-style wrapper"]},
  {"option": "aif", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-aif", "summary": "Run the Aggressive Instruction Finder gap-walk (the third Listing/xref consumer): the kuna analog of Ghidra's AggressiveInstructionFinderAnalyzer, a speculative gap-filler. Over the undefined gaps between discovered functions, speculatively decode each gap start and accept it as a NEW function entry when it BOTH (a) disassembles into a valid subroutine (followSubFlows reaches a clean RET without hitting a bad/undecodable byte or an out-of-range flow, > 2 instructions) AND (b) matches a function-start byte fingerprint (mask-histogram) shared by >= 4 already-discovered functions. Finds functions reachable ONLY through an indirect/data path (a .rodata function-pointer table) that entry discovery + funcsyms + .eh_frame miss. Emits the discovered starts as the existing entries fact -> the existing name_function/add_function commit seam (no new commit arm). Requires the Listing (option listing on) — a no-op when the Listing is absent. DISABLED by default (Ghidra ships setDefaultEnablement(false) with the warning 'IT MAY CREATE A LOT OF BAD CODE!'; a speculative gap-filler can create false-positive functions; real-ELF path only, so every parity gate is byte-identical).", "use_when": "Off (default; no Listing consumer runs and there is zero behavior change, and a speculative gap-filler can create false-positive functions). Flip on (with option listing on) when a function is reachable ONLY through an indirect/data path (its address taken into a .rodata function-pointer table and called via call *reg with an opaque index) — so it is in no symbol table, has no .eh_frame FDE, and no static CALL edge points at it, leaving it an undefined gap that entry discovery cannot reach. AIF's fingerprint+validity gap-walk recovers it as a named sub_<addr>.", "example": "option listing on --option aif on", "source_decompiler": "kuna", "inspiration": "Ghidra AggressiveInstructionFinderAnalyzer (speculative gap-fill, fingerprint histogram, checkValidSubroutine, default-off); PseudoDisassembler.followSubFlows", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["function reachable only through a rodata function-pointer table never discovered", "undefined gap between functions that clearly holds code", "call *reg targets missing from the function list"]},
  {"option": "aifstrict", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "GH-299", "summary": "Advance the Aggressive Instruction Finder gap cursor to the next 4-byte boundary instead of the next byte, so it stops planting function entries in the middle of function bodies (GH-299). run_aif walks the undefined-byte partition ONE BYTE at a time, because first_undefined_after is byte-granular by construction, so every byte of every hole is a candidate function start and the two acceptance tests - a 2-mnemonic prologue fingerprint shared by at least 4 discovered functions, and a valid-subroutine decode - are applied to addresses that cannot be instruction boundaries at all. A candidate starting mid-instruction reads the tail of one encoding plus the head of the next, and that synthetic pair matches a common prologue about as often as a real one does. On the reported 3.4 MB stripped i386 PE, AIF contributes 5,913 entries of which 2,067 land inside a function IDA decodes as one body, and 737 of those are inside a function kuna itself already has an entry for. With this on, a candidate is probed only when it is 4-byte aligned OR is the first byte of its hole: a hole boundary is evidence, since the recursive-descent walk decoded up to exactly there and stopped, while an interior byte the cursor slid onto is a guess. The exemption is also what keeps the aif_gap_x86_64 fixture green, whose indirect-only function at 0x13ae is 2-mod-4 but IS a hole start. 4 bytes on every architecture, deliberately: 16-byte alignment kills 91 percent of the bad Cortex-M entries but takes 80 percent of the real ones with it. The removal is not only a precision win: because an accept advances the cursor past the accepted body, a phantom accepted one halfword inside a literal pool CONSUMES the real function behind it, so declining that probe recovers the true entry. Measured bidirectionally over 110 stripped non-x86-64 decbench binaries against each stripped image unstripped twin symbol table: mid-body entries 11,010 to 6,728 (a 38.9 percent cut) and ground-truth recall 44,613 to 44,957 of 50,724 (plus 344). On the two large PE witnesses, mid-body entries 339 to 135 on a 590 KB i386 crackme scored against its unstripped twin (zero of its 636 AIF-recovered real entries lost, 9 gained) and 421 to 293 on IDA Pro 9.2 win32_remote32.exe (3 of 157 lost). u-boot A32, the image DIV-20 exists for, gains 56 functions at O2 and is unchanged at O2-noinline. The worst single-image cost is CMSIS_DAP.axf, where only 73.5 percent of ground-truth entries are 4-aligned and three Thumb functions at 2-mod-4 interior addresses are lost. This does NOT reach the acceptance bar pre-registered for the fix (a 60 percent corpus cut and 100 or fewer crackme mid-body entries), so it ships opt-in rather than as the default and GH-299 stays open. The issues other direction - reject a candidate bracketed by a known function - was implemented in two forms and measured OUT: on the Listings entry-ordered function model it costs u-boot 195 of 1,759 recovered functions, and A32 recovery is AIFs entire remaining justification. Inert without aif and without --option listing on.", "use_when": "Off (default) keeps the byte-granular cursor and reproduces the GH-299 measurement exactly. The `aggressive` preset carries it, which is where every number above was measured. Flip on for a stripped image whose function list is far longer than the binary can hold, or that contains a sub_<addr> sitting on an ordinary call or mov in the middle of another function - and especially when two overlapping entries decompile the same instructions twice. Flip it back off if a function you know is real disappears on an architecture whose function starts are not 4-byte aligned (Thumb allows a 2-mod-4 start; three are lost on CMSIS_DAP.axf).", "example": "--option aifstrict on", "source_decompiler": "kuna", "inspiration": "GH-299; Ghidra AggressiveInstructionFinderAnalyzer ships setDefaultEnablement(false) with the warning IT MAY CREATE A LOT OF BAD CODE!, and its reference analyzer defines pc-relative literal targets as DATA before AIF runs so the pool is never an undefined gap there; poolentry solves the same phantom on ARM by inferring the pools from their references", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a stripped binary reports far more functions than it has", "a discovered function starts in the middle of another functions body", "a sub_ entry lands on an ordinary call or mov instruction rather than a prologue", "two overlapping functions decompile the same instructions twice", "aif adds thousands of entries a reference decompiler does not agree are functions", "an arm function entry lands one halfword before the real prologue"]},
  {"option": "aifcorroborate", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "GH-313", "summary": "Restore upstream Ghidra's SECOND Aggressive Instruction Finder fingerprint test, so a gap candidate that corroborates nothing is refused instead of accepted on four functions sharing its prologue (GH-313). AggressiveInstructionFinderAnalyzer rejects a candidate twice: startCount < 4 at line 278, and then numInstr <= 2 || (!addsInfo && startCount < 50) at line 367, whose comment reads 'if the routine doesn't add good info, it must start like a large numbe of other functions'. kuna ported the first test and the numInstr half of the second and dropped the addsInfo clause entirely, so a self-contained routine that calls nothing, jumps nowhere known and merely reaches a ret becomes a function on a two-mnemonic coincidence. With this on an accept must EITHER add information - a call, or a jump into already-discovered code, computed upstream's way rather than from kuna's looser adds_info, which also counts a plain fall-through out of the hole and therefore fires on exactly the mid-body phantom this is meant to catch - OR match a prologue that 50 discovered functions share. A refused candidate still CONSUMES its body, because refusing an accept without that would hand the gap cursor back to the interior of the same hole: without the pairing the 3.4 MB PE witness turns a 361-entry mid-body cut into a 222-entry mid-body RISE. Applied only at the gap-walk accept; the raw Thumb-prologue seed, the code-pointer-table seed and the pointer-target validator carry their own corroboration and are untouched. MEASURED OUT FOR THE DEFAULT PATH, which is why it ships opt-in and in no preset. Over the same 110 stripped non-x86-64 decbench binaries aifstrict was measured on, each scored against its own unstripped twin symbol table and on top of aifstrict: AIF-marginal mid-body entries 6,728 to 4,653 (a 30.8 percent cut) but ground-truth recall 44,957 to 44,107 of 50,724 (minus 850), with recall raised on ZERO of the 110 images. The two u-boot A32 images, which carry 66 percent of AIF's corpus-wide true recall and are what DIV-20 exists for, lose 84 real functions at O2 and 141 at O2-noinline; CMSIS_DAP.axf loses 16. The PE witnesses are worse: a 590 KB i386 crackme gives up 24 real entries for 16 mid-body ones, and the 3.4 MB stripped i386 PE of GH-299 gives up 1,100 IDA Pro 9.2 agreed entries for 361. A threshold grid over 4, 8, 12, 20, 50 and 100 finds no escape - 4 is the existing fingerprint floor and so inert, and u-boot O2-noinline is already down 79 functions at 8 - so the upstream literal 50 is kept. What upstream assumes, that a function worth finding calls something the analyzer already knows, does not hold for the bare-metal ARM firmware that is AIF's remaining justification in kuna, where the routines only AIF can find are the leaf helpers that call nothing. Speed is inside the budget (interleaved min-of-4: minus 4.3, plus 2.5, minus 0.6 percent). Inert without aif and without --option listing on.", "use_when": "Off (default, and in no preset - it costs more real functions than it removes phantoms on every corpus measured). Flip on only when you are reading a stripped image for PRECISION rather than coverage and would rather miss a real function than chase a sub_<addr> that is not one: a triage pass over a huge stripped binary where the function list is the artifact, or a comparison against a reference decompiler where a mid-body entry is worse than a gap. Expect to lose roughly one real function for every two phantom entries it removes, and expect that ratio to be much worse on bare-metal ARM firmware and on large i386 PEs. If you are hunting mid-body entries and want to keep recall, use aifstrict instead - it is the one that removes them and raises recall, and the aggressive preset already carries it.", "example": "--option aifcorroborate on", "source_decompiler": "kuna", "inspiration": "GH-313; Ghidra AggressiveInstructionFinderAnalyzer line 367, the second of its two fingerprint rejects - numInstr <= 2 || (!addsInfo && startCount < 50) - whose comment reads 'if the routine doesn't add good info, it must start like a large numbe of other functions'; the addsInfo half was never ported", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a sub_ entry lands on a routine that calls nothing and only falls off the end of a hole", "aif accepts a function on nothing but four other functions sharing its first two instructions", "a stripped function list is full of leaf routines that are not functions"]},
  {"option": "tailcallentry", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-tailcallentry", "summary": "Recover the function entries the recursive-descent Listing walk absorbs at a tail call. walk.rs makes a new function only at a CALL target and treats every other flow target as a same-function successor, so a routine reached only by a tail `B` is swallowed by whichever function branched to it and never becomes a function of its own — 17.4% of the ARM Cortex-M entry-recall gap. This reads the COMPLETED walk (splitting at a tail call provably cannot change which instructions the walk decodes, because a function entry is walked and therefore decoded either way) and admits an unconditional-branch target as a NEW function entry when four containment guards hold: every predecessor of the target is an unconditional branch (no fall-through and no conditional-branch predecessor, which would make it ordinary intra-function flow); the branch leaves the caller's entry-ordered function region, so at least one other discovered entry lies between the branch and its target; the target's flow region reaches a RETURN or computed jump (terminating-routine validity, with no length floor, so a one-instruction `bx lr` handler qualifies); and the target does not open with a stack restore (a function does not begin by tearing down a frame it never built — that is the caller's shared epilogue). Emits the accepted targets as the existing entries fact and never rebuilds the Listing, so no already-discovered entry can be removed. ARM-only; requires the Listing (option listing on) — a no-op when the Listing is absent, so every parity gate is byte-identical.", "use_when": "Off in the shipped catalog (it discovers more functions, so it changes emitted C by construction), but the `aggressive` preset carries it together with `listing` (DIV-93) and `auto` selects `aggressive` under 500 KiB, so on the whole-binary surfaces it is live by default for a stripped image of that size; the catalog default is what the datatest corpus and an explicit `--mode reliable` see. Flip on (with option listing on) on stripped ARM firmware when a routine reached only by a tail `B` — a shared ISR stub's handler, a `pop {r4,lr} ; b helper` tail call, a jump-thunk target — produces no output at all because the walk absorbed it into an earlier function. Measured on 96 Cortex-M images: +561 ground-truth functions, 94.6% of the new entries are real function starts, zero real bodies split, zero entries lost.", "example": "option listing on --option tailcallentry on", "source_decompiler": "kuna", "inspiration": "decbench: kuna's own ARM entry-recall measurement (docs/features/arm-entry-granularity/); angr CFGFast's job model, where a branch to an address outside the current function's block set starts a new function", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["routine reached only by a tail B produces no decompiled output at all", "a shared ISR handler is swallowed by the two-instruction stub that branches to it", "function entry missing where the previous function ends in an unconditional branch", "kuna emits one oversized function where the ground truth has two"]},
  {"option": "entrythumbflow", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-entrythumbflow", "summary": "Carry the Thumb decode mode a container entry proves along the flow reachable from that entry, and nowhere else. The PE-family loaders share one ARM mode table: ARMNT declares a wholly Thumb stream on every container, ARM may interwork on every container, and machine 0x1c2 is read as its container family names it (THUMB on a PE, painted wholly Thumb; ARMTHUMB_MIXED on a UEFI TE, interworking). Where that table leaves the mode to the entry bit, the container makes no whole-image claim and the one fact it states is the entry's low bit. Ghidra's disassembler propagates that context along the flow it follows, but kuna's decompiler reads context per address and follows nothing, so this pass runs the same walk once at the analysis commit: decode from the even entry as Thumb through fall-through, direct branches, and direct BL calls (a BLX interworks and its target keeps the mode its own encoding selects), and paint TMode=1 over exactly the instruction bytes decoded, never a whole section, so A32 code elsewhere in the same section keeps its mode. The walk is bounded at 4096 instructions; reaching the bound keeps the ranges walked so far, says so once on stderr, and leaves the unreached code at the language default, which is what an even entry would have produced. Byte overlays are applied first, so the walk follows the effective instruction stream. Inert on every image without a Thumb-bit entry and on every non-ARM language.", "use_when": "On (default): a mixed ARM TE whose entry has the Thumb bit decompiles its entry function and the callees it reaches as Thumb while A32 code elsewhere stays A32. Flip off to decode the entry with the language default mode, or use --isa thumb when the whole image is Thumb and the 4096-instruction bound would otherwise leave code unpainted.", "example": "option entrythumbflow off", "source_decompiler": "kuna", "inspiration": "Ghidra's flow-following disassembler carrying TMode from an odd entry; EDK2 ARMTHUMB_MIXED TE modules whose entry is Thumb-2 while some assembly stays A32", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["a uefi te entry function decompiles as garbage a32 although its entry address is odd", "a callee reached by bl from a thumb entry decodes as a32", "a32 code in the same section as a thumb entry decodes as thumb", "loading a te reports the thumb context walk stopped"]},
  {"option": "gopclntab", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-gopclntab", "summary": "Run the Go pclntab function-name recovery pass: when the binary is Go (detect_compiler == Go), locate and parse the embedded pclntab (PC->line/name table) and emit a function symbol per Go function, so main.main / runtime.* / package functions render NAMED instead of sub_<addr> (the kuna analog of Ghidra's GolangSymbolAnalyzer, name-recovery half). Handles the go1.2/go1.16/go1.18/go1.20 header magics. Registered ONLY for a Go binary, so it is a structural no-op on every non-Go target. The existing symbol commit arm installs the names idempotently (a real .symtab name still wins; only a stripped Go binary's sub_<addr> functions take the recovered name).", "use_when": "On (default) names Go functions from the pclntab (main.main, runtime.gopanic, ...); off leaves a stripped Go binary's functions as sub_<addr>.", "example": "option gopclntab off", "source_decompiler": "kuna", "inspiration": "Ghidra GolangSymbolAnalyzer (pclntab / GoPcHeader / GoModuledata / GoFuncData)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["stripped go binary renders sub_<addr> instead of main.main and runtime.* names", "go package function names missing"]},
  {"option": "objc", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-objc", "summary": "Run the Mach-O Objective-C metadata recovery pass: when the binary is a Mach-O, walk the __objc_* ObjC 2.0 metadata web (the kuna analog of Ghidra's ObjcTypeMetadataAnalyzer, name-recovery half). From __objc_classlist (array of class_t*), per class follow data & FAST_DATA_MASK (~0x7) to the class_ro_t, read .name (class name) and .baseMethods (the - instance method_list_t), and follow class_t.isa to the metaclass baseMethods (the + class methods). Walk method_list_t handling BOTH the large absolute-pointer form and the small/relative form (entsizeAndFlags & 0x80000000), per method_t resolving the selector (ASCII), type encoding, and IMP (function VA). RENAME each IMP function -[Class sel] / +[Class sel] — but only when it still carries an engine FUN_*/sub_* placeholder (the label gate, the FID precedent; a real symbol is never overwritten) — plus emit _OBJC_CLASS_$_<name> + selector symbols. Selectors are plain ASCII, so no demangler is needed. Registered ONLY for a Mach-O binary, so it is a structural no-op on every non-Mach-O target. x86-64, no-chained-fixups path (the arm64 + LC_DYLD_CHAINED_FIXUPS resolver is a deferred follow-on). DISABLED by default (output-changing: renames IMP functions + adds symbols; real-binary-path only, so every parity gate is byte-identical).", "use_when": "Off (default; no IMP is renamed and there is zero behavior change). Flip on for a Mach-O Objective-C binary to recover method names — a stripped IMP renders -[Greeter greet:] instead of sub_<addr>, and the class/selector strings are named.", "example": "option objc on", "source_decompiler": "kuna", "inspiration": "Ghidra ObjcTypeMetadataAnalyzer (Objc2Class / Objc2ClassRW / Objc2Method / Objc2MethodList, ObjC 2.0 __objc_* metadata)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["objective-c methods render as sub_<addr> instead of -[Class sel]", "__objc_ metadata present but classes and selectors unnamed in a mach-o"]},
  {"option": "pdb", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "external-refinement", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-pdb", "summary": "Run the PE PDB (Program Database) metadata recovery pass: on a Windows PE, recover function names from the external .pdb debug file (the kuna analog of Ghidra's PdbUniversalAnalyzer, name-recovery half). PDB is Windows' DWARF, but the debug info lives in a SEPARATE .pdb file the PE only fingerprints. Read the PE CodeView debug record ({guid|sig, age, .pdb path}), then LOCATE the .pdb through an ordered candidate list: (1) an explicit path in the kuna_pdb_path environment variable (the s1_fid kuna_fid_db external-artifact precedent), (2) the CodeView record's own filename resolved beside the image — the recorded string is build-machine content, so only its last path segment is used and only inside the image's own directory, never opened as a path — and (3) <image stem>.pdb beside the image. Each candidate passes the FINGERPRINT GATE: open it via the `pdb` crate and verify its pdb_information().guid/age matches the PE's CodeView record. The first match wins; a MISMATCH or unreadable candidate moves on (with one stderr line naming it, so a stale sidecar is not silently indistinguishable from no sidecar) and an exhausted list emits nothing — never apply a wrong/stale PDB (the FID full-hash-match discipline of never applying wrong external knowledge). On a match, walk the global symbol stream (S_PUB32 publics + S_GPROC32 procedures), resolve each to its VMA (segment:offset -> RVA + the PE ImageBase), and RENAME each stripped function to its real name — but only when it still carries an engine FUN_*/sub_* placeholder (the label gate, the FID precedent; a real symbol is never overwritten). Registered ONLY for a PE binary, so it is a structural no-op on every non-PE target. NAME-level only (stripped FUN_<addr> -> the real name); typed prototypes, typed stack locals, and source lines are deferred. ENABLED by default (DIV-129): the fingerprint gate makes the automatic search safe by construction, so every /Zi Windows build decompiles with its real names at zero configuration. Real-binary path only, so every parity gate is byte-identical.", "use_when": "On by default. A Windows PE built with /Zi (or clang -gcodeview) whose matching .pdb sits beside it recovers its real function names with no configuration; a PE with no .pdb on disk, or with one whose GUID/age does not match, is byte-identical to having the pass off. Set kuna_pdb_path to point at a .pdb that lives somewhere else (a symbol store, a build tree). Flip OFF to see the stripped FUN_<addr>/sub_<addr> names — a bisect, an ablation, or a check that a name came from the binary rather than from the sidecar.", "example": "option pdb off", "source_decompiler": "kuna", "inspiration": "Ghidra PdbUniversalAnalyzer (PdbInfoCodeView/PdbInfoDotNet record + pdb2/pdbreader; kuna substitutes the `pdb` crate)", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["stripped windows pe functions stay FUN_<addr> although a matching .pdb exists", "pdb symbol names not applied to a pe", "a .pdb beside the exe is ignored", "windows function names need kuna_pdb_path to be set by hand"]},
  {"option": "relocobjects", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "angr-CLE-ET_REL", "summary": "Load relocatable objects — ELF `ET_REL` `.o` files (no PT_LOAD program headers, so they map zero bytes under the upstream loader) and COFF `.obj` files (every section at VMA 0, so they map one section on top of another): synthesize a section layout above 0x400000; apply generic 8/16/32/64-bit relocations and architecture-specific ARM, AArch64, and PowerPC64 instruction fields in the object's byte order, including REL in-place addends; leave failures unmodified and report them once in bounded architecture/type/reason groups; then rebase defined symbols and bind undefined externs to synthetic call targets — so an object decompiles fully instead of failing with 'Unable to load N bytes at ...' (ELF) or exposing only its first function (COFF).", "use_when": "On by default (a pure capability: linked images of every format are byte-identical — they keep the mapped-image path — and only a pre-link object takes the new path, which is otherwise ZERO output on ELF and first-function-only on COFF). Set off to restore the upstream mapped-image-only loader (an ELF `.o` then errors; a COFF `.obj` falls back to its first section). NB: this gates the loader, which runs at `load file` BEFORE per-function options, so flip it (or set env KUNA_RELOC_OBJECTS=0) before loading the object.", "example": "option relocobjects off", "source_decompiler": "angr", "inspiration": "test_decompiling_ptx_fix_output_parameters; CLE ELF relocatable backend; fix_output_parameters", "change_kind": "structure-recovery", "tier": "analysis", "symptoms": ["a .o relocatable object fails with 'Unable to load N bytes at ...'", "ET_REL object maps zero bytes so nothing decompiles", "a COFF .obj lists only its first function (the rest collide at address 0)"]},
  {"option": "tiedphitrim", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "merge-aggressiveness", "strength": "HARD", "rewind": "P6", "issue": "GH-468", "summary": "Give a loop that reads memory its own loop variable instead of printing every value it reads as a store into the stack slot or global it was entered from. Merge::mergeOp (merge.cc:719) forces a MULTIEQUAL's output and every input it has not trimmed into one HighVariable; when one input is a direct read of an address-tied location that HighVariable is the location, so each other input's definition prints as an assignment to it. Upstream trims an input only when the required tests or the Covers forbid the merge, and a read that is dead once the loop is entered conflicts with nothing. The witness is a byte loop over a stack buffer: MSVC /O2 guards `for (i = 0; buf[i]; i++)` by reading buf[0] from the frame and latches through `[RSP+RAX+0x40]`, and the loop prints `while (v10[0]) { ...; v10[0] = v10[v6]; }`; gcc and clang -O2 carry the byte in a register and print `v2 = *v1;` into the buffer's first byte. Upstream Ghidra 12.1 prints the same stores. ON acts in Merge::mergeOp after upstream's own trims, on a MULTIEQUAL whose output is not address-tied when every address-tied input reads one location a pointer can observe (a global, or a frame local below the parameter area that is not proven unaliased; never the function's entry value) and enters the loop from outside it, and some input on a back edge is read by a LOAD (directly or through COPYs and MULTIEQUALs). It declines when the location is written inside the loop, or written anywhere with a value that comes from inside the loop: the trim takes the loop-head read out of the location's variable, and without it a later merge could fold that in-loop value into the location, printing a store inside the loop that the loop's own LOAD reads back. Each accepted input gets upstream's trim COPY on its entering edge, so the loop reads the location once into its own variable. Every later merge test then treats the loop variable as still address-tied to the location: no address-tied variable (at that location or any other) merges with it, and nothing whose Cover intersects it merges into the location, so every other merge is the one upstream makes. OFF is upstream Ghidra's behavior exactly.", "use_when": "On by default (DIV-182). The OFF symptom is a loop that only reads memory printing a store into the location it started from: `while (v10[0]) { ...; v10[0] = v10[v6]; }` over a stack buffer, `v2 = *v1; } while (v2);` with `v2` declared at a stack offset, or `while (dat_293a0) { ...; dat_293a0 = (long *)dat_293a0[3]; }` over a global list head. When the buffer is used after such a loop the OFF C is also wrong, not just misleading: an in-place lowercase loop followed by `strcmp(buf, name)` compares an empty string because every iteration stored into buf[0]. The ON form reads the location once on the way in (`v3 = v10[0]; while (v3) { ...; v3 = v10[v6]; }`). A store the loop really performs and something later reads still prints; one that nothing reads is removed by dead-code elimination before merging, so ON no longer shows it through the merge. Flip OFF to reproduce upstream Ghidra's merge, or to bisect whether a `v = location;` copy before a loop came from this trim.", "example": "option tiedphitrim off", "source_decompiler": "kuna", "inspiration": "GH-468 follow-up: MSVC /O2 ab_o2.exe main, the inlined canary_check byte-sum loop, and the same loop from gcc -O2 and clang-12 -O2; upstream Merge::mergeOp (merge.cc:719), Merge::trimOpInput (merge.cc:692) and Merge::mergeOpcode (merge.cc:326)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a loop that only reads a stack buffer prints a store into its first element", "a while condition tests buf[0] and the loop body assigns a later element into buf[0]", "a byte fetched through a pointer is assigned to a stack-offset variable the binary never writes", "a loop over a global linked list advances the global head although the binary only reads it", "strcmp or strlen after an in-place string loop reads a buffer the C already overwrote"]},
  {"option": "hideshadow", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P6", "subphase": "merge-aggressiveness", "strength": "HARD", "rewind": "P6", "issue": "kuna-hideshadow", "summary": "Consolidate two copies of one value into a single chain so the repeated assignment stops printing. Two Varnodes reached from one ancestor through COPYs alone always hold the same value -- upstream Ghidra calls them shadows of one variable -- but when the two copy paths are not nested the merge passes leave them as separate assignments into one variable, and the emitted C repeats the assignment. ActionHideShadow (coreaction.cc:4976) is the upstream pass that repairs this, and Merge::hideShadows (merge.cc:1070) was ported with it, but the action body was a no-op stub, so the pass never ran. ON runs the upstream body: walk the written def-set, visit each HighVariable once (deduped through the bank mark bit, which is cleared again so no mark survives the pass), and re-point the later copy input at the earlier Varnode so ancestor -> first -> second becomes one chain; both are then instances of one HighVariable and ActionCopyMarker, scheduled directly below, marks the copy non-printing. The re-point is bounded by the two upstream tests, both about the value that will be read at the re-pointed input: Varnode::copyShadow must trace both Varnodes to one common ancestor through COPYs only, and the surviving Varnode Cover must contain the other definition point STRICTLY INTERIOR (containVarnodeDef == 1, not a boundary hit), so the value is live and unclobbered where the copy is made to read it. Two copies on exclusive branches fail the second test and are left alone, which is the common case. kuna reads the Cover through a plain accessor where the C++ getCover rebuilds a dirtied cover on the spot, so the pass refreshes the HighVariable member covers before each containment test -- the previous iteration opSetInput is what dirties them. OFF returns without walking anything, which is byte-for-byte what the tree did while the body was a stub.", "use_when": "On by default -- it is the upstream pass, which stock Ghidra runs unconditionally. The OFF symptom is the listing assigning the same value to one variable twice with nothing in between that could change it: `(v3 = a2, v4 = a0, v4 = a0, v5 = a1, ...)` in a gcc -O0 short-circuit condition, or a repeated `v14 = dat_26200;` around a conditional goto at -O2. Flip OFF to reproduce the tree before the action body was wired, or to bisect whether a missing assignment came from this pass. The flip is small and one-directional: over 444 stripped ELFs (coreutils, grep, gzip, diffutils, bzip2, findutils, tar, shadow at O0, O2 and O2-noinline) the emitted C is byte-identical on 434 and changes on 10, each losing one or two duplicated copy assignments and nothing else, and on a 40-binary instrumented run the Cover test accepted 110 HighVariables and declined 9,138. It removes a write, never a declaration, so no `vN` local is renumbered and `--assert name vN` / `--assert type vN` still address the same variables.", "example": "option hideshadow on", "source_decompiler": "ghidra-upstream", "inspiration": "Ghidra ActionHideShadow (coreaction.cc:4976) and Merge::hideShadows (merge.cc:1070), the merge-phalanx pass kuna carried as an inert stub", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["the same value is assigned to one variable twice in a row with nothing in between", "a comma-expression condition repeats an assignment: (v3 = a2, v4 = a0, v4 = a0, ...)", "a repeated vN = dat_XXXX; around a conditional goto", "kuna prints a copy assignment that stock Ghidra does not"]},
  {"option": "paramcopyhoist", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P6", "subphase": "merge-aggressiveness", "strength": "HARD", "rewind": "P6", "issue": "decbench-parameter-copy-shadow-placement", "summary": "Anchor the copy-shadow of an UNMODIFIED INCOMING PARAMETER in the function's entry block instead of at the tail of the MULTIEQUAL slot's predecessor, so a guarded parameter's `vN = aM;` prints where the source's spill is -- with the other parameter spills -- rather than below an earlier guard. gcc -O0 gives every parameter a stack home, and the source idiom `if (p && !*p) p = NULL;` leaves a MULTIEQUAL at the guard's join whose incoming slots are the raw input register. `Merge::mergeOp` cannot merge the input-register HighVariable with the address-tied stack-slot HighVariable, so `Merge::trimOpInput` snips each offending slot into a COPY and places it with `opInsertEnd(copyop, op->getParent()->getIn(slot))` -- the tail of that slot's PREDECESSOR -- and that COPY is what prints as `vN = aM;`. For the FIRST parameter the guard's join predecessors intersect at the entry block, so `Merge::buildDominantCopy`'s `findCommonBlock` lands the copy there and it renders correctly; for every later parameter the intersection is the previous guard's join, so the copy sinks below that guard and splits the source's single entry block in two. Both producing sub-cases are covered: two-or-more trimmed slots (later collapsed by buildDominantCopy) and exactly one trimmed slot (no dominant-copy pass at all, so the single opInsertEnd is final). Implemented as ActionParamCopyHoist, the LAST action in universal_sched: taking the decision inside `Merge::trimOpInput` itself defeats the trim's own purpose (the widened Cover fails mergeOp's test, mergeOp falls through to trimOpOutput, and markInternalCopies then hides both resulting COPYs, so the assignment DISAPPEARS from the emitted C), and running the move any earlier than ActionFinalStructure perturbs the P8 duplication passes. Legality is buildDominantCopy's own Cover test re-run against the hypothetical hoisted placement and the FINAL HighVariable (b_cover over the high's other instances vs an a_cover whose def point is taken at the START of the entry block, rejecting on `intersect > 1`), which over-approximates and errs toward rejecting; only a formal parameter's storage is ever hoisted, only when a MULTIEQUAL reads the copy, and only when the target variable has exactly one candidate.", "use_when": "Turn ON when a parameter's copy-shadow assignment (`v6 = a1;`) is emitted below an earlier guard instead of with the other parameter spills at the top of the function, adding CFG nodes the source does not have. Measured on decbench O0 e2fsprogs/e2fsck save_output (0x4eeb6): 53 nodes/77 edges GED 29.0 -> 51 nodes/77 edges GED 17.0 (-12), which is GED-OPTIMAL for the construct (deleting the copies entirely and writing through the parameter, which is literally what the source does, also scores 17.0). Broad on -O0: over four O0 binaries (e2fsprogs/e2fsck, coreutils/ls, gzip/gzip, bzip2/bzip2; 3031 functions) 121 functions (4.0%) emit at least one top-level parameter copy-shadow outside the entry block -- 200 sunk copies against 318 correctly in the entry -- and turning it on moves 92 of them (200 -> 108 sunk, 318 -> 479 in the entry) across 116 changed functions with zero new decompile errors. Every one of those 116 was diffed: 99 are pure re-orderings of `vN = aM;`, 17 have a secondary rendering effect (mostly a comma side-effect inside a short-circuit condition becoming a plain statement, which is an improvement; 2 leave an empty `if (c) { }` then-branch). Default-OFF opt-in: it is a deliberate DIVERGENCE from upstream Ghidra, which sinks the copy to exactly the same place, and although the default-ON ablation is clean on the datatests (0/675, PARITY OK) it regresses one stage assertion (condfold #3) and the decbench aggregate has not been measured, so the flip is left to a follow-up.", "example": "option paramcopyhoist on", "source_decompiler": "kuna", "inspiration": "decbench:O0-e2fsprogs-e2fsck-save_output; O0-e2fsprogs-e2fsck-ea_refcount_free (single-trim sub-case); Merge::trimOpInput / Merge::buildDominantCopy (merge.cc)", "change_kind": "structure-recovery", "tier": "transform", "symptoms": ["parameter copy-shadow assignment (v6 = a1;) emitted below an earlier guard instead of at the top of the function", "kuna places some parameter spill copies in the entry block and others after a guard in the same function", "extra CFG nodes versus the source around a cascade of if (p && !*p) p = NULL; parameter guards", "vN = aM; for an unmodified incoming parameter appearing in the middle of a guard cascade"]},
  {"option": "indirectonly", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P6", "subphase": "merge-aggressiveness", "strength": "HARD", "rewind": "P6", "issue": "kuna-indirectonly", "summary": "Set the `indirectonly` Varnode flag on a function input whose value is never really read, so the two merge-phalanx tests that carry an exception for that case can take it. An ILLEGAL INPUT is an input Varnode that ActionDirectWrite never reached -- nothing a formal parameter feeds can be responsible for its value -- which on a normal frame means the leftover contents of a stack slot the function overwrites before it reads. Merge::mergeTestAdjacent refuses every speculative merge involving such a variable, so an uninitialized value is not printed as a real one, and HighVariable::hasName refuses to name an unaffected input whose members are all of that kind. Both refusals end 'unless the illegal input is only used indirectly', and that exception needs a flag that nothing in kuna set: Funcdata::markIndirectOnly and Funcdata::checkIndirectUse (funcdata_varnode.cc:801-858) were unported and ActionMarkIndirectOnly was a documented no-op, so is_indirect_only() answered false everywhere and both tests rejected strictly more than upstream. checkIndirectUse is a worklist over the def-set: a reader that is a plain CPUI_INDIRECT is the accepting case and the walk stops there (an INDIRECT records that some other op MAY have overwritten the storage, it is not a use of the value); an INDIRECT caused by a STORE is accepted but the walk continues through its output, since the value survives the store; a CPUI_MULTIEQUAL is transparent and the walk continues through its output; ANY OTHER READER fails the whole test, so one arithmetic use, one compare or one call argument anywhere in the def-set leaves the input isolated exactly as before. ON runs that scan in the scheduled slot, one action ahead of ActionMergeAdjacent and well ahead of ActionNameVars, which is stock Ghidra behaviour; OFF, the default, restores the inert stub byte-for-byte.", "use_when": "OFF by default, although the body is upstream verbatim, because the merge it unlocks can fabricate a store. When the illegal input is the SOURCE of the copy -- the machine loads an escaped frame slot into a register and then mutates the register -- the merge makes the emitted C mutate the slot, and a later call that was handed the enclosing object reads a value the machine never wrote there. The heritage pass cannot catch it: an INDIRECT is only attached where the storage is still live in the SSA, so a slot whose last read is before the loop gets no INDIRECT at any call after it and the cover-intersection test has nothing to intersect. Stock Ghidra 12.1.2 emits the same fabricated store on the same input (docs/features/indirectonly/counterexample.md), so this is an upstream defect the port inherits rather than a porting error; kuna declines to inherit it by default. Turn it ON to reproduce Ghidra variable partitioning exactly, or for the merges that are sound: where the illegal input is the DESTINATION -- the machine really does store to the slot -- the value is computed into the slot instead of into a temporary that is assigned across, and the assignment, now a COPY inside one variable, is hidden by markInternalCopies; on coreutils `ls` -O2 `main` the two 8-byte loads that fill a 16-byte `struct dev_ino` on the frame stop being printed into two register locals that the following `hash_delete(..., &di)` never sees. The blast radius is small and was measured over 34 whole-binary `decompile-all` slices (ls, sort, grep, gzip, diff, bzip2, find, tar at -O0 and -O2, plus kmod, e2fsck, dpkg, dpkg-query, dash and bash at -O0, -O2 and -O2-noinline): 27 functions change and the corpus loses 19 declarations net, but 24 of the 27 write a frame slot inside an object whose address reaches a later call, so nothing in the emitted C certifies a safe subset -- of the six settled against the disassembly, four restore a store the machine really makes (`ls` -O2 `main`, `tar` -O0 `sub_429c6`, `dpkg` -O0 `sub_2019c`, `e2fsck` -O0 `sub_1680a`, three of them -O0 frames where the variable really does live in memory) while two fabricate one (`bzip2` -O2 `sub_3890`, `kmod` -O2-noinline `sub_97b0`, both -O2 where the value lives in a register for its whole lifetime).", "example": "option indirectonly on", "source_decompiler": "ghidra", "inspiration": "Funcdata::markIndirectOnly / Funcdata::checkIndirectUse (funcdata_varnode.cc:801-858); HighVariable::hasName (variable.cc:731-760); Merge::mergeTestAdjacent (merge.cc:192-196)", "change_kind": "presentation-default", "tier": "transform", "symptoms": ["a value stored into a stack slot is printed into a register local and the slot is never assigned", "a struct filled on the frame is passed by address to a call although the emitted C never wrote it", "a declared local is never written and never read anywhere in the function", "a `vN = slot;` ... `slot = vN;` round-trip copy pair around a loop that only reads the slot", "a call result assigned to a temporary that is immediately assigned to a stack slot"]},
  {"option": "macho-arm64e", "values": ["on", "off"], "default": "off", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-multiformat-macho-arm64e", "summary": "Select the Apple-Silicon pointer-authentication SLEIGH spec for an arm64e Mach-O. When on, a Mach-O whose header reports cpusubtype CPU_SUBTYPE_ARM64E loads with the AARCH64:LE:64:AppleSilicon language (the vendored Apple-Silicon v8.5-A spec, which models the pointer-auth/AMX extensions) instead of the generic AARCH64:LE:64:v8A. Pointer authentication does NOT change import naming or symbols (the __stubs indirect-symbol walk is unaffected) — only the spec selection differs (design 3.7). Spec selection is a LOAD-time decision (language_id_for), which runs before any console `option` command, so the live gate is the KUNA_MACHO_ARM64E env var the kuna CLI exports when it sees `--option macho-arm64e on`; this option name also records the requested state on the Architecture for catalog consistency. Default-OFF (opt-in, multi-format loader PR-8): a non-arm64e Mach-O, a non-Mach-O target, and every ELF/PE/COFF path are byte-identical, so all parity gates are structurally untouched.", "use_when": "Off (default; an arm64e Mach-O loads with the generic v8A spec, exactly as any arm64). Flip on to decompile an arm64e (Apple-Silicon, pointer-auth) Mach-O with the AppleSilicon spec that models its extensions.", "example": "--option macho-arm64e on", "source_decompiler": "kuna", "inspiration": "Ghidra MachoLoader arm64e cpusubtype -> AARCH64:LE:64:AppleSilicon language mapping", "change_kind": "analysis-enablement", "tier": "analysis", "symptoms": ["arm64e mach-o decoded with the generic v8A spec so pointer-auth ops are unmodeled", "pac instructions in an apple-silicon binary not modeled by the loaded spec"]},
  {"option": "outline", "values": ["off", "<fn>:<head>-<exit>[;...]"], "default": "off", "destructive_as_default": true, "phase": "P8", "subphase": "goto-quality", "strength": "HARD", "rewind": "P8", "issue": "kuna-inline-identification", "summary": "Stage 2 of inline identification: excise a SUPPLIED single-entry region and emit a call to a synthesized pseudofunction in its place. Where almostregion finds candidate inlined regions, this acts on one - and the choice does not have to come from almostregion, so a human reading the code or an LLM agent reading almostregion's report can drive the transform directly. The value names the owning function entry as well as the region, because an option value is global to the run and every function is decompiled under it. The region is a block SET re-derived from the CFG with the same single-entry predicate almostregion uses; the value only names the head and the exit, and is re-verified against the live bblocks every time rather than trusted. Runs in the pre-SSA window immediately before heritage, so the edge surgery needs no MULTIEQUAL patching and ActionFuncLink still materializes the call's argument and return varnodes. v1 declines - leaving the function untouched - unless the head/exit really bound a single-entry region, exactly one edge enters the head from outside it, at most one value is live out, and the region contains no call and no STORE. Liveness is computed over the region's continuation (blocks reachable from the exit) with sub-register aliases merged, because an x86 32-bit write zero-extends and would otherwise count one register twice.", "use_when": "Off (default; inert, and inert also when a region is supplied for a different function). Supply a region to pull an inlined callee body back out into a named call - typically taking the head and exit from `--option almostregion report`.", "example": "--option outline 0x401000:0x40100a-0x401018", "source_decompiler": "kuna", "inspiration": "Procedural abstraction / outlining as the inverse of compiler inlining; the region model is the angr RegionIdentifier's single-entry region", "change_kind": "opt-in-tool", "tier": "transform", "symptoms": ["an inlined callee body is spliced into the caller and should read as a call", "want to pull a chosen region out of a function into a named pseudofunction", "acting on an almostregion inline-candidate report by hand or with an llm"]},
  {"option": "pdatachained", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-pdatachained", "summary": "Refuse to create a function entry at a PE .pdata RUNTIME_FUNCTION whose UNWIND_INFO carries UNW_FLAG_CHAININFO, which is a separated code chunk of another function rather than a function of its own. MSVC splits one function across several records whenever it shrink-wraps a prologue or moves a cold block out of line: the first record is the function, and every later one points at an UNWIND_INFO with the chained flag set plus a trailing RUNTIME_FUNCTION naming the primary. kuna's .pdata oracle read only BeginAddress, so each chunk became a sub_<addr> of its own - and because p2's funcboundflow truncates a fall-through that reaches a known function entry, the real function was then cut off at its own chunk and emitted as a stub, sometimes as syntactically invalid C. On, the third dword is resolved against the loaded sections and a record whose flags have bit 0x4 set contributes no entry, exactly as Ghidra's ImageRuntimeFunctionEntries_X86.markup skips it. The read is total: a null UnwindInfoAddress, an RVA no section covers, or an empty slice all read as not-chained, so the filter can only ever subtract a record it has positively identified. Almost always the chunk's bytes are still reached as the primary's own fall-through or branch target, so the duplicate entry goes without losing code; the residual is a fragment the primary's flow never reaches, which then stops being decompiled at all - measured at 48 bytes over two unreferenced 24-byte fragments on a 240 KB MSVC PE whose 716 records include 93 chained, and both of those entries began mid-thunk or mid-instruction to start with. x64-form .pdata only; ARM and ARM64 images use the 8-byte record whose second dword is packed unwind data as often as an .xdata pointer, and Ghidra does not decode their chained fragments either.", "use_when": "On (default) on any MSVC-built PE. The tell-tale is kuna functions listing a sub_<addr> whose bytes are plainly a register-save or spill run rather than a prologue, together with kuna decompile of the function just above it stopping early and carrying the funcboundflow fall-through-reached-the-next-function-entry warning. Flip off to see every address the exception directory names, chunk starts included, or to get back a fragment nothing in the primary's flow reaches. Off restores the previous x86/x64 discovery set exactly, and only that one: the record stride follows FileHeader.Machine whichever way this option is set, so an ARM/ARM64/ARM64EC/ARM64X image reads its 8-byte records - and finds more functions than before - with the option off too, and a machine that is neither x86 nor ARM parses no exception directory at all.", "example": "--option pdatachained off", "source_decompiler": "kuna", "inspiration": "GH-403 (dobin/redtest): kuna cut sub_140002650 off at 0x14000270c and emitted `} while ;` because the shrink-wrapped register-save chunk at 0x140002712 had become sub_140002712; Ghidra decompiles the same function whole because ImageRuntimeFunctionEntries_X86.markup gates markAsFunction on !unwindInfo.hasChainedUnwindInfo()", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a pe function stops early with the funcboundflow fall-through-reached-the-next-function-entry warning", "kuna decompile of an msvc function emits `} while ;` or an empty if body where ghidra emits the whole function", "kuna functions reports an msvc function far smaller than its .pdata extent", "a sub_<addr> in a pe starts at a movaps or mov [rsp+N],reg register-save run instead of a prologue", "the second half of an msvc function is only reachable under a generated sub_<hex> name"]},
  {"option": "rexthunk", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P1", "subphase": "code-data-partition", "strength": "HARD", "rewind": "P1", "issue": "kuna-analysis-rexthunk", "summary": "On an x86-64 PE, do not name the FF 25 <disp32> one byte into a REX-prefixed jump through an Import Address Table slot (48 FF 25, jmp qword ptr [rip+disp]) as an import thunk. The PE import resolver scans the executable sections for FF 25 <disp32> and names each match whose target is an IAT slot as a thunk entry, so that a direct call to a linker-generated thunk renders the import name. Every PE linker emits that thunk bare, starting at the FF. MSVC, GCC and clang put a REX.W prefix on an indirect tail jump through __imp_X, the epilogue form the Windows x64 unwinder recognises, and because a RIP-relative displacement counts from the end of the instruction the slot still resolves from the FF one byte in. Each such tail jump became an import-named function entry in the middle of an instruction, 35 in a small MSVC /O2 image, each decompiling to a one-line call of the import and each cutting the enclosing function's reported size short at the phantom. On, a match whose preceding byte is 40-4F is dropped unless the six bytes before it are themselves a thunk through an IAT slot (a contiguous thunk table, whose displacement high byte can reach that range only in an image over 1 GiB) or the image references its FF byte: a direct call, jmp or jcc rel32 or a RIP-relative lea in the executable sections, the address as an eight-byte little-endian value in any section other than discardable data such as .reloc or .debug_* (a relocated pointer, a function table, a mov reg, imm64), a .pdata BeginAddress or an export. A real thunk that directly follows a byte in that range, such as the end of a jmp qword ptr [rax+0x48], keeps its name when it is reached through any of those references; one reached only by a short branch, a 32-bit absolute or image-relative value or a computed address loses its import name. The prefix byte is not named either: a function whose whole body is the tail jump is a compiler-emitted wrapper with a name of its own that the call-target walk discovers. x86-64 only; in 32-bit code 40-4F are one-byte inc/dec instructions.", "use_when": "On (default) on any x86-64 PE. The tell-tale of the defect is kuna functions listing an import-named sub_<addr> one byte past the start of a JMP qword ptr [import] instruction, which kuna disassemble shows as 48ff25..., whose decompile is a one-line call of the import, while the function that ends in that jump reports a size ending at the phantom. Flip off to restore the previous inventory exactly, e.g. to compare an older function list.", "example": "--option rexthunk off", "source_decompiler": "kuna", "inspiration": "GH-468 testbed (MSVC /O2 ab_o2.exe): __scrt_initialize_type_info at 0x140001ed0 ends in 48 ff 25 (jmp [InitializeSListHead]) and kuna functions listed sub_140001ed8 at its FF byte; the same scan planted 35 such entries in each of the /Od and /O2 images and 6 in the vendored MinGW pe_imports.exe, where one of them sits inside my_lconv_init", "change_kind": "correctness-fix", "tier": "analysis", "symptoms": ["a pe function entry starts one byte into a jmp qword ptr [import] instruction", "kuna functions lists a sub_<addr> named after an import in the middle of another function", "a one-line sub_<addr> wrapper around an import appears right after a function that tail-calls the same import", "kuna functions reports a pe function's size as ending one byte into its own last jump"]},
  {"option": "noreturnretuse", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "output-prototype", "strength": "HARD", "rewind": "P4", "issue": "kuna-noreturn-return-trial", "summary": "Let a CALL on a terminating no-return path coexist with the function's own RETURN when the output trial is scored. `Funcdata::only_op_use` (C++ `Funcdata::onlyOpUse`, funcdata_varnode.cc:1851) answers whether a Varnode reaching `opmatch` is used ONLY there; any other CALL consuming the same Varnode rejects it, and `ActionOutputPrototype` then types the function `void`. On an ABI whose return register is also the first argument register (ARM/AArch64 `r0`/`w0`) a status helper trips that for free: the status is returned on the normal path and handed to a failure handler on the error path, so `int guarded_status(int)` recovers as `void guarded_status(int)` with a bare `return;`. With this on, a CALL/CALLIND use is skipped when the matched op is a RETURN, the call is the second-to-last op of its block, and the block's last op is an artificial halt flagged no-return -- a block from which no RETURN is reachable, so the two uses never compete. A call that may return, a halt further down the block, and every non-RETURN match keep the upstream rejection.", "use_when": "On by default: the skipped use is on a path that provably cannot reach the RETURN it would otherwise veto, so the veto costs the whole signature and buys nothing. Byte-identical (0/675) on the datatest corpus and 0 changed lines across 23 linked binaries decompiled whole, spanning x86-64/ARM/AArch64/RISC-V/MIPS/PPC64/SPARC/Mach-O -- the shape needs the return register and the first argument register to be the same storage, which x86-64 does not have. It fires on ARM/AArch64 objects built with `-fstack-protector-strong`, where 4 of 6 measured functions change and every change is `void` -> the typed return the source declares. Set off to restore the upstream blanket rejection (`Funcdata::onlyOpUse` returning false on any competing call use).", "example": "option noreturnretuse off", "source_decompiler": "kuna", "inspiration": "AArch64/ARM ET_REL status helpers whose guard-failure path tail-calls __stack_chk_fail with the status still in w0", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a helper that plainly computes a status recovers as void with a bare return;", "the callee's return value is missing at the call site on ARM/AArch64", "a function whose error path calls a no-return handler loses its return type", "the return type differs between two builds of the same function that differ only in stack-protector hardening"]},
  {"option": "zeroidiomuse", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "kuna-zero-idiom-input-trial", "summary": "Stop the x86 register-clearing idiom from vetoing a call's arguments. `Funcdata::only_op_use` (C++ `Funcdata::onlyOpUse`, funcdata_varnode.cc:1851) walks every descendant of the Varnode reaching a CALL in a candidate argument register and rejects the trial when another CALL also consumes it; `FuncCallSpecs::check_input_trial_use` then marks the trial inactive, which is also CHECKED, so it is never re-scored and the argument is gone. `xor esi,esi` is a descendant like any other, so inside a loop the value passed at the bottom reaches the next iteration's clear -- through the killed-by-call INDIRECT, the loop-head MULTIEQUAL and the SUBPIECE that slices esi out of rsi -- and from there whatever call follows the clear, whose own trial for that register is active. Both arguments then disappear and the call renders `f()`. The competing use is not real: `INT_XOR(v,v)` is 0 whatever v is, so nothing downstream of it can observe the value. With this on, an `INT_XOR`/`INT_SUB` whose two operands are the same value is skipped entirely -- neither a rejection nor a step the walk continues through. Sameness is structural because the operands are still two distinct SUBPIECE Varnodes at this point in the schedule: identical Varnodes, equal constants, or the same pure reshaping op (COPY, SUBPIECE, PIECE, INT_ZEXT, INT_SEXT) over operands that are themselves the same, to a depth of two. Every other descendant keeps the upstream treatment.", "use_when": "On by default: the use it declines to count is provably independent of the value being scored, so the veto costs the call its whole argument list and buys nothing. It is one-directional -- it can only decline a veto, never promote a trial by itself -- and it admits no storage the upstream walk would not have admitted. Byte-identical (0/675) on the datatest corpus. A `decompile-all` sweep in both arms over 21 linked x86-64 ELF binaries (5,857 functions) changes 141 of them, and the direction is overwhelmingly one way: 148 argument positions recovered against 6 given up, and no function loses a statement. The recoveries are checkable from the output alone -- `__fprintf_chk(f,1,\"keycode %3d =\")` gains the value its format string demands, `OPENSSL_init_crypto(0xc)` gains its settings pointer, and in tar's date parser ten `dcgettext(...); warn();` pairs fuse back into `warn(dcgettext(...))`. The 6 given up are the upstream competition rule working as written: a value two calls share can be exclusively a parameter of only one of them, and in every case the value stays live at its other use in the same function. Keep it on when a call inside a loop renders with an empty argument list while the disassembly plainly loads its argument registers just before it, and the same function recovers arguments fine at a call outside the loop; that asymmetry is the tell. Set off to restore the upstream walk, where the zeroing idiom is followed like any other arithmetic.", "example": "option zeroidiomuse off", "source_decompiler": "kuna", "inspiration": "RE-friction round 12, need argument-recovery-drops-both: a stripped x86-64 VM input handler whose per-byte watchdog(timers, count) call renders watchdog() because the loop head clears esi/edi with xor before the next read", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call inside a loop renders with an empty argument list while the disassembly loads its argument registers immediately before it", "the same function recovers arguments at one call site and drops them at another with the identical mov-immediate-then-call shape", "an explicit prototype override restores the arguments but nothing else does", "a call loses its arguments only when a sibling call in the same loop is preceded by xor reg,reg or sub reg,reg"]},
  {"option": "exclusivearguse", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "active-input-trial-scoring", "strength": "HARD", "rewind": "P4", "issue": "kuna-exclusive-path-arg-use", "summary": "Stop a dereference on the other arm of a branch from vetoing a call's arguments. `Funcdata::only_op_use` (C++ `Funcdata::onlyOpUse`, funcdata_varnode.cc:1851) walks every descendant of the Varnode reaching a CALL in a candidate argument register and rejects the trial on any CPUI_LOAD or CPUI_STORE; `FuncCallSpecs::check_input_trial_use` then marks the trial inactive, which is also CHECKED, so the argument is gone along with every argument positioned behind it. A container append compiled with the one-element fast path inlined and the grow path left as a call sets its arguments up BEFORE the capacity test, because both arms need them -- `mov rdx,[rbx+8]; lea r8,[rsi+rdi]; cmp rdx,[rbx+0x10]; jz slow` then `movzx eax,[r8]; mov [rdx],al` on the fast path and `call append_slow` on the slow one. The fast path's LOAD and STORE are descendants of the very Varnodes the call is passed, so both trials go inactive and `append_slow(container,n,p)` renders `append_slow(container)`. The competing use cannot compete: an execution path is one walk from the entry block, so two blocks neither of which reaches the other never both run. With this on, a LOAD/STORE descendant is skipped when the matched op is a CALL/CALLIND, the trial Varnode is the ADDRESS operand of the access rather than the stored datum, the trial Varnode is DEFINED IN A BLOCK BOTH THE ACCESS AND THE CALL ARE IMMEDIATE SUCCESSORS OF, and the access and the call sit in distinct blocks with neither reachable from the other. Every other descendant keeps the upstream treatment, BRANCH/CBRANCH/BRANCHIND included.", "use_when": "On by default: the use it declines to count is on a path that provably cannot reach the call, so the veto costs real arguments and buys nothing. It is one-directional -- it can only decline a veto, never promote a trial by itself -- and it admits no storage the upstream walk would not have admitted. Byte-identical (0/675) on the datatest corpus. The failure mode of relaxing this walk is a SPURIOUS TRAILING ARGUMENT, which no gate observes, so the rule is bounded by where the value is DEFINED and not only by where it is used: the definition test was written against a measured false positive, phantomgate.exe's random_device constructor, which opens `mov r8,rcx` and dereferences r8 on the arms that succeed, so an earlier draft gave every throw call on a failing arm a third argument the disassembly does not pass. Requiring the definition to sit in the very block that branches says the value was set up FOR this branch rather than merely still live when it was taken. It does not overlap the `calleearity` family, all of which was built for a CBRANCH use in a block that DOMINATES the call (`test rcx,rcx; jz; call`): a dominating block always co-executes with the call, so this rule declines it, and this rule only relaxes LOAD/STORE. It does not overlap `spillargtrial` either, which owns the STORE's value slot while this owns its address slot. Keep it on when a call whose arguments the disassembly plainly sets up before a branch renders with only the arguments written on the call's own arm, and an explicit prototype override restores the rest; the tell is a fast path in the same function that dereferences the very registers the call is missing. Set off to restore the upstream rejection, where any LOAD or STORE of the value sinks the trial.", "example": "option exclusivearguse off", "source_decompiler": "kuna", "inspiration": "RE-friction round 12, need bytecode-append-call-loses: a stripped MSVC x86-64 bytecode builder whose append_slow(container, n, p) renders sub_140007be0(a0) because the inlined one-element fast path stores through rdx and loads through r8 on the branch arm the call is not on", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call renders with fewer arguments than the disassembly sets up and the missing registers are written before a nearby branch", "a call keeps only the arguments assigned on its own branch arm and drops the ones assigned before the test", "an explicit prototype override restores the arguments but no option does", "the callee decompiled on its own has more parameters than any of its call sites pass", "a container append or push_back helper is called with just the container"]},
  {"option": "callretpair", "values": ["on", "off"], "default": "on", "destructive_as_default": false, "phase": "P4", "subphase": "output-prototype", "strength": "HARD", "rewind": "P4", "issue": "repipe-bytecode-reader-return-discarded", "summary": "Complete the two-register CALL output arm of `FuncCallSpecs::buildOutputFromTrials` (C++ fspec.cc:5777) on any image, so a call whose cspec output rule asked for a register pair gets that pair instead of nothing. The x86-64 System V cspec returns any 16-byte aggregate in RAX:RDX -- its `<join_dual_class/>` output rule says so -- and kuna already recovers it: two active output trials on the call, both registers read by the caller. The arm that turns two trials into an output shipped as a stub (`STUB(W4 translate-on-handle)`), so the call got NO output at all, the INDIRECT creations standing for `the callee wrote something here` survived, and every read of either register rendered as a local the function never assigns. A plain `gcc -O2` QuickJS interpreter shows the gap at its sharpest: kuna recovers the callee as `undefined16 sub_875e0(int8,char *,int8,uint4)` when asked about it directly, and renders the call to it as `sub_875e0(a0,a1,a2,1);` followed by `v4 = (int4)v5;` with `unsigned long v5; // rdx` declared, read four times and never assigned. With this on the CALL gains a `join`-space output covering both registers, each half becomes a SUBPIECE of it placed after the call, and the INDIRECT creations are destroyed. The classification is kuna_rustabi::classify_call_output_pair, unchanged and shared: the model rule matched a justified non-overlapping pair, the caller reads both halves out of the call, and a bounded decode of the resolved callee did not prove the payload register is never written. That callee check is one-sided -- it refutes a pair, never confirms one -- so forming the pair means `no counter-example`, which is the evidence upstream ships this arm on. `option rustabi auto|always` reaches the same arm and additionally keeps the producer-side pair alive through SubvariableFlow, so a rustc image behaves the same whichever of the two is set.", "use_when": "On by default: it is the upstream behaviour of a branch kuna stubbed, and the alternative is a call with no output whose result is read through variables that have no definition anywhere in the program. 0/675 byte-identical on the datatest corpus. Keep it on whenever a call is followed by reads of a second return register -- on x86-64 the tell is a `// rdx` local that is declared and read but never assigned, next to a call rendered as a bare statement, while decompiling the callee on its own reports an `undefined16` return. That shape is what every 16-byte struct return looks like: a `JSValue`, a `div_t`, a Go or Rust two-word value, any `struct { void *; long; }`. The pair is rendered as a raw 16-byte container with `SUB168` extracting the halves until a later pass gives it an aggregate type, so the gain is that the payload EXISTS and is connected to its producer, not that it is spelled well. Set off to restore the stub -- worth trying when a call acquires a 16-byte output whose second half the disassembly shows is a clobber rather than a value (the callee-body veto only fires on a callee the bounded decode can read to every RETURN, so a callee containing a call cannot be refuted), or to bisect whether this arm rather than trial scoring changed a call's surroundings.", "example": "option callretpair off", "source_decompiler": "ghidra", "inspiration": "upstream Ghidra FuncCallSpecs::buildOutputFromTrials (fspec.cc:5813-5853), the multi-trial arm kuna shipped as a stub; RE-friction round 12, need bytecode-reader-return-discarded (a stripped gcc 9.4 QuickJS interpreter, caller sub_9d9c0 -> JSValue callee sub_875e0 @0x9d9d8)", "change_kind": "correctness-fix", "tier": "core", "symptoms": ["a call renders as a bare statement and the value it returned is read through a local the function never assigns", "a local commented `// rdx` or `// edx` is declared and read but has no definition anywhere in the function", "decompiling the callee reports an undefined16 return but every call site shows no output", "a 16-byte struct return (JSValue, div_t, a fat pointer, a two-word value) is lost at the call site", "a tag-and-payload pair is tested right after a call and the tag variable is never assigned"]}
]
